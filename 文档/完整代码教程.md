<a id="top"></a>

# Boardroom Sim MVP 完整代码教程

生成时间：2026-06-02 22:19:40

本版为 Markdown 阅读版，按函数组织内容。每个函数包含功能、参数、返回值、调用关系和完整源码；项目内调用可跳转，库函数会解释作用和返回值。

## 统计

- 纳入文件：35

- 函数/方法：281

- 类：15

- 配置/Prompt 文件：15

## 文件索引

| 文件 | 函数 | 类 | 作用 |
| --- | ---: | ---: | --- |
| [analyze_debate_accuracy.py](#module-analyze_debate_accuracy-py) | 38 | 0 | 讨论准确率分析脚本：比较初始预测、各轮预测、最终预测与真实标签之间的关系。 |
| [analyze_debate_changes.py](#module-analyze_debate_changes-py) | 22 | 2 | 讨论变化分析脚本：统计角色观点、数值预测和最终提案在讨论中的变化。 |
| [boardroom_sim/__init__.py](#module-boardroom_sim-__init__-py) | 0 | 0 | 项目源文件。教程按函数和类解释它在实验中的作用。 |
| [boardroom_sim/agents.py](#module-boardroom_sim-agents-py) | 20 | 1 | 角色智能体模块：负责观察案例、构造 prompt、调用 LLM、清洗角色预测和记录讨论行为。 |
| [boardroom_sim/baselines.py](#module-boardroom_sim-baselines-py) | 5 | 0 | 基线模块：实现单智能体历史信息预测，用来与多角色讨论实验对照。 |
| [boardroom_sim/config.py](#module-boardroom_sim-config-py) | 10 | 2 | 配置模块：把 TOML 配置转换成 Python dataclass，集中管理实验变量。 |
| [boardroom_sim/culture.py](#module-boardroom_sim-culture-py) | 4 | 0 | L5 文化层模块：把文化分数解释为会议行为约束和 prompt 指令。 |
| [boardroom_sim/evaluation.py](#module-boardroom_sim-evaluation-py) | 23 | 0 | 指标模块：读取结果并计算分类准确率、数值误差、案例级指标和摘要文本。 |
| [boardroom_sim/io.py](#module-boardroom_sim-io-py) | 4 | 0 | 输入输出模块：读写 JSONL 结果、案例和 trace。 |
| [boardroom_sim/llm.py](#module-boardroom_sim-llm-py) | 10 | 2 | LLM 客户端模块：封装 OpenAI 兼容接口、JSON 抽取、重试和用量记录。 |
| [boardroom_sim/models.py](#module-boardroom_sim-models-py) | 20 | 5 | 数据模型模块：定义 BoardCase、RoleDecision、TermSheetProposal、SimulationResult 等核心结构。 |
| [boardroom_sim/pitchbook.py](#module-boardroom_sim-pitchbook-py) | 36 | 0 | PitchBook/Revelio 数据接入模块：从 Excel 多张表中构造实验案例和真实标签。 |
| [boardroom_sim/process.py](#module-boardroom_sim-process-py) | 10 | 1 | 董事会流程控制模块：控制发言顺序、可见历史、archetype、L5 文化和每轮 prompt 上下文。 |
| [boardroom_sim/prompts.py](#module-boardroom_sim-prompts-py) | 4 | 1 | Prompt 模板模块：读取 Markdown prompt 并替换变量。 |
| [boardroom_sim/roles.py](#module-boardroom_sim-roles-py) | 7 | 0 | 角色策略模块：从 TOML 或内置默认值构建各董事会角色的 RolePolicy。 |
| [boardroom_sim/simulator.py](#module-boardroom_sim-simulator-py) | 17 | 1 | 模拟器模块：组织角色初始预测、会议讨论、最终聚合和 trace 记录。 |
| [configs/boardroom_default.toml](#file-configs-boardroom_default-toml) | 0 | 0 | 默认实验配置。这里是后续最常修改的位置：输入文件、输出目录、LLM、角色顺序、角色权重、讨论轮数、archetype、L5 文化层和可见历史字段都在这里控制。 |
| [evaluate_results.py](#module-evaluate_results-py) | 2 | 0 | 结果评估入口：从 results.jsonl 读取模拟输出并生成指标。 |
| [one_case.sh](#file-one_case-sh) | 0 | 0 | 单案例运行脚本示例，便于快速 smoke test。 |
| [policies/roles/cto.toml](#file-policies-roles-cto-toml) | 0 | 0 | 角色策略配置。每个 TOML 文件定义一个董事会角色的目标、可见字段、风险偏好、谈判杠杆和行为风格。 |
| [policies/roles/followon_vc_director.toml](#file-policies-roles-followon_vc_director-toml) | 0 | 0 | 角色策略配置。每个 TOML 文件定义一个董事会角色的目标、可见字段、风险偏好、谈判杠杆和行为风格。 |
| [policies/roles/founder_ceo.toml](#file-policies-roles-founder_ceo-toml) | 0 | 0 | 角色策略配置。每个 TOML 文件定义一个董事会角色的目标、可见字段、风险偏好、谈判杠杆和行为风格。 |
| [policies/roles/lead_vc_director.toml](#file-policies-roles-lead_vc_director-toml) | 0 | 0 | 角色策略配置。每个 TOML 文件定义一个董事会角色的目标、可见字段、风险偏好、谈判杠杆和行为风格。 |
| [prompts/bargaining_step.md](#file-prompts-bargaining_step-md) | 0 | 0 | LLM prompt 模板。代码会用 `.format(...)` 注入案例、角色、历史、流程和 L5 文化变量。 |
| [prompts/baseline_single_agent.md](#file-prompts-baseline_single_agent-md) | 0 | 0 | LLM prompt 模板。代码会用 `.format(...)` 注入案例、角色、历史、流程和 L5 文化变量。 |
| [prompts/baseline_system.md](#file-prompts-baseline_system-md) | 0 | 0 | LLM prompt 模板。代码会用 `.format(...)` 注入案例、角色、历史、流程和 L5 文化变量。 |
| [prompts/initial_prediction.md](#file-prompts-initial_prediction-md) | 0 | 0 | LLM prompt 模板。代码会用 `.format(...)` 注入案例、角色、历史、流程和 L5 文化变量。 |
| [prompts/response_generators/critical.md](#file-prompts-response_generators-critical-md) | 0 | 0 | 回复风格片段。PromptRenderer 会把它嵌入角色 prompt，用来控制模型回答的推理深度和批判性。 |
| [prompts/response_generators/reasoning.md](#file-prompts-response_generators-reasoning-md) | 0 | 0 | 回复风格片段。PromptRenderer 会把它嵌入角色 prompt，用来控制模型回答的推理深度和批判性。 |
| [prompts/response_generators/simple.md](#file-prompts-response_generators-simple-md) | 0 | 0 | 回复风格片段。PromptRenderer 会把它嵌入角色 prompt，用来控制模型回答的推理深度和批判性。 |
| [prompts/system_agent.md](#file-prompts-system_agent-md) | 0 | 0 | LLM prompt 模板。代码会用 `.format(...)` 注入案例、角色、历史、流程和 L5 文化变量。 |
| [render_trace_report.py](#module-render_trace_report-py) | 23 | 0 | trace 渲染脚本：把机器可读 traces.json 转为人类可读 Markdown 报告。 |
| [requirements.txt](#file-requirements-txt) | 0 | 0 | Python 依赖清单。新环境可以用 `python -m pip install -r requirements.txt` 一键安装。 |
| [run_batch_experiments.py](#module-run_batch_experiments-py) | 22 | 0 | 批量实验主脚本：读取 Excel/JSONL 输入，运行多次模拟、断点续跑、基线预测、指标计算和 manifest 输出。 |
| [run_experiment.py](#module-run_experiment-py) | 4 | 0 | 单次实验脚本：适合小规模调试，从输入案例构造模拟器并写出 results/traces。 |

<a id="workflow"></a>

## 函数调用主线

1. **读取配置与输入**：[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79) 调用 [load_experiment_config](#fn-boardroom_sim-config-py-load_experiment_config-185) 读取 TOML，再用 [build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31) 从 Excel 构造 BoardCase。

2. **构造角色和流程**：配置中的角色 TOML 进入 RolePolicy；[BoardProcessController.prompt_context](#fn-boardroom_sim-process-py-boardprocesscontroller-prompt_context-156) 每轮生成可见历史、会议阶段和 L5 文化指令。

3. **初始预测**：[BoardAgent.evaluate](#fn-boardroom_sim-agents-py-boardagent-evaluate-94) 为每个角色生成初始 RoleDecision。

4. **多轮讨论**：[BoardroomSimulator._run_bargaining_round](#fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208) 按发言顺序调用 [BoardAgent.bargaining_step](#fn-boardroom_sim-agents-py-boardagent-bargaining_step-123)，并记录会议行为字段。

5. **最终聚合**：[BoardroomSimulator._build_proposal](#fn-boardroom_sim-simulator-py-boardroomsimulator-_build_proposal-172) 把角色最终判断合成为 TermSheetProposal。

6. **评估与报告**：批量脚本写出 results/traces 后，再调用评估和分析脚本生成 case_metrics、aggregate_metrics、debate_accuracy_report 等文件。

<a id="progress"></a>
## 当前实验进度

当前代码已经从“硬编码 prompt 的简单模拟”改造成更接近可扩展框架的版本：角色、prompt、可见字段、讨论轮次、发言顺序、board archetype 和 L5 文化层都可以通过配置或模板文件调整。

- **框架化**：配置集中在 `configs/boardroom_default.toml`，角色放在 `policies/roles/*.toml`，prompt 放在 `prompts/*.md`。
- **过程变量**：`BoardProcessController` 负责发言顺序、可见历史、会议阶段和过程指令。
- **L5 文化层**：`culture.py` 把文化分数解释为行为控制，再进入每轮 prompt。
- **数据升级**：默认输入指向更完整的 PitchBook/Revelio Excel 文件，可从字段字典继续挑选更多变量。
- **可恢复运行**：批量脚本支持 checkpoint，中断后可以通过 resume 逻辑继续已完成 case 后面的工作。

<a id="classes"></a>

## 类与参数

这里集中解释项目中的类。类方法会链接到后面的逐函数讲解，字段用于理解每个对象携带的实验变量。

<a id="class-analyze_debate_changes-py-fieldchange-64"></a>

### FieldChange

文件：`analyze_debate_changes.py:64-72`

项目类。它把相关数据和行为组织在一起，字段与方法如下。

**父类/基类**：无显式父类

**字段/属性**

- `case_id`
- `role`
- `field`
- `before`
- `after`
- `round_label`

**方法**

- 无方法。

[回到顶部](#top)

<a id="class-analyze_debate_changes-py-numericchange-76"></a>

### NumericChange

文件：`analyze_debate_changes.py:76-85`

项目类。它把相关数据和行为组织在一起，字段与方法如下。

**父类/基类**：无显式父类

**字段/属性**

- `case_id`
- `role`
- `field`
- `before`
- `after`
- `delta`
- `round_label`

**方法**

- 无方法。

[回到顶部](#top)

<a id="class-boardroom_sim-agents-py-boardagent-74"></a>

### BoardAgent

文件：`boardroom_sim/agents.py:74-320`

董事会中的一个角色智能体。它知道自己的 RolePolicy，并通过 PromptRenderer 和 LLMClient 完成观察、预测和讨论发言。

**父类/基类**：无显式父类

**字段/属性**

- `policy`
- `role_name`
- `llm_client`
- `prompt_renderer`

**方法**

- [__init__](#fn-boardroom_sim-agents-py-boardagent-__init__-77)
- [observe](#fn-boardroom_sim-agents-py-boardagent-observe-89)
- [evaluate](#fn-boardroom_sim-agents-py-boardagent-evaluate-94)
- [opening_statement](#fn-boardroom_sim-agents-py-boardagent-opening_statement-111)
- [bargaining_step](#fn-boardroom_sim-agents-py-boardagent-bargaining_step-123)
- [rationale_line](#fn-boardroom_sim-agents-py-boardagent-rationale_line-154)
- [_system_prompt](#fn-boardroom_sim-agents-py-boardagent-_system_prompt-160)
- [_role_policy_text](#fn-boardroom_sim-agents-py-boardagent-_role_policy_text-168)
- [_decision_prompt](#fn-boardroom_sim-agents-py-boardagent-_decision_prompt-172)
- [_bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197)
- [_decision_from_json](#fn-boardroom_sim-agents-py-boardagent-_decision_from_json-227)
- [_meeting_artifacts_from_json](#fn-boardroom_sim-agents-py-boardagent-_meeting_artifacts_from_json-296)
- [_decision_payload](#fn-boardroom_sim-agents-py-boardagent-_decision_payload-308)

**例子**：例：一个 `BoardAgent(role_policy=founder_ceo, ...)` 只看到创始人角色允许看到的字段，并按创始人的目标发言。

[回到顶部](#top)

<a id="class-boardroom_sim-config-py-boardprocessconfig-140"></a>

### BoardProcessConfig

文件：`boardroom_sim/config.py:140-153`

董事会过程变量配置。它控制 archetype、发言协议、讨论轮数、可见历史、L5 文化层和角色权重。

**父类/基类**：无显式父类

**字段/属性**

- `archetype`
- `archetype_label`
- `archetype_description`
- `culture_layer`
- `culture`
- `protocol`
- `dominance_pattern`
- `challenge_required`
- `memory_scope`
- `memory_window`
- `role_weight_multipliers`

**方法**

- 无方法。

**例子**：例：`board_archetype='value_creating'` 会让会议更重视证据、挑战和替代方案；`discussion_rounds=3` 控制讨论轮数。

[回到顶部](#top)

<a id="class-boardroom_sim-config-py-experimentconfig-157"></a>

### ExperimentConfig

文件：`boardroom_sim/config.py:157-182`

实验级配置对象。它把输入文件、输出目录、prompt、角色策略、LLM 和过程控制统一起来。

**父类/基类**：无显式父类

**字段/属性**

- `name`
- `config_path`
- `role_policy_dir`
- `prompts_dir`
- `role_order`
- `role_weights`
- `bargaining_rounds`
- `history_limit`
- `discussion_paradigm`
- `response_generator`
- `decision_protocol`
- `round_tasks`
- `board_process`
- `raw`

**方法**

- [to_manifest_dict](#fn-boardroom_sim-config-py-experimentconfig-to_manifest_dict-175)

[回到顶部](#top)

<a id="class-boardroom_sim-llm-py-llmconfig-32"></a>

### LLMConfig

文件：`boardroom_sim/llm.py:32-92`

LLM 请求配置，包括 base_url、model、api_key、温度、超时和重试次数。

**父类/基类**：无显式父类

**字段/属性**

- `api_key`
- `model`
- `base_url`
- `temperature`
- `timeout_seconds`
- `http_max_retries`
- `retry_base_seconds`

**方法**

- [from_env](#fn-boardroom_sim-llm-py-llmconfig-from_env-44)

**例子**：例：`base_url='https://api.openai.com/v1/chat/completions'`、`model='gpt-4.1-mini'`、`timeout_seconds=120` 控制请求目标和超时。

[回到顶部](#top)

<a id="class-boardroom_sim-llm-py-llmclient-95"></a>

### LLMClient

文件：`boardroom_sim/llm.py:95-207`

OpenAI 兼容接口的轻量客户端，负责文本补全、JSON 补全、重试和用量统计。

**父类/基类**：无显式父类

**字段/属性**

- `config`
- `total_prompt_tokens`
- `total_completion_tokens`
- `total_tokens`

**方法**

- [__init__](#fn-boardroom_sim-llm-py-llmclient-__init__-98)
- [complete_text](#fn-boardroom_sim-llm-py-llmclient-complete_text-105)
- [complete_json](#fn-boardroom_sim-llm-py-llmclient-complete_json-151)
- [_update_usage](#fn-boardroom_sim-llm-py-llmclient-_update_usage-173)
- [_should_retry_http](#fn-boardroom_sim-llm-py-llmclient-_should_retry_http-179)
- [_sleep_before_retry](#fn-boardroom_sim-llm-py-llmclient-_sleep_before_retry-183)
- [_network_error_reason](#fn-boardroom_sim-llm-py-llmclient-_network_error_reason-193)
- [_retry_delay_seconds](#fn-boardroom_sim-llm-py-llmclient-_retry_delay_seconds-200)

[回到顶部](#top)

<a id="class-boardroom_sim-models-py-boardcase-120"></a>

### BoardCase

文件：`boardroom_sim/models.py:120-277`

一个融资决策案例的完整输入。它同时保存可见案例字段、历史融资、投资人背景、员工/治理画像、真实标签和 L5 文化变量。

**父类/基类**：无显式父类

**字段/属性**

- `case_id`
- `company_id`
- `target_deal_id`
- `company_label`
- `decision_date`
- `business_status`
- `company_financing_status`
- `ownership_status`
- `year_founded`
- `company_age_at_decision_years`
- `primary_industry`
- `industry_group`
- `industry_sector`
- `verticals`
- `keywords`
- `description`
- `prior_vc_deal_count`
- `prior_deal_date`
- `prior_deal_type`
- `prior_vc_round`
- `prior_deal_size_usd_m`
- `prior_pre_money_valuation_usd_m`
- `prior_post_money_valuation_usd_m`
- `prior_raised_to_date_usd_m`
- `prior_investor_ownership_pct`
- `months_since_prior_deal`
- `prior_valuation_markup_multiple`
- `prior_valuation_direction_label`
- `prior_investor_count`
- `prior_new_investor_count`
- `prior_followon_investor_count`
- `prior_lead_investor_count`
- `prior_lead_investor_types`
- `prior_lead_investor_amount_share`
- `prior_lead_preferred_deal_size_min_usd_m`
- `prior_lead_preferred_deal_size_max_usd_m`
- `prior_lead_preferred_company_valuation_min_usd_m`
- `prior_lead_preferred_company_valuation_max_usd_m`
- `prior_company_deal_history`
- `prior_lead_investor_deal_history`
- `prior_followon_investor_deal_history`
- `employee_count_at_decision`
- `previous_employee_count`
- `employee_growth_rate`
- `engineering_role_count`
- `executive_role_count`
- `founder_count`
- `current_ceo_count`
- `current_ceo_is_founder`
- `board_member_count`
- `investor_board_member_count`
- `competitor_count`
- `similar_company_count`
- `notes`

**方法**

- [from_dict](#fn-boardroom_sim-models-py-boardcase-from_dict-179)
- [to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250)
- [prior_valuation_markup](#fn-boardroom_sim-models-py-boardcase-prior_valuation_markup-254)
- [prior_valuation_direction](#fn-boardroom_sim-models-py-boardcase-prior_valuation_direction-260)
- [estimated_dilution_pct](#fn-boardroom_sim-models-py-boardcase-estimated_dilution_pct-273)

**例子**：例：`case_id='9244942_963111474T'` 表示一个交易案例；`deal_size_usd_m=12.5` 表示融资金额；`l5_culture={'trust': 0.7, ...}` 会进入流程控制和 prompt。

[回到顶部](#top)

<a id="class-boardroom_sim-models-py-rolepolicy-281"></a>

### RolePolicy

文件：`boardroom_sim/models.py:281-293`

一个角色的长期设定，包括可见字段、关注重点、风险偏好、谈判杠杆和输出风格。

**父类/基类**：无显式父类

**字段/属性**

- `role_name`
- `layer_1_goals`
- `layer_2_attention_fields`
- `layer_2_ignored_fields`
- `layer_3_heuristics`
- `layer_4_interaction_protocol`

**方法**

- [to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291)

[回到顶部](#top)

<a id="class-boardroom_sim-models-py-roledecision-297"></a>

### RoleDecision

文件：`boardroom_sim/models.py:297-314`

角色在某个时点对交易的结构化判断。实验会比较初始 RoleDecision、讨论中更新后的 RoleDecision 和最终聚合结果。

**父类/基类**：无显式父类

**字段/属性**

- `role_name`
- `financing_intent`
- `completion_view`
- `predicted_deal_size_usd_m`
- `predicted_post_money_valuation_usd_m`
- `predicted_investor_ownership_pct`
- `predicted_deal_type`
- `valuation_direction`
- `satisfaction_score`
- `rationale`
- `observed_fields`

**方法**

- [to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312)

**例子**：例：Founder CEO 可以给出 `financing_intent='yes'`、`deal_size_usd_m=15`、`rationale=['cash runway is short']`，后续讨论会更新这些字段。

[回到顶部](#top)

<a id="class-boardroom_sim-models-py-termsheetproposal-318"></a>

### TermSheetProposal

文件：`boardroom_sim/models.py:318-332`

多角色讨论后的最终条款预测，是实验主要输出对象。

**父类/基类**：无显式父类

**字段/属性**

- `recommended_deal_size_usd_m`
- `recommended_post_money_valuation_usd_m`
- `recommended_investor_ownership_pct`
- `recommended_deal_type`
- `valuation_direction`
- `estimated_dilution_pct`
- `investor_protection_level`
- `tech_budget_protected`

**方法**

- [to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330)

**例子**：例：最终输出 `deal_completion_view='completed'`、`deal_type='priced_round'`、`estimated_dilution_pct=18.0`，用于和真实标签计算误差。

[回到顶部](#top)

<a id="class-boardroom_sim-models-py-simulationresult-336"></a>

### SimulationResult

文件：`boardroom_sim/models.py:336-360`

单个案例完整运行结果，包括最终提案、角色决策、讨论历史、trace 和真实标签。

**父类/基类**：无显式父类

**字段/属性**

- `case_id`
- `company_name`
- `financing_initiation_decision`
- `financing_completion_view`
- `predicted_deal_size_usd_m`
- `predicted_post_money_valuation_usd_m`
- `predicted_investor_ownership_pct`
- `predicted_deal_type`
- `valuation_direction`
- `role_decisions`
- `proposal`
- `consensus_score`
- `deal_break_risk`
- `labels`
- `trace`

**方法**

- [to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355)

[回到顶部](#top)

<a id="class-boardroom_sim-process-py-boardprocesscontroller-99"></a>

### BoardProcessController

文件：`boardroom_sim/process.py:99-217`

董事会过程控制器。它把 BoardProcessConfig 转换为每轮的发言顺序、可见历史和 prompt 上下文。

**父类/基类**：无显式父类

**字段/属性**

- `config`
- `role_order`

**方法**

- [__init__](#fn-boardroom_sim-process-py-boardprocesscontroller-__init__-102)
- [to_trace_payload](#fn-boardroom_sim-process-py-boardprocesscontroller-to_trace_payload-106)
- [effective_role_weights](#fn-boardroom_sim-process-py-boardprocesscontroller-effective_role_weights-110)
- [speaking_order_for_round](#fn-boardroom_sim-process-py-boardprocesscontroller-speaking_order_for_round-123)
- [visible_history](#fn-boardroom_sim-process-py-boardprocesscontroller-visible_history-144)
- [prompt_context](#fn-boardroom_sim-process-py-boardprocesscontroller-prompt_context-156)
- [meeting_phase_for_round](#fn-boardroom_sim-process-py-boardprocesscontroller-meeting_phase_for_round-189)
- [_process_instruction](#fn-boardroom_sim-process-py-boardprocesscontroller-_process_instruction-196)
- [_challenge_instruction](#fn-boardroom_sim-process-py-boardprocesscontroller-_challenge_instruction-210)

**例子**：例：同样四个角色，在 `barbarian` 和 `clan` archetype 下会得到不同发言顺序、不同主席压力和不同 L5 行为指令。

[回到顶部](#top)

<a id="class-boardroom_sim-prompts-py-promptrenderer-15"></a>

### PromptRenderer

文件：`boardroom_sim/prompts.py:15-42`

Prompt 模板读取与渲染器。它把模板文件和变量合成为发给 LLM 的最终文本。

**父类/基类**：无显式父类

**字段/属性**

- `prompts_dir`
- `response_generator`
- `round_tasks`
- `_cache`

**方法**

- [__init__](#fn-boardroom_sim-prompts-py-promptrenderer-__init__-18)
- [render](#fn-boardroom_sim-prompts-py-promptrenderer-render-24)
- [response_guidance](#fn-boardroom_sim-prompts-py-promptrenderer-response_guidance-29)
- [_read_template](#fn-boardroom_sim-prompts-py-promptrenderer-_read_template-36)

[回到顶部](#top)

<a id="class-boardroom_sim-simulator-py-boardroomsimulator-29"></a>

### BoardroomSimulator

文件：`boardroom_sim/simulator.py:29-388`

项目类。它把相关数据和行为组织在一起，字段与方法如下。

**父类/基类**：无显式父类

**字段/属性**

- `config`
- `bargaining_rounds`
- `role_order`
- `process`
- `base_role_weights`
- `role_weights`
- `policies`
- `llm_client`
- `agents`

**方法**

- [__init__](#fn-boardroom_sim-simulator-py-boardroomsimulator-__init__-32)
- [simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)
- [_background_summary](#fn-boardroom_sim-simulator-py-boardroomsimulator-_background_summary-153)
- [_policy_snapshot](#fn-boardroom_sim-simulator-py-boardroomsimulator-_policy_snapshot-168)
- [_build_proposal](#fn-boardroom_sim-simulator-py-boardroomsimulator-_build_proposal-172)
- [_format_money](#fn-boardroom_sim-simulator-py-boardroomsimulator-_format_money-202)
- [_run_bargaining_round](#fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208)
- [_aggregate_financing_intent](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_financing_intent-297)
- [_aggregate_completion_view](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_completion_view-301)
- [_aggregate_deal_size](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_deal_size-305)
- [_aggregate_positive_numeric](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_positive_numeric-309)
- [_aggregate_text_choice](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_text_choice-324)
- [_aggregate_valuation](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_valuation-328)
- [_weighted_choice](#fn-boardroom_sim-simulator-py-boardroomsimulator-_weighted_choice-332)
- [_consensus_score](#fn-boardroom_sim-simulator-py-boardroomsimulator-_consensus_score-344)
- [_deal_break_risk](#fn-boardroom_sim-simulator-py-boardroomsimulator-_deal_break_risk-359)
- [_record](#fn-boardroom_sim-simulator-py-boardroomsimulator-_record-372)

[回到顶部](#top)

<a id="python-files"></a>

## 逐函数讲解

以下按文件排列。每个函数都保留完整源码块；重点解释函数功能、输入输出和它调用了哪些内部/外部函数。

<a id="module-analyze_debate_accuracy-py"></a>

## analyze_debate_accuracy.py

讨论准确率分析脚本：比较初始预测、各轮预测、最终预测与真实标签之间的关系。

**类**：无类定义

<a id="fn-analyze_debate_accuracy-py-parse_args-93"></a>

### parse_args

文件：`analyze_debate_accuracy.py:93-111`

**签名**

~~~~python
def parse_args() -> argparse.Namespace:
~~~~

**功能**

源码文档说明：Parse command-line arguments.

**参数**

无显式参数。

**返回值**

返回 `argparse.Namespace` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `argparse.ArgumentParser` | 库函数或对象方法 | 构造命令行参数解析器，返回 ArgumentParser。 |
| `parser.add_argument` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `parser.parse_args` | [analyze_debate_accuracy.parse_args](#fn-analyze_debate_accuracy-py-parse_args-93) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.parse_args](#fn-analyze_debate_accuracy-py-parse_args-93)、[analyze_debate_accuracy.main](#fn-analyze_debate_accuracy-py-main-114)

**完整源码**

~~~~python
def parse_args() -> argparse.Namespace:
    """Parse command-line arguments."""
    parser = argparse.ArgumentParser(description="Analyze whether bargaining improves prediction accuracy.")
    parser.add_argument("--input", type=Path, required=True, help="Trace JSON file, usually outputs/.../traces.json.")
    parser.add_argument("--output", type=Path, default=None, help="Optional Markdown report output path.")
    parser.add_argument("--case-csv-output", type=Path, default=None, help="Optional per-case stage CSV output path.")
    parser.add_argument(
        "--baseline-results",
        action="append",
        default=[],
        help="Optional baseline result JSONL in NAME=PATH format. Can be passed more than once.",
    )
    parser.add_argument(
        "--deal-size-tolerance",
        type=float,
        default=0.50,
        help="Relative tolerance for treating deal-size prediction as correct. Default: 0.50.",
    )
    return parser.parse_args()
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-main-114"></a>

### main

文件：`analyze_debate_accuracy.py:114-134`

**签名**

~~~~python
def main() -> None:
~~~~

**功能**

源码文档说明：Generate an accuracy-change report from a trace file.

**参数**

无显式参数。

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `parse_args` | [analyze_debate_accuracy.parse_args](#fn-analyze_debate_accuracy-py-parse_args-93) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `read_trace_cases` | [analyze_debate_accuracy.read_trace_cases](#fn-analyze_debate_accuracy-py-read_trace_cases-137) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `analyze_cases` | [analyze_debate_accuracy.analyze_cases](#fn-analyze_debate_accuracy-py-analyze_cases-163) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `read_baseline_metrics` | [analyze_debate_accuracy.read_baseline_metrics](#fn-analyze_debate_accuracy-py-read_baseline_metrics-209) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `render_report` | [analyze_debate_accuracy.render_report](#fn-analyze_debate_accuracy-py-render_report-406) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `args.output.parent.mkdir` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `args.output.write_text` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `write_case_stage_csv` | [analyze_debate_accuracy.write_case_stage_csv](#fn-analyze_debate_accuracy-py-write_case_stage_csv-568) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def main() -> None:
    """Generate an accuracy-change report from a trace file."""
    args = parse_args()
    if args.deal_size_tolerance < 0:
        raise ValueError("--deal-size-tolerance must be non-negative.")
    cases = read_trace_cases(args.input)
    analysis = analyze_cases(cases, deal_size_tolerance=args.deal_size_tolerance)
    baseline_metrics = read_baseline_metrics(args.baseline_results, deal_size_tolerance=args.deal_size_tolerance)
    report = render_report(
        analysis,
        deal_size_tolerance=args.deal_size_tolerance,
        baseline_metrics=baseline_metrics,
    )
    print(report)
    if args.output is not None:
        args.output.parent.mkdir(parents=True, exist_ok=True)
        args.output.write_text(report + "\n", encoding="utf-8")
        print(f"\nWrote debate-accuracy report to {args.output}.")
    if args.case_csv_output is not None:
        write_case_stage_csv(args.case_csv_output, analysis["case_stage_rows"])
        print(f"Wrote per-case stage accuracy CSV to {args.case_csv_output}.")
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-read_trace_cases-137"></a>

### read_trace_cases

文件：`analyze_debate_accuracy.py:137-160`

**签名**

~~~~python
def read_trace_cases(path: Path) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Read traces from JSON or JSONL.

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `any` | 库函数或对象方法 | 内置逻辑聚合；任一元素为真时返回 True。 |
| `path.suffix.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `json.loads` | 库函数或对象方法 | 把 JSON 字符串解析为 Python 对象，通常返回 dict 或 list。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `enumerate` | 库函数或对象方法 | 内置枚举；返回 index 和元素组成的迭代器。 |
| `path.read_text` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `line.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `items.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `item.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[analyze_debate_accuracy.main](#fn-analyze_debate_accuracy-py-main-114)

**完整源码**

~~~~python
def read_trace_cases(path: Path) -> List[Dict[str, Any]]:
    """Read traces from JSON or JSONL."""
    if path.suffix.lower() == ".jsonl":
        items: List[Dict[str, Any]] = []
        with path.open("r", encoding="utf-8") as handle:
            for line_number, line in enumerate(handle, start=1):
                stripped = line.strip()
                if not stripped:
                    continue
                item = json.loads(stripped)
                if not isinstance(item, dict):
                    raise ValueError(f"Expected object on line {line_number} of {path}")
                items.append(item)
    else:
        data = json.loads(path.read_text(encoding="utf-8"))
        if isinstance(data, list):
            items = data
        elif isinstance(data, dict):
            items = [data]
        else:
            raise ValueError(f"Expected JSON object or list in {path}")
    if any(not item.get("trace") for item in items):
        raise ValueError("This analysis requires detailed traces. Use traces.json, not compact results.jsonl.")
    return items
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-analyze_cases-163"></a>

### analyze_cases

文件：`analyze_debate_accuracy.py:163-177`

**签名**

~~~~python
def analyze_cases(cases: Sequence[Dict[str, Any]], *, deal_size_tolerance: float) -> Dict[str, Any]:
~~~~

**功能**

按阶段评估讨论前后预测是否更接近标签，用于回答多轮讨论是否改善准确率。

**参数**

| 参数 | 含义 |
| --- | --- |
| `cases` | 多个 BoardCase 或 trace case，用于批量运行或统计。 |
| `deal_size_tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `ordered_stages` | [analyze_debate_accuracy.ordered_stages](#fn-analyze_debate_accuracy-py-ordered_stages-615) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `compute_transitions` | [analyze_debate_accuracy.compute_transitions](#fn-analyze_debate_accuracy-py-compute_transitions-367) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `build_case_sequence` | [analyze_debate_accuracy.build_case_sequence](#fn-analyze_debate_accuracy-py-build_case_sequence-225) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `compute_stage_metrics` | [analyze_debate_accuracy.compute_stage_metrics](#fn-analyze_debate_accuracy-py-compute_stage_metrics-338) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `flatten_case_stage_rows` | [analyze_debate_accuracy.flatten_case_stage_rows](#fn-analyze_debate_accuracy-py-flatten_case_stage_rows-578) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.main](#fn-analyze_debate_accuracy-py-main-114)、[run_batch_experiments._write_debate_accuracy_report](#fn-run_batch_experiments-py-_write_debate_accuracy_report-569)

**完整源码**

~~~~python
def analyze_cases(cases: Sequence[Dict[str, Any]], *, deal_size_tolerance: float) -> Dict[str, Any]:
    """Analyze stage-level accuracy and correctness transitions."""
    case_sequences = [build_case_sequence(case, deal_size_tolerance=deal_size_tolerance) for case in cases]
    stages = ordered_stages(case_sequences)
    stage_metrics = {stage: compute_stage_metrics(case_sequences, stage) for stage in stages}
    transitions = compute_transitions(case_sequences, stages)
    initial_to_final = compute_transitions(case_sequences, ["initial", "final"])
    return {
        "case_count": len(cases),
        "stages": stages,
        "stage_metrics": stage_metrics,
        "transitions": transitions,
        "initial_to_final": initial_to_final,
        "case_stage_rows": flatten_case_stage_rows(case_sequences),
    }
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-analyze_baseline_rows-180"></a>

### analyze_baseline_rows

文件：`analyze_debate_accuracy.py:180-206`

**签名**

~~~~python
def analyze_baseline_rows( rows: Sequence[Dict[str, Any]], *, baseline_name: str, deal_size_tolerance: float, ) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Score single-agent baseline result rows as one final-stage predictor.

**参数**

| 参数 | 含义 |
| --- | --- |
| `rows` | 结果行或 DataFrame 行集合。 |
| `baseline_name` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `deal_size_tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `compute_stage_metrics` | [analyze_debate_accuracy.compute_stage_metrics](#fn-analyze_debate_accuracy-py-compute_stage_metrics-338) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `case_sequences.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `score_prediction` | [analyze_debate_accuracy.score_prediction](#fn-analyze_debate_accuracy-py-score_prediction-296) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.read_baseline_metrics](#fn-analyze_debate_accuracy-py-read_baseline_metrics-209)、[run_batch_experiments._write_debate_accuracy_report](#fn-run_batch_experiments-py-_write_debate_accuracy_report-569)

**完整源码**

~~~~python
def analyze_baseline_rows(
    rows: Sequence[Dict[str, Any]],
    *,
    baseline_name: str,
    deal_size_tolerance: float,
) -> Dict[str, Any]:
    """Score single-agent baseline result rows as one final-stage predictor."""
    case_sequences = []
    for row in rows:
        labels = row.get("labels", {}) if isinstance(row.get("labels", {}), dict) else {}
        prediction = {
            "financing_initiation_decision": row.get("financing_initiation_decision"),
            "financing_completion_view": row.get("financing_completion_view"),
            "predicted_deal_size_usd_m": row.get("predicted_deal_size_usd_m"),
            "predicted_post_money_valuation_usd_m": row.get("predicted_post_money_valuation_usd_m"),
            "predicted_investor_ownership_pct": row.get("predicted_investor_ownership_pct"),
            "predicted_deal_type": row.get("predicted_deal_type"),
            "valuation_direction": row.get("valuation_direction"),
        }
        case_sequences.append(
            {
                "case_id": row.get("case_id", ""),
                "labels": labels,
                "stages": [score_prediction(baseline_name, prediction, labels, deal_size_tolerance)],
            }
        )
    return compute_stage_metrics(case_sequences, baseline_name)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-read_baseline_metrics-209"></a>

### read_baseline_metrics

文件：`analyze_debate_accuracy.py:209-222`

**签名**

~~~~python
def read_baseline_metrics(specs: Sequence[str], *, deal_size_tolerance: float) -> Dict[str, Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Read optional NAME=PATH baseline specs and score them for report rendering.

**参数**

| 参数 | 含义 |
| --- | --- |
| `specs` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `deal_size_tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |

**返回值**

返回 `Dict[str, Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `spec.split` | 库函数或对象方法 | 字符串拆分，返回 list[str]。 |
| `name.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `Path` | 库函数或对象方法 | pathlib 路径构造器；返回 Path 对象。 |
| `read_result_rows` | [boardroom_sim.evaluation.read_result_rows](#fn-boardroom_sim-evaluation-py-read_result_rows-103) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `analyze_baseline_rows` | [analyze_debate_accuracy.analyze_baseline_rows](#fn-analyze_debate_accuracy-py-analyze_baseline_rows-180) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `raw_path.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |

**被谁调用**

[analyze_debate_accuracy.main](#fn-analyze_debate_accuracy-py-main-114)

**完整源码**

~~~~python
def read_baseline_metrics(specs: Sequence[str], *, deal_size_tolerance: float) -> Dict[str, Dict[str, Any]]:
    """Read optional NAME=PATH baseline specs and score them for report rendering."""
    metrics: Dict[str, Dict[str, Any]] = {}
    for spec in specs:
        if "=" not in spec:
            raise ValueError(f"--baseline-results must use NAME=PATH format, got: {spec}")
        name, raw_path = spec.split("=", 1)
        name = name.strip()
        path = Path(raw_path.strip())
        if not name:
            raise ValueError(f"Baseline name is empty in spec: {spec}")
        rows = read_result_rows([path])
        metrics[name] = analyze_baseline_rows(rows, baseline_name=name, deal_size_tolerance=deal_size_tolerance)
    return metrics
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-build_case_sequence-225"></a>

### build_case_sequence

文件：`analyze_debate_accuracy.py:225-268`

**签名**

~~~~python
def build_case_sequence(case: Dict[str, Any], *, deal_size_tolerance: float) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Build one case's aggregate prediction sequence across bargaining stages.

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `deal_size_tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `stages.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `event.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `stage_snapshot` | [analyze_debate_accuracy.stage_snapshot](#fn-analyze_debate_accuracy-py-stage_snapshot-271) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `stage.startswith` | 库函数或对象方法 | 字符串前缀判断，返回 bool。 |
| `score_prediction` | [analyze_debate_accuracy.score_prediction](#fn-analyze_debate_accuracy-py-score_prediction-296) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `context.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `payload.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `stage.rsplit` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[analyze_debate_accuracy.analyze_cases](#fn-analyze_debate_accuracy-py-analyze_cases-163)

**完整源码**

~~~~python
def build_case_sequence(case: Dict[str, Any], *, deal_size_tolerance: float) -> Dict[str, Any]:
    """Build one case's aggregate prediction sequence across bargaining stages."""
    labels = case.get("labels", {}) if isinstance(case.get("labels", {}), dict) else {}
    context: Dict[str, Dict[str, Any]] = {}
    stages: List[Dict[str, Any]] = []

    for event in case.get("trace", []):
        stage = event.get("stage")
        actor = event.get("actor")
        payload = event.get("payload", {}) if isinstance(event.get("payload", {}), dict) else {}
        if stage == "private_assessment" and actor:
            context[str(actor)] = payload

    stages.append(stage_snapshot("initial", context, labels, deal_size_tolerance))

    context = {role: dict(decision) for role, decision in context.items()}
    for event in case.get("trace", []):
        stage = str(event.get("stage", ""))
        actor = str(event.get("actor", ""))
        payload = event.get("payload", {}) if isinstance(event.get("payload", {}), dict) else {}
        if stage.startswith("bargaining_round_"):
            updated = payload.get("updated_decision", {})
            if actor and isinstance(updated, dict):
                context[actor] = updated
            continue
        if stage.startswith("proposal_after_round_"):
            round_name = "round_" + stage.rsplit("_", 1)[-1]
            stages.append(stage_snapshot(round_name, context, labels, deal_size_tolerance))

    final_prediction = {
        "financing_initiation_decision": case.get("financing_initiation_decision"),
        "financing_completion_view": case.get("financing_completion_view"),
        "predicted_deal_size_usd_m": case.get("predicted_deal_size_usd_m"),
        "predicted_post_money_valuation_usd_m": case.get("predicted_post_money_valuation_usd_m"),
        "predicted_investor_ownership_pct": case.get("predicted_investor_ownership_pct"),
        "predicted_deal_type": case.get("predicted_deal_type"),
        "valuation_direction": case.get("valuation_direction"),
    }
    stages.append(score_prediction("final", final_prediction, labels, deal_size_tolerance))
    return {
        "case_id": case.get("case_id", ""),
        "labels": labels,
        "stages": stages,
    }
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-stage_snapshot-271"></a>

### stage_snapshot

文件：`analyze_debate_accuracy.py:271-293`

**签名**

~~~~python
def stage_snapshot( stage: str, context: Dict[str, Dict[str, Any]], labels: Dict[str, Any], deal_size_tolerance: float, ) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Aggregate role decisions and score the stage.

**参数**

| 参数 | 含义 |
| --- | --- |
| `stage` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |
| `labels` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `deal_size_tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `score_prediction` | [analyze_debate_accuracy.score_prediction](#fn-analyze_debate_accuracy-py-score_prediction-296) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `weighted_choice` | [analyze_debate_accuracy.weighted_choice](#fn-analyze_debate_accuracy-py-weighted_choice-636) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `aggregate_deal_size` | [analyze_debate_accuracy.aggregate_deal_size](#fn-analyze_debate_accuracy-py-aggregate_deal_size-648) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `aggregate_positive_numeric` | [analyze_debate_accuracy.aggregate_positive_numeric](#fn-analyze_debate_accuracy-py-aggregate_positive_numeric-653) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.build_case_sequence](#fn-analyze_debate_accuracy-py-build_case_sequence-225)

**完整源码**

~~~~python
def stage_snapshot(
    stage: str,
    context: Dict[str, Dict[str, Any]],
    labels: Dict[str, Any],
    deal_size_tolerance: float,
) -> Dict[str, Any]:
    """Aggregate role decisions and score the stage."""
    prediction = {
        "financing_initiation_decision": weighted_choice(context, "financing_intent", "raise_now"),
        "financing_completion_view": weighted_choice(context, "completion_view", "likely_complete"),
        "predicted_deal_size_usd_m": aggregate_deal_size(context),
        "predicted_post_money_valuation_usd_m": aggregate_positive_numeric(
            context,
            "predicted_post_money_valuation_usd_m",
        ),
        "predicted_investor_ownership_pct": aggregate_positive_numeric(
            context,
            "predicted_investor_ownership_pct",
        ),
        "predicted_deal_type": weighted_choice(context, "predicted_deal_type", "unknown"),
        "valuation_direction": weighted_choice(context, "valuation_direction", "flat"),
    }
    return score_prediction(stage, prediction, labels, deal_size_tolerance)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-score_prediction-296"></a>

### score_prediction

文件：`analyze_debate_accuracy.py:296-335`

**签名**

~~~~python
def score_prediction( stage: str, prediction: Dict[str, Any], labels: Dict[str, Any], deal_size_tolerance: float, ) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Score one aggregate prediction against labels.

**参数**

| 参数 | 含义 |
| --- | --- |
| `stage` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `prediction` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `labels` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `deal_size_tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `normalize` | [analyze_debate_accuracy.normalize](#fn-analyze_debate_accuracy-py-normalize-698) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `optional_float` | [analyze_debate_accuracy.optional_float](#fn-analyze_debate_accuracy-py-optional_float-782) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `prediction.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `labels.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `abs` | 库函数或对象方法 | 内置绝对值；返回数值。 |
| `is_missing_label` | [analyze_debate_accuracy.is_missing_label](#fn-analyze_debate_accuracy-py-is_missing_label-766) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.analyze_baseline_rows](#fn-analyze_debate_accuracy-py-analyze_baseline_rows-180)、[analyze_debate_accuracy.build_case_sequence](#fn-analyze_debate_accuracy-py-build_case_sequence-225)、[analyze_debate_accuracy.stage_snapshot](#fn-analyze_debate_accuracy-py-stage_snapshot-271)

**完整源码**

~~~~python
def score_prediction(
    stage: str,
    prediction: Dict[str, Any],
    labels: Dict[str, Any],
    deal_size_tolerance: float,
) -> Dict[str, Any]:
    """Score one aggregate prediction against labels."""
    scored: Dict[str, Any] = {"stage": stage, "prediction": prediction, "scores": {}}
    for metric in CATEGORICAL_METRICS:
        pred = normalize(prediction.get(metric["prediction_field"]), metric["normalizer"])
        label = normalize(labels.get(metric["label_field"]), metric["normalizer"])
        scored["scores"][metric["name"]] = {
            "prediction": pred,
            "label": label,
            "correct": None if is_missing_label(label) else pred == label,
        }

    for metric in NUMERIC_METRICS:
        predicted = optional_float(prediction.get(metric["prediction_field"]))
        true = optional_float(labels.get(metric["label_field"]))
        if predicted is None or true is None or true <= 0:
            numeric_score = {
                "prediction": predicted,
                "label": true,
                "correct": None,
                "abs_error": None,
                "abs_pct_error": None,
            }
        else:
            abs_error = abs(predicted - true)
            abs_pct_error = abs_error / true
            numeric_score = {
                "prediction": predicted,
                "label": true,
                "correct": abs_pct_error <= deal_size_tolerance,
                "abs_error": abs_error,
                "abs_pct_error": abs_pct_error,
            }
        scored["scores"][metric["name"]] = numeric_score
    return scored
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-compute_stage_metrics-338"></a>

### compute_stage_metrics

文件：`analyze_debate_accuracy.py:338-364`

**签名**

~~~~python
def compute_stage_metrics(case_sequences: Sequence[Dict[str, Any]], stage: str) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Compute accuracy and error metrics for one stage.

**参数**

| 参数 | 含义 |
| --- | --- |
| `case_sequences` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `stage` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `get_stage` | [analyze_debate_accuracy.get_stage](#fn-analyze_debate_accuracy-py-get_stage-628) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `sum` | 库函数或对象方法 | 内置求和；返回可迭代数值总和。 |
| `item.update` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `score.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `mean` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `median` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[analyze_debate_accuracy.analyze_cases](#fn-analyze_debate_accuracy-py-analyze_cases-163)、[analyze_debate_accuracy.analyze_baseline_rows](#fn-analyze_debate_accuracy-py-analyze_baseline_rows-180)

**完整源码**

~~~~python
def compute_stage_metrics(case_sequences: Sequence[Dict[str, Any]], stage: str) -> Dict[str, Any]:
    """Compute accuracy and error metrics for one stage."""
    snapshots = [get_stage(case, stage) for case in case_sequences]
    snapshots = [item for item in snapshots if item is not None]
    metrics: Dict[str, Any] = {"case_count": len(snapshots)}
    for metric in METRIC_NAMES:
        scores = [snapshot["scores"][metric] for snapshot in snapshots]
        evaluated = [score for score in scores if score["correct"] is not None]
        correct_count = sum(1 for score in evaluated if score["correct"])
        item = {
            "evaluated_count": len(evaluated),
            "skipped_count": len(scores) - len(evaluated),
            "correct_count": correct_count,
            "accuracy": correct_count / len(evaluated) if evaluated else None,
        }
        if metric in {item["name"] for item in NUMERIC_METRICS}:
            pct_errors = [score["abs_pct_error"] for score in scores if score.get("abs_pct_error") is not None]
            abs_errors = [score["abs_error"] for score in scores if score.get("abs_error") is not None]
            item.update(
                {
                    "mae_usd_m": mean(abs_errors) if abs_errors else None,
                    "mape": mean(pct_errors) if pct_errors else None,
                    "median_ape": median(pct_errors) if pct_errors else None,
                }
            )
        metrics[metric] = item
    return metrics
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-compute_transitions-367"></a>

### compute_transitions

文件：`analyze_debate_accuracy.py:367-403`

**签名**

~~~~python
def compute_transitions(case_sequences: Sequence[Dict[str, Any]], stages: Sequence[str]) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Compute correctness transitions between adjacent stages.

**参数**

| 参数 | 含义 |
| --- | --- |
| `case_sequences` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `stages` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `zip` | 库函数或对象方法 | 内置配对；返回按位置组合的迭代器。 |
| `Counter` | 库函数或对象方法 | collections 计数器；返回可统计频次的字典子类。 |
| `get_stage` | [analyze_debate_accuracy.get_stage](#fn-analyze_debate_accuracy-py-get_stage-628) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `transition_label` | [analyze_debate_accuracy.transition_label](#fn-analyze_debate_accuracy-py-transition_label-676) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `before_score.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `after_score.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `error_change_label` | [analyze_debate_accuracy.error_change_label](#fn-analyze_debate_accuracy-py-error_change_label-687) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `counter.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |

**被谁调用**

[analyze_debate_accuracy.analyze_cases](#fn-analyze_debate_accuracy-py-analyze_cases-163)

**完整源码**

~~~~python
def compute_transitions(case_sequences: Sequence[Dict[str, Any]], stages: Sequence[str]) -> Dict[str, Any]:
    """Compute correctness transitions between adjacent stages."""
    transitions: Dict[str, Any] = {}
    for before_stage, after_stage in zip(stages, stages[1:]):
        key = f"{before_stage}_to_{after_stage}"
        transitions[key] = {}
        for metric in METRIC_NAMES:
            counter: Counter[str] = Counter()
            error_counter: Counter[str] = Counter()
            evaluated = 0
            for case in case_sequences:
                before = get_stage(case, before_stage)
                after = get_stage(case, after_stage)
                if before is None or after is None:
                    continue
                before_score = before["scores"][metric]
                after_score = after["scores"][metric]
                before_correct = before_score["correct"]
                after_correct = after_score["correct"]
                if before_correct is None or after_correct is None:
                    counter["skipped"] += 1
                    continue
                evaluated += 1
                counter[transition_label(before_correct, after_correct)] += 1
                if metric in {item["name"] for item in NUMERIC_METRICS}:
                    before_error = before_score.get("abs_pct_error")
                    after_error = after_score.get("abs_pct_error")
                    error_counter[error_change_label(before_error, after_error)] += 1
            transitions[key][metric] = {
                "evaluated_count": evaluated,
                "skipped_count": counter["skipped"],
                "counts": dict(counter),
                "rates": {name: count / evaluated for name, count in counter.items() if name != "skipped" and evaluated},
                "numeric_error_change_counts": dict(error_counter) if metric in {item["name"] for item in NUMERIC_METRICS} else None,
                "deal_size_error_change_counts": dict(error_counter) if metric == "deal_size" else None,
            }
    return transitions
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-render_report-406"></a>

### render_report

文件：`analyze_debate_accuracy.py:406-457`

**签名**

~~~~python
def render_report( analysis: Dict[str, Any], *, deal_size_tolerance: float, baseline_metrics: Optional[Dict[str, Dict[str, Any]]] = None, ) -> str:
~~~~

**功能**

源码文档说明：Render a Markdown report.

**参数**

| 参数 | 含义 |
| --- | --- |
| `analysis` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `deal_size_tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |
| `baseline_metrics` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `lines.extend` | 库函数或对象方法 | 列表扩展方法；把多个元素追加到列表末尾，返回 None。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `render_stage_accuracy_table` | [analyze_debate_accuracy.render_stage_accuracy_table](#fn-analyze_debate_accuracy-py-render_stage_accuracy_table-460) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `render_transition_block` | [analyze_debate_accuracy.render_transition_block](#fn-analyze_debate_accuracy-py-render_transition_block-493) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `render_all_transitions` | [analyze_debate_accuracy.render_all_transitions](#fn-analyze_debate_accuracy-py-render_all_transitions-515) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `render_numeric_error_transitions` | [analyze_debate_accuracy.render_numeric_error_transitions](#fn-analyze_debate_accuracy-py-render_numeric_error_transitions-539) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `render_numeric_error_table` | [analyze_debate_accuracy.render_numeric_error_table](#fn-analyze_debate_accuracy-py-render_numeric_error_table-520) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `render_baseline_comparison_table` | [analyze_debate_accuracy.render_baseline_comparison_table](#fn-analyze_debate_accuracy-py-render_baseline_comparison_table-474) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.main](#fn-analyze_debate_accuracy-py-main-114)

**完整源码**

~~~~python
def render_report(
    analysis: Dict[str, Any],
    *,
    deal_size_tolerance: float,
    baseline_metrics: Optional[Dict[str, Dict[str, Any]]] = None,
) -> str:
    """Render a Markdown report."""
    section = 1
    lines = [
        "# 辩论前后准确度变化报告",
        "",
        f"- 分析案例数：{analysis['case_count']}",
        f"- 数值指标正确判定阈值：预测值相对真实值误差 <= {deal_size_tolerance:.0%}",
        "- 交易完成判断暂不计入准确率，因为当前样本几乎全部是已完成交易。",
        "",
        f"## {section}. 各阶段预测准确度",
        "",
        render_stage_accuracy_table(analysis["stage_metrics"], analysis["stages"]),
        "",
    ]
    section += 1
    if baseline_metrics:
        lines.extend(
            [
                f"## {section}. 单 Agent Baseline 对比",
                "",
                render_baseline_comparison_table(analysis, baseline_metrics),
                "",
            ]
        )
        section += 1

    lines.extend(
        [
        f"## {section}. 辩论前 vs 辩论后",
        "",
        render_transition_block(analysis["initial_to_final"], "initial_to_final"),
        "",
        f"## {section + 1}. 随辩论轮次推进的正确性变化",
        "",
        render_all_transitions(analysis["transitions"]),
        "",
        f"## {section + 2}. 数值预测是否更接近真实值",
        "",
        render_numeric_error_transitions(analysis["initial_to_final"], analysis["transitions"]),
        "",
        f"## {section + 3}. 数值误差随阶段变化",
        "",
        render_numeric_error_table(analysis["stage_metrics"], analysis["stages"]),
        ]
    )
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-render_stage_accuracy_table-460"></a>

### render_stage_accuracy_table

文件：`analyze_debate_accuracy.py:460-471`

**签名**

~~~~python
def render_stage_accuracy_table(stage_metrics: Dict[str, Any], stages: Sequence[str]) -> str:
~~~~

**功能**

源码文档说明：Render stage-level accuracy table.

**参数**

| 参数 | 含义 |
| --- | --- |
| `stage_metrics` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `stages` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `values.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `format_pct` | [analyze_debate_accuracy.format_pct](#fn-analyze_debate_accuracy-py-format_pct-800) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.render_report](#fn-analyze_debate_accuracy-py-render_report-406)

**完整源码**

~~~~python
def render_stage_accuracy_table(stage_metrics: Dict[str, Any], stages: Sequence[str]) -> str:
    """Render stage-level accuracy table."""
    headers = ["stage", "financing_initiation", "deal_type", "valuation_direction", "deal_size", "post_money_valuation", "investor_ownership"]
    lines = ["| " + " | ".join(headers) + " |", "| --- | ---: | ---: | ---: | ---: | ---: | ---: |"]
    for stage in stages:
        metrics = stage_metrics[stage]
        values = []
        for metric in headers[1:]:
            item = metrics[metric]
            values.append(f"{item['correct_count']}/{item['evaluated_count']} ({format_pct(item['accuracy'])})")
        lines.append(f"| {stage} | " + " | ".join(values) + " |")
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-render_baseline_comparison_table-474"></a>

### render_baseline_comparison_table

文件：`analyze_debate_accuracy.py:474-490`

**签名**

~~~~python
def render_baseline_comparison_table( analysis: Dict[str, Any], baseline_metrics: Dict[str, Dict[str, Any]], ) -> str:
~~~~

**功能**

源码文档说明：Render final multi-agent accuracy next to single-agent baselines.

**参数**

| 参数 | 含义 |
| --- | --- |
| `analysis` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `baseline_metrics` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `rows.extend` | 库函数或对象方法 | 列表扩展方法；把多个元素追加到列表末尾，返回 None。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `baseline_metrics.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `values.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `metrics.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `format_pct` | [analyze_debate_accuracy.format_pct](#fn-analyze_debate_accuracy-py-format_pct-800) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.render_report](#fn-analyze_debate_accuracy-py-render_report-406)

**完整源码**

~~~~python
def render_baseline_comparison_table(
    analysis: Dict[str, Any],
    baseline_metrics: Dict[str, Dict[str, Any]],
) -> str:
    """Render final multi-agent accuracy next to single-agent baselines."""
    headers = ["system", "rows", "financing_initiation", "deal_type", "valuation_direction", "deal_size", "post_money_valuation", "investor_ownership"]
    lines = ["| " + " | ".join(headers) + " |", "| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |"]
    final_stage = "final" if "final" in analysis["stage_metrics"] else analysis["stages"][-1]
    rows: List[Tuple[str, Dict[str, Any]]] = [("multi_agent_final", analysis["stage_metrics"][final_stage])]
    rows.extend(baseline_metrics.items())
    for name, metrics in rows:
        values = [name, str(metrics.get("case_count", 0))]
        for metric in headers[2:]:
            item = metrics[metric]
            values.append(f"{item['correct_count']}/{item['evaluated_count']} ({format_pct(item['accuracy'])})")
        lines.append("| " + " | ".join(values) + " |")
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-render_transition_block-493"></a>

### render_transition_block

文件：`analyze_debate_accuracy.py:493-512`

**签名**

~~~~python
def render_transition_block(transitions: Dict[str, Any], transition_key: str) -> str:
~~~~

**功能**

源码文档说明：Render one transition table.

**参数**

| 参数 | 含义 |
| --- | --- |
| `transitions` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `transition_key` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `transitions.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `count_rate` | [analyze_debate_accuracy.count_rate](#fn-analyze_debate_accuracy-py-count_rate-793) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `counts.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[analyze_debate_accuracy.render_report](#fn-analyze_debate_accuracy-py-render_report-406)、[analyze_debate_accuracy.render_all_transitions](#fn-analyze_debate_accuracy-py-render_all_transitions-515)

**完整源码**

~~~~python
def render_transition_block(transitions: Dict[str, Any], transition_key: str) -> str:
    """Render one transition table."""
    if transition_key not in transitions:
        return "无可用变化。"
    lines = [
        f"### {transition_key}",
        "",
        "| metric | wrong->correct | correct->wrong | correct->correct | wrong->wrong | evaluated |",
        "| --- | ---: | ---: | ---: | ---: | ---: |",
    ]
    for metric, item in transitions[transition_key].items():
        counts = item["counts"]
        evaluated = item["evaluated_count"]
        lines.append(
            f"| {metric} | {count_rate(counts.get('wrong_to_correct', 0), evaluated)} | "
            f"{count_rate(counts.get('correct_to_wrong', 0), evaluated)} | "
            f"{count_rate(counts.get('correct_to_correct', 0), evaluated)} | "
            f"{count_rate(counts.get('wrong_to_wrong', 0), evaluated)} | {evaluated} |"
        )
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-render_all_transitions-515"></a>

### render_all_transitions

文件：`analyze_debate_accuracy.py:515-517`

**签名**

~~~~python
def render_all_transitions(transitions: Dict[str, Any]) -> str:
~~~~

**功能**

源码文档说明：Render all adjacent-stage transition tables.

**参数**

| 参数 | 含义 |
| --- | --- |
| `transitions` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `render_transition_block` | [analyze_debate_accuracy.render_transition_block](#fn-analyze_debate_accuracy-py-render_transition_block-493) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.render_report](#fn-analyze_debate_accuracy-py-render_report-406)

**完整源码**

~~~~python
def render_all_transitions(transitions: Dict[str, Any]) -> str:
    """Render all adjacent-stage transition tables."""
    return "\n\n".join(render_transition_block(transitions, key) for key in transitions)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-render_numeric_error_table-520"></a>

### render_numeric_error_table

文件：`analyze_debate_accuracy.py:520-536`

**签名**

~~~~python
def render_numeric_error_table(stage_metrics: Dict[str, Any], stages: Sequence[str]) -> str:
~~~~

**功能**

源码文档说明：Render continuous numeric error by stage.

**参数**

| 参数 | 含义 |
| --- | --- |
| `stage_metrics` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `stages` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `metric.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `format_float` | [analyze_debate_accuracy.format_float](#fn-analyze_debate_accuracy-py-format_float-806) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `format_pct` | [analyze_debate_accuracy.format_pct](#fn-analyze_debate_accuracy-py-format_pct-800) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `item.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[analyze_debate_accuracy.render_report](#fn-analyze_debate_accuracy-py-render_report-406)

**完整源码**

~~~~python
def render_numeric_error_table(stage_metrics: Dict[str, Any], stages: Sequence[str]) -> str:
    """Render continuous numeric error by stage."""
    lines = [
        "| metric | stage | evaluated | MAE | MAPE | Median APE |",
        "| --- | --- | ---: | ---: | ---: | ---: |",
    ]
    for metric in NUMERIC_METRICS:
        name = metric["name"]
        unit = metric.get("unit", "")
        unit_suffix = f" {unit}" if unit else ""
        for stage in stages:
            item = stage_metrics[stage][name]
            lines.append(
                f"| {name} | {stage} | {item['evaluated_count']} | {format_float(item.get('mae_usd_m'))}{unit_suffix} | "
                f"{format_pct(item.get('mape'))} | {format_pct(item.get('median_ape'))} |"
            )
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-render_numeric_error_transitions-539"></a>

### render_numeric_error_transitions

文件：`analyze_debate_accuracy.py:539-565`

**签名**

~~~~python
def render_numeric_error_transitions(initial_to_final: Dict[str, Any], transitions: Dict[str, Any]) -> str:
~~~~

**功能**

源码文档说明：Render whether continuous numeric errors became closer or farther.

**参数**

| 参数 | 含义 |
| --- | --- |
| `initial_to_final` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `transitions` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `transitions.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `sum` | 库函数或对象方法 | 内置求和；返回可迭代数值总和。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `rows.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `item.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `counts.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `count_rate` | [analyze_debate_accuracy.count_rate](#fn-analyze_debate_accuracy-py-count_rate-793) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.render_report](#fn-analyze_debate_accuracy-py-render_report-406)

**完整源码**

~~~~python
def render_numeric_error_transitions(initial_to_final: Dict[str, Any], transitions: Dict[str, Any]) -> str:
    """Render whether continuous numeric errors became closer or farther."""
    rows: List[Tuple[str, str, Dict[str, Any]]] = []
    if "initial_to_final" in initial_to_final:
        for metric in NUMERIC_METRICS:
            name = metric["name"]
            rows.append((name, "initial_to_final", initial_to_final["initial_to_final"][name]))
    for key, item in transitions.items():
        for metric in NUMERIC_METRICS:
            name = metric["name"]
            rows.append((name, key, item[name]))
    if not rows:
        return "无可用数值误差变化。"

    lines = [
        "| metric | transition | closer | farther | same | evaluated |",
        "| --- | --- | ---: | ---: | ---: | ---: |",
    ]
    for metric, transition, item in rows:
        counts = item.get("numeric_error_change_counts") or item.get("deal_size_error_change_counts") or {}
        total = sum(counts.get(name, 0) for name in ("closer", "farther", "same"))
        lines.append(
            f"| {metric} | {transition} | {count_rate(counts.get('closer', 0), total)} | "
            f"{count_rate(counts.get('farther', 0), total)} | "
            f"{count_rate(counts.get('same', 0), total)} | {total} |"
        )
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-write_case_stage_csv-568"></a>

### write_case_stage_csv

文件：`analyze_debate_accuracy.py:568-575`

**签名**

~~~~python
def write_case_stage_csv(path: Path, rows: Iterable[Dict[str, Any]]) -> None:
~~~~

**功能**

源码文档说明：Write per-case stage-level correctness rows.

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `rows` | 结果行或 DataFrame 行集合。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `csv.DictWriter` | 库函数或对象方法 | 构造 CSV 字典写入器，返回 writer 对象。 |
| `writer.writeheader` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `writer.writerow` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[analyze_debate_accuracy.main](#fn-analyze_debate_accuracy-py-main-114)、[run_batch_experiments._write_debate_accuracy_report](#fn-run_batch_experiments-py-_write_debate_accuracy_report-569)

**完整源码**

~~~~python
def write_case_stage_csv(path: Path, rows: Iterable[Dict[str, Any]]) -> None:
    """Write per-case stage-level correctness rows."""
    path.parent.mkdir(parents=True, exist_ok=True)
    with path.open("w", encoding="utf-8", newline="") as handle:
        writer = csv.DictWriter(handle, fieldnames=CASE_CSV_FIELDS, extrasaction="ignore")
        writer.writeheader()
        for row in rows:
            writer.writerow(row)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-flatten_case_stage_rows-578"></a>

### flatten_case_stage_rows

文件：`analyze_debate_accuracy.py:578-612`

**签名**

~~~~python
def flatten_case_stage_rows(case_sequences: Sequence[Dict[str, Any]]) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Flatten case stage snapshots for CSV inspection.

**参数**

| 参数 | 含义 |
| --- | --- |
| `case_sequences` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `rows.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `snapshot.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[analyze_debate_accuracy.analyze_cases](#fn-analyze_debate_accuracy-py-analyze_cases-163)

**完整源码**

~~~~python
def flatten_case_stage_rows(case_sequences: Sequence[Dict[str, Any]]) -> List[Dict[str, Any]]:
    """Flatten case stage snapshots for CSV inspection."""
    rows: List[Dict[str, Any]] = []
    for case in case_sequences:
        for snapshot in case["stages"]:
            scores = snapshot["scores"]
            rows.append(
                {
                    "case_id": case["case_id"],
                    "stage": snapshot["stage"],
                    "financing_initiation_prediction": scores["financing_initiation"]["prediction"],
                    "financing_initiation_correct": scores["financing_initiation"]["correct"],
                    "financing_completion_prediction": snapshot["prediction"].get("financing_completion_view"),
                    "deal_type_prediction": scores["deal_type"]["prediction"],
                    "deal_type_correct": scores["deal_type"]["correct"],
                    "valuation_direction_prediction": scores["valuation_direction"]["prediction"],
                    "valuation_direction_correct": scores["valuation_direction"]["correct"],
                    "deal_size_prediction": scores["deal_size"]["prediction"],
                    "deal_size_true": scores["deal_size"]["label"],
                    "deal_size_abs_error": scores["deal_size"]["abs_error"],
                    "deal_size_abs_pct_error": scores["deal_size"]["abs_pct_error"],
                    "deal_size_within_tolerance": scores["deal_size"]["correct"],
                    "post_money_valuation_prediction": scores["post_money_valuation"]["prediction"],
                    "post_money_valuation_true": scores["post_money_valuation"]["label"],
                    "post_money_valuation_abs_error": scores["post_money_valuation"]["abs_error"],
                    "post_money_valuation_abs_pct_error": scores["post_money_valuation"]["abs_pct_error"],
                    "post_money_valuation_within_tolerance": scores["post_money_valuation"]["correct"],
                    "investor_ownership_prediction": scores["investor_ownership"]["prediction"],
                    "investor_ownership_true": scores["investor_ownership"]["label"],
                    "investor_ownership_abs_error": scores["investor_ownership"]["abs_error"],
                    "investor_ownership_abs_pct_error": scores["investor_ownership"]["abs_pct_error"],
                    "investor_ownership_within_tolerance": scores["investor_ownership"]["correct"],
                }
            )
    return rows
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-ordered_stages-615"></a>

### ordered_stages

文件：`analyze_debate_accuracy.py:615-625`

**签名**

~~~~python
def ordered_stages(case_sequences: Sequence[Dict[str, Any]]) -> List[str]:
~~~~

**功能**

源码文档说明：Return stable stage names across all cases.

**参数**

| 参数 | 含义 |
| --- | --- |
| `case_sequences` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `List[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `sorted` | 库函数或对象方法 | 内置排序；返回新的 list。 |
| `names.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `name.startswith` | 库函数或对象方法 | 字符串前缀判断，返回 bool。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `name.split` | 库函数或对象方法 | 字符串拆分，返回 list[str]。 |

**被谁调用**

[analyze_debate_accuracy.analyze_cases](#fn-analyze_debate_accuracy-py-analyze_cases-163)

**完整源码**

~~~~python
def ordered_stages(case_sequences: Sequence[Dict[str, Any]]) -> List[str]:
    """Return stable stage names across all cases."""
    names = []
    for case in case_sequences:
        for snapshot in case["stages"]:
            name = snapshot["stage"]
            if name not in names:
                names.append(name)
    round_names = sorted([name for name in names if name.startswith("round_")], key=lambda name: int(name.split("_")[1]))
    result = ["initial"] + round_names + ["final"]
    return [name for name in result if name in names]
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-get_stage-628"></a>

### get_stage

文件：`analyze_debate_accuracy.py:628-633`

**签名**

~~~~python
def get_stage(case: Dict[str, Any], stage: str) -> Optional[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Return one named stage snapshot.

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `stage` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[analyze_debate_accuracy.compute_stage_metrics](#fn-analyze_debate_accuracy-py-compute_stage_metrics-338)、[analyze_debate_accuracy.compute_transitions](#fn-analyze_debate_accuracy-py-compute_transitions-367)

**完整源码**

~~~~python
def get_stage(case: Dict[str, Any], stage: str) -> Optional[Dict[str, Any]]:
    """Return one named stage snapshot."""
    for snapshot in case["stages"]:
        if snapshot["stage"] == stage:
            return snapshot
    return None
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-weighted_choice-636"></a>

### weighted_choice

文件：`analyze_debate_accuracy.py:636-645`

**签名**

~~~~python
def weighted_choice(context: Dict[str, Dict[str, Any]], field: str, default: str) -> str:
~~~~

**功能**

源码文档说明：Weighted modal choice matching the simulator.

**参数**

| 参数 | 含义 |
| --- | --- |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |
| `field` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `Counter` | 库函数或对象方法 | collections 计数器；返回可统计频次的字典子类。 |
| `context.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `decision.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `ROLE_WEIGHTS.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `scores.most_common` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[analyze_debate_accuracy.stage_snapshot](#fn-analyze_debate_accuracy-py-stage_snapshot-271)

**完整源码**

~~~~python
def weighted_choice(context: Dict[str, Dict[str, Any]], field: str, default: str) -> str:
    """Weighted modal choice matching the simulator."""
    scores: Counter[str] = Counter()
    for role, decision in context.items():
        value = decision.get(field, default)
        if value:
            scores[str(value)] += ROLE_WEIGHTS.get(role, 1.0)
    if not scores:
        return default
    return scores.most_common(1)[0][0]
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-aggregate_deal_size-648"></a>

### aggregate_deal_size

文件：`analyze_debate_accuracy.py:648-650`

**签名**

~~~~python
def aggregate_deal_size(context: Dict[str, Dict[str, Any]]) -> Optional[float]:
~~~~

**功能**

源码文档说明：Weighted average deal size matching the simulator.

**参数**

| 参数 | 含义 |
| --- | --- |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `aggregate_positive_numeric` | [analyze_debate_accuracy.aggregate_positive_numeric](#fn-analyze_debate_accuracy-py-aggregate_positive_numeric-653) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.stage_snapshot](#fn-analyze_debate_accuracy-py-stage_snapshot-271)

**完整源码**

~~~~python
def aggregate_deal_size(context: Dict[str, Dict[str, Any]]) -> Optional[float]:
    """Weighted average deal size matching the simulator."""
    return aggregate_positive_numeric(context, "predicted_deal_size_usd_m")
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-aggregate_positive_numeric-653"></a>

### aggregate_positive_numeric

文件：`analyze_debate_accuracy.py:653-673`

**签名**

~~~~python
def aggregate_positive_numeric(context: Dict[str, Dict[str, Any]], field: str) -> Optional[float]:
~~~~

**功能**

源码文档说明：Weighted average of a positive numeric role prediction matching the simulator.

**参数**

| 参数 | 含义 |
| --- | --- |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |
| `field` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `context.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `round` | 库函数或对象方法 | 内置四舍五入；返回数值。 |
| `optional_float` | [analyze_debate_accuracy.optional_float](#fn-analyze_debate_accuracy-py-optional_float-782) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `ROLE_WEIGHTS.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `decision.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[analyze_debate_accuracy.stage_snapshot](#fn-analyze_debate_accuracy-py-stage_snapshot-271)、[analyze_debate_accuracy.aggregate_deal_size](#fn-analyze_debate_accuracy-py-aggregate_deal_size-648)

**完整源码**

~~~~python
def aggregate_positive_numeric(context: Dict[str, Dict[str, Any]], field: str) -> Optional[float]:
    """Weighted average of a positive numeric role prediction matching the simulator."""
    weighted_total = 0.0
    weight_total = 0.0
    observed_count = 0
    for role, decision in context.items():
        if field not in decision:
            continue
        value = optional_float(decision.get(field))
        if value is not None:
            observed_count += 1
        if value is None or value <= 0 or decision.get("financing_intent") == "avoid":
            continue
        weight = ROLE_WEIGHTS.get(role, 1.0)
        weighted_total += value * weight
        weight_total += weight
    if observed_count <= 0:
        return None
    if weight_total <= 0:
        return 0.0
    return round(weighted_total / weight_total, 6)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-transition_label-676"></a>

### transition_label

文件：`analyze_debate_accuracy.py:676-684`

**签名**

~~~~python
def transition_label(before_correct: bool, after_correct: bool) -> str:
~~~~

**功能**

源码文档说明：Name a correctness transition.

**参数**

| 参数 | 含义 |
| --- | --- |
| `before_correct` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `after_correct` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[analyze_debate_accuracy.compute_transitions](#fn-analyze_debate_accuracy-py-compute_transitions-367)

**完整源码**

~~~~python
def transition_label(before_correct: bool, after_correct: bool) -> str:
    """Name a correctness transition."""
    if not before_correct and after_correct:
        return "wrong_to_correct"
    if before_correct and not after_correct:
        return "correct_to_wrong"
    if before_correct and after_correct:
        return "correct_to_correct"
    return "wrong_to_wrong"
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-error_change_label-687"></a>

### error_change_label

文件：`analyze_debate_accuracy.py:687-695`

**签名**

~~~~python
def error_change_label(before_error: Optional[float], after_error: Optional[float], tolerance: float = 1e-9) -> str:
~~~~

**功能**

源码文档说明：Name continuous error movement.

**参数**

| 参数 | 含义 |
| --- | --- |
| `before_error` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `after_error` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[analyze_debate_accuracy.compute_transitions](#fn-analyze_debate_accuracy-py-compute_transitions-367)

**完整源码**

~~~~python
def error_change_label(before_error: Optional[float], after_error: Optional[float], tolerance: float = 1e-9) -> str:
    """Name continuous error movement."""
    if before_error is None or after_error is None:
        return "skipped"
    if after_error < before_error - tolerance:
        return "closer"
    if after_error > before_error + tolerance:
        return "farther"
    return "same"
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-normalize-698"></a>

### normalize

文件：`analyze_debate_accuracy.py:698-708`

**签名**

~~~~python
def normalize(value: Any, normalizer: str) -> Optional[str]:
~~~~

**功能**

源码文档说明：Normalize predictions and labels for comparison.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `normalizer` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing_value` | [analyze_debate_accuracy.is_missing_value](#fn-analyze_debate_accuracy-py-is_missing_value-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `normalize_token` | [analyze_debate_accuracy.normalize_token](#fn-analyze_debate_accuracy-py-normalize_token-711) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `normalize_completion` | [analyze_debate_accuracy.normalize_completion](#fn-analyze_debate_accuracy-py-normalize_completion-721) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `normalize_deal_type` | [analyze_debate_accuracy.normalize_deal_type](#fn-analyze_debate_accuracy-py-normalize_deal_type-735) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `normalize_valuation_direction` | [analyze_debate_accuracy.normalize_valuation_direction](#fn-analyze_debate_accuracy-py-normalize_valuation_direction-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.score_prediction](#fn-analyze_debate_accuracy-py-score_prediction-296)

**完整源码**

~~~~python
def normalize(value: Any, normalizer: str) -> Optional[str]:
    """Normalize predictions and labels for comparison."""
    if is_missing_value(value):
        return None
    if normalizer == "completion":
        return normalize_completion(value)
    if normalizer == "deal_type":
        return normalize_deal_type(value)
    if normalizer == "valuation_direction":
        return normalize_valuation_direction(value)
    return normalize_token(value)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-normalize_token-711"></a>

### normalize_token

文件：`analyze_debate_accuracy.py:711-718`

**签名**

~~~~python
def normalize_token(value: Any) -> Optional[str]:
~~~~

**功能**

源码文档说明：Normalize simple categorical text.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing_value` | [analyze_debate_accuracy.is_missing_value](#fn-analyze_debate_accuracy-py-is_missing_value-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `str.strip.lower.replace.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `text.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `str.strip.lower.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `str.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[analyze_debate_accuracy.normalize](#fn-analyze_debate_accuracy-py-normalize-698)、[analyze_debate_accuracy.normalize_completion](#fn-analyze_debate_accuracy-py-normalize_completion-721)、[analyze_debate_accuracy.normalize_valuation_direction](#fn-analyze_debate_accuracy-py-normalize_valuation_direction-752)

**完整源码**

~~~~python
def normalize_token(value: Any) -> Optional[str]:
    """Normalize simple categorical text."""
    if is_missing_value(value):
        return None
    text = str(value).strip().lower().replace("-", "_").replace(" ", "_")
    while "__" in text:
        text = text.replace("__", "_")
    return text or None
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-normalize_completion-721"></a>

### normalize_completion

文件：`analyze_debate_accuracy.py:721-732`

**签名**

~~~~python
def normalize_completion(value: Any) -> Optional[str]:
~~~~

**功能**

源码文档说明：Normalize deal completion labels.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `normalize_token` | [analyze_debate_accuracy.normalize_token](#fn-analyze_debate_accuracy-py-normalize_token-711) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.normalize](#fn-analyze_debate_accuracy-py-normalize-698)

**完整源码**

~~~~python
def normalize_completion(value: Any) -> Optional[str]:
    """Normalize deal completion labels."""
    token = normalize_token(value)
    if token is None:
        return None
    if token in {"completed", "complete", "closed", "done", "likely_complete"}:
        return "likely_complete"
    if token in {"cancelled", "canceled", "terminated", "failed", "dead", "unlikely_complete"}:
        return "unlikely_complete"
    if token in {"announced", "pending", "in_progress", "open", "uncertain"}:
        return "uncertain"
    return token
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-normalize_deal_type-735"></a>

### normalize_deal_type

文件：`analyze_debate_accuracy.py:735-749`

**签名**

~~~~python
def normalize_deal_type(value: Any) -> Optional[str]:
~~~~

**功能**

源码文档说明：Normalize deal-type labels.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing_value` | [analyze_debate_accuracy.is_missing_value](#fn-analyze_debate_accuracy-py-is_missing_value-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `aliases.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `str.strip.lower.replace.replace.split` | 库函数或对象方法 | 字符串拆分，返回 list[str]。 |
| `str.strip.lower.replace.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `str.strip.lower.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `str.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[analyze_debate_accuracy.normalize](#fn-analyze_debate_accuracy-py-normalize-698)

**完整源码**

~~~~python
def normalize_deal_type(value: Any) -> Optional[str]:
    """Normalize deal-type labels."""
    if is_missing_value(value):
        return None
    text = " ".join(str(value).strip().lower().replace("-", " ").replace("_", " ").split())
    aliases = {
        "seed": "seed round",
        "seed vc": "seed round",
        "early stage": "early stage vc",
        "early stage venture": "early stage vc",
        "later stage": "later stage vc",
        "later stage venture": "later stage vc",
        "venture capital": "other",
    }
    return aliases.get(text, text)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-normalize_valuation_direction-752"></a>

### normalize_valuation_direction

文件：`analyze_debate_accuracy.py:752-763`

**签名**

~~~~python
def normalize_valuation_direction(value: Any) -> Optional[str]:
~~~~

**功能**

源码文档说明：Normalize valuation-direction labels.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `normalize_token` | [analyze_debate_accuracy.normalize_token](#fn-analyze_debate_accuracy-py-normalize_token-711) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `is_missing_value` | [analyze_debate_accuracy.is_missing_value](#fn-analyze_debate_accuracy-py-is_missing_value-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `str.lower.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `str.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[analyze_debate_accuracy.normalize](#fn-analyze_debate_accuracy-py-normalize-698)

**完整源码**

~~~~python
def normalize_valuation_direction(value: Any) -> Optional[str]:
    """Normalize valuation-direction labels."""
    token = normalize_token(str(value).lower().replace(" round", "")) if not is_missing_value(value) else None
    if token in {"up", "upround", "up_round"}:
        return "up"
    if token in {"flat", "flattish", "flatround", "flat_round"}:
        return "flat"
    if token in {"down", "downround", "down_round"}:
        return "down"
    if token in {"unknown", "none", "nan", "n/a", "na"}:
        return None
    return token
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-is_missing_label-766"></a>

### is_missing_label

文件：`analyze_debate_accuracy.py:766-768`

**签名**

~~~~python
def is_missing_label(value: Optional[str]) -> bool:
~~~~

**功能**

源码文档说明：Return whether a normalized label should be skipped.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `bool` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[analyze_debate_accuracy.score_prediction](#fn-analyze_debate_accuracy-py-score_prediction-296)

**完整源码**

~~~~python
def is_missing_label(value: Optional[str]) -> bool:
    """Return whether a normalized label should be skipped."""
    return value is None or value in {"unknown", "none", "nan", "n/a", "na", "unknown_deal_type", "unknown deal type"}
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-is_missing_value-771"></a>

### is_missing_value

文件：`analyze_debate_accuracy.py:771-779`

**签名**

~~~~python
def is_missing_value(value: Any) -> bool:
~~~~

**功能**

源码文档说明：Return whether a raw value is missing.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `bool` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `math.isnan` | 库函数或对象方法 | 判断浮点 NaN，返回 bool。 |
| `value.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |

**被谁调用**

[analyze_debate_accuracy.normalize](#fn-analyze_debate_accuracy-py-normalize-698)、[analyze_debate_accuracy.normalize_token](#fn-analyze_debate_accuracy-py-normalize_token-711)、[analyze_debate_accuracy.normalize_deal_type](#fn-analyze_debate_accuracy-py-normalize_deal_type-735)、[analyze_debate_accuracy.normalize_valuation_direction](#fn-analyze_debate_accuracy-py-normalize_valuation_direction-752)、[analyze_debate_accuracy.optional_float](#fn-analyze_debate_accuracy-py-optional_float-782)

**完整源码**

~~~~python
def is_missing_value(value: Any) -> bool:
    """Return whether a raw value is missing."""
    if value is None:
        return True
    if isinstance(value, float) and math.isnan(value):
        return True
    if isinstance(value, str) and not value.strip():
        return True
    return False
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-optional_float-782"></a>

### optional_float

文件：`analyze_debate_accuracy.py:782-790`

**签名**

~~~~python
def optional_float(value: Any) -> Optional[float]:
~~~~

**功能**

源码文档说明：Parse a finite float.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing_value` | [analyze_debate_accuracy.is_missing_value](#fn-analyze_debate_accuracy-py-is_missing_value-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `math.isfinite` | 库函数或对象方法 | math 数学函数；返回数值或布尔判断。 |

**被谁调用**

[analyze_debate_accuracy.score_prediction](#fn-analyze_debate_accuracy-py-score_prediction-296)、[analyze_debate_accuracy.aggregate_positive_numeric](#fn-analyze_debate_accuracy-py-aggregate_positive_numeric-653)、[analyze_debate_accuracy.format_pct](#fn-analyze_debate_accuracy-py-format_pct-800)、[analyze_debate_accuracy.format_float](#fn-analyze_debate_accuracy-py-format_float-806)

**完整源码**

~~~~python
def optional_float(value: Any) -> Optional[float]:
    """Parse a finite float."""
    if is_missing_value(value):
        return None
    try:
        number = float(value)
    except (TypeError, ValueError):
        return None
    return number if math.isfinite(number) else None
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-count_rate-793"></a>

### count_rate

文件：`analyze_debate_accuracy.py:793-797`

**签名**

~~~~python
def count_rate(count: int, total: int) -> str:
~~~~

**功能**

源码文档说明：Format count and rate.

**参数**

| 参数 | 含义 |
| --- | --- |
| `count` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `total` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[analyze_debate_accuracy.render_transition_block](#fn-analyze_debate_accuracy-py-render_transition_block-493)、[analyze_debate_accuracy.render_numeric_error_transitions](#fn-analyze_debate_accuracy-py-render_numeric_error_transitions-539)

**完整源码**

~~~~python
def count_rate(count: int, total: int) -> str:
    """Format count and rate."""
    if total <= 0:
        return f"{count} (n/a)"
    return f"{count} ({count / total:.2%})"
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-format_pct-800"></a>

### format_pct

文件：`analyze_debate_accuracy.py:800-803`

**签名**

~~~~python
def format_pct(value: Any) -> str:
~~~~

**功能**

源码文档说明：Format a nullable float percentage.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `optional_float` | [analyze_debate_accuracy.optional_float](#fn-analyze_debate_accuracy-py-optional_float-782) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.render_stage_accuracy_table](#fn-analyze_debate_accuracy-py-render_stage_accuracy_table-460)、[analyze_debate_accuracy.render_baseline_comparison_table](#fn-analyze_debate_accuracy-py-render_baseline_comparison_table-474)、[analyze_debate_accuracy.render_numeric_error_table](#fn-analyze_debate_accuracy-py-render_numeric_error_table-520)

**完整源码**

~~~~python
def format_pct(value: Any) -> str:
    """Format a nullable float percentage."""
    number = optional_float(value)
    return "n/a" if number is None else f"{number:.2%}"
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_accuracy-py-format_float-806"></a>

### format_float

文件：`analyze_debate_accuracy.py:806-809`

**签名**

~~~~python
def format_float(value: Any) -> str:
~~~~

**功能**

源码文档说明：Format a nullable float.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `optional_float` | [analyze_debate_accuracy.optional_float](#fn-analyze_debate_accuracy-py-optional_float-782) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_accuracy.render_numeric_error_table](#fn-analyze_debate_accuracy-py-render_numeric_error_table-520)

**完整源码**

~~~~python
def format_float(value: Any) -> str:
    """Format a nullable float."""
    number = optional_float(value)
    return "n/a" if number is None else f"{number:.3f}"
~~~~

[回到顶部](#top)

<a id="module-analyze_debate_changes-py"></a>

## analyze_debate_changes.py

讨论变化分析脚本：统计角色观点、数值预测和最终提案在讨论中的变化。

**类**：[FieldChange](#class-analyze_debate_changes-py-fieldchange-64)、[NumericChange](#class-analyze_debate_changes-py-numericchange-76)

<a id="fn-analyze_debate_changes-py-parse_args-88"></a>

### parse_args

文件：`analyze_debate_changes.py:88-110`

**签名**

~~~~python
def parse_args() -> argparse.Namespace:
~~~~

**功能**

源码文档说明：Parse command-line arguments.

**参数**

无显式参数。

**返回值**

返回 `argparse.Namespace` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `argparse.ArgumentParser` | 库函数或对象方法 | 构造命令行参数解析器，返回 ArgumentParser。 |
| `parser.add_argument` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `parser.parse_args` | [analyze_debate_changes.parse_args](#fn-analyze_debate_changes-py-parse_args-88) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_changes.parse_args](#fn-analyze_debate_changes-py-parse_args-88)、[analyze_debate_changes.main](#fn-analyze_debate_changes-py-main-113)

**完整源码**

~~~~python
def parse_args() -> argparse.Namespace:
    """Parse command-line arguments."""
    parser = argparse.ArgumentParser(description="Analyze prediction changes in boardroom trace files.")
    parser.add_argument(
        "--input",
        type=Path,
        required=True,
        help="Trace JSON file, usually outputs/.../run_001/traces.json.",
    )
    parser.add_argument(
        "--output",
        type=Path,
        default=None,
        help="Optional Markdown report output path. If omitted, prints only to stdout.",
    )
    parser.add_argument(
        "--case-csv-output",
        type=Path,
        default=None,
        help="Optional CSV with one row per case summarizing debate changes.",
    )
    parser.add_argument("--top-cases", type=int, default=12, help="Number of most changed cases to show.")
    return parser.parse_args()
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-main-113"></a>

### main

文件：`analyze_debate_changes.py:113-126`

**签名**

~~~~python
def main() -> None:
~~~~

**功能**

源码文档说明：Read traces, analyze debate changes, and write a human-readable report.

**参数**

无显式参数。

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `parse_args` | [analyze_debate_changes.parse_args](#fn-analyze_debate_changes-py-parse_args-88) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `read_trace_cases` | [analyze_debate_changes.read_trace_cases](#fn-analyze_debate_changes-py-read_trace_cases-129) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `analyze_cases` | [analyze_debate_changes.analyze_cases](#fn-analyze_debate_changes-py-analyze_cases-161) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `render_report` | [analyze_debate_changes.render_report](#fn-analyze_debate_changes-py-render_report-282) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |
| `args.output.parent.mkdir` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `args.output.write_text` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `write_case_csv` | [analyze_debate_changes.write_case_csv](#fn-analyze_debate_changes-py-write_case_csv-361) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def main() -> None:
    """Read traces, analyze debate changes, and write a human-readable report."""
    args = parse_args()
    cases = read_trace_cases(args.input)
    analysis = analyze_cases(cases)
    report = render_report(analysis, top_cases=args.top_cases)
    print(report)
    if args.output is not None:
        args.output.parent.mkdir(parents=True, exist_ok=True)
        args.output.write_text(report + "\n", encoding="utf-8")
        print(f"\nWrote debate-change report to {args.output}.")
    if args.case_csv_output is not None:
        write_case_csv(args.case_csv_output, analysis["case_summaries"])
        print(f"Wrote case-level debate-change CSV to {args.case_csv_output}.")
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-read_trace_cases-129"></a>

### read_trace_cases

文件：`analyze_debate_changes.py:129-158`

**签名**

~~~~python
def read_trace_cases(path: Path) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Read a trace JSON file with embedded per-case traces.

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.suffix.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `json.loads` | 库函数或对象方法 | 把 JSON 字符串解析为 Python 对象，通常返回 dict 或 list。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `item.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `enumerate` | 库函数或对象方法 | 内置枚举；返回 index 和元素组成的迭代器。 |
| `path.read_text` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `line.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `cases.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |

**被谁调用**

[analyze_debate_changes.main](#fn-analyze_debate_changes-py-main-113)

**完整源码**

~~~~python
def read_trace_cases(path: Path) -> List[Dict[str, Any]]:
    """Read a trace JSON file with embedded per-case traces."""
    if path.suffix.lower() == ".jsonl":
        cases = []
        with path.open("r", encoding="utf-8") as handle:
            for line_number, line in enumerate(handle, start=1):
                stripped = line.strip()
                if not stripped:
                    continue
                item = json.loads(stripped)
                if not isinstance(item, dict):
                    raise ValueError(f"Expected object on line {line_number} of {path}")
                cases.append(item)
    else:
        data = json.loads(path.read_text(encoding="utf-8"))
        if isinstance(data, list):
            cases = data
        elif isinstance(data, dict):
            cases = [data]
        else:
            raise ValueError(f"Expected a JSON object or list in {path}")

    missing_trace = [item.get("case_id", "<unknown>") for item in cases if not item.get("trace")]
    if missing_trace:
        sample = ", ".join(missing_trace[:5])
        raise ValueError(
            "Input does not contain detailed traces. Use traces.json, not compact results.jsonl. "
            f"Missing trace for {len(missing_trace)} case(s), including: {sample}"
        )
    return cases
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-analyze_cases-161"></a>

### analyze_cases

文件：`analyze_debate_changes.py:161-279`

**签名**

~~~~python
def analyze_cases(cases: Sequence[Dict[str, Any]]) -> Dict[str, Any]:
~~~~

**功能**

统计角色观点和最终提案在讨论过程中的变化方向、变化幅度和典型案例。

**参数**

| 参数 | 含义 |
| --- | --- |
| `cases` | 多个 BoardCase 或 trace case，用于批量运行或统计。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `Counter` | 库函数或对象方法 | collections 计数器；返回可统计频次的字典子类。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `_initial_role_decisions` | [analyze_debate_changes._initial_role_decisions](#fn-analyze_debate_changes-py-_initial_role_decisions-371) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_final_role_decisions` | [analyze_debate_changes._final_role_decisions](#fn-analyze_debate_changes-py-_final_role_decisions-382) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `sorted` | 库函数或对象方法 | 内置排序；返回新的 list。 |
| `_round_level_changes` | [analyze_debate_changes._round_level_changes](#fn-analyze_debate_changes-py-_round_level_changes-399) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_initial_proposal` | [analyze_debate_changes._initial_proposal](#fn-analyze_debate_changes-py-_initial_proposal-387) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_final_proposal` | [analyze_debate_changes._final_proposal](#fn-analyze_debate_changes-py-_final_proposal-394) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `case_summaries.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `initial_decisions.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `final_decisions.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `_optional_float` | [analyze_debate_changes._optional_float](#fn-analyze_debate_changes-py-_optional_float-532) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_numeric_changed` | [analyze_debate_changes._numeric_changed](#fn-analyze_debate_changes-py-_numeric_changed-542) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `set` | 库函数或对象方法 | 内置容器构造；返回 set。 |
| `role_field_changes.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `role_numeric_changes.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `_normalized` | [analyze_debate_changes._normalized](#fn-analyze_debate_changes-py-_normalized-524) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `proposal_field_changes.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `initial_proposal.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `final_proposal.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `proposal_numeric_changes.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `_delta` | [analyze_debate_changes._delta](#fn-analyze_debate_changes-py-_delta-550) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `labels.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `FieldChange` | [analyze_debate_changes.FieldChange](#class-analyze_debate_changes-py-fieldchange-64) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `before.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `after.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `NumericChange` | [analyze_debate_changes.NumericChange](#class-analyze_debate_changes-py-numericchange-76) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_changes.main](#fn-analyze_debate_changes-py-main-113)、[run_batch_experiments._write_debate_accuracy_report](#fn-run_batch_experiments-py-_write_debate_accuracy_report-569)

**完整源码**

~~~~python
def analyze_cases(cases: Sequence[Dict[str, Any]]) -> Dict[str, Any]:
    """Analyze role-level and proposal-level changes across cases."""
    role_field_changes: List[FieldChange] = []
    role_numeric_changes: List[NumericChange] = []
    round_field_changes: Counter[Tuple[str, str]] = Counter()
    round_numeric_changes: Counter[Tuple[str, str]] = Counter()
    proposal_field_changes: List[FieldChange] = []
    proposal_numeric_changes: List[NumericChange] = []
    case_summaries: List[Dict[str, Any]] = []

    for case in cases:
        case_id = str(case.get("case_id", "unknown_case"))
        company_name = str(case.get("company_name", ""))
        initial_decisions = _initial_role_decisions(case)
        final_decisions = _final_role_decisions(case)
        role_order = sorted(set(initial_decisions) | set(final_decisions))

        case_role_field_changes = 0
        case_role_size_changes = 0
        case_role_satisfaction_changes = 0
        case_proposal_field_changes = 0

        for role in role_order:
            before = initial_decisions.get(role, {})
            after = final_decisions.get(role, {})
            for field in ROLE_FIELDS:
                if _normalized(before.get(field)) != _normalized(after.get(field)):
                    change = FieldChange(case_id, role, field, before.get(field), after.get(field), "initial_to_final")
                    role_field_changes.append(change)
                    case_role_field_changes += 1
            for field in NUMERIC_ROLE_FIELDS:
                before_value = _optional_float(before.get(field))
                after_value = _optional_float(after.get(field))
                if _numeric_changed(before_value, after_value):
                    change = NumericChange(
                        case_id=case_id,
                        role=role,
                        field=field,
                        before=before_value,
                        after=after_value,
                        delta=_delta(before_value, after_value),
                        round_label="initial_to_final",
                    )
                    role_numeric_changes.append(change)
                    if field == "predicted_deal_size_usd_m":
                        case_role_size_changes += 1
                    elif field == "satisfaction_score":
                        case_role_satisfaction_changes += 1

        for change in _round_level_changes(case, initial_decisions):
            if isinstance(change, FieldChange):
                role_field_changes.append(change)
                round_field_changes[(change.round_label, change.field)] += 1
            else:
                role_numeric_changes.append(change)
                round_numeric_changes[(change.round_label, change.field)] += 1

        initial_proposal = _initial_proposal(case)
        final_proposal = _final_proposal(case)
        for field in PROPOSAL_FIELDS:
            if _normalized(initial_proposal.get(field)) != _normalized(final_proposal.get(field)):
                proposal_field_changes.append(
                    FieldChange(case_id, "system", field, initial_proposal.get(field), final_proposal.get(field), "initial_to_final")
                )
                case_proposal_field_changes += 1
        for field in NUMERIC_PROPOSAL_FIELDS:
            before_value = _optional_float(initial_proposal.get(field))
            after_value = _optional_float(final_proposal.get(field))
            if _numeric_changed(before_value, after_value):
                proposal_numeric_changes.append(
                    NumericChange(
                        case_id=case_id,
                        role="system",
                        field=field,
                        before=before_value,
                        after=after_value,
                        delta=_delta(before_value, after_value),
                        round_label="initial_to_final",
                    )
                )

        labels = case.get("labels", {}) if isinstance(case.get("labels", {}), dict) else {}
        case_summaries.append(
            {
                "case_id": case_id,
                "company_name": company_name,
                "role_field_changes": case_role_field_changes,
                "role_size_changes": case_role_size_changes,
                "role_satisfaction_changes": case_role_satisfaction_changes,
                "proposal_field_changes": case_proposal_field_changes,
                "proposal_size_delta": _delta(
                    _optional_float(initial_proposal.get("recommended_deal_size_usd_m")),
                    _optional_float(final_proposal.get("recommended_deal_size_usd_m")),
                ),
                "initial_proposal_size": _optional_float(initial_proposal.get("recommended_deal_size_usd_m")),
                "final_proposal_size": _optional_float(final_proposal.get("recommended_deal_size_usd_m")),
                "initial_proposal_type": initial_proposal.get("recommended_deal_type"),
                "final_proposal_type": final_proposal.get("recommended_deal_type"),
                "initial_proposal_valuation": initial_proposal.get("valuation_direction"),
                "final_proposal_valuation": final_proposal.get("valuation_direction"),
                "final_deal_type": case.get("predicted_deal_type"),
                "true_deal_type": labels.get("real_deal_type"),
                "final_valuation_direction": case.get("valuation_direction"),
                "true_valuation_direction": labels.get("real_valuation_direction_label"),
                "final_deal_size_usd_m": _optional_float(case.get("predicted_deal_size_usd_m")),
                "true_deal_size_usd_m": _optional_float(labels.get("real_deal_size_usd_m")),
            }
        )

    return {
        "case_count": len(cases),
        "role_field_changes": role_field_changes,
        "role_numeric_changes": role_numeric_changes,
        "round_field_changes": round_field_changes,
        "round_numeric_changes": round_numeric_changes,
        "proposal_field_changes": proposal_field_changes,
        "proposal_numeric_changes": proposal_numeric_changes,
        "case_summaries": case_summaries,
    }
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-render_report-282"></a>

### render_report

文件：`analyze_debate_changes.py:282-358`

**签名**

~~~~python
def render_report(analysis: Dict[str, Any], *, top_cases: int) -> str:
~~~~

**功能**

源码文档说明：Render a Markdown report describing debate-induced changes.

**参数**

| 参数 | 含义 |
| --- | --- |
| `analysis` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `top_cases` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `_render_counter_table` | [analyze_debate_changes._render_counter_table](#fn-analyze_debate_changes-py-_render_counter_table-434) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_render_transition_table` | [analyze_debate_changes._render_transition_table](#fn-analyze_debate_changes-py-_render_transition_table-443) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_render_numeric_summary` | [analyze_debate_changes._render_numeric_summary](#fn-analyze_debate_changes-py-_render_numeric_summary-453) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_render_round_table` | [analyze_debate_changes._render_round_table](#fn-analyze_debate_changes-py-_render_round_table-478) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_render_top_cases` | [analyze_debate_changes._render_top_cases](#fn-analyze_debate_changes-py-_render_top_cases-495) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `Counter` | 库函数或对象方法 | collections 计数器；返回可统计频次的字典子类。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |

**被谁调用**

[analyze_debate_changes.main](#fn-analyze_debate_changes-py-main-113)

**完整源码**

~~~~python
def render_report(analysis: Dict[str, Any], *, top_cases: int) -> str:
    """Render a Markdown report describing debate-induced changes."""
    case_count = analysis["case_count"]
    role_field_changes: List[FieldChange] = analysis["role_field_changes"]
    role_numeric_changes: List[NumericChange] = analysis["role_numeric_changes"]
    proposal_field_changes: List[FieldChange] = analysis["proposal_field_changes"]
    proposal_numeric_changes: List[NumericChange] = analysis["proposal_numeric_changes"]
    case_summaries: List[Dict[str, Any]] = analysis["case_summaries"]

    final_role_field_changes = [item for item in role_field_changes if item.round_label == "initial_to_final"]
    final_role_numeric_changes = [item for item in role_numeric_changes if item.round_label == "initial_to_final"]
    size_changes = [item for item in final_role_numeric_changes if item.field == "predicted_deal_size_usd_m"]
    post_money_changes = [item for item in final_role_numeric_changes if item.field == "predicted_post_money_valuation_usd_m"]
    ownership_changes = [item for item in final_role_numeric_changes if item.field == "predicted_investor_ownership_pct"]
    satisfaction_changes = [item for item in final_role_numeric_changes if item.field == "satisfaction_score"]

    lines = [
        "# 辩论变化分析报告",
        "",
        "## 总览",
        "",
        f"- 分析案例数：{case_count}",
        f"- 角色分类字段在辩论前后发生变化：{len(final_role_field_changes)} 次",
        f"- 角色预测融资额在辩论前后发生变化：{len(size_changes)} 次",
        f"- 角色预测投后估值在辩论前后发生变化：{len(post_money_changes)} 次",
        f"- 角色预测投资人持股在辩论前后发生变化：{len(ownership_changes)} 次",
        f"- 角色满意度在辩论前后发生变化：{len(satisfaction_changes)} 次",
        f"- 最终提案分类字段在辩论前后发生变化：{len(proposal_field_changes)} 次",
        f"- 最终提案数值字段在辩论前后发生变化：{len(proposal_numeric_changes)} 次",
        "",
        "解释：这里的“辩论前后”指每个角色的初始 private assessment 与全部 bargaining 结束后的最终 role decision 之间的差异。",
        "",
        "## 角色字段变化",
        "",
        _render_counter_table(
            "按字段统计",
            Counter(change.field for change in final_role_field_changes),
            ["field", "changes"],
        ),
        "",
        _render_counter_table(
            "按角色统计",
            Counter(change.role for change in final_role_field_changes),
            ["role", "changes"],
        ),
        "",
        _render_transition_table("分类字段变化方向", final_role_field_changes),
        "",
        "## 数值字段变化",
        "",
        _render_numeric_summary("角色预测融资额", size_changes),
        "",
        _render_numeric_summary("角色预测投后估值", post_money_changes),
        "",
        _render_numeric_summary("角色预测投资人持股", ownership_changes),
        "",
        _render_numeric_summary("角色满意度", satisfaction_changes),
        "",
        "## 最终提案变化",
        "",
        _render_counter_table(
            "提案分类字段变化",
            Counter(change.field for change in proposal_field_changes),
            ["field", "changes"],
        ),
        "",
        _render_numeric_summary("提案数值字段变化", proposal_numeric_changes),
        "",
        "## 分轮变化",
        "",
        _render_round_table(analysis["round_field_changes"], analysis["round_numeric_changes"]),
        "",
        "## 变化最多的案例",
        "",
        _render_top_cases(case_summaries, top_cases),
    ]
    return "\n".join(line for line in lines if line is not None)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-write_case_csv-361"></a>

### write_case_csv

文件：`analyze_debate_changes.py:361-368`

**签名**

~~~~python
def write_case_csv(path: Path, rows: Iterable[Dict[str, Any]]) -> None:
~~~~

**功能**

源码文档说明：Write case-level debate summaries to CSV.

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `rows` | 结果行或 DataFrame 行集合。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `csv.DictWriter` | 库函数或对象方法 | 构造 CSV 字典写入器，返回 writer 对象。 |
| `writer.writeheader` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `writer.writerow` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[analyze_debate_changes.main](#fn-analyze_debate_changes-py-main-113)

**完整源码**

~~~~python
def write_case_csv(path: Path, rows: Iterable[Dict[str, Any]]) -> None:
    """Write case-level debate summaries to CSV."""
    path.parent.mkdir(parents=True, exist_ok=True)
    with path.open("w", encoding="utf-8", newline="") as handle:
        writer = csv.DictWriter(handle, fieldnames=CASE_CSV_FIELDS, extrasaction="ignore")
        writer.writeheader()
        for row in rows:
            writer.writerow(row)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_initial_role_decisions-371"></a>

### _initial_role_decisions

文件：`analyze_debate_changes.py:371-379`

**签名**

~~~~python
def _initial_role_decisions(case: Dict[str, Any]) -> Dict[str, Dict[str, Any]]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |

**返回值**

返回 `Dict[str, Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `event.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |

**被谁调用**

[analyze_debate_changes.analyze_cases](#fn-analyze_debate_changes-py-analyze_cases-161)

**完整源码**

~~~~python
def _initial_role_decisions(case: Dict[str, Any]) -> Dict[str, Dict[str, Any]]:
    decisions: Dict[str, Dict[str, Any]] = {}
    for event in case.get("trace", []):
        if event.get("stage") == "private_assessment":
            actor = str(event.get("actor", ""))
            payload = event.get("payload", {})
            if actor and isinstance(payload, dict):
                decisions[actor] = payload
    return decisions
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_final_role_decisions-382"></a>

### _final_role_decisions

文件：`analyze_debate_changes.py:382-384`

**签名**

~~~~python
def _final_role_decisions(case: Dict[str, Any]) -> Dict[str, Dict[str, Any]]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |

**返回值**

返回 `Dict[str, Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |

**被谁调用**

[analyze_debate_changes.analyze_cases](#fn-analyze_debate_changes-py-analyze_cases-161)

**完整源码**

~~~~python
def _final_role_decisions(case: Dict[str, Any]) -> Dict[str, Dict[str, Any]]:
    role_decisions = case.get("role_decisions", {})
    return role_decisions if isinstance(role_decisions, dict) else {}
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_initial_proposal-387"></a>

### _initial_proposal

文件：`analyze_debate_changes.py:387-391`

**签名**

~~~~python
def _initial_proposal(case: Dict[str, Any]) -> Dict[str, Any]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `event.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[analyze_debate_changes.analyze_cases](#fn-analyze_debate_changes-py-analyze_cases-161)

**完整源码**

~~~~python
def _initial_proposal(case: Dict[str, Any]) -> Dict[str, Any]:
    for event in case.get("trace", []):
        if event.get("stage") == "initial_proposal" and isinstance(event.get("payload"), dict):
            return event["payload"]
    return {}
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_final_proposal-394"></a>

### _final_proposal

文件：`analyze_debate_changes.py:394-396`

**签名**

~~~~python
def _final_proposal(case: Dict[str, Any]) -> Dict[str, Any]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |

**被谁调用**

[analyze_debate_changes.analyze_cases](#fn-analyze_debate_changes-py-analyze_cases-161)

**完整源码**

~~~~python
def _final_proposal(case: Dict[str, Any]) -> Dict[str, Any]:
    proposal = case.get("proposal", {})
    return proposal if isinstance(proposal, dict) else {}
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_round_level_changes-399"></a>

### _round_level_changes

文件：`analyze_debate_changes.py:399-431`

**签名**

~~~~python
def _round_level_changes(case: Dict[str, Any], initial_decisions: Dict[str, Dict[str, Any]]) -> List[FieldChange | NumericChange]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `initial_decisions` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `List[FieldChange | NumericChange]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `payload.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `previous_by_role.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `initial_decisions.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `event.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `stage.startswith` | 库函数或对象方法 | 字符串前缀判断，返回 bool。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `_optional_float` | [analyze_debate_changes._optional_float](#fn-analyze_debate_changes-py-_optional_float-532) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_numeric_changed` | [analyze_debate_changes._numeric_changed](#fn-analyze_debate_changes-py-_numeric_changed-542) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_normalized` | [analyze_debate_changes._normalized](#fn-analyze_debate_changes-py-_normalized-524) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `changes.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `before.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `updated.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `FieldChange` | [analyze_debate_changes.FieldChange](#class-analyze_debate_changes-py-fieldchange-64) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `NumericChange` | [analyze_debate_changes.NumericChange](#class-analyze_debate_changes-py-numericchange-76) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_delta` | [analyze_debate_changes._delta](#fn-analyze_debate_changes-py-_delta-550) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_changes.analyze_cases](#fn-analyze_debate_changes-py-analyze_cases-161)

**完整源码**

~~~~python
def _round_level_changes(case: Dict[str, Any], initial_decisions: Dict[str, Dict[str, Any]]) -> List[FieldChange | NumericChange]:
    changes: List[FieldChange | NumericChange] = []
    previous_by_role = {role: dict(decision) for role, decision in initial_decisions.items()}
    for event in case.get("trace", []):
        stage = str(event.get("stage", ""))
        if not stage.startswith("bargaining_round_"):
            continue
        actor = str(event.get("actor", ""))
        payload = event.get("payload", {}) if isinstance(event.get("payload", {}), dict) else {}
        updated = payload.get("updated_decision", {})
        if not actor or not isinstance(updated, dict):
            continue
        before = previous_by_role.get(actor, {})
        for field in ROLE_FIELDS:
            if _normalized(before.get(field)) != _normalized(updated.get(field)):
                changes.append(FieldChange(str(case.get("case_id", "")), actor, field, before.get(field), updated.get(field), stage))
        for field in NUMERIC_ROLE_FIELDS:
            before_value = _optional_float(before.get(field))
            after_value = _optional_float(updated.get(field))
            if _numeric_changed(before_value, after_value):
                changes.append(
                    NumericChange(
                        case_id=str(case.get("case_id", "")),
                        role=actor,
                        field=field,
                        before=before_value,
                        after=after_value,
                        delta=_delta(before_value, after_value),
                        round_label=stage,
                    )
                )
        previous_by_role[actor] = updated
    return changes
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_render_counter_table-434"></a>

### _render_counter_table

文件：`analyze_debate_changes.py:434-440`

**签名**

~~~~python
def _render_counter_table(title: str, counter: Counter[Any], headers: Sequence[str]) -> str:
~~~~

**功能**

渲染函数。它把结构化结果转换为 Markdown/HTML/表格文本，方便人类阅读实验过程或指标。

**参数**

| 参数 | 含义 |
| --- | --- |
| `title` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `counter` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `headers` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `counter.most_common` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |

**被谁调用**

[analyze_debate_changes.render_report](#fn-analyze_debate_changes-py-render_report-282)

**完整源码**

~~~~python
def _render_counter_table(title: str, counter: Counter[Any], headers: Sequence[str]) -> str:
    if not counter:
        return f"### {title}\n\n无变化。"
    lines = [f"### {title}", "", f"| {headers[0]} | {headers[1]} |", "| --- | ---: |"]
    for key, count in counter.most_common():
        lines.append(f"| {key} | {count} |")
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_render_transition_table-443"></a>

### _render_transition_table

文件：`analyze_debate_changes.py:443-450`

**签名**

~~~~python
def _render_transition_table(title: str, changes: Sequence[FieldChange]) -> str:
~~~~

**功能**

渲染函数。它把结构化结果转换为 Markdown/HTML/表格文本，方便人类阅读实验过程或指标。

**参数**

| 参数 | 含义 |
| --- | --- |
| `title` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `changes` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `Counter` | 库函数或对象方法 | collections 计数器；返回可统计频次的字典子类。 |
| `counter.most_common` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `_display` | [analyze_debate_changes._display](#fn-analyze_debate_changes-py-_display-563) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_changes.render_report](#fn-analyze_debate_changes-py-render_report-282)

**完整源码**

~~~~python
def _render_transition_table(title: str, changes: Sequence[FieldChange]) -> str:
    if not changes:
        return f"### {title}\n\n无分类字段变化。"
    counter = Counter((item.field, _display(item.before), _display(item.after)) for item in changes)
    lines = ["### " + title, "", "| field | before | after | count |", "| --- | --- | --- | ---: |"]
    for (field, before, after), count in counter.most_common(25):
        lines.append(f"| {field} | {before} | {after} | {count} |")
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_render_numeric_summary-453"></a>

### _render_numeric_summary

文件：`analyze_debate_changes.py:453-475`

**签名**

~~~~python
def _render_numeric_summary(title: str, changes: Sequence[NumericChange]) -> str:
~~~~

**功能**

渲染函数。它把结构化结果转换为 Markdown/HTML/表格文本，方便人类阅读实验过程或指标。

**参数**

| 参数 | 含义 |
| --- | --- |
| `title` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `changes` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `defaultdict` | 库函数或对象方法 | collections 默认字典；访问缺失键时自动构造默认值。 |
| `sorted` | 库函数或对象方法 | 内置排序；返回新的 list。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `by_field.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `by_field.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `sum` | 库函数或对象方法 | 内置求和；返回可迭代数值总和。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `abs` | 库函数或对象方法 | 内置绝对值；返回数值。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `_fmt` | [analyze_debate_changes._fmt](#fn-analyze_debate_changes-py-_fmt-556) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `mean` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `median` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |

**被谁调用**

[analyze_debate_changes.render_report](#fn-analyze_debate_changes-py-render_report-282)

**完整源码**

~~~~python
def _render_numeric_summary(title: str, changes: Sequence[NumericChange]) -> str:
    if not changes:
        return f"### {title}\n\n无数值字段变化。"
    by_field: Dict[str, List[NumericChange]] = defaultdict(list)
    for change in changes:
        by_field[change.field].append(change)
    lines = [
        "### " + title,
        "",
        "| field | changes | increased | decreased | mean delta | median abs delta | max abs delta |",
        "| --- | ---: | ---: | ---: | ---: | ---: | ---: |",
    ]
    for field, items in sorted(by_field.items()):
        deltas = [item.delta for item in items if item.delta is not None]
        increased = sum(1 for value in deltas if value > 0)
        decreased = sum(1 for value in deltas if value < 0)
        abs_deltas = [abs(value) for value in deltas]
        lines.append(
            f"| {field} | {len(items)} | {increased} | {decreased} | "
            f"{_fmt(mean(deltas) if deltas else None)} | {_fmt(median(abs_deltas) if abs_deltas else None)} | "
            f"{_fmt(max(abs_deltas) if abs_deltas else None)} |"
        )
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_render_round_table-478"></a>

### _render_round_table

文件：`analyze_debate_changes.py:478-492`

**签名**

~~~~python
def _render_round_table( field_changes: Counter[Tuple[str, str]], numeric_changes: Counter[Tuple[str, str]], ) -> str:
~~~~

**功能**

渲染函数。它把结构化结果转换为 Markdown/HTML/表格文本，方便人类阅读实验过程或指标。

**参数**

| 参数 | 含义 |
| --- | --- |
| `field_changes` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `numeric_changes` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `sorted` | 库函数或对象方法 | 内置排序；返回新的 list。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `field_changes.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `numeric_changes.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |

**被谁调用**

[analyze_debate_changes.render_report](#fn-analyze_debate_changes-py-render_report-282)

**完整源码**

~~~~python
def _render_round_table(
    field_changes: Counter[Tuple[str, str]],
    numeric_changes: Counter[Tuple[str, str]],
) -> str:
    if not field_changes and not numeric_changes:
        return "未发现分轮变化。"
    all_rounds = sorted({round_label for round_label, _ in field_changes} | {round_label for round_label, _ in numeric_changes})
    all_fields = sorted({field for _, field in field_changes} | {field for _, field in numeric_changes})
    lines = ["| round | field | changes |", "| --- | --- | ---: |"]
    for round_label in all_rounds:
        for field in all_fields:
            count = field_changes.get((round_label, field), 0) + numeric_changes.get((round_label, field), 0)
            if count:
                lines.append(f"| {round_label} | {field} | {count} |")
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_render_top_cases-495"></a>

### _render_top_cases

文件：`analyze_debate_changes.py:495-521`

**签名**

~~~~python
def _render_top_cases(rows: Sequence[Dict[str, Any]], top_cases: int) -> str:
~~~~

**功能**

渲染函数。它把结构化结果转换为 Markdown/HTML/表格文本，方便人类阅读实验过程或指标。

**参数**

| 参数 | 含义 |
| --- | --- |
| `rows` | 结果行或 DataFrame 行集合。 |
| `top_cases` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `sorted` | 库函数或对象方法 | 内置排序；返回新的 list。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `_display` | [analyze_debate_changes._display](#fn-analyze_debate_changes-py-_display-563) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_fmt` | [analyze_debate_changes._fmt](#fn-analyze_debate_changes-py-_fmt-556) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `abs` | 库函数或对象方法 | 内置绝对值；返回数值。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |

**被谁调用**

[analyze_debate_changes.render_report](#fn-analyze_debate_changes-py-render_report-282)

**完整源码**

~~~~python
def _render_top_cases(rows: Sequence[Dict[str, Any]], top_cases: int) -> str:
    ranked = sorted(
        rows,
        key=lambda row: (
            int(row.get("role_field_changes") or 0)
            + int(row.get("role_size_changes") or 0)
            + int(row.get("role_satisfaction_changes") or 0)
            + int(row.get("proposal_field_changes") or 0),
            abs(float(row.get("proposal_size_delta") or 0)),
        ),
        reverse=True,
    )[:top_cases]
    if not ranked:
        return "No cases available."
    lines = [
        "| case_id | role categorical | role size | role satisfaction | proposal categorical | proposal size delta | final type / true | final size / true |",
        "| --- | ---: | ---: | ---: | ---: | ---: | --- | --- |",
    ]
    for row in ranked:
        final_type = f"{_display(row.get('final_deal_type'))} / {_display(row.get('true_deal_type'))}"
        final_size = f"{_fmt(row.get('final_deal_size_usd_m'))} / {_fmt(row.get('true_deal_size_usd_m'))}"
        lines.append(
            f"| {row.get('case_id')} | {row.get('role_field_changes')} | {row.get('role_size_changes')} | "
            f"{row.get('role_satisfaction_changes')} | {row.get('proposal_field_changes')} | "
            f"{_fmt(row.get('proposal_size_delta'))} | {final_type} | {final_size} |"
        )
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_normalized-524"></a>

### _normalized

文件：`analyze_debate_changes.py:524-529`

**签名**

~~~~python
def _normalized(value: Any) -> str:
~~~~

**功能**

数据清洗/标准化函数。它把原始值转换为实验需要的类型或标签，并在缺失、非法、大小写不一致时提供稳定兜底。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `str.strip.lower.split` | 库函数或对象方法 | 字符串拆分，返回 list[str]。 |
| `str.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[analyze_debate_changes.analyze_cases](#fn-analyze_debate_changes-py-analyze_cases-161)、[analyze_debate_changes._round_level_changes](#fn-analyze_debate_changes-py-_round_level_changes-399)

**完整源码**

~~~~python
def _normalized(value: Any) -> str:
    if value is None:
        return ""
    if isinstance(value, bool):
        return "true" if value else "false"
    return " ".join(str(value).strip().lower().split())
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_optional_float-532"></a>

### _optional_float

文件：`analyze_debate_changes.py:532-539`

**签名**

~~~~python
def _optional_float(value: Any) -> Optional[float]:
~~~~

**功能**

数据清洗/标准化函数。它把原始值转换为实验需要的类型或标签，并在缺失、非法、大小写不一致时提供稳定兜底。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `math.isfinite` | 库函数或对象方法 | math 数学函数；返回数值或布尔判断。 |

**被谁调用**

[analyze_debate_changes.analyze_cases](#fn-analyze_debate_changes-py-analyze_cases-161)、[analyze_debate_changes._round_level_changes](#fn-analyze_debate_changes-py-_round_level_changes-399)、[analyze_debate_changes._fmt](#fn-analyze_debate_changes-py-_fmt-556)

**完整源码**

~~~~python
def _optional_float(value: Any) -> Optional[float]:
    if value is None or value == "":
        return None
    try:
        number = float(value)
    except (TypeError, ValueError):
        return None
    return number if math.isfinite(number) else None
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_numeric_changed-542"></a>

### _numeric_changed

文件：`analyze_debate_changes.py:542-547`

**签名**

~~~~python
def _numeric_changed(before: Optional[float], after: Optional[float], tolerance: float = 1e-9) -> bool:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `before` | 变化前的结构化判断或数值。 |
| `after` | 变化后的结构化判断或数值。 |
| `tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |

**返回值**

返回 `bool` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `abs` | 库函数或对象方法 | 内置绝对值；返回数值。 |

**被谁调用**

[analyze_debate_changes.analyze_cases](#fn-analyze_debate_changes-py-analyze_cases-161)、[analyze_debate_changes._round_level_changes](#fn-analyze_debate_changes-py-_round_level_changes-399)

**完整源码**

~~~~python
def _numeric_changed(before: Optional[float], after: Optional[float], tolerance: float = 1e-9) -> bool:
    if before is None and after is None:
        return False
    if before is None or after is None:
        return True
    return abs(before - after) > tolerance
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_delta-550"></a>

### _delta

文件：`analyze_debate_changes.py:550-553`

**签名**

~~~~python
def _delta(before: Optional[float], after: Optional[float]) -> Optional[float]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `before` | 变化前的结构化判断或数值。 |
| `after` | 变化后的结构化判断或数值。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[analyze_debate_changes.analyze_cases](#fn-analyze_debate_changes-py-analyze_cases-161)、[analyze_debate_changes._round_level_changes](#fn-analyze_debate_changes-py-_round_level_changes-399)

**完整源码**

~~~~python
def _delta(before: Optional[float], after: Optional[float]) -> Optional[float]:
    if before is None or after is None:
        return None
    return after - before
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_fmt-556"></a>

### _fmt

文件：`analyze_debate_changes.py:556-560`

**签名**

~~~~python
def _fmt(value: Any) -> str:
~~~~

**功能**

格式化函数。它把数值或标签转成报告中更易读的字符串。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_optional_float` | [analyze_debate_changes._optional_float](#fn-analyze_debate_changes-py-_optional_float-532) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[analyze_debate_changes._render_numeric_summary](#fn-analyze_debate_changes-py-_render_numeric_summary-453)、[analyze_debate_changes._render_top_cases](#fn-analyze_debate_changes-py-_render_top_cases-495)

**完整源码**

~~~~python
def _fmt(value: Any) -> str:
    number = _optional_float(value)
    if number is None:
        return "n/a"
    return f"{number:.3f}"
~~~~

[回到顶部](#top)

<a id="fn-analyze_debate_changes-py-_display-563"></a>

### _display

文件：`analyze_debate_changes.py:563-566`

**签名**

~~~~python
def _display(value: Any) -> str:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[analyze_debate_changes._render_transition_table](#fn-analyze_debate_changes-py-_render_transition_table-443)、[analyze_debate_changes._render_top_cases](#fn-analyze_debate_changes-py-_render_top_cases-495)

**完整源码**

~~~~python
def _display(value: Any) -> str:
    if value is None or value == "":
        return "n/a"
    return str(value)
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-__init__-py"></a>

## boardroom_sim/__init__.py

项目源文件。教程按函数和类解释它在实验中的作用。

**类**：无类定义

<a id="module-boardroom_sim-agents-py"></a>

## boardroom_sim/agents.py

角色智能体模块：负责观察案例、构造 prompt、调用 LLM、清洗角色预测和记录讨论行为。

**类**：[BoardAgent](#class-boardroom_sim-agents-py-boardagent-74)

<a id="fn-boardroom_sim-agents-py-clamp_score-27"></a>

### clamp_score

文件：`boardroom_sim/agents.py:27-29`

**签名**

~~~~python
def clamp_score(value: float) -> float:
~~~~

**功能**

源码文档说明：Clamp a satisfaction score to the inclusive range 0 to 100.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `float` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `min` | 库函数或对象方法 | 内置最小值函数；返回最小元素。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent._decision_from_json](#fn-boardroom_sim-agents-py-boardagent-_decision_from_json-227)

**完整源码**

~~~~python
def clamp_score(value: float) -> float:
    """Clamp a satisfaction score to the inclusive range 0 to 100."""
    return max(0.0, min(100.0, value))
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-coerce_choice-32"></a>

### coerce_choice

文件：`boardroom_sim/agents.py:32-38`

**签名**

~~~~python
def coerce_choice(value: Any, allowed: Iterable[str], default: str) -> str:
~~~~

**功能**

源码文档说明：Normalize an LLM string choice into one of the allowed labels.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `allowed` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `value.strip.lower.replace.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `value.strip.lower.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `value.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `value.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent._decision_from_json](#fn-boardroom_sim-agents-py-boardagent-_decision_from_json-227)、[boardroom_sim.baselines._coerce_prediction](#fn-boardroom_sim-baselines-py-_coerce_prediction-177)

**完整源码**

~~~~python
def coerce_choice(value: Any, allowed: Iterable[str], default: str) -> str:
    """Normalize an LLM string choice into one of the allowed labels."""
    if isinstance(value, str):
        normalized = value.strip().lower().replace("-", "_").replace(" ", "_")
        if normalized in allowed:
            return normalized
    return default
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-coerce_float-41"></a>

### coerce_float

文件：`boardroom_sim/agents.py:41-46`

**签名**

~~~~python
def coerce_float(value: Any, default: float = 50.0) -> float:
~~~~

**功能**

源码文档说明：Normalize an LLM numeric value into a float.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `float` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent._decision_from_json](#fn-boardroom_sim-agents-py-boardagent-_decision_from_json-227)、[boardroom_sim.baselines._coerce_prediction](#fn-boardroom_sim-baselines-py-_coerce_prediction-177)

**完整源码**

~~~~python
def coerce_float(value: Any, default: float = 50.0) -> float:
    """Normalize an LLM numeric value into a float."""
    try:
        return float(value)
    except (TypeError, ValueError):
        return default
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-coerce_text-49"></a>

### coerce_text

文件：`boardroom_sim/agents.py:49-53`

**签名**

~~~~python
def coerce_text(value: Any, default: str) -> str:
~~~~

**功能**

源码文档说明：Normalize an LLM text field into a non-empty string.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `value.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent._decision_from_json](#fn-boardroom_sim-agents-py-boardagent-_decision_from_json-227)、[boardroom_sim.agents.BoardAgent._meeting_artifacts_from_json](#fn-boardroom_sim-agents-py-boardagent-_meeting_artifacts_from_json-296)、[boardroom_sim.baselines._coerce_prediction](#fn-boardroom_sim-baselines-py-_coerce_prediction-177)

**完整源码**

~~~~python
def coerce_text(value: Any, default: str) -> str:
    """Normalize an LLM text field into a non-empty string."""
    if isinstance(value, str) and value.strip():
        return value.strip()
    return default
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-coerce_rationale-56"></a>

### coerce_rationale

文件：`boardroom_sim/agents.py:56-62`

**签名**

~~~~python
def coerce_rationale(value: Any) -> List[str]:
~~~~

**功能**

源码文档说明：Normalize LLM rationale into a short list of strings.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `List[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `value.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent._decision_from_json](#fn-boardroom_sim-agents-py-boardagent-_decision_from_json-227)、[boardroom_sim.baselines.predict_single_case](#fn-boardroom_sim-baselines-py-predict_single_case-102)

**完整源码**

~~~~python
def coerce_rationale(value: Any) -> List[str]:
    """Normalize LLM rationale into a short list of strings."""
    if isinstance(value, list):
        return [str(item).strip() for item in value if str(item).strip()]
    if isinstance(value, str) and value.strip():
        return [value.strip()]
    return ["LLM returned no explicit rationale."]
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-coerce_string_list-65"></a>

### coerce_string_list

文件：`boardroom_sim/agents.py:65-71`

**签名**

~~~~python
def coerce_string_list(value: Any) -> List[str]:
~~~~

**功能**

源码文档说明：Normalize an optional LLM list field into a list of strings.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `List[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `value.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent._meeting_artifacts_from_json](#fn-boardroom_sim-agents-py-boardagent-_meeting_artifacts_from_json-296)

**完整源码**

~~~~python
def coerce_string_list(value: Any) -> List[str]:
    """Normalize an optional LLM list field into a list of strings."""
    if isinstance(value, list):
        return [str(item).strip() for item in value if str(item).strip()]
    if isinstance(value, str) and value.strip():
        return [value.strip()]
    return []
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-__init__-77"></a>

### BoardAgent.__init__

文件：`boardroom_sim/agents.py:77-87`

**签名**

~~~~python
def __init__( self, policy: RolePolicy, llm_client: LLMClient, prompt_renderer: Optional[PromptRenderer] = None, ) -> None:
~~~~

**功能**

源码文档说明：Initialize an agent with its role policy and shared LLM client.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `policy` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `llm_client` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `prompt_renderer` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

初始化方法，通常返回 `None`；主要副作用是给 `self` 写入属性。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `PromptRenderer` | [boardroom_sim.prompts.PromptRenderer](#class-boardroom_sim-prompts-py-promptrenderer-15) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
    def __init__(
        self,
        policy: RolePolicy,
        llm_client: LLMClient,
        prompt_renderer: Optional[PromptRenderer] = None,
    ) -> None:
        """Initialize an agent with its role policy and shared LLM client."""
        self.policy = policy
        self.role_name = policy.role_name
        self.llm_client = llm_client
        self.prompt_renderer = prompt_renderer or PromptRenderer()
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-observe-89"></a>

### BoardAgent.observe

文件：`boardroom_sim/agents.py:89-92`

**签名**

~~~~python
def observe(self, case: BoardCase) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Return only the fields this role is allowed to attend to.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `case.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `case_dict.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.evaluate](#fn-boardroom_sim-agents-py-boardagent-evaluate-94)、[boardroom_sim.agents.BoardAgent.bargaining_step](#fn-boardroom_sim-agents-py-boardagent-bargaining_step-123)、[boardroom_sim.agents.BoardAgent._bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197)

**完整源码**

~~~~python
    def observe(self, case: BoardCase) -> Dict[str, Any]:
        """Return only the fields this role is allowed to attend to."""
        case_dict = case.to_dict()
        return {field: case_dict.get(field) for field in self.policy.layer_2_attention_fields}
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-evaluate-94"></a>

### BoardAgent.evaluate

文件：`boardroom_sim/agents.py:94-109`

**签名**

~~~~python
def evaluate(self, case: BoardCase, context: Dict[str, RoleDecision]) -> RoleDecision:
~~~~

**功能**

让单个角色根据当前案例和其他角色初始判断生成自己的结构化预测。它把系统 prompt、角色规则、案例可见字段和上下文拼成消息，调用 LLM 返回 JSON，然后规范化为 RoleDecision。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |

**返回值**

返回 `RoleDecision` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self.observe` | [boardroom_sim.agents.BoardAgent.observe](#fn-boardroom_sim-agents-py-boardagent-observe-89) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self.llm_client.complete_json` | [boardroom_sim.llm.LLMClient.complete_json](#fn-boardroom_sim-llm-py-llmclient-complete_json-151) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._decision_from_json` | [boardroom_sim.agents.BoardAgent._decision_from_json](#fn-boardroom_sim-agents-py-boardagent-_decision_from_json-227) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._system_prompt` | [boardroom_sim.agents.BoardAgent._system_prompt](#fn-boardroom_sim-agents-py-boardagent-_system_prompt-160) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._decision_prompt` | [boardroom_sim.agents.BoardAgent._decision_prompt](#fn-boardroom_sim-agents-py-boardagent-_decision_prompt-172) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)

**完整源码**

~~~~python
    def evaluate(self, case: BoardCase, context: Dict[str, RoleDecision]) -> RoleDecision:
        """Ask the LLM to predict financing outcomes from this role's perspective."""
        observed_fields = self.observe(case)
        messages = [
            {"role": "system", "content": self._system_prompt()},
            {
                "role": "user",
                "content": self._decision_prompt(
                    case=case,
                    observed_fields=observed_fields,
                    context=context,
                ),
            },
        ]
        raw = self.llm_client.complete_json(messages)
        return self._decision_from_json(raw, observed_fields)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-opening_statement-111"></a>

### BoardAgent.opening_statement

文件：`boardroom_sim/agents.py:111-121`

**签名**

~~~~python
def opening_statement(self, decision: RoleDecision) -> str:
~~~~

**功能**

源码文档说明：Create a compact opening statement based on the role prediction.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `decision` | 某个角色当前的 RoleDecision。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self.rationale_line` | [boardroom_sim.agents.BoardAgent.rationale_line](#fn-boardroom_sim-agents-py-boardagent-rationale_line-154) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.bargaining_step](#fn-boardroom_sim-agents-py-boardagent-bargaining_step-123)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)

**完整源码**

~~~~python
    def opening_statement(self, decision: RoleDecision) -> str:
        """Create a compact opening statement based on the role prediction."""
        return (
            f"{self.role_name} starts with intent={decision.financing_intent}, "
            f"size={decision.predicted_deal_size_usd_m:.2f}M, "
            f"post-money={decision.predicted_post_money_valuation_usd_m:.2f}M, "
            f"ownership={decision.predicted_investor_ownership_pct:.2f}%, "
            f"type={decision.predicted_deal_type}, "
            f"valuation={decision.valuation_direction}. "
            f"Reason: {self.rationale_line(decision.rationale)}"
        )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-bargaining_step-123"></a>

### BoardAgent.bargaining_step

文件：`boardroom_sim/agents.py:123-152`

**签名**

~~~~python
def bargaining_step( self, case: BoardCase, decision: RoleDecision, proposal: TermSheetProposal, round_index: int, context: Dict[str, RoleDecision], prior_messages: List[Dict[str, Any]], process_context: Dict[str, Any] | None = None, ) -> Tuple[str, RoleDecision, Dict[str, Any]]:
~~~~

**功能**

让某个角色在指定讨论轮次发言，并根据会议内容更新自己的预测。返回三部分：发言文本、更新后的 RoleDecision、以及结构化会议行为记录。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `decision` | 某个角色当前的 RoleDecision。 |
| `proposal` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `round_index` | 从 0 开始的讨论轮次索引。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |
| `prior_messages` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `process_context` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Tuple[str, RoleDecision, Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self.llm_client.complete_json` | [boardroom_sim.llm.LLMClient.complete_json](#fn-boardroom_sim-llm-py-llmclient-complete_json-151) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._decision_from_json` | [boardroom_sim.agents.BoardAgent._decision_from_json](#fn-boardroom_sim-agents-py-boardagent-_decision_from_json-227) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `self.opening_statement` | [boardroom_sim.agents.BoardAgent.opening_statement](#fn-boardroom_sim-agents-py-boardagent-opening_statement-111) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self.observe` | [boardroom_sim.agents.BoardAgent.observe](#fn-boardroom_sim-agents-py-boardagent-observe-89) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._meeting_artifacts_from_json` | [boardroom_sim.agents.BoardAgent._meeting_artifacts_from_json](#fn-boardroom_sim-agents-py-boardagent-_meeting_artifacts_from_json-296) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._system_prompt` | [boardroom_sim.agents.BoardAgent._system_prompt](#fn-boardroom_sim-agents-py-boardagent-_system_prompt-160) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._bargaining_prompt` | [boardroom_sim.agents.BoardAgent._bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `raw.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator._run_bargaining_round](#fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208)

**完整源码**

~~~~python
    def bargaining_step(
        self,
        case: BoardCase,
        decision: RoleDecision,
        proposal: TermSheetProposal,
        round_index: int,
        context: Dict[str, RoleDecision],
        prior_messages: List[Dict[str, Any]],
        process_context: Dict[str, Any] | None = None,
    ) -> Tuple[str, RoleDecision, Dict[str, Any]]:
        """Ask the LLM to produce one bargaining message and an updated prediction."""
        messages = [
            {"role": "system", "content": self._system_prompt()},
            {
                "role": "user",
                "content": self._bargaining_prompt(
                    case=case,
                    decision=decision,
                    proposal=proposal,
                    round_index=round_index,
                    context=context,
                    prior_messages=prior_messages,
                    process_context=process_context or {},
                ),
            },
        ]
        raw = self.llm_client.complete_json(messages)
        message = str(raw.get("message") or raw.get("speech") or "").strip() or self.opening_statement(decision)
        updated_decision = self._decision_from_json(raw, self.observe(case), fallback=decision)
        return message, updated_decision, self._meeting_artifacts_from_json(raw)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-rationale_line-154"></a>

### BoardAgent.rationale_line

文件：`boardroom_sim/agents.py:154-158`

**签名**

~~~~python
def rationale_line(self, rationale: List[str]) -> str:
~~~~

**功能**

源码文档说明：Join rationale fragments into one readable trace line.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `rationale` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.opening_statement](#fn-boardroom_sim-agents-py-boardagent-opening_statement-111)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)

**完整源码**

~~~~python
    def rationale_line(self, rationale: List[str]) -> str:
        """Join rationale fragments into one readable trace line."""
        if not rationale:
            return "No major rationale recorded."
        return " ".join(rationale)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-_system_prompt-160"></a>

### BoardAgent._system_prompt

文件：`boardroom_sim/agents.py:160-166`

**签名**

~~~~python
def _system_prompt(self) -> str:
~~~~

**功能**

源码文档说明：Return the stable system prompt shared by all board agents.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self.prompt_renderer.render` | [boardroom_sim.prompts.PromptRenderer.render](#fn-boardroom_sim-prompts-py-promptrenderer-render-24) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._role_policy_text` | [boardroom_sim.agents.BoardAgent._role_policy_text](#fn-boardroom_sim-agents-py-boardagent-_role_policy_text-168) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.evaluate](#fn-boardroom_sim-agents-py-boardagent-evaluate-94)、[boardroom_sim.agents.BoardAgent.bargaining_step](#fn-boardroom_sim-agents-py-boardagent-bargaining_step-123)

**完整源码**

~~~~python
    def _system_prompt(self) -> str:
        """Return the stable system prompt shared by all board agents."""
        return self.prompt_renderer.render(
            "system_agent",
            role_name=self.role_name,
            role_policy_json=self._role_policy_text(),
        )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-_role_policy_text-168"></a>

### BoardAgent._role_policy_text

文件：`boardroom_sim/agents.py:168-170`

**签名**

~~~~python
def _role_policy_text(self) -> str:
~~~~

**功能**

源码文档说明：Format the four-layer role policy for an LLM prompt.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `json.dumps` | 库函数或对象方法 | 把 Python 对象序列化为 JSON 字符串，返回 str。 |
| `self.policy.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent._system_prompt](#fn-boardroom_sim-agents-py-boardagent-_system_prompt-160)、[boardroom_sim.agents.BoardAgent._decision_prompt](#fn-boardroom_sim-agents-py-boardagent-_decision_prompt-172)、[boardroom_sim.agents.BoardAgent._bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197)

**完整源码**

~~~~python
    def _role_policy_text(self) -> str:
        """Format the four-layer role policy for an LLM prompt."""
        return json.dumps(self.policy.to_dict(), ensure_ascii=False, indent=2)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-_decision_prompt-172"></a>

### BoardAgent._decision_prompt

文件：`boardroom_sim/agents.py:172-195`

**签名**

~~~~python
def _decision_prompt( self, *, case: BoardCase, observed_fields: Dict[str, Any], context: Dict[str, RoleDecision], ) -> str:
~~~~

**功能**

源码文档说明：Build the initial prediction prompt.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `observed_fields` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self.prompt_renderer.render` | [boardroom_sim.prompts.PromptRenderer.render](#fn-boardroom_sim-prompts-py-promptrenderer-render-24) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._decision_payload` | [boardroom_sim.agents.BoardAgent._decision_payload](#fn-boardroom_sim-agents-py-boardagent-_decision_payload-308) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `context.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `self._role_policy_text` | [boardroom_sim.agents.BoardAgent._role_policy_text](#fn-boardroom_sim-agents-py-boardagent-_role_policy_text-168) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `json.dumps` | 库函数或对象方法 | 把 Python 对象序列化为 JSON 字符串，返回 str。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.evaluate](#fn-boardroom_sim-agents-py-boardagent-evaluate-94)

**完整源码**

~~~~python
    def _decision_prompt(
        self,
        *,
        case: BoardCase,
        observed_fields: Dict[str, Any],
        context: Dict[str, RoleDecision],
    ) -> str:
        """Build the initial prediction prompt."""
        context_payload = {role: self._decision_payload(decision) for role, decision in context.items()}
        case_metadata = {
            "case_id": case.case_id,
            "company_label": case.company_label,
            "company_id": case.company_id,
            "decision_date": case.decision_date,
            "primary_industry": case.primary_industry,
        }
        return self.prompt_renderer.render(
            "initial_prediction",
            role_name=self.role_name,
            role_policy_json=self._role_policy_text(),
            observed_fields_json=json.dumps(observed_fields, ensure_ascii=False, indent=2),
            case_metadata_json=json.dumps(case_metadata, ensure_ascii=False, indent=2),
            context_payload_json=json.dumps(context_payload, ensure_ascii=False, indent=2),
        )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197"></a>

### BoardAgent._bargaining_prompt

文件：`boardroom_sim/agents.py:197-225`

**签名**

~~~~python
def _bargaining_prompt( self, *, case: BoardCase, decision: RoleDecision, proposal: TermSheetProposal, round_index: int, context: Dict[str, RoleDecision], prior_messages: List[Dict[str, Any]], process_context: Dict[str, Any], ) -> str:
~~~~

**功能**

源码文档说明：Build the prompt for one bargaining-round utterance and decision update.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `decision` | 某个角色当前的 RoleDecision。 |
| `proposal` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `round_index` | 从 0 开始的讨论轮次索引。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |
| `prior_messages` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `process_context` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self.prompt_renderer.render` | [boardroom_sim.prompts.PromptRenderer.render](#fn-boardroom_sim-prompts-py-promptrenderer-render-24) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._decision_payload` | [boardroom_sim.agents.BoardAgent._decision_payload](#fn-boardroom_sim-agents-py-boardagent-_decision_payload-308) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |
| `min` | 库函数或对象方法 | 内置最小值函数；返回最小元素。 |
| `context.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `self._role_policy_text` | [boardroom_sim.agents.BoardAgent._role_policy_text](#fn-boardroom_sim-agents-py-boardagent-_role_policy_text-168) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `json.dumps` | 库函数或对象方法 | 把 Python 对象序列化为 JSON 字符串，返回 str。 |
| `self.prompt_renderer.response_guidance` | [boardroom_sim.prompts.PromptRenderer.response_guidance](#fn-boardroom_sim-prompts-py-promptrenderer-response_guidance-29) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `self.observe` | [boardroom_sim.agents.BoardAgent.observe](#fn-boardroom_sim-agents-py-boardagent-observe-89) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `proposal.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.bargaining_step](#fn-boardroom_sim-agents-py-boardagent-bargaining_step-123)

**完整源码**

~~~~python
    def _bargaining_prompt(
        self,
        *,
        case: BoardCase,
        decision: RoleDecision,
        proposal: TermSheetProposal,
        round_index: int,
        context: Dict[str, RoleDecision],
        prior_messages: List[Dict[str, Any]],
        process_context: Dict[str, Any],
    ) -> str:
        """Build the prompt for one bargaining-round utterance and decision update."""
        context_payload = {role: self._decision_payload(item) for role, item in context.items()}
        round_tasks = self.prompt_renderer.round_tasks or list(DEFAULT_ROUND_TASKS)
        round_specific_task = round_tasks[min(round_index, len(round_tasks) - 1)]
        return self.prompt_renderer.render(
            "bargaining_step",
            role_name=self.role_name,
            role_policy_json=self._role_policy_text(),
            observed_fields_json=json.dumps(self.observe(case), ensure_ascii=False, indent=2),
            proposal_json=json.dumps(proposal.to_dict(), ensure_ascii=False, indent=2),
            current_decision_json=json.dumps(self._decision_payload(decision), ensure_ascii=False, indent=2),
            context_payload_json=json.dumps(context_payload, ensure_ascii=False, indent=2),
            prior_messages_json=json.dumps(prior_messages, ensure_ascii=False, indent=2),
            process_context_json=json.dumps(process_context, ensure_ascii=False, indent=2),
            round_index=round_index + 1,
            round_specific_task=round_specific_task,
            response_guidance=self.prompt_renderer.response_guidance(),
        )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-_decision_from_json-227"></a>

### BoardAgent._decision_from_json

文件：`boardroom_sim/agents.py:227-294`

**签名**

~~~~python
def _decision_from_json( self, raw: Dict[str, Any], observed_fields: Dict[str, Any], fallback: Optional[RoleDecision] = None, ) -> RoleDecision:
~~~~

**功能**

把 LLM 返回的原始 JSON 压成实验可用的 RoleDecision。这里负责字段兜底、枚举值校验、数值范围裁剪和理由列表清洗。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `raw` | 从文件、TOML、JSON 或 LLM 读取的原始数据。 |
| `observed_fields` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `fallback` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `RoleDecision` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `coerce_choice` | [boardroom_sim.agents.coerce_choice](#fn-boardroom_sim-agents-py-coerce_choice-32) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `min` | 库函数或对象方法 | 内置最小值函数；返回最小元素。 |
| `coerce_text` | [boardroom_sim.agents.coerce_text](#fn-boardroom_sim-agents-py-coerce_text-49) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `clamp_score` | [boardroom_sim.agents.clamp_score](#fn-boardroom_sim-agents-py-clamp_score-27) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `RoleDecision` | [boardroom_sim.models.RoleDecision](#class-boardroom_sim-models-py-roledecision-297) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `raw.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `coerce_float` | [boardroom_sim.agents.coerce_float](#fn-boardroom_sim-agents-py-coerce_float-41) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `coerce_rationale` | [boardroom_sim.agents.coerce_rationale](#fn-boardroom_sim-agents-py-coerce_rationale-56) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `round` | 库函数或对象方法 | 内置四舍五入；返回数值。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.evaluate](#fn-boardroom_sim-agents-py-boardagent-evaluate-94)、[boardroom_sim.agents.BoardAgent.bargaining_step](#fn-boardroom_sim-agents-py-boardagent-bargaining_step-123)

**完整源码**

~~~~python
    def _decision_from_json(
        self,
        raw: Dict[str, Any],
        observed_fields: Dict[str, Any],
        fallback: Optional[RoleDecision] = None,
    ) -> RoleDecision:
        """Convert a parsed LLM JSON object into a validated RoleDecision."""
        financing_intent = coerce_choice(
            raw.get("financing_intent"),
            FINANCING_INTENTS,
            fallback.financing_intent if fallback else "raise_now",
        )
        completion_view = coerce_choice(
            raw.get("completion_view"),
            COMPLETION_VIEWS,
            fallback.completion_view if fallback and fallback.completion_view in COMPLETION_VIEWS else "likely_complete",
        )
        predicted_deal_size = max(
            0.0,
            coerce_float(
                raw.get("predicted_deal_size_usd_m"),
                fallback.predicted_deal_size_usd_m if fallback else 0.0,
            ),
        )
        predicted_post_money_valuation = max(
            0.0,
            coerce_float(
                raw.get("predicted_post_money_valuation_usd_m"),
                fallback.predicted_post_money_valuation_usd_m if fallback else 0.0,
            ),
        )
        predicted_investor_ownership = min(
            100.0,
            max(
                0.0,
                coerce_float(
                    raw.get("predicted_investor_ownership_pct"),
                    fallback.predicted_investor_ownership_pct if fallback else 0.0,
                ),
            ),
        )
        predicted_deal_type = coerce_text(
            raw.get("predicted_deal_type"),
            fallback.predicted_deal_type if fallback else "unknown",
        )
        valuation_direction = coerce_choice(
            raw.get("valuation_direction"),
            VALUATION_CHOICES,
            fallback.valuation_direction if fallback and fallback.valuation_direction in VALUATION_CHOICES else "flat",
        )
        satisfaction_score = clamp_score(
            coerce_float(raw.get("satisfaction_score"), fallback.satisfaction_score if fallback else 50.0)
        )
        rationale = coerce_rationale(raw.get("rationale")) if "rationale" in raw else (fallback.rationale if fallback else [])

        return RoleDecision(
            role_name=self.role_name,
            financing_intent=financing_intent,  # type: ignore[arg-type]
            completion_view=completion_view,  # type: ignore[arg-type]
            predicted_deal_size_usd_m=round(predicted_deal_size, 6),
            predicted_post_money_valuation_usd_m=round(predicted_post_money_valuation, 6),
            predicted_investor_ownership_pct=round(predicted_investor_ownership, 6),
            predicted_deal_type=predicted_deal_type,
            valuation_direction=valuation_direction,  # type: ignore[arg-type]
            satisfaction_score=satisfaction_score,
            rationale=rationale or ["LLM returned no explicit rationale."],
            observed_fields=observed_fields,
        )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-_meeting_artifacts_from_json-296"></a>

### BoardAgent._meeting_artifacts_from_json

文件：`boardroom_sim/agents.py:296-306`

**签名**

~~~~python
def _meeting_artifacts_from_json(self, raw: Dict[str, Any]) -> Dict[str, Any]:
~~~~

**功能**

从 LLM 的讨论回复中提取会议行为字段，例如提出的问题、回应对象、证据锚点、替代方案和承诺事项，用于 trace 与后续分析。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `raw` | 从文件、TOML、JSON 或 LLM 读取的原始数据。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `coerce_text` | [boardroom_sim.agents.coerce_text](#fn-boardroom_sim-agents-py-coerce_text-49) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `coerce_string_list` | [boardroom_sim.agents.coerce_string_list](#fn-boardroom_sim-agents-py-coerce_string_list-65) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `raw.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.bargaining_step](#fn-boardroom_sim-agents-py-boardagent-bargaining_step-123)

**完整源码**

~~~~python
    def _meeting_artifacts_from_json(self, raw: Dict[str, Any]) -> Dict[str, Any]:
        """Extract richer boardroom-process fields from a bargaining response."""
        return {
            "boardroom_act": coerce_text(raw.get("boardroom_act"), "unspecified"),
            "direct_response_to": coerce_text(raw.get("direct_response_to"), ""),
            "questions_raised": coerce_string_list(raw.get("questions_raised")),
            "evidence_anchors": coerce_string_list(raw.get("evidence_anchors")),
            "alternative_options": coerce_string_list(raw.get("alternative_options")),
            "role_commitment": coerce_text(raw.get("role_commitment"), ""),
            "prediction_update_reason": coerce_text(raw.get("prediction_update_reason"), ""),
        }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-boardagent-_decision_payload-308"></a>

### BoardAgent._decision_payload

文件：`boardroom_sim/agents.py:308-320`

**签名**

~~~~python
def _decision_payload(self, decision: RoleDecision) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Return a compact decision payload for prompts.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `decision` | 某个角色当前的 RoleDecision。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.agents.BoardAgent._decision_prompt](#fn-boardroom_sim-agents-py-boardagent-_decision_prompt-172)、[boardroom_sim.agents.BoardAgent._bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197)

**完整源码**

~~~~python
    def _decision_payload(self, decision: RoleDecision) -> Dict[str, Any]:
        """Return a compact decision payload for prompts."""
        return {
            "financing_intent": decision.financing_intent,
            "completion_view": decision.completion_view,
            "predicted_deal_size_usd_m": decision.predicted_deal_size_usd_m,
            "predicted_post_money_valuation_usd_m": decision.predicted_post_money_valuation_usd_m,
            "predicted_investor_ownership_pct": decision.predicted_investor_ownership_pct,
            "predicted_deal_type": decision.predicted_deal_type,
            "valuation_direction": decision.valuation_direction,
            "satisfaction_score": decision.satisfaction_score,
            "rationale": decision.rationale,
        }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-agents-py-build_agents-323"></a>

### build_agents

文件：`boardroom_sim/agents.py:323-329`

**签名**

~~~~python
def build_agents( policies: Dict[str, RolePolicy], llm_client: LLMClient, prompt_renderer: Optional[PromptRenderer] = None, ) -> Dict[str, BoardAgent]:
~~~~

**功能**

源码文档说明：Instantiate all MVP agents from their role policies and shared LLM client.

**参数**

| 参数 | 含义 |
| --- | --- |
| `policies` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `llm_client` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `prompt_renderer` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, BoardAgent]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `BoardAgent` | [boardroom_sim.agents.BoardAgent](#class-boardroom_sim-agents-py-boardagent-74) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `policies.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.__init__](#fn-boardroom_sim-simulator-py-boardroomsimulator-__init__-32)

**完整源码**

~~~~python
def build_agents(
    policies: Dict[str, RolePolicy],
    llm_client: LLMClient,
    prompt_renderer: Optional[PromptRenderer] = None,
) -> Dict[str, BoardAgent]:
    """Instantiate all MVP agents from their role policies and shared LLM client."""
    return {role_name: BoardAgent(policy, llm_client, prompt_renderer) for role_name, policy in policies.items()}
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-baselines-py"></a>

## boardroom_sim/baselines.py

基线模块：实现单智能体历史信息预测，用来与多角色讨论实验对照。

**类**：无类定义

<a id="fn-boardroom_sim-baselines-py-run_single_agent_baseline-77"></a>

### run_single_agent_baseline

文件：`boardroom_sim/baselines.py:77-99`

**签名**

~~~~python
def run_single_agent_baseline( cases: Iterable[BoardCase], llm_client: LLMClient, *, include_role_rules: bool, baseline_name: str, role_policy_dir: Path | None = None, prompts_dir: Path | None = None, ) -> List[Dict[str, Any]]:
~~~~

**功能**

运行单智能体基线，对同一批案例直接预测，便于和多角色董事会讨论结果比较。

**参数**

| 参数 | 含义 |
| --- | --- |
| `cases` | 多个 BoardCase 或 trace case，用于批量运行或统计。 |
| `llm_client` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `include_role_rules` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `baseline_name` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `role_policy_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `prompts_dir` | 目录路径参数，用于读取配置或写出结果。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `rows.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `predict_single_case` | [boardroom_sim.baselines.predict_single_case](#fn-boardroom_sim-baselines-py-predict_single_case-102) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def run_single_agent_baseline(
    cases: Iterable[BoardCase],
    llm_client: LLMClient,
    *,
    include_role_rules: bool,
    baseline_name: str,
    role_policy_dir: Path | None = None,
    prompts_dir: Path | None = None,
) -> List[Dict[str, Any]]:
    """Run a single-call baseline for each case."""
    rows: List[Dict[str, Any]] = []
    for case in cases:
        rows.append(
            predict_single_case(
                case,
                llm_client,
                include_role_rules=include_role_rules,
                baseline_name=baseline_name,
                role_policy_dir=role_policy_dir,
                prompts_dir=prompts_dir,
            )
        )
    return rows
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-baselines-py-predict_single_case-102"></a>

### predict_single_case

文件：`boardroom_sim/baselines.py:102-136`

**签名**

~~~~python
def predict_single_case( case: BoardCase, llm_client: LLMClient, *, include_role_rules: bool, baseline_name: str, role_policy_dir: Path | None = None, prompts_dir: Path | None = None, ) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Ask one LLM call to produce the same core prediction fields as the simulator.

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `llm_client` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `include_role_rules` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `baseline_name` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `role_policy_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `prompts_dir` | 目录路径参数，用于读取配置或写出结果。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `llm_client.complete_json` | [boardroom_sim.llm.LLMClient.complete_json](#fn-boardroom_sim-llm-py-llmclient-complete_json-151) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_coerce_prediction` | [boardroom_sim.baselines._coerce_prediction](#fn-boardroom_sim-baselines-py-_coerce_prediction-177) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_baseline_messages` | [boardroom_sim.baselines._baseline_messages](#fn-boardroom_sim-baselines-py-_baseline_messages-139) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `coerce_rationale` | [boardroom_sim.agents.coerce_rationale](#fn-boardroom_sim-agents-py-coerce_rationale-56) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `raw.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `case.notes.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.baselines.run_single_agent_baseline](#fn-boardroom_sim-baselines-py-run_single_agent_baseline-77)、[run_batch_experiments._run_or_resume_baseline](#fn-run_batch_experiments-py-_run_or_resume_baseline-331)

**完整源码**

~~~~python
def predict_single_case(
    case: BoardCase,
    llm_client: LLMClient,
    *,
    include_role_rules: bool,
    baseline_name: str,
    role_policy_dir: Path | None = None,
    prompts_dir: Path | None = None,
) -> Dict[str, Any]:
    """Ask one LLM call to produce the same core prediction fields as the simulator."""
    raw = llm_client.complete_json(
        _baseline_messages(
            case,
            include_role_rules=include_role_rules,
            role_policy_dir=role_policy_dir,
            prompts_dir=prompts_dir,
        )
    )
    prediction = _coerce_prediction(raw)
    return {
        "case_id": case.case_id,
        "company_name": case.company_label,
        "baseline_name": baseline_name,
        "baseline_include_role_rules": include_role_rules,
        **prediction,
        "proposal": {
            "recommended_deal_size_usd_m": prediction["predicted_deal_size_usd_m"],
            "recommended_post_money_valuation_usd_m": prediction["predicted_post_money_valuation_usd_m"],
            "recommended_investor_ownership_pct": prediction["predicted_investor_ownership_pct"],
            "recommended_deal_type": prediction["predicted_deal_type"],
            "valuation_direction": prediction["valuation_direction"],
        },
        "rationale": coerce_rationale(raw.get("rationale")),
        "labels": case.notes.get("labels", {}) if isinstance(case.notes.get("labels", {}), dict) else {},
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-baselines-py-_baseline_messages-139"></a>

### _baseline_messages

文件：`boardroom_sim/baselines.py:139-169`

**签名**

~~~~python
def _baseline_messages( case: BoardCase, *, include_role_rules: bool, role_policy_dir: Path | None = None, prompts_dir: Path | None = None, ) -> List[Dict[str, str]]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `include_role_rules` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `role_policy_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `prompts_dir` | 目录路径参数，用于读取配置或写出结果。 |

**返回值**

返回 `List[Dict[str, str]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `PromptRenderer` | [boardroom_sim.prompts.PromptRenderer](#class-boardroom_sim-prompts-py-promptrenderer-15) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `json.dumps` | 库函数或对象方法 | 把 Python 对象序列化为 JSON 字符串，返回 str。 |
| `renderer.render` | [boardroom_sim.prompts.PromptRenderer.render](#fn-boardroom_sim-prompts-py-promptrenderer-render-24) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `policy.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `build_role_policies.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `_historical_payload` | [boardroom_sim.baselines._historical_payload](#fn-boardroom_sim-baselines-py-_historical_payload-172) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `build_role_policies` | [boardroom_sim.roles.build_role_policies](#fn-boardroom_sim-roles-py-build_role_policies-17) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.baselines.predict_single_case](#fn-boardroom_sim-baselines-py-predict_single_case-102)

**完整源码**

~~~~python
def _baseline_messages(
    case: BoardCase,
    *,
    include_role_rules: bool,
    role_policy_dir: Path | None = None,
    prompts_dir: Path | None = None,
) -> List[Dict[str, str]]:
    role_rules = (
        json.dumps(
            {name: policy.to_dict() for name, policy in build_role_policies(policy_dir=role_policy_dir).items()},
            ensure_ascii=False,
            indent=2,
        )
        if include_role_rules
        else "未提供。请只根据历史时点字段做直接预测。"
    )
    renderer = PromptRenderer(prompts_dir=prompts_dir)
    return [
        {
            "role": "system",
            "content": renderer.render("baseline_system"),
        },
        {
            "role": "user",
            "content": renderer.render(
                "baseline_single_agent",
                historical_payload_json=json.dumps(_historical_payload(case), ensure_ascii=False, indent=2),
                role_rules_json=role_rules,
            ),
        },
    ]
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-baselines-py-_historical_payload-172"></a>

### _historical_payload

文件：`boardroom_sim/baselines.py:172-174`

**签名**

~~~~python
def _historical_payload(case: BoardCase) -> Dict[str, Any]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `case.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `case_dict.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.baselines._baseline_messages](#fn-boardroom_sim-baselines-py-_baseline_messages-139)

**完整源码**

~~~~python
def _historical_payload(case: BoardCase) -> Dict[str, Any]:
    case_dict = case.to_dict()
    return {field: case_dict.get(field) for field in HISTORICAL_BASELINE_FIELDS}
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-baselines-py-_coerce_prediction-177"></a>

### _coerce_prediction

文件：`boardroom_sim/baselines.py:177-196`

**签名**

~~~~python
def _coerce_prediction(raw: Dict[str, Any]) -> Dict[str, Any]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `raw` | 从文件、TOML、JSON 或 LLM 读取的原始数据。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `coerce_choice` | [boardroom_sim.agents.coerce_choice](#fn-boardroom_sim-agents-py-coerce_choice-32) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `raw.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `round` | 库函数或对象方法 | 内置四舍五入；返回数值。 |
| `coerce_text` | [boardroom_sim.agents.coerce_text](#fn-boardroom_sim-agents-py-coerce_text-49) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `min` | 库函数或对象方法 | 内置最小值函数；返回最小元素。 |
| `coerce_float` | [boardroom_sim.agents.coerce_float](#fn-boardroom_sim-agents-py-coerce_float-41) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.baselines.predict_single_case](#fn-boardroom_sim-baselines-py-predict_single_case-102)

**完整源码**

~~~~python
def _coerce_prediction(raw: Dict[str, Any]) -> Dict[str, Any]:
    financing_intent = coerce_choice(raw.get("financing_intent"), FINANCING_INTENTS, "raise_now")
    completion_view = coerce_choice(raw.get("completion_view"), COMPLETION_VIEWS, "likely_complete")
    return {
        "financing_initiation_decision": financing_intent,
        "financing_completion_view": completion_view,
        "predicted_deal_size_usd_m": round(max(0.0, coerce_float(raw.get("predicted_deal_size_usd_m"), 0.0)), 6),
        "predicted_post_money_valuation_usd_m": round(
            max(0.0, coerce_float(raw.get("predicted_post_money_valuation_usd_m"), 0.0)),
            6,
        ),
        "predicted_investor_ownership_pct": round(
            min(100.0, max(0.0, coerce_float(raw.get("predicted_investor_ownership_pct"), 0.0))),
            6,
        ),
        "predicted_deal_type": coerce_text(raw.get("predicted_deal_type"), "unknown"),
        "valuation_direction": coerce_choice(raw.get("valuation_direction"), VALUATION_CHOICES, "flat"),
        "consensus_score": None,
        "deal_break_risk": None,
    }
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-config-py"></a>

## boardroom_sim/config.py

配置模块：把 TOML 配置转换成 Python dataclass，集中管理实验变量。

**类**：[BoardProcessConfig](#class-boardroom_sim-config-py-boardprocessconfig-140)、[ExperimentConfig](#class-boardroom_sim-config-py-experimentconfig-157)

<a id="fn-boardroom_sim-config-py-experimentconfig-to_manifest_dict-175"></a>

### ExperimentConfig.to_manifest_dict

文件：`boardroom_sim/config.py:175-182`

**签名**

~~~~python
def to_manifest_dict(self) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Serialize config fields for experiment manifests.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `asdict` | 库函数或对象方法 | dataclasses 工具；把 dataclass 实例递归转换为 dict。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `data.pop` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[run_batch_experiments._write_manifest](#fn-run_batch_experiments-py-_write_manifest-593)

**完整源码**

~~~~python
    def to_manifest_dict(self) -> Dict[str, Any]:
        """Serialize config fields for experiment manifests."""
        data = asdict(self)
        data["config_path"] = str(self.config_path) if self.config_path else None
        data["role_policy_dir"] = str(self.role_policy_dir)
        data["prompts_dir"] = str(self.prompts_dir)
        data.pop("raw", None)
        return data
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-config-py-load_experiment_config-185"></a>

### load_experiment_config

文件：`boardroom_sim/config.py:185-225`

**签名**

~~~~python
def load_experiment_config(path: Optional[Path] = None) -> ExperimentConfig:
~~~~

**功能**

读取 TOML 配置，并构造 ExperimentConfig。它把 LLM、输入输出、prompt、角色和 board_process 相关配置统一装配起来。

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |

**返回值**

返回 `ExperimentConfig` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_resolve_path` | [boardroom_sim.config._resolve_path](#fn-boardroom_sim-config-py-_resolve_path-228) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `raw.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_string_list` | [boardroom_sim.config._string_list](#fn-boardroom_sim-config-py-_string_list-233) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_board_process_from_sections` | [boardroom_sim.config._board_process_from_sections](#fn-boardroom_sim-config-py-_board_process_from_sections-249) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `ExperimentConfig` | [boardroom_sim.config.ExperimentConfig](#class-boardroom_sim-config-py-experimentconfig-157) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `resolved_path.open` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `tomllib.load` | 库函数或对象方法 | 读取 TOML 文件对象并解析，返回 dict。 |
| `agents.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `discussion.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `DEFAULT_CONFIG_PATH.exists` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `dict.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `experiment.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `paths.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `decision.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.__init__](#fn-boardroom_sim-simulator-py-boardroomsimulator-__init__-32)、[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)、[run_experiment.main](#fn-run_experiment-py-main-78)

**完整源码**

~~~~python
def load_experiment_config(path: Optional[Path] = None) -> ExperimentConfig:
    """Load a boardroom experiment config, falling back to built-in defaults."""
    config_path = path or (DEFAULT_CONFIG_PATH if DEFAULT_CONFIG_PATH.exists() else None)
    if config_path is None:
        return ExperimentConfig()

    resolved_path = _resolve_path(config_path)
    with resolved_path.open("rb") as handle:
        raw = tomllib.load(handle)

    experiment = raw.get("experiment", {})
    paths = raw.get("paths", {})
    agents = raw.get("agents", {})
    board = raw.get("board", {})
    discussion = raw.get("discussion", {})
    decision = raw.get("decision", {})

    role_order = _string_list(agents.get("role_order"), DEFAULT_ROLE_ORDER)
    role_weights = {
        str(role): float(weight)
        for role, weight in dict(decision.get("weights", DEFAULT_ROLE_WEIGHTS)).items()
    }
    round_tasks = _string_list(discussion.get("round_tasks"), DEFAULT_ROUND_TASKS)
    board_process = _board_process_from_sections(board, discussion)

    return ExperimentConfig(
        name=str(experiment.get("name", resolved_path.stem)),
        config_path=resolved_path,
        role_policy_dir=_resolve_path(paths.get("role_policy_dir", "policies/roles")),
        prompts_dir=_resolve_path(paths.get("prompts_dir", "prompts")),
        role_order=role_order,
        role_weights=role_weights,
        bargaining_rounds=int(experiment.get("bargaining_rounds", 3)),
        history_limit=int(experiment.get("history_limit", 10)),
        discussion_paradigm=str(discussion.get("paradigm", "memory")),
        response_generator=str(discussion.get("response_generator", "critical")),
        decision_protocol=str(decision.get("protocol", "weighted_vote")),
        round_tasks=round_tasks,
        board_process=board_process,
        raw=raw,
    )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-config-py-_resolve_path-228"></a>

### _resolve_path

文件：`boardroom_sim/config.py:228-230`

**签名**

~~~~python
def _resolve_path(value: Any) -> Path:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Path` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `Path` | 库函数或对象方法 | pathlib 路径构造器；返回 Path 对象。 |
| `path.is_absolute` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.config.load_experiment_config](#fn-boardroom_sim-config-py-load_experiment_config-185)

**完整源码**

~~~~python
def _resolve_path(value: Any) -> Path:
    path = value if isinstance(value, Path) else Path(str(value))
    return path if path.is_absolute() else PROJECT_ROOT / path
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-config-py-_string_list-233"></a>

### _string_list

文件：`boardroom_sim/config.py:233-237`

**签名**

~~~~python
def _string_list(value: Any, default: List[str]) -> List[str]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `List[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.config.load_experiment_config](#fn-boardroom_sim-config-py-load_experiment_config-185)

**完整源码**

~~~~python
def _string_list(value: Any, default: List[str]) -> List[str]:
    if not isinstance(value, list):
        return list(default)
    cleaned = [str(item) for item in value if str(item)]
    return cleaned or list(default)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-config-py-canonicalize_board_archetype-240"></a>

### canonicalize_board_archetype

文件：`boardroom_sim/config.py:240-246`

**签名**

~~~~python
def canonicalize_board_archetype(value: Any) -> str:
~~~~

**功能**

源码文档说明：Return the canonical board archetype id used by process profiles.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `str.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `BOARD_ARCHETYPE_ALIASES.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.config._board_process_from_sections](#fn-boardroom_sim-config-py-_board_process_from_sections-249)

**完整源码**

~~~~python
def canonicalize_board_archetype(value: Any) -> str:
    """Return the canonical board archetype id used by process profiles."""
    raw = str(value or "value_creating").strip().lower()
    normalized = BOARD_ARCHETYPE_ALIASES.get(raw, raw)
    if normalized not in BOARD_ARCHETYPE_PROFILES:
        return "value_creating"
    return normalized
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-config-py-_board_process_from_sections-249"></a>

### _board_process_from_sections

文件：`boardroom_sim/config.py:249-270`

**签名**

~~~~python
def _board_process_from_sections(board: Dict[str, Any], discussion: Dict[str, Any]) -> BoardProcessConfig:
~~~~

**功能**

把配置文件中的 [board] 与 [discussion] 段落合成 BoardProcessConfig，是修改 archetype、L5 文化、可见历史和讨论流程的核心入口。

**参数**

| 参数 | 含义 |
| --- | --- |
| `board` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `discussion` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `BoardProcessConfig` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `canonicalize_board_archetype` | [boardroom_sim.config.canonicalize_board_archetype](#fn-boardroom_sim-config-py-canonicalize_board_archetype-240) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_merge_float_dict` | [boardroom_sim.config._merge_float_dict](#fn-boardroom_sim-config-py-_merge_float_dict-285) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `BoardProcessConfig` | [boardroom_sim.config.BoardProcessConfig](#class-boardroom_sim-config-py-boardprocessconfig-140) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `board.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `profile.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `_bool_value` | [boardroom_sim.config._bool_value](#fn-boardroom_sim-config-py-_bool_value-297) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_int_value` | [boardroom_sim.config._int_value](#fn-boardroom_sim-config-py-_int_value-312) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `discussion.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `bool` | 库函数或对象方法 | 内置类型转换；返回布尔值。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |

**被谁调用**

[boardroom_sim.config.load_experiment_config](#fn-boardroom_sim-config-py-load_experiment_config-185)

**完整源码**

~~~~python
def _board_process_from_sections(board: Dict[str, Any], discussion: Dict[str, Any]) -> BoardProcessConfig:
    archetype = canonicalize_board_archetype(board.get("archetype", "value_creating"))
    profile = BOARD_ARCHETYPE_PROFILES[archetype]
    culture = _merge_float_dict(profile["culture"], board.get("culture"))
    role_weight_multipliers = _merge_float_dict(
        profile.get("role_weight_multipliers", {}),
        board.get("role_weight_multipliers"),
    )

    return BoardProcessConfig(
        archetype=archetype,
        archetype_label=str(profile["label"]),
        archetype_description=str(profile["description"]),
        culture_layer="L5",
        culture=culture,
        protocol=str(discussion.get("protocol", profile["protocol"])),
        dominance_pattern=str(discussion.get("dominance_pattern", profile["dominance_pattern"])),
        challenge_required=_bool_value(discussion.get("challenge_required"), bool(profile["challenge_required"])),
        memory_scope=str(discussion.get("memory_scope", profile["memory_scope"])),
        memory_window=_int_value(discussion.get("memory_window"), int(profile["memory_window"])),
        role_weight_multipliers=role_weight_multipliers,
    )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-config-py-_float_dict-273"></a>

### _float_dict

文件：`boardroom_sim/config.py:273-282`

**签名**

~~~~python
def _float_dict(value: Any, default: Dict[str, float]) -> Dict[str, float]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `Dict[str, float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `value.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def _float_dict(value: Any, default: Dict[str, float]) -> Dict[str, float]:
    if not isinstance(value, dict):
        return dict(default)
    result: Dict[str, float] = {}
    for key, item in value.items():
        try:
            result[str(key)] = float(item)
        except (TypeError, ValueError):
            continue
    return result or dict(default)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-config-py-_merge_float_dict-285"></a>

### _merge_float_dict

文件：`boardroom_sim/config.py:285-294`

**签名**

~~~~python
def _merge_float_dict(default: Dict[str, float], override: Any) -> Dict[str, float]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `default` | 值缺失或非法时使用的默认值。 |
| `override` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `override.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.config._board_process_from_sections](#fn-boardroom_sim-config-py-_board_process_from_sections-249)

**完整源码**

~~~~python
def _merge_float_dict(default: Dict[str, float], override: Any) -> Dict[str, float]:
    result = dict(default)
    if not isinstance(override, dict):
        return result
    for key, item in override.items():
        try:
            result[str(key)] = float(item)
        except (TypeError, ValueError):
            continue
    return result
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-config-py-_bool_value-297"></a>

### _bool_value

文件：`boardroom_sim/config.py:297-309`

**签名**

~~~~python
def _bool_value(value: Any, default: bool) -> bool:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `bool` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `str.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `bool` | 库函数或对象方法 | 内置类型转换；返回布尔值。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.config._board_process_from_sections](#fn-boardroom_sim-config-py-_board_process_from_sections-249)

**完整源码**

~~~~python
def _bool_value(value: Any, default: bool) -> bool:
    if value is None:
        return default
    if isinstance(value, bool):
        return value
    if isinstance(value, (int, float)):
        return bool(value)
    text = str(value).strip().lower()
    if text in {"true", "1", "yes", "y", "on"}:
        return True
    if text in {"false", "0", "no", "n", "off"}:
        return False
    return default
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-config-py-_int_value-312"></a>

### _int_value

文件：`boardroom_sim/config.py:312-318`

**签名**

~~~~python
def _int_value(value: Any, default: int) -> int:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `int` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |

**被谁调用**

[boardroom_sim.config._board_process_from_sections](#fn-boardroom_sim-config-py-_board_process_from_sections-249)

**完整源码**

~~~~python
def _int_value(value: Any, default: int) -> int:
    if value is None:
        return default
    try:
        return int(value)
    except (TypeError, ValueError):
        return default
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-culture-py"></a>

## boardroom_sim/culture.py

L5 文化层模块：把文化分数解释为会议行为约束和 prompt 指令。

**类**：无类定义

<a id="fn-boardroom_sim-culture-py-level_for_score-63"></a>

### level_for_score

文件：`boardroom_sim/culture.py:63-74`

**签名**

~~~~python
def level_for_score(score: float) -> str:
~~~~

**功能**

源码文档说明：Map a normalized culture score to a stable qualitative level.

**参数**

| 参数 | 含义 |
| --- | --- |
| `score` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `min` | 库函数或对象方法 | 内置最小值函数；返回最小元素。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |

**被谁调用**

[boardroom_sim.culture.interpret_l5_culture](#fn-boardroom_sim-culture-py-interpret_l5_culture-77)

**完整源码**

~~~~python
def level_for_score(score: float) -> str:
    """Map a normalized culture score to a stable qualitative level."""
    bounded = max(0.0, min(1.0, float(score)))
    if bounded < 0.20:
        return "very_low"
    if bounded < 0.40:
        return "low"
    if bounded < 0.60:
        return "medium"
    if bounded < 0.80:
        return "high"
    return "very_high"
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-culture-py-interpret_l5_culture-77"></a>

### interpret_l5_culture

文件：`boardroom_sim/culture.py:77-97`

**签名**

~~~~python
def interpret_l5_culture(culture: Dict[str, float]) -> Dict[str, Any]:
~~~~

**功能**

把配置中的 L5 文化层数值解释成离散等级、行为控制和会议指令，让 LLM 看到的不只是裸数值，而是可执行的讨论规则。

**参数**

| 参数 | 含义 |
| --- | --- |
| `culture` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `L5_CULTURE_DIMENSIONS.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `_derive_behavior_controls` | [boardroom_sim.culture._derive_behavior_controls](#fn-boardroom_sim-culture-py-_derive_behavior_controls-100) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `level_for_score` | [boardroom_sim.culture.level_for_score](#fn-boardroom_sim-culture-py-level_for_score-63) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_meeting_directive` | [boardroom_sim.culture._meeting_directive](#fn-boardroom_sim-culture-py-_meeting_directive-153) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `round` | 库函数或对象方法 | 内置四舍五入；返回数值。 |
| `culture.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `min` | 库函数或对象方法 | 内置最小值函数；返回最小元素。 |

**被谁调用**

[boardroom_sim.process.BoardProcessController.prompt_context](#fn-boardroom_sim-process-py-boardprocesscontroller-prompt_context-156)

**完整源码**

~~~~python
def interpret_l5_culture(culture: Dict[str, float]) -> Dict[str, Any]:
    """Translate L5 numeric scores into levels, behavior rules, and controls."""
    dimensions: Dict[str, Dict[str, Any]] = {}
    for name, note in L5_CULTURE_DIMENSIONS.items():
        score = float(culture.get(name, 0.0) or 0.0)
        level = level_for_score(score)
        dimensions[name] = {
            "score": round(max(0.0, min(1.0, score)), 3),
            "level": level,
            "level_label": LEVEL_LABELS[level],
            "meaning": note,
            "behavior_rule": DIMENSION_BEHAVIOR_RULES[name][level],
        }

    controls = _derive_behavior_controls(dimensions)
    return {
        "definition": "L5 是董事会整体文化层，约束角色之间如何挑战、支持、让步和吸收证据；它不是单个角色的事实证据。",
        "dimensions": dimensions,
        "behavior_controls": controls,
        "meeting_directive": _meeting_directive(controls),
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-culture-py-_derive_behavior_controls-100"></a>

### _derive_behavior_controls

文件：`boardroom_sim/culture.py:100-150`

**签名**

~~~~python
def _derive_behavior_controls(dimensions: Dict[str, Dict[str, Any]]) -> Dict[str, Any]:
~~~~

**功能**

把五个文化维度的等级转换成过程控制变量，例如是否必须回应前人、是否要求替代方案、证据锚点数量和共识压力。

**参数**

| 参数 | 含义 |
| --- | --- |
| `dimensions` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.culture.interpret_l5_culture](#fn-boardroom_sim-culture-py-interpret_l5_culture-77)

**完整源码**

~~~~python
def _derive_behavior_controls(dimensions: Dict[str, Dict[str, Any]]) -> Dict[str, Any]:
    criticality = dimensions["criticality"]["level"]
    creativity = dimensions["creativity"]["level"]
    cohesiveness = dimensions["cohesiveness"]["level"]
    openness = dimensions["openness_generosity"]["level"]
    preparation = dimensions["preparation_involvement"]["level"]

    evidence_anchor_count = {
        "very_low": 0,
        "low": 1,
        "medium": 1,
        "high": 2,
        "very_high": 3,
    }[preparation]
    challenge_mode = {
        "very_low": "confirm_unless_contradiction",
        "low": "soft_question",
        "medium": "check_one_assumption",
        "high": "required_challenge",
        "very_high": "hard_challenge",
    }[criticality]
    alternative_options = {
        "very_low": "do_not_expand_option_space",
        "low": "minor_adjustments_only",
        "medium": "one_optional_alternative",
        "high": "one_required_alternative",
        "very_high": "multiple_required_alternatives",
    }[creativity]
    consensus_pressure = {
        "very_low": "open_conflict_allowed",
        "low": "direct_disagreement_allowed",
        "medium": "link_disagreement_to_common_goal",
        "high": "constructive_disagreement_required",
        "very_high": "common_goal_first_required",
    }[cohesiveness]
    prior_response = {
        "very_low": "self_evidence_only",
        "low": "optional_response",
        "medium": "respond_to_one_prior_claim",
        "high": "acknowledge_or_refute_prior_claim",
        "very_high": "integrate_prior_claims",
    }[openness]

    return {
        "challenge_mode": challenge_mode,
        "alternative_options": alternative_options,
        "consensus_pressure": consensus_pressure,
        "prior_response_requirement": prior_response,
        "minimum_evidence_anchors": evidence_anchor_count,
        "speech_length": "boardroom_substantive",
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-culture-py-_meeting_directive-153"></a>

### _meeting_directive

文件：`boardroom_sim/culture.py:153-161`

**签名**

~~~~python
def _meeting_directive(controls: Dict[str, Any]) -> str:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `controls` | 由 L5 文化层推导出的过程控制变量。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.culture.interpret_l5_culture](#fn-boardroom_sim-culture-py-interpret_l5_culture-77)

**完整源码**

~~~~python
def _meeting_directive(controls: Dict[str, Any]) -> str:
    parts = [
        f"挑战模式={controls['challenge_mode']}",
        f"替代方案要求={controls['alternative_options']}",
        f"共识压力={controls['consensus_pressure']}",
        f"回应前序发言={controls['prior_response_requirement']}",
        f"最少证据锚点={controls['minimum_evidence_anchors']}",
    ]
    return "；".join(parts) + "。"
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-evaluation-py"></a>

## boardroom_sim/evaluation.py

指标模块：读取结果并计算分类准确率、数值误差、案例级指标和摘要文本。

**类**：无类定义

<a id="fn-boardroom_sim-evaluation-py-read_result_rows-103"></a>

### read_result_rows

文件：`boardroom_sim/evaluation.py:103-121`

**签名**

~~~~python
def read_result_rows(paths: Sequence[Path]) -> List[ResultRow]:
~~~~

**功能**

源码文档说明：Read one or more compact results JSONL files.

**参数**

| 参数 | 含义 |
| --- | --- |
| `paths` | 一组文件路径。 |

**返回值**

返回 `List[ResultRow]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `enumerate` | 库函数或对象方法 | 内置枚举；返回 index 和元素组成的迭代器。 |
| `line.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `row.setdefault` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `rows.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `json.loads` | 库函数或对象方法 | 把 JSON 字符串解析为 Python 对象，通常返回 dict 或 list。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[analyze_debate_accuracy.read_baseline_metrics](#fn-analyze_debate_accuracy-py-read_baseline_metrics-209)、[evaluate_results.main](#fn-evaluate_results-py-main-26)、[run_batch_experiments._maybe_read_completed_run](#fn-run_batch_experiments-py-_maybe_read_completed_run-311)、[run_batch_experiments._read_existing_rows](#fn-run_batch_experiments-py-_read_existing_rows-419)

**完整源码**

~~~~python
def read_result_rows(paths: Sequence[Path]) -> List[ResultRow]:
    """Read one or more compact results JSONL files."""
    rows: List[ResultRow] = []
    for path in paths:
        with path.open("r", encoding="utf-8") as handle:
            for line_number, line in enumerate(handle, start=1):
                stripped = line.strip()
                if not stripped:
                    continue
                try:
                    row = json.loads(stripped)
                except json.JSONDecodeError as exc:
                    raise ValueError(f"Invalid JSON on line {line_number} of {path}") from exc
                if not isinstance(row, dict):
                    raise ValueError(f"Expected object on line {line_number} of {path}")
                row.setdefault("_source_file", str(path))
                row.setdefault("_run_id", path.parent.name)
                rows.append(row)
    return rows
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-evaluate_results-124"></a>

### evaluate_results

文件：`boardroom_sim/evaluation.py:124-145`

**签名**

~~~~python
def evaluate_results(rows: Sequence[ResultRow]) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Compute categorical accuracy and numeric error metrics.

**参数**

| 参数 | 含义 |
| --- | --- |
| `rows` | 结果行或 DataFrame 行集合。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `build_case_metrics` | [boardroom_sim.evaluation.build_case_metrics](#fn-boardroom_sim-evaluation-py-build_case_metrics-178) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_categorical_metric` | [boardroom_sim.evaluation._categorical_metric](#fn-boardroom_sim-evaluation-py-_categorical_metric-276) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_numeric_metric` | [boardroom_sim.evaluation._numeric_metric](#fn-boardroom_sim-evaluation-py-_numeric_metric-309) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |

**被谁调用**

[evaluate_results.main](#fn-evaluate_results-py-main-26)、[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)、[run_batch_experiments._write_partial_run_outputs](#fn-run_batch_experiments-py-_write_partial_run_outputs-486)

**完整源码**

~~~~python
def evaluate_results(rows: Sequence[ResultRow]) -> Dict[str, Any]:
    """Compute categorical accuracy and numeric error metrics."""
    case_metrics = build_case_metrics(rows)
    categorical = {
        spec["name"]: _categorical_metric(rows, spec)
        for spec in CATEGORICAL_METRICS
    }
    numeric = {spec["name"]: _numeric_metric(rows, spec) for spec in NUMERIC_METRICS}
    return {
        "row_count": len(rows),
        "categorical": categorical,
        "numeric": numeric,
        "deal_size": numeric["deal_size"],
        "notes": [
            "Current data is positive-sample VC backtesting; financing initiation accuracy is a positive-sample hit rate unless negative samples are added.",
            "Financing completion is not counted in accuracy because almost all current target events are completed transactions.",
            "Valuation direction excludes rows whose true label is missing or unknown.",
            "Numeric metrics skip rows whose true numeric label is missing or non-positive.",
            "CEO replacement is intentionally excluded from current evaluation.",
        ],
        "case_metrics": case_metrics,
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-metrics_summary-148"></a>

### metrics_summary

文件：`boardroom_sim/evaluation.py:148-175`

**签名**

~~~~python
def metrics_summary(metrics: Dict[str, Any]) -> str:
~~~~

**功能**

源码文档说明：Return a compact human-readable metrics report.

**参数**

| 参数 | 含义 |
| --- | --- |
| `metrics` | 评估指标字典。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `metrics.get.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `lines.extend` | 库函数或对象方法 | 列表扩展方法；把多个元素追加到列表末尾，返回 None。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `item.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_format_pct` | [boardroom_sim.evaluation._format_pct](#fn-boardroom_sim-evaluation-py-_format_pct-528) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `metrics.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_format_float` | [boardroom_sim.evaluation._format_float](#fn-boardroom_sim-evaluation-py-_format_float-533) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[evaluate_results.main](#fn-evaluate_results-py-main-26)、[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)

**完整源码**

~~~~python
def metrics_summary(metrics: Dict[str, Any]) -> str:
    """Return a compact human-readable metrics report."""
    lines = [f"Rows evaluated: {metrics.get('row_count', 0)}", "", "Categorical accuracy:"]
    for name, item in metrics.get("categorical", {}).items():
        evaluated = item.get("evaluated_count", 0)
        correct = item.get("correct_count", 0)
        accuracy = item.get("accuracy")
        accuracy_text = _format_pct(accuracy)
        skipped = item.get("skipped_count", 0)
        lines.append(f"  {name}: {correct}/{evaluated} = {accuracy_text} (skipped: {skipped})")

    lines.extend(["", "Numeric error:"])
    for name, item in metrics.get("numeric", {"deal_size": metrics.get("deal_size", {})}).items():
        unit = item.get("unit", "")
        unit_suffix = f" {unit}" if unit else ""
        lines.extend(
            [
                f"  {name}:",
                f"    evaluated: {item.get('evaluated_count', 0)} (skipped: {item.get('skipped_count', 0)})",
                f"    MAE: {_format_float(item.get('mae'))}{unit_suffix}",
                f"    RMSE: {_format_float(item.get('rmse'))}{unit_suffix}",
                f"    MAPE: {_format_pct(item.get('mape'))}",
                f"    Median APE: {_format_pct(item.get('median_ape'))}",
                f"    within +/-25%: {_format_pct(item.get('within_25pct_rate'))}",
                f"    within +/-50%: {_format_pct(item.get('within_50pct_rate'))}",
            ]
        )
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-build_case_metrics-178"></a>

### build_case_metrics

文件：`boardroom_sim/evaluation.py:178-254`

**签名**

~~~~python
def build_case_metrics(rows: Sequence[ResultRow]) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Build one per-case metrics row for CSV inspection.

**参数**

| 参数 | 含义 |
| --- | --- |
| `rows` | 结果行或 DataFrame 行集合。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_labels` | [boardroom_sim.evaluation._labels](#fn-boardroom_sim-evaluation-py-_labels-417) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_add_categorical_case_metric` | [boardroom_sim.evaluation._add_categorical_case_metric](#fn-boardroom_sim-evaluation-py-_add_categorical_case_metric-361) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_normalize` | [boardroom_sim.evaluation._normalize](#fn-boardroom_sim-evaluation-py-_normalize-422) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_add_numeric_case_metric` | [boardroom_sim.evaluation._add_numeric_case_metric](#fn-boardroom_sim-evaluation-py-_add_numeric_case_metric-384) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `case_rows.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `labels.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.evaluation.evaluate_results](#fn-boardroom_sim-evaluation-py-evaluate_results-124)

**完整源码**

~~~~python
def build_case_metrics(rows: Sequence[ResultRow]) -> List[Dict[str, Any]]:
    """Build one per-case metrics row for CSV inspection."""
    case_rows: List[Dict[str, Any]] = []
    for row in rows:
        labels = _labels(row)
        case_metric = {
            "source_file": row.get("_source_file", ""),
            "run_id": row.get("_run_id", ""),
            "case_id": row.get("case_id", ""),
            "company_name": row.get("company_name", ""),
        }

        _add_categorical_case_metric(
            case_metric,
            row,
            labels,
            metric_prefix="financing_initiation",
            prediction_field="financing_initiation_decision",
            label_field="true_financing_initiated_label",
            normalizer="financing_intent",
        )
        case_metric["pred_financing_completion"] = _normalize(row.get("financing_completion_view"), "completion")
        case_metric["true_financing_completion"] = _normalize(labels.get("real_deal_completed_label"), "completion")
        _add_categorical_case_metric(
            case_metric,
            row,
            labels,
            metric_prefix="deal_type",
            prediction_field="predicted_deal_type",
            label_field="real_deal_type",
            normalizer="deal_type",
        )
        _add_categorical_case_metric(
            case_metric,
            row,
            labels,
            metric_prefix="valuation_direction",
            prediction_field="valuation_direction",
            label_field="real_valuation_direction_label",
            normalizer="valuation_direction",
        )

        _add_numeric_case_metric(
            case_metric,
            row,
            labels,
            prefix="deal_size",
            prediction_field="predicted_deal_size_usd_m",
            label_field="real_deal_size_usd_m",
            prediction_column="pred_deal_size_usd_m",
            label_column="true_deal_size_usd_m",
            absolute_error_column="deal_size_abs_error_usd_m",
        )
        _add_numeric_case_metric(
            case_metric,
            row,
            labels,
            prefix="post_money_valuation",
            prediction_field="predicted_post_money_valuation_usd_m",
            label_field="real_post_money_valuation_usd_m",
            prediction_column="pred_post_money_valuation_usd_m",
            label_column="true_post_money_valuation_usd_m",
            absolute_error_column="post_money_valuation_abs_error",
        )
        _add_numeric_case_metric(
            case_metric,
            row,
            labels,
            prefix="investor_ownership",
            prediction_field="predicted_investor_ownership_pct",
            label_field="real_investor_ownership_pct",
            prediction_column="pred_investor_ownership_pct",
            label_column="true_investor_ownership_pct",
            absolute_error_column="investor_ownership_abs_error",
        )
        case_rows.append(case_metric)
    return case_rows
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-write_metrics_json-257"></a>

### write_metrics_json

文件：`boardroom_sim/evaluation.py:257-263`

**签名**

~~~~python
def write_metrics_json(path: Path, metrics: Dict[str, Any]) -> None:
~~~~

**功能**

源码文档说明：Write aggregate metrics without embedding per-case rows.

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `metrics` | 评估指标字典。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `payload.pop` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `json.dump` | 库函数或对象方法 | 把 Python 对象写入文件对象为 JSON，返回 None。 |

**被谁调用**

[evaluate_results.main](#fn-evaluate_results-py-main-26)、[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)、[run_batch_experiments._write_partial_run_outputs](#fn-run_batch_experiments-py-_write_partial_run_outputs-486)

**完整源码**

~~~~python
def write_metrics_json(path: Path, metrics: Dict[str, Any]) -> None:
    """Write aggregate metrics without embedding per-case rows."""
    path.parent.mkdir(parents=True, exist_ok=True)
    payload = dict(metrics)
    payload.pop("case_metrics", None)
    with path.open("w", encoding="utf-8") as handle:
        json.dump(payload, handle, ensure_ascii=False, indent=2, allow_nan=False)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-write_case_metrics_csv-266"></a>

### write_case_metrics_csv

文件：`boardroom_sim/evaluation.py:266-273`

**签名**

~~~~python
def write_case_metrics_csv(path: Path, case_metrics: Iterable[Dict[str, Any]]) -> None:
~~~~

**功能**

源码文档说明：Write per-case metric rows to CSV.

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `case_metrics` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `csv.DictWriter` | 库函数或对象方法 | 构造 CSV 字典写入器，返回 writer 对象。 |
| `writer.writeheader` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `writer.writerow` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[evaluate_results.main](#fn-evaluate_results-py-main-26)、[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)、[run_batch_experiments._write_partial_run_outputs](#fn-run_batch_experiments-py-_write_partial_run_outputs-486)

**完整源码**

~~~~python
def write_case_metrics_csv(path: Path, case_metrics: Iterable[Dict[str, Any]]) -> None:
    """Write per-case metric rows to CSV."""
    path.parent.mkdir(parents=True, exist_ok=True)
    with path.open("w", encoding="utf-8", newline="") as handle:
        writer = csv.DictWriter(handle, fieldnames=CASE_METRIC_FIELDS, extrasaction="ignore")
        writer.writeheader()
        for row in case_metrics:
            writer.writerow(row)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_categorical_metric-276"></a>

### _categorical_metric

文件：`boardroom_sim/evaluation.py:276-306`

**签名**

~~~~python
def _categorical_metric(rows: Sequence[ResultRow], spec: Dict[str, Any]) -> Dict[str, Any]:
~~~~

**功能**

评估辅助函数。它服务于结果标准化、指标计算或案例级指标写出。

**参数**

| 参数 | 含义 |
| --- | --- |
| `rows` | 结果行或 DataFrame 行集合。 |
| `spec` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_labels` | [boardroom_sim.evaluation._labels](#fn-boardroom_sim-evaluation-py-_labels-417) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_normalize` | [boardroom_sim.evaluation._normalize](#fn-boardroom_sim-evaluation-py-_normalize-422) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_is_missing_label` | [boardroom_sim.evaluation._is_missing_label](#fn-boardroom_sim-evaluation-py-_is_missing_label-500) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `confusion.setdefault` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `labels.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `spec.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `confusion.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.evaluation.evaluate_results](#fn-boardroom_sim-evaluation-py-evaluate_results-124)

**完整源码**

~~~~python
def _categorical_metric(rows: Sequence[ResultRow], spec: Dict[str, Any]) -> Dict[str, Any]:
    correct_count = 0
    evaluated_count = 0
    skipped_count = 0
    confusion: Dict[str, Dict[str, int]] = {}

    for row in rows:
        labels = _labels(row)
        prediction = _normalize(row.get(spec["prediction_field"]), spec["normalizer"])
        label = _normalize(labels.get(spec["label_field"]), spec["normalizer"])
        if _is_missing_label(label, spec.get("drop_unknown_label", True)):
            skipped_count += 1
            continue
        evaluated_count += 1
        predicted_key = prediction or "missing_prediction"
        label_key = label or "missing_label"
        confusion.setdefault(label_key, {})
        confusion[label_key][predicted_key] = confusion[label_key].get(predicted_key, 0) + 1
        if prediction == label:
            correct_count += 1

    accuracy = correct_count / evaluated_count if evaluated_count else None
    return {
        "prediction_field": spec["prediction_field"],
        "label_field": spec["label_field"],
        "evaluated_count": evaluated_count,
        "skipped_count": skipped_count,
        "correct_count": correct_count,
        "accuracy": accuracy,
        "confusion": confusion,
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_numeric_metric-309"></a>

### _numeric_metric

文件：`boardroom_sim/evaluation.py:309-353`

**签名**

~~~~python
def _numeric_metric(rows: Sequence[ResultRow], spec: Dict[str, Any]) -> Dict[str, Any]:
~~~~

**功能**

评估辅助函数。它服务于结果标准化、指标计算或案例级指标写出。

**参数**

| 参数 | 含义 |
| --- | --- |
| `rows` | 结果行或 DataFrame 行集合。 |
| `spec` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `_labels` | [boardroom_sim.evaluation._labels](#fn-boardroom_sim-evaluation-py-_labels-417) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_optional_float` | [boardroom_sim.evaluation._optional_float](#fn-boardroom_sim-evaluation-py-_optional_float-514) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `abs` | 库函数或对象方法 | 内置绝对值；返回数值。 |
| `errors.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `squared_errors.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `predicted_values.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `true_values.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `spec.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_mean` | [boardroom_sim.evaluation._mean](#fn-boardroom_sim-evaluation-py-_mean-524) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `labels.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `pct_errors.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `math.sqrt` | 库函数或对象方法 | math 数学函数；返回数值或布尔判断。 |
| `median` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.evaluation.evaluate_results](#fn-boardroom_sim-evaluation-py-evaluate_results-124)、[boardroom_sim.evaluation._deal_size_metric](#fn-boardroom_sim-evaluation-py-_deal_size_metric-356)

**完整源码**

~~~~python
def _numeric_metric(rows: Sequence[ResultRow], spec: Dict[str, Any]) -> Dict[str, Any]:
    errors: List[float] = []
    squared_errors: List[float] = []
    pct_errors: List[float] = []
    predicted_values: List[float] = []
    true_values: List[float] = []
    skipped_count = 0

    for row in rows:
        labels = _labels(row)
        predicted = _optional_float(row.get(spec["prediction_field"]))
        true = _optional_float(labels.get(spec["label_field"]))
        if predicted is None or true is None or (spec.get("positive_label_required", True) and true <= 0):
            skipped_count += 1
            continue
        error = abs(predicted - true)
        errors.append(error)
        squared_errors.append(error**2)
        if true != 0:
            pct_errors.append(error / abs(true))
        predicted_values.append(predicted)
        true_values.append(true)

    evaluated_count = len(errors)
    metric = {
        "prediction_field": spec["prediction_field"],
        "label_field": spec["label_field"],
        "unit": spec.get("unit", ""),
        "evaluated_count": evaluated_count,
        "skipped_count": skipped_count,
        "mae": _mean(errors),
        "rmse": math.sqrt(_mean(squared_errors)) if squared_errors else None,
        "mape": _mean(pct_errors),
        "median_ape": median(pct_errors) if pct_errors else None,
        "within_25pct_rate": _mean([1.0 if value <= 0.25 else 0.0 for value in pct_errors]),
        "within_50pct_rate": _mean([1.0 if value <= 0.50 else 0.0 for value in pct_errors]),
        "mean_predicted": _mean(predicted_values),
        "mean_true": _mean(true_values),
    }
    if spec["name"] == "deal_size":
        metric["mae_usd_m"] = metric["mae"]
        metric["rmse_usd_m"] = metric["rmse"]
        metric["mean_predicted_usd_m"] = metric["mean_predicted"]
        metric["mean_true_usd_m"] = metric["mean_true"]
    return metric
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_deal_size_metric-356"></a>

### _deal_size_metric

文件：`boardroom_sim/evaluation.py:356-358`

**签名**

~~~~python
def _deal_size_metric(rows: Sequence[ResultRow]) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Backward-compatible deal-size metric wrapper.

**参数**

| 参数 | 含义 |
| --- | --- |
| `rows` | 结果行或 DataFrame 行集合。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_numeric_metric` | [boardroom_sim.evaluation._numeric_metric](#fn-boardroom_sim-evaluation-py-_numeric_metric-309) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def _deal_size_metric(rows: Sequence[ResultRow]) -> Dict[str, Any]:
    """Backward-compatible deal-size metric wrapper."""
    return _numeric_metric(rows, NUMERIC_METRICS[0])
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_add_categorical_case_metric-361"></a>

### _add_categorical_case_metric

文件：`boardroom_sim/evaluation.py:361-381`

**签名**

~~~~python
def _add_categorical_case_metric( case_metric: Dict[str, Any], row: ResultRow, labels: Dict[str, Any], *, metric_prefix: str, prediction_field: str, label_field: str, normalizer: str, ) -> None:
~~~~

**功能**

评估辅助函数。它服务于结果标准化、指标计算或案例级指标写出。

**参数**

| 参数 | 含义 |
| --- | --- |
| `case_metric` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `row` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `labels` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `metric_prefix` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `prediction_field` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `label_field` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `normalizer` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_normalize` | [boardroom_sim.evaluation._normalize](#fn-boardroom_sim-evaluation-py-_normalize-422) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `labels.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_is_missing_label` | [boardroom_sim.evaluation._is_missing_label](#fn-boardroom_sim-evaluation-py-_is_missing_label-500) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.evaluation.build_case_metrics](#fn-boardroom_sim-evaluation-py-build_case_metrics-178)

**完整源码**

~~~~python
def _add_categorical_case_metric(
    case_metric: Dict[str, Any],
    row: ResultRow,
    labels: Dict[str, Any],
    *,
    metric_prefix: str,
    prediction_field: str,
    label_field: str,
    normalizer: str,
) -> None:
    prediction = _normalize(row.get(prediction_field), normalizer)
    label = _normalize(labels.get(label_field), normalizer)
    short_prefix = {
        "financing_initiation": "financing_initiation",
        "financing_completion": "financing_completion",
        "deal_type": "deal_type",
        "valuation_direction": "valuation_direction",
    }[metric_prefix]
    case_metric[f"pred_{short_prefix}"] = prediction
    case_metric[f"true_{short_prefix}"] = label
    case_metric[f"{short_prefix}_correct"] = None if _is_missing_label(label, True) else prediction == label
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_add_numeric_case_metric-384"></a>

### _add_numeric_case_metric

文件：`boardroom_sim/evaluation.py:384-414`

**签名**

~~~~python
def _add_numeric_case_metric( case_metric: Dict[str, Any], row: ResultRow, labels: Dict[str, Any], *, prefix: str, prediction_field: str, label_field: str, prediction_column: str, label_column: str, absolute_error_column: str, ) -> None:
~~~~

**功能**

评估辅助函数。它服务于结果标准化、指标计算或案例级指标写出。

**参数**

| 参数 | 含义 |
| --- | --- |
| `case_metric` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `row` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `labels` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `prefix` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `prediction_field` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `label_field` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `prediction_column` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `label_column` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `absolute_error_column` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_optional_float` | [boardroom_sim.evaluation._optional_float](#fn-boardroom_sim-evaluation-py-_optional_float-514) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `labels.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `abs` | 库函数或对象方法 | 内置绝对值；返回数值。 |

**被谁调用**

[boardroom_sim.evaluation.build_case_metrics](#fn-boardroom_sim-evaluation-py-build_case_metrics-178)

**完整源码**

~~~~python
def _add_numeric_case_metric(
    case_metric: Dict[str, Any],
    row: ResultRow,
    labels: Dict[str, Any],
    *,
    prefix: str,
    prediction_field: str,
    label_field: str,
    prediction_column: str,
    label_column: str,
    absolute_error_column: str,
) -> None:
    predicted = _optional_float(row.get(prediction_field))
    true = _optional_float(labels.get(label_field))
    case_metric[prediction_column] = predicted
    case_metric[label_column] = true
    pct_error_column = f"{prefix}_abs_pct_error"
    within_25_column = f"{prefix}_within_25pct"
    within_50_column = f"{prefix}_within_50pct"
    if predicted is not None and true is not None and true > 0:
        absolute_error = abs(predicted - true)
        absolute_pct_error = absolute_error / true
        case_metric[absolute_error_column] = absolute_error
        case_metric[pct_error_column] = absolute_pct_error
        case_metric[within_25_column] = absolute_pct_error <= 0.25
        case_metric[within_50_column] = absolute_pct_error <= 0.50
    else:
        case_metric[absolute_error_column] = None
        case_metric[pct_error_column] = None
        case_metric[within_25_column] = None
        case_metric[within_50_column] = None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_labels-417"></a>

### _labels

文件：`boardroom_sim/evaluation.py:417-419`

**签名**

~~~~python
def _labels(row: ResultRow) -> Dict[str, Any]:
~~~~

**功能**

评估辅助函数。它服务于结果标准化、指标计算或案例级指标写出。

**参数**

| 参数 | 含义 |
| --- | --- |
| `row` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |

**被谁调用**

[boardroom_sim.evaluation.build_case_metrics](#fn-boardroom_sim-evaluation-py-build_case_metrics-178)、[boardroom_sim.evaluation._categorical_metric](#fn-boardroom_sim-evaluation-py-_categorical_metric-276)、[boardroom_sim.evaluation._numeric_metric](#fn-boardroom_sim-evaluation-py-_numeric_metric-309)

**完整源码**

~~~~python
def _labels(row: ResultRow) -> Dict[str, Any]:
    labels = row.get("labels", {})
    return labels if isinstance(labels, dict) else {}
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_normalize-422"></a>

### _normalize

文件：`boardroom_sim/evaluation.py:422-433`

**签名**

~~~~python
def _normalize(value: Any, normalizer: str) -> Optional[str]:
~~~~

**功能**

数据清洗/标准化函数。它把原始值转换为实验需要的类型或标签，并在缺失、非法、大小写不一致时提供稳定兜底。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `normalizer` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_is_missing_value` | [boardroom_sim.evaluation._is_missing_value](#fn-boardroom_sim-evaluation-py-_is_missing_value-490) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_normalize_token` | [boardroom_sim.evaluation._normalize_token](#fn-boardroom_sim-evaluation-py-_normalize_token-436) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_normalize_completion` | [boardroom_sim.evaluation._normalize_completion](#fn-boardroom_sim-evaluation-py-_normalize_completion-445) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_normalize_deal_type` | [boardroom_sim.evaluation._normalize_deal_type](#fn-boardroom_sim-evaluation-py-_normalize_deal_type-461) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_normalize_valuation_direction` | [boardroom_sim.evaluation._normalize_valuation_direction](#fn-boardroom_sim-evaluation-py-_normalize_valuation_direction-477) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.evaluation.build_case_metrics](#fn-boardroom_sim-evaluation-py-build_case_metrics-178)、[boardroom_sim.evaluation._categorical_metric](#fn-boardroom_sim-evaluation-py-_categorical_metric-276)、[boardroom_sim.evaluation._add_categorical_case_metric](#fn-boardroom_sim-evaluation-py-_add_categorical_case_metric-361)

**完整源码**

~~~~python
def _normalize(value: Any, normalizer: str) -> Optional[str]:
    if _is_missing_value(value):
        return None
    if normalizer == "financing_intent":
        return _normalize_token(value)
    if normalizer == "completion":
        return _normalize_completion(value)
    if normalizer == "deal_type":
        return _normalize_deal_type(value)
    if normalizer == "valuation_direction":
        return _normalize_valuation_direction(value)
    return _normalize_token(value)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_normalize_token-436"></a>

### _normalize_token

文件：`boardroom_sim/evaluation.py:436-442`

**签名**

~~~~python
def _normalize_token(value: Any) -> Optional[str]:
~~~~

**功能**

数据清洗/标准化函数。它把原始值转换为实验需要的类型或标签，并在缺失、非法、大小写不一致时提供稳定兜底。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_is_missing_value` | [boardroom_sim.evaluation._is_missing_value](#fn-boardroom_sim-evaluation-py-_is_missing_value-490) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `str.strip.lower.replace.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `text.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `str.strip.lower.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `str.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.evaluation._normalize](#fn-boardroom_sim-evaluation-py-_normalize-422)、[boardroom_sim.evaluation._normalize_completion](#fn-boardroom_sim-evaluation-py-_normalize_completion-445)、[boardroom_sim.evaluation._normalize_valuation_direction](#fn-boardroom_sim-evaluation-py-_normalize_valuation_direction-477)

**完整源码**

~~~~python
def _normalize_token(value: Any) -> Optional[str]:
    if _is_missing_value(value):
        return None
    text = str(value).strip().lower().replace("-", "_").replace(" ", "_")
    while "__" in text:
        text = text.replace("__", "_")
    return text or None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_normalize_completion-445"></a>

### _normalize_completion

文件：`boardroom_sim/evaluation.py:445-458`

**签名**

~~~~python
def _normalize_completion(value: Any) -> Optional[str]:
~~~~

**功能**

数据清洗/标准化函数。它把原始值转换为实验需要的类型或标签，并在缺失、非法、大小写不一致时提供稳定兜底。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_normalize_token` | [boardroom_sim.evaluation._normalize_token](#fn-boardroom_sim-evaluation-py-_normalize_token-436) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.evaluation._normalize](#fn-boardroom_sim-evaluation-py-_normalize-422)

**完整源码**

~~~~python
def _normalize_completion(value: Any) -> Optional[str]:
    token = _normalize_token(value)
    if token is None:
        return None
    completed = {"completed", "complete", "closed", "done", "likely_complete"}
    failed = {"cancelled", "canceled", "terminated", "failed", "dead", "unlikely_complete"}
    pending = {"announced", "pending", "in_progress", "open", "uncertain"}
    if token in completed:
        return "likely_complete"
    if token in failed:
        return "unlikely_complete"
    if token in pending:
        return "uncertain"
    return token
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_normalize_deal_type-461"></a>

### _normalize_deal_type

文件：`boardroom_sim/evaluation.py:461-474`

**签名**

~~~~python
def _normalize_deal_type(value: Any) -> Optional[str]:
~~~~

**功能**

数据清洗/标准化函数。它把原始值转换为实验需要的类型或标签，并在缺失、非法、大小写不一致时提供稳定兜底。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_is_missing_value` | [boardroom_sim.evaluation._is_missing_value](#fn-boardroom_sim-evaluation-py-_is_missing_value-490) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `aliases.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `str.strip.lower.replace.replace.split` | 库函数或对象方法 | 字符串拆分，返回 list[str]。 |
| `str.strip.lower.replace.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `str.strip.lower.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `str.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.evaluation._normalize](#fn-boardroom_sim-evaluation-py-_normalize-422)

**完整源码**

~~~~python
def _normalize_deal_type(value: Any) -> Optional[str]:
    if _is_missing_value(value):
        return None
    text = " ".join(str(value).strip().lower().replace("-", " ").replace("_", " ").split())
    aliases = {
        "seed": "seed round",
        "seed vc": "seed round",
        "early stage": "early stage vc",
        "early stage venture": "early stage vc",
        "later stage": "later stage vc",
        "later stage venture": "later stage vc",
        "venture capital": "other",
    }
    return aliases.get(text, text)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_normalize_valuation_direction-477"></a>

### _normalize_valuation_direction

文件：`boardroom_sim/evaluation.py:477-487`

**签名**

~~~~python
def _normalize_valuation_direction(value: Any) -> Optional[str]:
~~~~

**功能**

数据清洗/标准化函数。它把原始值转换为实验需要的类型或标签，并在缺失、非法、大小写不一致时提供稳定兜底。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_normalize_token` | [boardroom_sim.evaluation._normalize_token](#fn-boardroom_sim-evaluation-py-_normalize_token-436) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_is_missing_value` | [boardroom_sim.evaluation._is_missing_value](#fn-boardroom_sim-evaluation-py-_is_missing_value-490) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `str.lower.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `str.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.evaluation._normalize](#fn-boardroom_sim-evaluation-py-_normalize-422)

**完整源码**

~~~~python
def _normalize_valuation_direction(value: Any) -> Optional[str]:
    token = _normalize_token(str(value).lower().replace(" round", "")) if not _is_missing_value(value) else None
    if token in {"up", "upround", "up_round"}:
        return "up"
    if token in {"flat", "flattish", "flatround", "flat_round"}:
        return "flat"
    if token in {"down", "downround", "down_round"}:
        return "down"
    if token in {"unknown", "none", "nan", "n/a", "na"}:
        return None
    return token
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_is_missing_value-490"></a>

### _is_missing_value

文件：`boardroom_sim/evaluation.py:490-497`

**签名**

~~~~python
def _is_missing_value(value: Any) -> bool:
~~~~

**功能**

判断函数。它检查输入是否满足某种条件，返回 bool。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `bool` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `math.isnan` | 库函数或对象方法 | 判断浮点 NaN，返回 bool。 |
| `value.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |

**被谁调用**

[boardroom_sim.evaluation._normalize](#fn-boardroom_sim-evaluation-py-_normalize-422)、[boardroom_sim.evaluation._normalize_token](#fn-boardroom_sim-evaluation-py-_normalize_token-436)、[boardroom_sim.evaluation._normalize_deal_type](#fn-boardroom_sim-evaluation-py-_normalize_deal_type-461)、[boardroom_sim.evaluation._normalize_valuation_direction](#fn-boardroom_sim-evaluation-py-_normalize_valuation_direction-477)、[boardroom_sim.evaluation._optional_float](#fn-boardroom_sim-evaluation-py-_optional_float-514)

**完整源码**

~~~~python
def _is_missing_value(value: Any) -> bool:
    if value is None:
        return True
    if isinstance(value, float) and math.isnan(value):
        return True
    if isinstance(value, str) and not value.strip():
        return True
    return False
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_is_missing_label-500"></a>

### _is_missing_label

文件：`boardroom_sim/evaluation.py:500-511`

**签名**

~~~~python
def _is_missing_label(value: Optional[str], drop_unknown_label: bool) -> bool:
~~~~

**功能**

判断函数。它检查输入是否满足某种条件，返回 bool。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `drop_unknown_label` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `bool` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.evaluation._categorical_metric](#fn-boardroom_sim-evaluation-py-_categorical_metric-276)、[boardroom_sim.evaluation._add_categorical_case_metric](#fn-boardroom_sim-evaluation-py-_add_categorical_case_metric-361)

**完整源码**

~~~~python
def _is_missing_label(value: Optional[str], drop_unknown_label: bool) -> bool:
    if value is None:
        return True
    return drop_unknown_label and value in {
        "unknown",
        "none",
        "nan",
        "n/a",
        "na",
        "unknown_deal_type",
        "unknown deal type",
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_optional_float-514"></a>

### _optional_float

文件：`boardroom_sim/evaluation.py:514-521`

**签名**

~~~~python
def _optional_float(value: Any) -> Optional[float]:
~~~~

**功能**

数据清洗/标准化函数。它把原始值转换为实验需要的类型或标签，并在缺失、非法、大小写不一致时提供稳定兜底。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_is_missing_value` | [boardroom_sim.evaluation._is_missing_value](#fn-boardroom_sim-evaluation-py-_is_missing_value-490) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `math.isfinite` | 库函数或对象方法 | math 数学函数；返回数值或布尔判断。 |

**被谁调用**

[boardroom_sim.evaluation._numeric_metric](#fn-boardroom_sim-evaluation-py-_numeric_metric-309)、[boardroom_sim.evaluation._add_numeric_case_metric](#fn-boardroom_sim-evaluation-py-_add_numeric_case_metric-384)、[boardroom_sim.evaluation._format_pct](#fn-boardroom_sim-evaluation-py-_format_pct-528)、[boardroom_sim.evaluation._format_float](#fn-boardroom_sim-evaluation-py-_format_float-533)

**完整源码**

~~~~python
def _optional_float(value: Any) -> Optional[float]:
    if _is_missing_value(value):
        return None
    try:
        number = float(value)
    except (TypeError, ValueError):
        return None
    return number if math.isfinite(number) else None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_mean-524"></a>

### _mean

文件：`boardroom_sim/evaluation.py:524-525`

**签名**

~~~~python
def _mean(values: Sequence[float]) -> Optional[float]:
~~~~

**功能**

评估辅助函数。它服务于结果标准化、指标计算或案例级指标写出。

**参数**

| 参数 | 含义 |
| --- | --- |
| `values` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `sum` | 库函数或对象方法 | 内置求和；返回可迭代数值总和。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |

**被谁调用**

[boardroom_sim.evaluation._numeric_metric](#fn-boardroom_sim-evaluation-py-_numeric_metric-309)

**完整源码**

~~~~python
def _mean(values: Sequence[float]) -> Optional[float]:
    return sum(values) / len(values) if values else None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_format_pct-528"></a>

### _format_pct

文件：`boardroom_sim/evaluation.py:528-530`

**签名**

~~~~python
def _format_pct(value: Any) -> str:
~~~~

**功能**

格式化函数。它把数值或标签转成报告中更易读的字符串。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_optional_float` | [boardroom_sim.evaluation._optional_float](#fn-boardroom_sim-evaluation-py-_optional_float-514) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.evaluation.metrics_summary](#fn-boardroom_sim-evaluation-py-metrics_summary-148)

**完整源码**

~~~~python
def _format_pct(value: Any) -> str:
    number = _optional_float(value)
    return "n/a" if number is None else f"{number * 100:.2f}%"
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-evaluation-py-_format_float-533"></a>

### _format_float

文件：`boardroom_sim/evaluation.py:533-535`

**签名**

~~~~python
def _format_float(value: Any) -> str:
~~~~

**功能**

格式化函数。它把数值或标签转成报告中更易读的字符串。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_optional_float` | [boardroom_sim.evaluation._optional_float](#fn-boardroom_sim-evaluation-py-_optional_float-514) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.evaluation.metrics_summary](#fn-boardroom_sim-evaluation-py-metrics_summary-148)

**完整源码**

~~~~python
def _format_float(value: Any) -> str:
    number = _optional_float(value)
    return "n/a" if number is None else f"{number:.3f}"
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-io-py"></a>

## boardroom_sim/io.py

输入输出模块：读写 JSONL 结果、案例和 trace。

**类**：无类定义

<a id="fn-boardroom_sim-io-py-read_cases_jsonl-12"></a>

### read_cases_jsonl

文件：`boardroom_sim/io.py:12-25`

**签名**

~~~~python
def read_cases_jsonl(path: Path) -> List[BoardCase]:
~~~~

**功能**

源码文档说明：Read board simulation cases from a JSONL file.

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |

**返回值**

返回 `List[BoardCase]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `enumerate` | 库函数或对象方法 | 内置枚举；返回 index 和元素组成的迭代器。 |
| `line.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `cases.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `json.loads` | 库函数或对象方法 | 把 JSON 字符串解析为 Python 对象，通常返回 dict 或 list。 |
| `BoardCase.from_dict` | [boardroom_sim.models.BoardCase.from_dict](#fn-boardroom_sim-models-py-boardcase-from_dict-179) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[run_experiment.load_cases](#fn-run_experiment-py-load_cases-48)

**完整源码**

~~~~python
def read_cases_jsonl(path: Path) -> List[BoardCase]:
    """Read board simulation cases from a JSONL file."""
    cases: List[BoardCase] = []
    with path.open("r", encoding="utf-8") as handle:
        for line_number, line in enumerate(handle, start=1):
            stripped = line.strip()
            if not stripped:
                continue
            try:
                raw = json.loads(stripped)
            except json.JSONDecodeError as exc:
                raise ValueError(f"Invalid JSON on line {line_number} of {path}") from exc
            cases.append(BoardCase.from_dict(raw))
    return cases
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-io-py-write_cases_jsonl-28"></a>

### write_cases_jsonl

文件：`boardroom_sim/io.py:28-33`

**签名**

~~~~python
def write_cases_jsonl(path: Path, cases: Iterable[BoardCase]) -> None:
~~~~

**功能**

源码文档说明：Write normalized board simulation cases to a JSONL file.

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `cases` | 多个 BoardCase 或 trace case，用于批量运行或统计。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `handle.write` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `json.dumps` | 库函数或对象方法 | 把 Python 对象序列化为 JSON 字符串，返回 str。 |
| `case.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def write_cases_jsonl(path: Path, cases: Iterable[BoardCase]) -> None:
    """Write normalized board simulation cases to a JSONL file."""
    path.parent.mkdir(parents=True, exist_ok=True)
    with path.open("w", encoding="utf-8") as handle:
        for case in cases:
            handle.write(json.dumps(case.to_dict(), ensure_ascii=False, allow_nan=False) + "\n")
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-io-py-write_results_jsonl-36"></a>

### write_results_jsonl

文件：`boardroom_sim/io.py:36-41`

**签名**

~~~~python
def write_results_jsonl(path: Path, results: Iterable[SimulationResult]) -> None:
~~~~

**功能**

源码文档说明：Write compact simulation results to a JSONL file.

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `results` | SimulationResult 或基线预测结果集合。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `handle.write` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `json.dumps` | 库函数或对象方法 | 把 Python 对象序列化为 JSON 字符串，返回 str。 |
| `result.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |

**被谁调用**

[run_experiment.main](#fn-run_experiment-py-main-78)

**完整源码**

~~~~python
def write_results_jsonl(path: Path, results: Iterable[SimulationResult]) -> None:
    """Write compact simulation results to a JSONL file."""
    path.parent.mkdir(parents=True, exist_ok=True)
    with path.open("w", encoding="utf-8") as handle:
        for result in results:
            handle.write(json.dumps(result.to_dict(include_trace=False), ensure_ascii=False, allow_nan=False) + "\n")
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-io-py-write_traces_json-44"></a>

### write_traces_json

文件：`boardroom_sim/io.py:44-49`

**签名**

~~~~python
def write_traces_json(path: Path, results: Iterable[SimulationResult]) -> None:
~~~~

**功能**

源码文档说明：Write detailed simulation traces to one JSON file.

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `results` | SimulationResult 或基线预测结果集合。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `result.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `json.dump` | 库函数或对象方法 | 把 Python 对象写入文件对象为 JSON，返回 None。 |

**被谁调用**

[run_experiment.main](#fn-run_experiment-py-main-78)

**完整源码**

~~~~python
def write_traces_json(path: Path, results: Iterable[SimulationResult]) -> None:
    """Write detailed simulation traces to one JSON file."""
    path.parent.mkdir(parents=True, exist_ok=True)
    payload = [result.to_dict(include_trace=True) for result in results]
    with path.open("w", encoding="utf-8") as handle:
        json.dump(payload, handle, ensure_ascii=False, indent=2, allow_nan=False)
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-llm-py"></a>

## boardroom_sim/llm.py

LLM 客户端模块：封装 OpenAI 兼容接口、JSON 抽取、重试和用量记录。

**类**：[LLMConfig](#class-boardroom_sim-llm-py-llmconfig-32)、[LLMClient](#class-boardroom_sim-llm-py-llmclient-95)

<a id="fn-boardroom_sim-llm-py-llmconfig-from_env-44"></a>

### LLMConfig.from_env

文件：`boardroom_sim/llm.py:44-92`

**签名**

~~~~python
def from_env( cls, *, api_key: Optional[str] = None, api_key_env: str = "BOARDROOM_LLM_API_KEY", model: Optional[str] = None, base_url: Optional[str] = None, temperature: Optional[float] = None, timeout_seconds: Optional[int] = None, http_max_retries: Optional[int] = None, retry_base_seconds: Optional[float] = None, ) -> "LLMConfig":
~~~~

**功能**

源码文档说明：Create configuration from environment variables and CLI overrides.

**参数**

| 参数 | 含义 |
| --- | --- |
| `cls` | 当前类对象，常用于 from_dict 这类类方法构造实例。 |
| `api_key` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `api_key_env` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `model` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `base_url` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `temperature` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `timeout_seconds` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `http_max_retries` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `retry_base_seconds` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `'LLMConfig'` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `cls` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `os.environ.get.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `os.environ.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |

**被谁调用**

[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)、[run_experiment.main](#fn-run_experiment-py-main-78)

**完整源码**

~~~~python
    def from_env(
        cls,
        *,
        api_key: Optional[str] = None,
        api_key_env: str = "BOARDROOM_LLM_API_KEY",
        model: Optional[str] = None,
        base_url: Optional[str] = None,
        temperature: Optional[float] = None,
        timeout_seconds: Optional[int] = None,
        http_max_retries: Optional[int] = None,
        retry_base_seconds: Optional[float] = None,
    ) -> "LLMConfig":
        """Create configuration from environment variables and CLI overrides."""
        api_key = api_key or os.environ.get(api_key_env, "").strip()
        if not api_key:
            raise ValueError(f"Missing API key. Please set environment variable {api_key_env} or pass --api-key.")

        resolved_model = model or os.environ.get("BOARDROOM_LLM_MODEL", "glm-4.7-flash")
        resolved_base_url = base_url or os.environ.get("BOARDROOM_LLM_BASE_URL", cls.base_url)
        resolved_temperature = (
            temperature
            if temperature is not None
            else float(os.environ.get("BOARDROOM_LLM_TEMPERATURE", cls.temperature))
        )
        resolved_timeout = (
            timeout_seconds
            if timeout_seconds is not None
            else int(os.environ.get("BOARDROOM_LLM_TIMEOUT_SECONDS", cls.timeout_seconds))
        )
        resolved_http_max_retries = (
            http_max_retries
            if http_max_retries is not None
            else int(os.environ.get("BOARDROOM_LLM_HTTP_MAX_RETRIES", cls.http_max_retries))
        )
        resolved_retry_base_seconds = (
            retry_base_seconds
            if retry_base_seconds is not None
            else float(os.environ.get("BOARDROOM_LLM_RETRY_BASE_SECONDS", cls.retry_base_seconds))
        )

        return cls(
            api_key=api_key,
            model=resolved_model,
            base_url=resolved_base_url,
            temperature=resolved_temperature,
            timeout_seconds=resolved_timeout,
            http_max_retries=resolved_http_max_retries,
            retry_base_seconds=resolved_retry_base_seconds,
        )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-llm-py-llmclient-__init__-98"></a>

### LLMClient.__init__

文件：`boardroom_sim/llm.py:98-103`

**签名**

~~~~python
def __init__(self, config: LLMConfig) -> None:
~~~~

**功能**

源码文档说明：Initialize the client from a validated LLM configuration.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `config` | 配置对象，控制 LLM、角色、prompt、过程或输出。 |

**返回值**

初始化方法，通常返回 `None`；主要副作用是给 `self` 写入属性。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
    def __init__(self, config: LLMConfig) -> None:
        """Initialize the client from a validated LLM configuration."""
        self.config = config
        self.total_prompt_tokens = 0
        self.total_completion_tokens = 0
        self.total_tokens = 0
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-llm-py-llmclient-complete_text-105"></a>

### LLMClient.complete_text

文件：`boardroom_sim/llm.py:105-149`

**签名**

~~~~python
def complete_text(self, messages: List[Dict[str, str]]) -> str:
~~~~

**功能**

执行一次 OpenAI 兼容聊天补全请求，带重试、错误处理和 token 用量记录。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `messages` | 发给 LLM 的 chat messages。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `json.dumps.encode` | 库函数或对象方法 | json 标准库函数；用于 JSON 序列化或反序列化。 |
| `range` | 库函数或对象方法 | 内置整数序列；返回 range 对象。 |
| `self._update_usage` | [boardroom_sim.llm.LLMClient._update_usage](#fn-boardroom_sim-llm-py-llmclient-_update_usage-173) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `urllib.request.Request` | 库函数或对象方法 | 构造 HTTP 请求对象。 |
| `response_payload.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `response_payload.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `self.config.base_url.rstrip` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `json.dumps` | 库函数或对象方法 | 把 Python 对象序列化为 JSON 字符串，返回 str。 |
| `RuntimeError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `urllib.request.urlopen` | 库函数或对象方法 | 发送 HTTP 请求，返回响应对象；读取响应体需要调用 response.read()。 |
| `json.loads` | 库函数或对象方法 | 把 JSON 字符串解析为 Python 对象，通常返回 dict 或 list。 |
| `exc.read.decode` | 库函数或对象方法 | bytes 解码为字符串，返回 str。 |
| `self._should_retry_http` | [boardroom_sim.llm.LLMClient._should_retry_http](#fn-boardroom_sim-llm-py-llmclient-_should_retry_http-179) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `response.read.decode` | 库函数或对象方法 | bytes 解码为字符串，返回 str。 |
| `self._sleep_before_retry` | [boardroom_sim.llm.LLMClient._sleep_before_retry](#fn-boardroom_sim-llm-py-llmclient-_sleep_before_retry-183) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `exc.read` | 库函数或对象方法 | 文件或响应对象读取；返回 bytes 或 str，取决于对象类型。 |
| `exc.headers.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `self._network_error_reason` | [boardroom_sim.llm.LLMClient._network_error_reason](#fn-boardroom_sim-llm-py-llmclient-_network_error_reason-193) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `response.read` | 库函数或对象方法 | 文件或响应对象读取；返回 bytes 或 str，取决于对象类型。 |
| `type` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.llm.LLMClient.complete_json](#fn-boardroom_sim-llm-py-llmclient-complete_json-151)

**完整源码**

~~~~python
    def complete_text(self, messages: List[Dict[str, str]]) -> str:
        """Call the chat-completions endpoint and return assistant text."""
        url = f"{self.config.base_url.rstrip('/')}/chat/completions"
        payload = {
            "model": self.config.model,
            "messages": messages,
            "temperature": self.config.temperature,
        }
        body = json.dumps(payload).encode("utf-8")

        for attempt in range(self.config.http_max_retries + 1):
            request = urllib.request.Request(
                url,
                data=body,
                headers={
                    "Authorization": f"Bearer {self.config.api_key}",
                    "Content-Type": "application/json",
                },
                method="POST",
            )
            try:
                with urllib.request.urlopen(request, timeout=self.config.timeout_seconds) as response:
                    response_payload = json.loads(response.read().decode("utf-8"))
                break
            except urllib.error.HTTPError as exc:
                error_body = exc.read().decode("utf-8", errors="replace")
                if self._should_retry_http(exc.code, attempt):
                    retry_after = exc.headers.get("Retry-After") if exc.headers else None
                    self._sleep_before_retry(attempt, f"HTTP {exc.code}", retry_after)
                    continue
                raise RuntimeError(f"LLM HTTP error {exc.code}: {error_body}") from exc
            except RETRYABLE_NETWORK_EXCEPTIONS as exc:
                if attempt < self.config.http_max_retries:
                    self._sleep_before_retry(attempt, self._network_error_reason(exc))
                    continue
                raise RuntimeError(
                    f"LLM request failed after {attempt + 1} attempts: "
                    f"{type(exc).__name__}: {exc}"
                ) from exc

        self._update_usage(response_payload.get("usage", {}))
        try:
            return response_payload["choices"][0]["message"]["content"].strip()
        except (KeyError, IndexError, TypeError) as exc:
            raise RuntimeError(f"Unexpected LLM response format: {response_payload}") from exc
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-llm-py-llmclient-complete_json-151"></a>

### LLMClient.complete_json

文件：`boardroom_sim/llm.py:151-171`

**签名**

~~~~python
def complete_json(self, messages: List[Dict[str, str]], max_retries: int = 2) -> Dict[str, Any]:
~~~~

**功能**

调用 complete_text 并解析 JSON；如果返回无法解析，会追加修复指令重试。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `messages` | 发给 LLM 的 chat messages。 |
| `max_retries` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |
| `range` | 库函数或对象方法 | 内置整数序列；返回 range 对象。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `self.complete_text` | [boardroom_sim.llm.LLMClient.complete_text](#fn-boardroom_sim-llm-py-llmclient-complete_text-105) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `extract_json_object` | [boardroom_sim.llm.extract_json_object](#fn-boardroom_sim-llm-py-extract_json_object-210) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `attempt_messages.extend` | 库函数或对象方法 | 列表扩展方法；把多个元素追加到列表末尾，返回 None。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.evaluate](#fn-boardroom_sim-agents-py-boardagent-evaluate-94)、[boardroom_sim.agents.BoardAgent.bargaining_step](#fn-boardroom_sim-agents-py-boardagent-bargaining_step-123)、[boardroom_sim.baselines.predict_single_case](#fn-boardroom_sim-baselines-py-predict_single_case-102)

**完整源码**

~~~~python
    def complete_json(self, messages: List[Dict[str, str]], max_retries: int = 2) -> Dict[str, Any]:
        """Call the LLM and parse the response as a JSON object."""
        attempt_messages = list(messages)
        last_text = ""
        for attempt in range(max_retries + 1):
            last_text = self.complete_text(attempt_messages)
            try:
                return extract_json_object(last_text)
            except ValueError:
                if attempt >= max_retries:
                    break
                attempt_messages.extend(
                    [
                        {"role": "assistant", "content": last_text},
                        {
                            "role": "user",
                            "content": "你上一次的回答不是合法 JSON。请只返回一个 JSON 对象，不要输出 Markdown 或解释。",
                        },
                    ]
                )
        raise ValueError(f"LLM did not return valid JSON after retries. Last response: {last_text}")
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-llm-py-llmclient-_update_usage-173"></a>

### LLMClient._update_usage

文件：`boardroom_sim/llm.py:173-177`

**签名**

~~~~python
def _update_usage(self, usage: Dict[str, Any]) -> None:
~~~~

**功能**

源码文档说明：Update token counters when the provider returns usage metadata.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `usage` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `usage.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.llm.LLMClient.complete_text](#fn-boardroom_sim-llm-py-llmclient-complete_text-105)

**完整源码**

~~~~python
    def _update_usage(self, usage: Dict[str, Any]) -> None:
        """Update token counters when the provider returns usage metadata."""
        self.total_prompt_tokens += int(usage.get("prompt_tokens", 0) or 0)
        self.total_completion_tokens += int(usage.get("completion_tokens", 0) or 0)
        self.total_tokens += int(usage.get("total_tokens", 0) or 0)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-llm-py-llmclient-_should_retry_http-179"></a>

### LLMClient._should_retry_http

文件：`boardroom_sim/llm.py:179-181`

**签名**

~~~~python
def _should_retry_http(self, status_code: int, attempt: int) -> bool:
~~~~

**功能**

源码文档说明：Return whether an HTTP failure is transient enough to retry.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `status_code` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `attempt` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `bool` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.llm.LLMClient.complete_text](#fn-boardroom_sim-llm-py-llmclient-complete_text-105)

**完整源码**

~~~~python
    def _should_retry_http(self, status_code: int, attempt: int) -> bool:
        """Return whether an HTTP failure is transient enough to retry."""
        return status_code in RETRYABLE_HTTP_STATUS_CODES and attempt < self.config.http_max_retries
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-llm-py-llmclient-_sleep_before_retry-183"></a>

### LLMClient._sleep_before_retry

文件：`boardroom_sim/llm.py:183-191`

**签名**

~~~~python
def _sleep_before_retry(self, attempt: int, reason: str, retry_after: Optional[str] = None) -> None:
~~~~

**功能**

源码文档说明：Sleep with bounded exponential backoff before retrying a transient LLM error.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `attempt` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `reason` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `retry_after` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self._retry_delay_seconds` | [boardroom_sim.llm.LLMClient._retry_delay_seconds](#fn-boardroom_sim-llm-py-llmclient-_retry_delay_seconds-200) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |
| `time.sleep` | 库函数或对象方法 | 暂停当前线程，返回 None。 |

**被谁调用**

[boardroom_sim.llm.LLMClient.complete_text](#fn-boardroom_sim-llm-py-llmclient-complete_text-105)

**完整源码**

~~~~python
    def _sleep_before_retry(self, attempt: int, reason: str, retry_after: Optional[str] = None) -> None:
        """Sleep with bounded exponential backoff before retrying a transient LLM error."""
        delay = self._retry_delay_seconds(attempt, retry_after)
        print(
            f"LLM request failed with {reason}; retrying in {delay:.1f}s "
            f"({attempt + 1}/{self.config.http_max_retries}).",
            file=sys.stderr,
        )
        time.sleep(delay)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-llm-py-llmclient-_network_error_reason-193"></a>

### LLMClient._network_error_reason

文件：`boardroom_sim/llm.py:193-198`

**签名**

~~~~python
def _network_error_reason(self, exc: BaseException) -> str:
~~~~

**功能**

源码文档说明：Return a compact retry reason for transient network failures.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `exc` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `getattr` | 库函数或对象方法 | 内置属性读取；返回对象属性值或默认值。 |
| `type` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.llm.LLMClient.complete_text](#fn-boardroom_sim-llm-py-llmclient-complete_text-105)

**完整源码**

~~~~python
    def _network_error_reason(self, exc: BaseException) -> str:
        """Return a compact retry reason for transient network failures."""
        if isinstance(exc, urllib.error.URLError):
            reason = getattr(exc, "reason", exc)
            return f"network error {type(reason).__name__}"
        return f"network error {type(exc).__name__}"
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-llm-py-llmclient-_retry_delay_seconds-200"></a>

### LLMClient._retry_delay_seconds

文件：`boardroom_sim/llm.py:200-207`

**签名**

~~~~python
def _retry_delay_seconds(self, attempt: int, retry_after: Optional[str] = None) -> float:
~~~~

**功能**

源码文档说明：Compute retry delay, honoring Retry-After when the provider sends it.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `attempt` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `retry_after` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `float` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `min` | 库函数或对象方法 | 内置最小值函数；返回最小元素。 |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |

**被谁调用**

[boardroom_sim.llm.LLMClient._sleep_before_retry](#fn-boardroom_sim-llm-py-llmclient-_sleep_before_retry-183)

**完整源码**

~~~~python
    def _retry_delay_seconds(self, attempt: int, retry_after: Optional[str] = None) -> float:
        """Compute retry delay, honoring Retry-After when the provider sends it."""
        if retry_after:
            try:
                return min(60.0, max(0.0, float(retry_after)))
            except ValueError:
                pass
        return min(60.0, self.config.retry_base_seconds * (2**attempt))
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-llm-py-extract_json_object-210"></a>

### extract_json_object

文件：`boardroom_sim/llm.py:210-227`

**签名**

~~~~python
def extract_json_object(text: str) -> Dict[str, Any]:
~~~~

**功能**

从模型输出文本中抽取第一个 JSON 对象，兼容模型在 JSON 前后夹带解释文字的情况。

**参数**

| 参数 | 含义 |
| --- | --- |
| `text` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `text.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `re.search` | 库函数或对象方法 | 正则搜索；返回 match 对象或 None。 |
| `fenced_match.group.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `json.loads` | 库函数或对象方法 | 把 JSON 字符串解析为 Python 对象，通常返回 dict 或 list。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `fenced_match.group` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `object_match.group` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `type` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.llm.LLMClient.complete_json](#fn-boardroom_sim-llm-py-llmclient-complete_json-151)

**完整源码**

~~~~python
def extract_json_object(text: str) -> Dict[str, Any]:
    """Extract one JSON object from raw model text."""
    cleaned = text.strip()
    fenced_match = re.search(r"```(?:json)?\s*(\{.*?\})\s*```", cleaned, flags=re.DOTALL)
    if fenced_match:
        cleaned = fenced_match.group(1).strip()

    try:
        parsed = json.loads(cleaned)
    except json.JSONDecodeError:
        object_match = re.search(r"\{.*\}", cleaned, flags=re.DOTALL)
        if not object_match:
            raise ValueError(f"No JSON object found in response: {text}")
        parsed = json.loads(object_match.group(0))

    if not isinstance(parsed, dict):
        raise ValueError(f"Expected a JSON object but received: {type(parsed).__name__}")
    return parsed
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-models-py"></a>

## boardroom_sim/models.py

数据模型模块：定义 BoardCase、RoleDecision、TermSheetProposal、SimulationResult 等核心结构。

**类**：[BoardCase](#class-boardroom_sim-models-py-boardcase-120)、[RolePolicy](#class-boardroom_sim-models-py-rolepolicy-281)、[RoleDecision](#class-boardroom_sim-models-py-roledecision-297)、[TermSheetProposal](#class-boardroom_sim-models-py-termsheetproposal-318)、[SimulationResult](#class-boardroom_sim-models-py-simulationresult-336)

<a id="fn-boardroom_sim-models-py-is_missing-16"></a>

### is_missing

文件：`boardroom_sim/models.py:16-18`

**签名**

~~~~python
def is_missing(value: Any) -> bool:
~~~~

**功能**

源码文档说明：Return whether a loose JSON/Python value should be treated as missing.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `bool` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `math.isnan` | 库函数或对象方法 | 判断浮点 NaN，返回 bool。 |

**被谁调用**

[boardroom_sim.models.first_present](#fn-boardroom_sim-models-py-first_present-21)、[boardroom_sim.models.safe_float](#fn-boardroom_sim-models-py-safe_float-29)、[boardroom_sim.models.safe_optional_float](#fn-boardroom_sim-models-py-safe_optional_float-39)、[boardroom_sim.models.safe_int](#fn-boardroom_sim-models-py-safe_int-50)、[boardroom_sim.models.safe_optional_int](#fn-boardroom_sim-models-py-safe_optional_int-60)、[boardroom_sim.models.safe_bool](#fn-boardroom_sim-models-py-safe_bool-70)、[boardroom_sim.models.safe_str](#fn-boardroom_sim-models-py-safe_str-86)、[boardroom_sim.models.safe_str_list](#fn-boardroom_sim-models-py-safe_str_list-93)

**完整源码**

~~~~python
def is_missing(value: Any) -> bool:
    """Return whether a loose JSON/Python value should be treated as missing."""
    return value is None or value == "" or (isinstance(value, float) and math.isnan(value))
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-first_present-21"></a>

### first_present

文件：`boardroom_sim/models.py:21-26`

**签名**

~~~~python
def first_present(*values: Any) -> Any:
~~~~

**功能**

源码文档说明：Return the first value that is not missing.

**参数**

| 参数 | 含义 |
| --- | --- |
| `*values` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Any` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing` | [boardroom_sim.models.is_missing](#fn-boardroom_sim-models-py-is_missing-16) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.models.BoardCase.from_dict](#fn-boardroom_sim-models-py-boardcase-from_dict-179)

**完整源码**

~~~~python
def first_present(*values: Any) -> Any:
    """Return the first value that is not missing."""
    for value in values:
        if not is_missing(value):
            return value
    return None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-safe_float-29"></a>

### safe_float

文件：`boardroom_sim/models.py:29-36`

**签名**

~~~~python
def safe_float(value: Any, default: float = 0.0) -> float:
~~~~

**功能**

源码文档说明：Convert a value to float while providing a stable default for missing data.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `float` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing` | [boardroom_sim.models.is_missing](#fn-boardroom_sim-models-py-is_missing-16) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def safe_float(value: Any, default: float = 0.0) -> float:
    """Convert a value to float while providing a stable default for missing data."""
    if is_missing(value):
        return default
    try:
        return float(value)
    except (TypeError, ValueError):
        return default
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-safe_optional_float-39"></a>

### safe_optional_float

文件：`boardroom_sim/models.py:39-47`

**签名**

~~~~python
def safe_optional_float(value: Any) -> Optional[float]:
~~~~

**功能**

源码文档说明：Convert a value to float while preserving missing data as None.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing` | [boardroom_sim.models.is_missing](#fn-boardroom_sim-models-py-is_missing-16) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `math.isfinite` | 库函数或对象方法 | math 数学函数；返回数值或布尔判断。 |

**被谁调用**

[boardroom_sim.models.BoardCase.from_dict](#fn-boardroom_sim-models-py-boardcase-from_dict-179)

**完整源码**

~~~~python
def safe_optional_float(value: Any) -> Optional[float]:
    """Convert a value to float while preserving missing data as None."""
    if is_missing(value):
        return None
    try:
        number = float(value)
    except (TypeError, ValueError):
        return None
    return number if math.isfinite(number) else None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-safe_int-50"></a>

### safe_int

文件：`boardroom_sim/models.py:50-57`

**签名**

~~~~python
def safe_int(value: Any, default: int = 0) -> int:
~~~~

**功能**

源码文档说明：Convert a value to int while providing a stable default for missing data.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `int` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing` | [boardroom_sim.models.is_missing](#fn-boardroom_sim-models-py-is_missing-16) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |

**被谁调用**

[boardroom_sim.models.BoardCase.from_dict](#fn-boardroom_sim-models-py-boardcase-from_dict-179)

**完整源码**

~~~~python
def safe_int(value: Any, default: int = 0) -> int:
    """Convert a value to int while providing a stable default for missing data."""
    if is_missing(value):
        return default
    try:
        return int(float(value))
    except (TypeError, ValueError):
        return default
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-safe_optional_int-60"></a>

### safe_optional_int

文件：`boardroom_sim/models.py:60-67`

**签名**

~~~~python
def safe_optional_int(value: Any) -> Optional[int]:
~~~~

**功能**

源码文档说明：Convert a value to int while preserving missing data as None.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[int]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing` | [boardroom_sim.models.is_missing](#fn-boardroom_sim-models-py-is_missing-16) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |

**被谁调用**

[boardroom_sim.models.BoardCase.from_dict](#fn-boardroom_sim-models-py-boardcase-from_dict-179)

**完整源码**

~~~~python
def safe_optional_int(value: Any) -> Optional[int]:
    """Convert a value to int while preserving missing data as None."""
    if is_missing(value):
        return None
    try:
        return int(float(value))
    except (TypeError, ValueError):
        return None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-safe_bool-70"></a>

### safe_bool

文件：`boardroom_sim/models.py:70-83`

**签名**

~~~~python
def safe_bool(value: Any, default: bool = False) -> bool:
~~~~

**功能**

源码文档说明：Convert a loose value to bool while handling missing data.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `bool` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing` | [boardroom_sim.models.is_missing](#fn-boardroom_sim-models-py-is_missing-16) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `str.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `bool` | 库函数或对象方法 | 内置类型转换；返回布尔值。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.models.BoardCase.from_dict](#fn-boardroom_sim-models-py-boardcase-from_dict-179)

**完整源码**

~~~~python
def safe_bool(value: Any, default: bool = False) -> bool:
    """Convert a loose value to bool while handling missing data."""
    if is_missing(value):
        return default
    if isinstance(value, bool):
        return value
    if isinstance(value, (int, float)):
        return bool(value)
    text = str(value).strip().lower()
    if text in {"true", "1", "yes", "y"}:
        return True
    if text in {"false", "0", "no", "n"}:
        return False
    return default
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-safe_str-86"></a>

### safe_str

文件：`boardroom_sim/models.py:86-90`

**签名**

~~~~python
def safe_str(value: Any, default: str = "unknown") -> str:
~~~~

**功能**

源码文档说明：Convert a value to string while preserving a stable default for missing data.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing` | [boardroom_sim.models.is_missing](#fn-boardroom_sim-models-py-is_missing-16) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.models.safe_direction](#fn-boardroom_sim-models-py-safe_direction-111)、[boardroom_sim.models.BoardCase.from_dict](#fn-boardroom_sim-models-py-boardcase-from_dict-179)

**完整源码**

~~~~python
def safe_str(value: Any, default: str = "unknown") -> str:
    """Convert a value to string while preserving a stable default for missing data."""
    if is_missing(value):
        return default
    return str(value)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-safe_str_list-93"></a>

### safe_str_list

文件：`boardroom_sim/models.py:93-101`

**签名**

~~~~python
def safe_str_list(value: Any) -> List[str]:
~~~~

**功能**

源码文档说明：Convert a loose value into a clean list of strings.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `List[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `is_missing` | [boardroom_sim.models.is_missing](#fn-boardroom_sim-models-py-is_missing-16) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.models.BoardCase.from_dict](#fn-boardroom_sim-models-py-boardcase-from_dict-179)

**完整源码**

~~~~python
def safe_str_list(value: Any) -> List[str]:
    """Convert a loose value into a clean list of strings."""
    if is_missing(value):
        return []
    if isinstance(value, list):
        return [str(item).strip() for item in value if str(item).strip()]
    if isinstance(value, tuple) or isinstance(value, set):
        return [str(item).strip() for item in value if str(item).strip()]
    return [str(value).strip()] if str(value).strip() else []
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-safe_dict_list-104"></a>

### safe_dict_list

文件：`boardroom_sim/models.py:104-108`

**签名**

~~~~python
def safe_dict_list(value: Any) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Convert a loose value into a list of dictionaries.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |

**被谁调用**

[boardroom_sim.models.BoardCase.from_dict](#fn-boardroom_sim-models-py-boardcase-from_dict-179)

**完整源码**

~~~~python
def safe_dict_list(value: Any) -> List[Dict[str, Any]]:
    """Convert a loose value into a list of dictionaries."""
    if not isinstance(value, list):
        return []
    return [dict(item) for item in value if isinstance(item, dict)]
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-safe_direction-111"></a>

### safe_direction

文件：`boardroom_sim/models.py:111-116`

**签名**

~~~~python
def safe_direction(value: Any) -> ValuationDirection:
~~~~

**功能**

源码文档说明：Normalize a valuation direction label.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `ValuationDirection` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `safe_str.strip.lower.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `safe_str.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `safe_str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `safe_str` | [boardroom_sim.models.safe_str](#fn-boardroom_sim-models-py-safe_str-86) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.models.BoardCase.from_dict](#fn-boardroom_sim-models-py-boardcase-from_dict-179)

**完整源码**

~~~~python
def safe_direction(value: Any) -> ValuationDirection:
    """Normalize a valuation direction label."""
    text = safe_str(value, "unknown").strip().lower().replace(" round", "")
    if text in {"up", "flat", "down", "unknown"}:
        return text  # type: ignore[return-value]
    return "unknown"
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-boardcase-from_dict-179"></a>

### BoardCase.from_dict

文件：`boardroom_sim/models.py:179-248`

**签名**

~~~~python
def from_dict(cls, data: Dict[str, Any]) -> "BoardCase":
~~~~

**功能**

源码文档说明：Build a BoardCase from a loose JSON dictionary.

**参数**

| 参数 | 含义 |
| --- | --- |
| `cls` | 当前类对象，常用于 from_dict 这类类方法构造实例。 |
| `data` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `'BoardCase'` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `cls` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `safe_str` | [boardroom_sim.models.safe_str](#fn-boardroom_sim-models-py-safe_str-86) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `safe_optional_int` | [boardroom_sim.models.safe_optional_int](#fn-boardroom_sim-models-py-safe_optional_int-60) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `safe_optional_float` | [boardroom_sim.models.safe_optional_float](#fn-boardroom_sim-models-py-safe_optional_float-39) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `safe_str_list` | [boardroom_sim.models.safe_str_list](#fn-boardroom_sim-models-py-safe_str_list-93) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `safe_int` | [boardroom_sim.models.safe_int](#fn-boardroom_sim-models-py-safe_int-50) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `safe_direction` | [boardroom_sim.models.safe_direction](#fn-boardroom_sim-models-py-safe_direction-111) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `safe_dict_list` | [boardroom_sim.models.safe_dict_list](#fn-boardroom_sim-models-py-safe_dict_list-104) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `safe_bool` | [boardroom_sim.models.safe_bool](#fn-boardroom_sim-models-py-safe_bool-70) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `data.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `first_present` | [boardroom_sim.models.first_present](#fn-boardroom_sim-models-py-first_present-21) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |

**被谁调用**

[boardroom_sim.io.read_cases_jsonl](#fn-boardroom_sim-io-py-read_cases_jsonl-12)

**完整源码**

~~~~python
    def from_dict(cls, data: Dict[str, Any]) -> "BoardCase":
        """Build a BoardCase from a loose JSON dictionary."""
        return cls(
            case_id=safe_str(data.get("case_id"), "case_unknown"),
            company_id=safe_str(data.get("company_id"), "unknown_company"),
            target_deal_id=safe_str(data.get("target_deal_id") or data.get("deal_id"), "unknown_deal"),
            company_label=safe_str(data.get("company_label") or data.get("company_name"), "unknown_company"),
            decision_date=safe_str(data.get("decision_date") or data.get("deal_date"), ""),
            business_status=safe_str(data.get("business_status"), "unknown"),
            company_financing_status=safe_str(data.get("company_financing_status"), "unknown"),
            ownership_status=safe_str(data.get("ownership_status"), "unknown"),
            year_founded=safe_optional_int(data.get("year_founded")),
            company_age_at_decision_years=safe_optional_float(
                first_present(data.get("company_age_at_decision_years"), data.get("company_age_at_deal_years"))
            ),
            primary_industry=safe_str(data.get("primary_industry") or data.get("industry"), "unknown_industry"),
            industry_group=safe_str(data.get("industry_group"), "unknown"),
            industry_sector=safe_str(data.get("industry_sector"), "unknown"),
            verticals=safe_str_list(data.get("verticals")),
            keywords=safe_str(data.get("keywords"), ""),
            description=safe_str(data.get("description"), ""),
            prior_vc_deal_count=safe_int(data.get("prior_vc_deal_count")),
            prior_deal_date=safe_str(data.get("prior_deal_date"), ""),
            prior_deal_type=safe_str(data.get("prior_deal_type"), "unknown_prior_deal_type"),
            prior_vc_round=safe_str(data.get("prior_vc_round"), "unknown_prior_vc_round"),
            prior_deal_size_usd_m=safe_optional_float(
                first_present(data.get("prior_deal_size_usd_m"), data.get("previous_deal_size_usd_m"))
            ),
            prior_pre_money_valuation_usd_m=safe_optional_float(data.get("prior_pre_money_valuation_usd_m")),
            prior_post_money_valuation_usd_m=safe_optional_float(
                first_present(data.get("prior_post_money_valuation_usd_m"), data.get("previous_post_money_valuation_usd_m"))
            ),
            prior_raised_to_date_usd_m=safe_optional_float(data.get("prior_raised_to_date_usd_m")),
            prior_investor_ownership_pct=safe_optional_float(data.get("prior_investor_ownership_pct")),
            months_since_prior_deal=safe_optional_float(data.get("months_since_prior_deal")),
            prior_valuation_markup_multiple=safe_optional_float(data.get("prior_valuation_markup_multiple")),
            prior_valuation_direction_label=safe_direction(data.get("prior_valuation_direction_label")),
            prior_investor_count=safe_optional_int(data.get("prior_investor_count")),
            prior_new_investor_count=safe_optional_int(data.get("prior_new_investor_count")),
            prior_followon_investor_count=safe_optional_int(data.get("prior_followon_investor_count")),
            prior_lead_investor_count=safe_int(data.get("prior_lead_investor_count")),
            prior_lead_investor_types=safe_str_list(data.get("prior_lead_investor_types")),
            prior_lead_investor_amount_share=safe_optional_float(data.get("prior_lead_investor_amount_share")),
            prior_lead_preferred_deal_size_min_usd_m=safe_optional_float(data.get("prior_lead_preferred_deal_size_min_usd_m")),
            prior_lead_preferred_deal_size_max_usd_m=safe_optional_float(data.get("prior_lead_preferred_deal_size_max_usd_m")),
            prior_lead_preferred_company_valuation_min_usd_m=safe_optional_float(
                data.get("prior_lead_preferred_company_valuation_min_usd_m")
            ),
            prior_lead_preferred_company_valuation_max_usd_m=safe_optional_float(
                data.get("prior_lead_preferred_company_valuation_max_usd_m")
            ),
            prior_company_deal_history=safe_dict_list(data.get("prior_company_deal_history")),
            prior_lead_investor_deal_history=safe_dict_list(data.get("prior_lead_investor_deal_history")),
            prior_followon_investor_deal_history=safe_dict_list(data.get("prior_followon_investor_deal_history")),
            employee_count_at_decision=safe_optional_float(
                first_present(data.get("employee_count_at_decision"), data.get("employee_count_at_deal"))
            ),
            previous_employee_count=safe_optional_float(data.get("previous_employee_count")),
            employee_growth_rate=safe_optional_float(data.get("employee_growth_rate")),
            engineering_role_count=safe_int(data.get("engineering_role_count")),
            executive_role_count=safe_int(data.get("executive_role_count")),
            founder_count=safe_int(data.get("founder_count")),
            current_ceo_count=safe_int(data.get("current_ceo_count")),
            current_ceo_is_founder=safe_bool(data.get("current_ceo_is_founder")),
            board_member_count=safe_int(data.get("board_member_count")),
            investor_board_member_count=safe_int(data.get("investor_board_member_count")),
            competitor_count=safe_int(data.get("competitor_count")),
            similar_company_count=safe_int(data.get("similar_company_count")),
            notes=data.get("notes", {}) if isinstance(data.get("notes", {}), dict) else {},
        )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-boardcase-to_dict-250"></a>

### BoardCase.to_dict

文件：`boardroom_sim/models.py:250-252`

**签名**

~~~~python
def to_dict(self) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Serialize the case to a plain dictionary.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `asdict` | 库函数或对象方法 | dataclasses 工具；把 dataclass 实例递归转换为 dict。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.observe](#fn-boardroom_sim-agents-py-boardagent-observe-89)、[boardroom_sim.agents.BoardAgent._role_policy_text](#fn-boardroom_sim-agents-py-boardagent-_role_policy_text-168)、[boardroom_sim.agents.BoardAgent._bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197)、[boardroom_sim.baselines._baseline_messages](#fn-boardroom_sim-baselines-py-_baseline_messages-139)、[boardroom_sim.baselines._historical_payload](#fn-boardroom_sim-baselines-py-_historical_payload-172)、[boardroom_sim.io.write_cases_jsonl](#fn-boardroom_sim-io-py-write_cases_jsonl-28)、[boardroom_sim.io.write_results_jsonl](#fn-boardroom_sim-io-py-write_results_jsonl-36)、[boardroom_sim.io.write_traces_json](#fn-boardroom_sim-io-py-write_traces_json-44)、[boardroom_sim.pitchbook._primary_industry_by_company](#fn-boardroom_sim-pitchbook-py-_primary_industry_by_company-391)、[boardroom_sim.pitchbook._index_by_id](#fn-boardroom_sim-pitchbook-py-_index_by_id-415)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、另有 7 个调用方

**完整源码**

~~~~python
    def to_dict(self) -> Dict[str, Any]:
        """Serialize the case to a plain dictionary."""
        return asdict(self)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-boardcase-prior_valuation_markup-254"></a>

### BoardCase.prior_valuation_markup

文件：`boardroom_sim/models.py:254-258`

**签名**

~~~~python
def prior_valuation_markup(self) -> Optional[float]:
~~~~

**功能**

源码文档说明：Return the latest historical valuation markup when available.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.models.BoardCase.prior_valuation_direction](#fn-boardroom_sim-models-py-boardcase-prior_valuation_direction-260)

**完整源码**

~~~~python
    def prior_valuation_markup(self) -> Optional[float]:
        """Return the latest historical valuation markup when available."""
        if self.prior_valuation_markup_multiple is not None and self.prior_valuation_markup_multiple > 0:
            return self.prior_valuation_markup_multiple
        return None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-boardcase-prior_valuation_direction-260"></a>

### BoardCase.prior_valuation_direction

文件：`boardroom_sim/models.py:260-271`

**签名**

~~~~python
def prior_valuation_direction(self) -> ValuationDirection:
~~~~

**功能**

源码文档说明：Return the latest historical valuation direction.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `ValuationDirection` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self.prior_valuation_markup` | [boardroom_sim.models.BoardCase.prior_valuation_markup](#fn-boardroom_sim-models-py-boardcase-prior_valuation_markup-254) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator._background_summary](#fn-boardroom_sim-simulator-py-boardroomsimulator-_background_summary-153)

**完整源码**

~~~~python
    def prior_valuation_direction(self) -> ValuationDirection:
        """Return the latest historical valuation direction."""
        if self.prior_valuation_direction_label != "unknown":
            return self.prior_valuation_direction_label
        markup = self.prior_valuation_markup()
        if markup is None:
            return "unknown"
        if markup >= 1.15:
            return "up"
        if markup <= 0.85:
            return "down"
        return "flat"
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-boardcase-estimated_dilution_pct-273"></a>

### BoardCase.estimated_dilution_pct

文件：`boardroom_sim/models.py:273-277`

**签名**

~~~~python
def estimated_dilution_pct(self, deal_size_usd_m: float) -> Optional[float]:
~~~~

**功能**

源码文档说明：Estimate dilution using predicted new money and prior post-money valuation.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `deal_size_usd_m` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator._build_proposal](#fn-boardroom_sim-simulator-py-boardroomsimulator-_build_proposal-172)

**完整源码**

~~~~python
    def estimated_dilution_pct(self, deal_size_usd_m: float) -> Optional[float]:
        """Estimate dilution using predicted new money and prior post-money valuation."""
        if deal_size_usd_m <= 0 or self.prior_post_money_valuation_usd_m is None or self.prior_post_money_valuation_usd_m <= 0:
            return None
        return deal_size_usd_m / (self.prior_post_money_valuation_usd_m + deal_size_usd_m) * 100.0
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-rolepolicy-to_dict-291"></a>

### RolePolicy.to_dict

文件：`boardroom_sim/models.py:291-293`

**签名**

~~~~python
def to_dict(self) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Serialize a role policy to a plain dictionary.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `asdict` | 库函数或对象方法 | dataclasses 工具；把 dataclass 实例递归转换为 dict。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.observe](#fn-boardroom_sim-agents-py-boardagent-observe-89)、[boardroom_sim.agents.BoardAgent._role_policy_text](#fn-boardroom_sim-agents-py-boardagent-_role_policy_text-168)、[boardroom_sim.agents.BoardAgent._bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197)、[boardroom_sim.baselines._baseline_messages](#fn-boardroom_sim-baselines-py-_baseline_messages-139)、[boardroom_sim.baselines._historical_payload](#fn-boardroom_sim-baselines-py-_historical_payload-172)、[boardroom_sim.io.write_cases_jsonl](#fn-boardroom_sim-io-py-write_cases_jsonl-28)、[boardroom_sim.io.write_results_jsonl](#fn-boardroom_sim-io-py-write_results_jsonl-36)、[boardroom_sim.io.write_traces_json](#fn-boardroom_sim-io-py-write_traces_json-44)、[boardroom_sim.pitchbook._primary_industry_by_company](#fn-boardroom_sim-pitchbook-py-_primary_industry_by_company-391)、[boardroom_sim.pitchbook._index_by_id](#fn-boardroom_sim-pitchbook-py-_index_by_id-415)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、另有 7 个调用方

**完整源码**

~~~~python
    def to_dict(self) -> Dict[str, Any]:
        """Serialize a role policy to a plain dictionary."""
        return asdict(self)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-roledecision-to_dict-312"></a>

### RoleDecision.to_dict

文件：`boardroom_sim/models.py:312-314`

**签名**

~~~~python
def to_dict(self) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Serialize a role decision to a plain dictionary.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `asdict` | 库函数或对象方法 | dataclasses 工具；把 dataclass 实例递归转换为 dict。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.observe](#fn-boardroom_sim-agents-py-boardagent-observe-89)、[boardroom_sim.agents.BoardAgent._role_policy_text](#fn-boardroom_sim-agents-py-boardagent-_role_policy_text-168)、[boardroom_sim.agents.BoardAgent._bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197)、[boardroom_sim.baselines._baseline_messages](#fn-boardroom_sim-baselines-py-_baseline_messages-139)、[boardroom_sim.baselines._historical_payload](#fn-boardroom_sim-baselines-py-_historical_payload-172)、[boardroom_sim.io.write_cases_jsonl](#fn-boardroom_sim-io-py-write_cases_jsonl-28)、[boardroom_sim.io.write_results_jsonl](#fn-boardroom_sim-io-py-write_results_jsonl-36)、[boardroom_sim.io.write_traces_json](#fn-boardroom_sim-io-py-write_traces_json-44)、[boardroom_sim.pitchbook._primary_industry_by_company](#fn-boardroom_sim-pitchbook-py-_primary_industry_by_company-391)、[boardroom_sim.pitchbook._index_by_id](#fn-boardroom_sim-pitchbook-py-_index_by_id-415)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、另有 7 个调用方

**完整源码**

~~~~python
    def to_dict(self) -> Dict[str, Any]:
        """Serialize a role decision to a plain dictionary."""
        return asdict(self)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-termsheetproposal-to_dict-330"></a>

### TermSheetProposal.to_dict

文件：`boardroom_sim/models.py:330-332`

**签名**

~~~~python
def to_dict(self) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Serialize a term-sheet proposal to a plain dictionary.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `asdict` | 库函数或对象方法 | dataclasses 工具；把 dataclass 实例递归转换为 dict。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.observe](#fn-boardroom_sim-agents-py-boardagent-observe-89)、[boardroom_sim.agents.BoardAgent._role_policy_text](#fn-boardroom_sim-agents-py-boardagent-_role_policy_text-168)、[boardroom_sim.agents.BoardAgent._bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197)、[boardroom_sim.baselines._baseline_messages](#fn-boardroom_sim-baselines-py-_baseline_messages-139)、[boardroom_sim.baselines._historical_payload](#fn-boardroom_sim-baselines-py-_historical_payload-172)、[boardroom_sim.io.write_cases_jsonl](#fn-boardroom_sim-io-py-write_cases_jsonl-28)、[boardroom_sim.io.write_results_jsonl](#fn-boardroom_sim-io-py-write_results_jsonl-36)、[boardroom_sim.io.write_traces_json](#fn-boardroom_sim-io-py-write_traces_json-44)、[boardroom_sim.pitchbook._primary_industry_by_company](#fn-boardroom_sim-pitchbook-py-_primary_industry_by_company-391)、[boardroom_sim.pitchbook._index_by_id](#fn-boardroom_sim-pitchbook-py-_index_by_id-415)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、另有 7 个调用方

**完整源码**

~~~~python
    def to_dict(self) -> Dict[str, Any]:
        """Serialize a term-sheet proposal to a plain dictionary."""
        return asdict(self)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-models-py-simulationresult-to_dict-355"></a>

### SimulationResult.to_dict

文件：`boardroom_sim/models.py:355-360`

**签名**

~~~~python
def to_dict(self, include_trace: bool = True) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Serialize the result and optionally omit the detailed trace.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `include_trace` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `asdict` | 库函数或对象方法 | dataclasses 工具；把 dataclass 实例递归转换为 dict。 |
| `data.pop` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent.observe](#fn-boardroom_sim-agents-py-boardagent-observe-89)、[boardroom_sim.agents.BoardAgent._role_policy_text](#fn-boardroom_sim-agents-py-boardagent-_role_policy_text-168)、[boardroom_sim.agents.BoardAgent._bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197)、[boardroom_sim.baselines._baseline_messages](#fn-boardroom_sim-baselines-py-_baseline_messages-139)、[boardroom_sim.baselines._historical_payload](#fn-boardroom_sim-baselines-py-_historical_payload-172)、[boardroom_sim.io.write_cases_jsonl](#fn-boardroom_sim-io-py-write_cases_jsonl-28)、[boardroom_sim.io.write_results_jsonl](#fn-boardroom_sim-io-py-write_results_jsonl-36)、[boardroom_sim.io.write_traces_json](#fn-boardroom_sim-io-py-write_traces_json-44)、[boardroom_sim.pitchbook._primary_industry_by_company](#fn-boardroom_sim-pitchbook-py-_primary_industry_by_company-391)、[boardroom_sim.pitchbook._index_by_id](#fn-boardroom_sim-pitchbook-py-_index_by_id-415)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、另有 7 个调用方

**完整源码**

~~~~python
    def to_dict(self, include_trace: bool = True) -> Dict[str, Any]:
        """Serialize the result and optionally omit the detailed trace."""
        data = asdict(self)
        if not include_trace:
            data.pop("trace", None)
        return data
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-pitchbook-py"></a>

## boardroom_sim/pitchbook.py

PitchBook/Revelio 数据接入模块：从 Excel 多张表中构造实验案例和真实标签。

**类**：无类定义

<a id="fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31"></a>

### build_cases_from_pitchbook

文件：`boardroom_sim/pitchbook.py:31-233`

**签名**

~~~~python
def build_cases_from_pitchbook( path: Path, limit: Optional[int] = None, history_limit: Optional[int] = 10, ) -> List[BoardCase]:
~~~~

**功能**

从 PitchBook/Revelio Excel 工作簿中组装 BoardCase 列表。它关联公司、交易、投资人、员工画像、治理/董事会记录和历史融资信息。

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `limit` | 数量上限，用于截断历史记录、案例数量或输出条目。 |
| `history_limit` | 数量上限，用于截断历史记录、案例数量或输出条目。 |

**返回值**

返回 `List[BoardCase]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `pd.read_excel` | 库函数或对象方法 | 读取 Excel 文件，返回 DataFrame 或 sheet_name 到 DataFrame 的字典。 |
| `_clean_table` | [boardroom_sim.pitchbook._clean_table](#fn-boardroom_sim-pitchbook-py-_clean_table-236) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_select_vc_deals` | [boardroom_sim.pitchbook._select_vc_deals](#fn-boardroom_sim-pitchbook-py-_select_vc_deals-245) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_normalize_history_limit` | [boardroom_sim.pitchbook._normalize_history_limit](#fn-boardroom_sim-pitchbook-py-_normalize_history_limit-253) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `financing_deals.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `case_deals.sort_values` | 库函数或对象方法 | pandas DataFrame 排序，返回新的 DataFrame。 |
| `_index_by_id` | [boardroom_sim.pitchbook._index_by_id](#fn-boardroom_sim-pitchbook-py-_index_by_id-415) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_primary_industry_by_company` | [boardroom_sim.pitchbook._primary_industry_by_company](#fn-boardroom_sim-pitchbook-py-_primary_industry_by_company-391) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_list_by_company` | [boardroom_sim.pitchbook._list_by_company](#fn-boardroom_sim-pitchbook-py-_list_by_company-404) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_count_by_company` | [boardroom_sim.pitchbook._count_by_company](#fn-boardroom_sim-pitchbook-py-_count_by_company-427) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `case_deals.iterrows` | 库函数或对象方法 | pandas DataFrame 行遍历，返回 index 与 row 的迭代器。 |
| `case_deals.head` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `deal.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_prior_deals` | [boardroom_sim.pitchbook._prior_deals](#fn-boardroom_sim-pitchbook-py-_prior_deals-434) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `company_by_id.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `industry_by_company.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_investor_context` | [boardroom_sim.pitchbook._investor_context](#fn-boardroom_sim-pitchbook-py-_investor_context-445) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_investor_ids_for_deal` | [boardroom_sim.pitchbook._investor_ids_for_deal](#fn-boardroom_sim-pitchbook-py-_investor_ids_for_deal-275) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_company_deal_history` | [boardroom_sim.pitchbook._company_deal_history](#fn-boardroom_sim-pitchbook-py-_company_deal_history-260) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_investor_deal_history` | [boardroom_sim.pitchbook._investor_deal_history](#fn-boardroom_sim-pitchbook-py-_investor_deal_history-297) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_employee_context` | [boardroom_sim.pitchbook._employee_context](#fn-boardroom_sim-pitchbook-py-_employee_context-501) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_governance_context` | [boardroom_sim.pitchbook._governance_context](#fn-boardroom_sim-pitchbook-py-_governance_context-521) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_optional_float` | [boardroom_sim.pitchbook._optional_float](#fn-boardroom_sim-pitchbook-py-_optional_float-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_valuation_markup` | [boardroom_sim.pitchbook._valuation_markup](#fn-boardroom_sim-pitchbook-py-_valuation_markup-689) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_valuation_direction_from_label_or_markup` | [boardroom_sim.pitchbook._valuation_direction_from_label_or_markup](#fn-boardroom_sim-pitchbook-py-_valuation_direction_from_label_or_markup-694) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_ceo_replacement_label` | [boardroom_sim.pitchbook._ceo_replacement_label](#fn-boardroom_sim-pitchbook-py-_ceo_replacement_label-574) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `cases.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `previous_deal.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_field_sources` | [boardroom_sim.pitchbook._field_sources](#fn-boardroom_sim-pitchbook-py-_field_sources-717) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `BoardCase` | [boardroom_sim.models.BoardCase](#class-boardroom_sim-models-py-boardcase-120) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `prior_previous_deal.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_date_str` | [boardroom_sim.pitchbook._date_str](#fn-boardroom_sim-pitchbook-py-_date_str-745) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_optional_int` | [boardroom_sim.pitchbook._optional_int](#fn-boardroom_sim-pitchbook-py-_optional_int-792) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_company_age_years` | [boardroom_sim.pitchbook._company_age_years](#fn-boardroom_sim-pitchbook-py-_company_age_years-673) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `verticals_by_company.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `_months_between` | [boardroom_sim.pitchbook._months_between](#fn-boardroom_sim-pitchbook-py-_months_between-681) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `competitor_counts.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `similar_counts.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `company.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `industry_context.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[run_experiment.load_cases](#fn-run_experiment-py-load_cases-48)

**完整源码**

~~~~python
def build_cases_from_pitchbook(
    path: Path,
    limit: Optional[int] = None,
    history_limit: Optional[int] = 10,
) -> List[BoardCase]:
    """Read the PitchBook workbook and convert eligible VC rounds into point-in-time cases."""
    tables = pd.read_excel(path, sheet_name=None)
    deal_df = _clean_table(tables["deal"])
    financing_deals = _select_vc_deals(deal_df)
    history_limit = _normalize_history_limit(history_limit)

    # Use every observable VC-like transaction as a positive event sample. Missing numeric labels
    # are preserved as None and skipped by the relevant evaluation metrics.
    case_deals = financing_deals.copy()
    case_deals = case_deals.sort_values(["companyid", "dealdate", "dealid"], na_position="last")
    if limit is not None:
        case_deals = case_deals.head(limit)

    company_by_id = _index_by_id(_clean_table(tables["company"]), "companyid")
    industry_by_company = _primary_industry_by_company(_clean_table(tables["companyindustryrelation"]))
    verticals_by_company = _list_by_company(_clean_table(tables["companyverticalrelation"]), "vertical")
    investor_by_id = _index_by_id(_clean_table(tables["investor"]), "investorid")
    deal_investors = _clean_table(tables["dealinvestorrelation"])
    employee_history = _clean_table(tables["companyemployeehistoryrelation"])
    person_roles = _clean_table(tables["_sample_person_roles"])
    person_positions = _clean_table(tables["personpositionrelation"])
    board_team = _clean_table(tables["companyboardteamrelation"])
    board_seats = _clean_table(tables["personboardseatrelation"])
    competitor_counts = _count_by_company(_clean_table(tables["companycompetitorrelation"]))
    similar_counts = _count_by_company(_clean_table(tables["companysimilarrelation"]))

    cases: List[BoardCase] = []
    for _, deal in case_deals.iterrows():
        company_id = _as_str(deal.get("companyid"))
        deal_id = _as_str(deal.get("dealid"), "unknown_deal")
        decision_date = deal.get("dealdate")
        case_id = f"{company_id}_{deal_id}" if company_id else deal_id

        prior_deals = _prior_deals(financing_deals, company_id, decision_date)
        previous_deal = prior_deals.iloc[-1] if not prior_deals.empty else None
        prior_previous_deal = prior_deals.iloc[-2] if len(prior_deals) > 1 else None
        previous_deal_id = previous_deal.get("dealid") if previous_deal is not None else None

        company = company_by_id.get(company_id, {})
        industry_context = industry_by_company.get(company_id, {})
        prior_investor_context = _investor_context(
            previous_deal_id,
            deal_investors,
            investor_by_id,
        )
        real_investor_context = _investor_context(deal_id, deal_investors, investor_by_id)
        lead_investor_ids = _investor_ids_for_deal(deal_id, deal_investors, investor_role="lead")
        followon_investor_ids = _investor_ids_for_deal(deal_id, deal_investors, investor_role="followon")
        prior_company_deal_history = _company_deal_history(prior_deals, history_limit)
        prior_lead_investor_deal_history = _investor_deal_history(
            lead_investor_ids,
            financing_deals,
            deal_investors,
            investor_by_id,
            decision_date,
            history_limit,
        )
        prior_followon_investor_deal_history = _investor_deal_history(
            followon_investor_ids,
            financing_deals,
            deal_investors,
            investor_by_id,
            decision_date,
            history_limit,
        )
        employee_context = _employee_context(company_id, decision_date, employee_history)
        governance_context = _governance_context(
            company_id=company_id,
            decision_date=decision_date,
            person_roles=person_roles,
            person_positions=person_positions,
            board_team=board_team,
            board_seats=board_seats,
        )

        prior_post_valuation = _optional_float(previous_deal.get("postvaluation") if previous_deal is not None else None)
        prior_prior_post_valuation = _optional_float(
            prior_previous_deal.get("postvaluation") if prior_previous_deal is not None else None
        )
        prior_markup = _valuation_markup(prior_post_valuation, prior_prior_post_valuation)
        prior_direction = _valuation_direction_from_label_or_markup(
            previous_deal.get("vcroundup_down_flat") if previous_deal is not None else None,
            prior_markup,
        )

        real_post_valuation = _optional_float(deal.get("postvaluation"))
        real_markup = _valuation_markup(real_post_valuation, prior_post_valuation)
        real_direction = _valuation_direction_from_label_or_markup(deal.get("vcroundup_down_flat"), real_markup)
        ceo_replacement = _ceo_replacement_label(company_id, decision_date, person_roles, person_positions)

        labels = {
            "true_financing_initiated_label": "raise_now",
            "real_deal_completed_label": _as_str(deal.get("dealstatus"), "unknown"),
            "real_deal_size_usd_m": _optional_float(deal.get("dealsize")),
            "real_deal_type": _as_str(deal.get("dealtype"), "unknown_deal_type"),
            "real_pre_money_valuation_usd_m": _optional_float(deal.get("premoneyvaluation")),
            "real_post_money_valuation_usd_m": real_post_valuation,
            "real_investor_ownership_pct": _optional_float(deal.get("investorownership")),
            "real_vc_round_direction_raw": _as_str(deal.get("vcroundup_down_flat"), "unknown"),
            "real_valuation_direction_label": real_direction,
            "real_lead_investor_count": real_investor_context["lead_investor_count"],
            "real_lead_investor_types": real_investor_context["lead_investor_types"],
            "real_ceo_replacement_label": ceo_replacement["label"],
            "real_ceo_replacement_detail": ceo_replacement,
        }
        notes = {
            "source": "pitchbook_xlsx",
            "companyid": company_id,
            "target_dealid": deal_id,
            "prior_dealid": _as_str(previous_deal.get("dealid")) if previous_deal is not None else "",
            "labels": labels,
            "field_sources": _field_sources(),
            "caveats": [
                "Positive-event backtest: every case has an observable VC-like transaction as the target event.",
                "Current transaction outcomes are stored only in notes.labels and are not exposed through role attention fields.",
                "Missing target numeric labels are kept as null and skipped by numeric evaluation metrics.",
                "Some company status fields may be PitchBook snapshot fields rather than strict point-in-time facts.",
            ],
        }

        cases.append(
            BoardCase(
                case_id=case_id,
                company_id=company_id,
                target_deal_id=deal_id,
                company_label=company_id or "unknown_company",
                decision_date=_date_str(decision_date),
                business_status=_as_str(company.get("businessstatus"), "unknown"),
                company_financing_status=_as_str(company.get("companyfinancingstatus"), "unknown"),
                ownership_status=_as_str(company.get("ownershipstatus"), "unknown"),
                year_founded=_optional_int(company.get("yearfounded")),
                company_age_at_decision_years=_company_age_years(company.get("yearfounded"), decision_date),
                primary_industry=_as_str(industry_context.get("industrycode"), "unknown_industry"),
                industry_group=_as_str(industry_context.get("industrygroup"), "unknown"),
                industry_sector=_as_str(industry_context.get("industrysector"), "unknown"),
                verticals=verticals_by_company.get(company_id, []),
                keywords=_as_str(company.get("keywords"), ""),
                description=_as_str(company.get("description"), ""),
                prior_vc_deal_count=int(len(prior_deals)),
                prior_deal_date=_date_str(previous_deal.get("dealdate")) if previous_deal is not None else "",
                prior_deal_type=_as_str(previous_deal.get("dealtype") if previous_deal is not None else None, "none"),
                prior_vc_round=_as_str(previous_deal.get("vcround") if previous_deal is not None else None, "none"),
                prior_deal_size_usd_m=_optional_float(previous_deal.get("dealsize") if previous_deal is not None else None),
                prior_pre_money_valuation_usd_m=_optional_float(
                    previous_deal.get("premoneyvaluation") if previous_deal is not None else None
                ),
                prior_post_money_valuation_usd_m=prior_post_valuation,
                prior_raised_to_date_usd_m=_optional_float(
                    previous_deal.get("raisedtodate") if previous_deal is not None else None
                ),
                prior_investor_ownership_pct=_optional_float(
                    previous_deal.get("investorownership") if previous_deal is not None else None
                ),
                months_since_prior_deal=_months_between(
                    previous_deal.get("dealdate") if previous_deal is not None else None,
                    decision_date,
                ),
                prior_valuation_markup_multiple=prior_markup,
                prior_valuation_direction_label=prior_direction,
                prior_investor_count=_optional_int(previous_deal.get("investors") if previous_deal is not None else None),
                prior_new_investor_count=_optional_int(previous_deal.get("newinvestors") if previous_deal is not None else None),
                prior_followon_investor_count=_optional_int(
                    previous_deal.get("followoninvestors") if previous_deal is not None else None
                ),
                prior_lead_investor_count=prior_investor_context["lead_investor_count"],
                prior_lead_investor_types=prior_investor_context["lead_investor_types"],
                prior_lead_investor_amount_share=prior_investor_context["lead_investor_amount_share"],
                prior_lead_preferred_deal_size_min_usd_m=prior_investor_context[
                    "lead_preferred_deal_size_min_usd_m"
                ],
                prior_lead_preferred_deal_size_max_usd_m=prior_investor_context[
                    "lead_preferred_deal_size_max_usd_m"
                ],
                prior_lead_preferred_company_valuation_min_usd_m=prior_investor_context[
                    "lead_preferred_company_valuation_min_usd_m"
                ],
                prior_lead_preferred_company_valuation_max_usd_m=prior_investor_context[
                    "lead_preferred_company_valuation_max_usd_m"
                ],
                prior_company_deal_history=prior_company_deal_history,
                prior_lead_investor_deal_history=prior_lead_investor_deal_history,
                prior_followon_investor_deal_history=prior_followon_investor_deal_history,
                employee_count_at_decision=employee_context["employee_count_at_decision"],
                previous_employee_count=employee_context["previous_employee_count"],
                employee_growth_rate=employee_context["employee_growth_rate"],
                engineering_role_count=governance_context["engineering_role_count"],
                executive_role_count=governance_context["executive_role_count"],
                founder_count=governance_context["founder_count"],
                current_ceo_count=governance_context["current_ceo_count"],
                current_ceo_is_founder=governance_context["current_ceo_is_founder"],
                board_member_count=governance_context["board_member_count"],
                investor_board_member_count=governance_context["investor_board_member_count"],
                competitor_count=competitor_counts.get(company_id, 0),
                similar_company_count=similar_counts.get(company_id, 0),
                notes=notes,
            )
        )
    return cases
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_clean_table-236"></a>

### _clean_table

文件：`boardroom_sim/pitchbook.py:236-242`

**签名**

~~~~python
def _clean_table(df: pd.DataFrame) -> pd.DataFrame:
~~~~

**功能**

源码文档说明：Return a copy with normalized date-like columns where useful.

**参数**

| 参数 | 含义 |
| --- | --- |
| `df` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |

**返回值**

返回 `pd.DataFrame` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `df.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `pd.to_datetime` | 库函数或对象方法 | 把日期文本/序列转换为 datetime，返回 Timestamp 或 Series。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _clean_table(df: pd.DataFrame) -> pd.DataFrame:
    """Return a copy with normalized date-like columns where useful."""
    cleaned = df.copy()
    for column in ("dealdate", "announceddate", "date", "startdate", "enddate", "investorsince", "investorexit"):
        if column in cleaned.columns:
            cleaned[column] = pd.to_datetime(cleaned[column], errors="coerce")
    return cleaned
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_select_vc_deals-245"></a>

### _select_vc_deals

文件：`boardroom_sim/pitchbook.py:245-250`

**签名**

~~~~python
def _select_vc_deals(deal_df: pd.DataFrame) -> pd.DataFrame:
~~~~

**功能**

源码文档说明：Select observable venture financing events from the deal table.

**参数**

| 参数 | 含义 |
| --- | --- |
| `deal_df` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |

**返回值**

返回 `pd.DataFrame` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `deal_df.fillna.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `deal_df.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `deal_type.isin` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `deal_class.eq` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `deal_df.fillna` | 库函数或对象方法 | pandas 缺失值填充，返回 Series/DataFrame。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _select_vc_deals(deal_df: pd.DataFrame) -> pd.DataFrame:
    """Select observable venture financing events from the deal table."""
    deal_type = deal_df["dealtype"].fillna("").astype(str)
    deal_class = deal_df["dealclass"].fillna("").astype(str)
    mask = deal_type.isin(VC_DEAL_TYPES) | deal_class.eq("Venture Capital")
    return deal_df[mask].copy()
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_normalize_history_limit-253"></a>

### _normalize_history_limit

文件：`boardroom_sim/pitchbook.py:253-257`

**签名**

~~~~python
def _normalize_history_limit(history_limit: Optional[int]) -> Optional[int]:
~~~~

**功能**

源码文档说明：Normalize the optional history limit; negative values mean no limit.

**参数**

| 参数 | 含义 |
| --- | --- |
| `history_limit` | 数量上限，用于截断历史记录、案例数量或输出条目。 |

**返回值**

返回 `Optional[int]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _normalize_history_limit(history_limit: Optional[int]) -> Optional[int]:
    """Normalize the optional history limit; negative values mean no limit."""
    if history_limit is None or history_limit < 0:
        return None
    return int(history_limit)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_company_deal_history-260"></a>

### _company_deal_history

文件：`boardroom_sim/pitchbook.py:260-272`

**签名**

~~~~python
def _company_deal_history(prior_deals: pd.DataFrame, history_limit: Optional[int]) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Return same-company historical deal records visible before the decision date.

**参数**

| 参数 | 含义 |
| --- | --- |
| `prior_deals` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `history_limit` | 数量上限，用于截断历史记录、案例数量或输出条目。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `prior_deals.iterrows` | 库函数或对象方法 | pandas DataFrame 行遍历，返回 index 与 row 的迭代器。 |
| `_tail` | [boardroom_sim.pitchbook._tail](#fn-boardroom_sim-pitchbook-py-_tail-382) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_deal_history_payload` | [boardroom_sim.pitchbook._deal_history_payload](#fn-boardroom_sim-pitchbook-py-_deal_history_payload-357) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `enriched_rows.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `_optional_float` | [boardroom_sim.pitchbook._optional_float](#fn-boardroom_sim-pitchbook-py-_optional_float-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _company_deal_history(prior_deals: pd.DataFrame, history_limit: Optional[int]) -> List[Dict[str, Any]]:
    """Return same-company historical deal records visible before the decision date."""
    if prior_deals.empty or history_limit == 0:
        return []
    previous_post_valuation: Optional[float] = None
    enriched_rows: List[Dict[str, Any]] = []
    for _, row in prior_deals.iterrows():
        payload = _deal_history_payload(row, previous_post_valuation=previous_post_valuation)
        enriched_rows.append(payload)
        post_valuation = _optional_float(row.get("postvaluation"))
        if post_valuation is not None and post_valuation > 0:
            previous_post_valuation = post_valuation
    return _tail(enriched_rows, history_limit)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_investor_ids_for_deal-275"></a>

### _investor_ids_for_deal

文件：`boardroom_sim/pitchbook.py:275-294`

**签名**

~~~~python
def _investor_ids_for_deal(deal_id: Any, deal_investors: pd.DataFrame, *, investor_role: str) -> Set[str]:
~~~~

**功能**

源码文档说明：Return investor IDs from one target deal for a role-specific investor representative.

**参数**

| 参数 | 含义 |
| --- | --- |
| `deal_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `deal_investors` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `investor_role` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Set[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `deal_investors.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `set` | 库函数或对象方法 | 内置容器构造；返回 set。 |
| `pd.to_numeric.fillna.astype` | 库函数或对象方法 | pandas 库函数；用于表格读取、转换、筛选或时间/数值处理，返回 pandas 对象或标量。 |
| `relation.fillna.astype.str.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `pd.to_numeric.fillna` | 库函数或对象方法 | pandas 缺失值填充，返回 Series/DataFrame。 |
| `status.str.contains` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `deal_investors.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `pd.to_numeric` | 库函数或对象方法 | 把 Series 或标量转换为数值，返回数值或 Series。 |
| `relation.fillna.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `relation.fillna` | 库函数或对象方法 | pandas 缺失值填充，返回 Series/DataFrame。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _investor_ids_for_deal(deal_id: Any, deal_investors: pd.DataFrame, *, investor_role: str) -> Set[str]:
    """Return investor IDs from one target deal for a role-specific investor representative."""
    deal_id_text = _as_str(deal_id)
    if not deal_id_text or deal_investors.empty:
        return set()
    relation = deal_investors[deal_investors["dealid"].astype(str) == deal_id_text].copy()
    if relation.empty:
        return set()

    is_lead = pd.to_numeric(relation["isleadinvestor"], errors="coerce").fillna(0).astype(int) == 1
    if investor_role == "lead":
        selected = relation[is_lead]
    elif investor_role == "followon":
        status = relation["investorstatus"].fillna("").astype(str).str.lower()
        selected = relation[status.str.contains("follow-on", regex=False)]
        if selected.empty:
            selected = relation[~is_lead]
    else:
        selected = relation.iloc[0:0]
    return {_as_str(investor_id) for investor_id in selected["investorid"] if _as_str(investor_id)}
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_investor_deal_history-297"></a>

### _investor_deal_history

文件：`boardroom_sim/pitchbook.py:297-354`

**签名**

~~~~python
def _investor_deal_history( investor_ids: Set[str], financing_deals: pd.DataFrame, deal_investors: pd.DataFrame, investor_by_id: Dict[str, Dict[str, Any]], decision_date: Any, history_limit: Optional[int], ) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Return historical investor-deal records for the represented investor IDs.

**参数**

| 参数 | 含义 |
| --- | --- |
| `investor_ids` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `financing_deals` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `deal_investors` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `investor_by_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `decision_date` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `history_limit` | 数量上限，用于截断历史记录、案例数量或输出条目。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `deal_investors.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `relation.iterrows` | 库函数或对象方法 | pandas DataFrame 行遍历，返回 index 与 row 的迭代器。 |
| `items.sort` | 库函数或对象方法 | 列表原地排序方法；返回 None。 |
| `_tail` | [boardroom_sim.pitchbook._tail](#fn-boardroom_sim-pitchbook-py-_tail-382) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `deal_by_id.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `pd.to_datetime` | 库函数或对象方法 | 把日期文本/序列转换为 datetime，返回 Timestamp 或 Series。 |
| `investor_by_id.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_deal_history_payload` | [boardroom_sim.pitchbook._deal_history_payload](#fn-boardroom_sim-pitchbook-py-_deal_history_payload-357) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `payload.update` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `items.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `financing_deals.iterrows` | 库函数或对象方法 | pandas DataFrame 行遍历，返回 index 与 row 的迭代器。 |
| `investor_deal.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `deal.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `deal_investors.astype.isin` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `bool` | 库函数或对象方法 | 内置类型转换；返回布尔值。 |
| `_optional_float` | [boardroom_sim.pitchbook._optional_float](#fn-boardroom_sim-pitchbook-py-_optional_float-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `investor_profile.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `deal_investors.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `_optional_int` | [boardroom_sim.pitchbook._optional_int](#fn-boardroom_sim-pitchbook-py-_optional_int-792) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _investor_deal_history(
    investor_ids: Set[str],
    financing_deals: pd.DataFrame,
    deal_investors: pd.DataFrame,
    investor_by_id: Dict[str, Dict[str, Any]],
    decision_date: Any,
    history_limit: Optional[int],
) -> List[Dict[str, Any]]:
    """Return historical investor-deal records for the represented investor IDs."""
    if not investor_ids or pd.isna(decision_date) or history_limit == 0:
        return []

    deal_by_id = {
        _as_str(row.get("dealid")): row
        for _, row in financing_deals.iterrows()
        if _as_str(row.get("dealid"))
    }
    relation = deal_investors[deal_investors["investorid"].astype(str).isin(investor_ids)].copy()
    items: List[tuple[pd.Timestamp, str, Dict[str, Any]]] = []
    for _, investor_deal in relation.iterrows():
        deal_id = _as_str(investor_deal.get("dealid"))
        deal = deal_by_id.get(deal_id)
        if deal is None:
            continue
        deal_date = pd.to_datetime(deal.get("dealdate"), errors="coerce")
        if pd.isna(deal_date) or deal_date >= decision_date:
            continue
        investor_id = _as_str(investor_deal.get("investorid"))
        investor_profile = investor_by_id.get(investor_id, {})
        payload = _deal_history_payload(deal, previous_post_valuation=None)
        payload.update(
            {
                "investor_id": investor_id,
                "investor_primary_type": _as_str(investor_profile.get("primaryinvestortype"), ""),
                "investor_status_in_deal": _as_str(investor_deal.get("investorstatus"), ""),
                "is_lead_investor": bool(_optional_int(investor_deal.get("isleadinvestor")) == 1),
                "investor_investment_amount_usd_m": _optional_float(investor_deal.get("investorinvestmentamount")),
                "investor_preferred_deal_size_min_usd_m": _optional_float(
                    investor_profile.get("preferreddealsizemin") or investor_profile.get("preferreddealsize")
                ),
                "investor_preferred_deal_size_max_usd_m": _optional_float(
                    investor_profile.get("preferreddealsizemax") or investor_profile.get("preferreddealsize")
                ),
                "investor_preferred_company_valuation_min_usd_m": _optional_float(
                    investor_profile.get("preferredcompanyvaluationmin")
                    or investor_profile.get("preferredcompanyvaluation")
                ),
                "investor_preferred_company_valuation_max_usd_m": _optional_float(
                    investor_profile.get("preferredcompanyvaluationmax")
                    or investor_profile.get("preferredcompanyvaluation")
                ),
            }
        )
        items.append((deal_date, f"{deal_id}_{investor_id}", payload))

    items.sort(key=lambda item: (item[0], item[1]))
    payloads = [item[2] for item in items]
    return _tail(payloads, history_limit)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_deal_history_payload-357"></a>

### _deal_history_payload

文件：`boardroom_sim/pitchbook.py:357-379`

**签名**

~~~~python
def _deal_history_payload(row: Any, *, previous_post_valuation: Optional[float]) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Serialize one historical deal row without exposing future target labels.

**参数**

| 参数 | 含义 |
| --- | --- |
| `row` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `previous_post_valuation` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_optional_float` | [boardroom_sim.pitchbook._optional_float](#fn-boardroom_sim-pitchbook-py-_optional_float-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_valuation_markup` | [boardroom_sim.pitchbook._valuation_markup](#fn-boardroom_sim-pitchbook-py-_valuation_markup-689) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_date_str` | [boardroom_sim.pitchbook._date_str](#fn-boardroom_sim-pitchbook-py-_date_str-745) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_optional_int` | [boardroom_sim.pitchbook._optional_int](#fn-boardroom_sim-pitchbook-py-_optional_int-792) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_valuation_direction_from_label_or_markup` | [boardroom_sim.pitchbook._valuation_direction_from_label_or_markup](#fn-boardroom_sim-pitchbook-py-_valuation_direction_from_label_or_markup-694) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.pitchbook._company_deal_history](#fn-boardroom_sim-pitchbook-py-_company_deal_history-260)、[boardroom_sim.pitchbook._investor_deal_history](#fn-boardroom_sim-pitchbook-py-_investor_deal_history-297)

**完整源码**

~~~~python
def _deal_history_payload(row: Any, *, previous_post_valuation: Optional[float]) -> Dict[str, Any]:
    """Serialize one historical deal row without exposing future target labels."""
    post_valuation = _optional_float(row.get("postvaluation"))
    markup = _valuation_markup(post_valuation, previous_post_valuation)
    return {
        "deal_id": _as_str(row.get("dealid")),
        "company_id": _as_str(row.get("companyid")),
        "deal_date": _date_str(row.get("dealdate")),
        "deal_class": _as_str(row.get("dealclass"), ""),
        "deal_type": _as_str(row.get("dealtype"), ""),
        "deal_status": _as_str(row.get("dealstatus"), ""),
        "vc_round": _as_str(row.get("vcround"), ""),
        "deal_size_usd_m": _optional_float(row.get("dealsize")),
        "pre_money_valuation_usd_m": _optional_float(row.get("premoneyvaluation")),
        "post_money_valuation_usd_m": post_valuation,
        "investor_ownership_pct": _optional_float(row.get("investorownership")),
        "raised_to_date_usd_m": _optional_float(row.get("raisedtodate")),
        "investor_count": _optional_int(row.get("investors")),
        "new_investor_count": _optional_int(row.get("newinvestors")),
        "followon_investor_count": _optional_int(row.get("followoninvestors")),
        "valuation_markup_multiple": markup,
        "valuation_direction_label": _valuation_direction_from_label_or_markup(row.get("vcroundup_down_flat"), markup),
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_tail-382"></a>

### _tail

文件：`boardroom_sim/pitchbook.py:382-388`

**签名**

~~~~python
def _tail(items: List[Dict[str, Any]], limit: Optional[int]) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Return the most recent history records while preserving chronological order.

**参数**

| 参数 | 含义 |
| --- | --- |
| `items` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `limit` | 数量上限，用于截断历史记录、案例数量或输出条目。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.pitchbook._company_deal_history](#fn-boardroom_sim-pitchbook-py-_company_deal_history-260)、[boardroom_sim.pitchbook._investor_deal_history](#fn-boardroom_sim-pitchbook-py-_investor_deal_history-297)

**完整源码**

~~~~python
def _tail(items: List[Dict[str, Any]], limit: Optional[int]) -> List[Dict[str, Any]]:
    """Return the most recent history records while preserving chronological order."""
    if limit is None:
        return items
    if limit <= 0:
        return []
    return items[-limit:]
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_primary_industry_by_company-391"></a>

### _primary_industry_by_company

文件：`boardroom_sim/pitchbook.py:391-401`

**签名**

~~~~python
def _primary_industry_by_company(df: pd.DataFrame) -> Dict[str, Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Map company id to the primary industry row, falling back to first row.

**参数**

| 参数 | 含义 |
| --- | --- |
| `df` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |

**返回值**

返回 `Dict[str, Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `df.sort_values` | 库函数或对象方法 | pandas DataFrame 排序，返回新的 DataFrame。 |
| `sorted_df.iterrows` | 库函数或对象方法 | pandas DataFrame 行遍历，返回 index 与 row 的迭代器。 |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `row.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _primary_industry_by_company(df: pd.DataFrame) -> Dict[str, Dict[str, Any]]:
    """Map company id to the primary industry row, falling back to first row."""
    result: Dict[str, Dict[str, Any]] = {}
    if df.empty:
        return result
    sorted_df = df.sort_values("isprimary", ascending=False)
    for _, row in sorted_df.iterrows():
        company_id = _as_str(row.get("companyid"))
        if company_id and company_id not in result:
            result[company_id] = row.to_dict()
    return result
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_list_by_company-404"></a>

### _list_by_company

文件：`boardroom_sim/pitchbook.py:404-412`

**签名**

~~~~python
def _list_by_company(df: pd.DataFrame, column: str) -> Dict[str, List[str]]:
~~~~

**功能**

源码文档说明：Group one text column into lists by company id.

**参数**

| 参数 | 含义 |
| --- | --- |
| `df` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |
| `column` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, List[str]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `df.groupby` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `sorted` | 库函数或对象方法 | 内置排序；返回新的 list。 |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _list_by_company(df: pd.DataFrame, column: str) -> Dict[str, List[str]]:
    """Group one text column into lists by company id."""
    result: Dict[str, List[str]] = {}
    if df.empty or column not in df.columns:
        return result
    for company_id, group in df.groupby("companyid", dropna=True):
        values = sorted({_as_str(value) for value in group[column] if _as_str(value)})
        result[_as_str(company_id)] = values
    return result
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_index_by_id-415"></a>

### _index_by_id

文件：`boardroom_sim/pitchbook.py:415-424`

**签名**

~~~~python
def _index_by_id(df: pd.DataFrame, key: str) -> Dict[str, Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Index a dataframe by one id column.

**参数**

| 参数 | 含义 |
| --- | --- |
| `df` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |
| `key` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `df.iterrows` | 库函数或对象方法 | pandas DataFrame 行遍历，返回 index 与 row 的迭代器。 |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `row.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _index_by_id(df: pd.DataFrame, key: str) -> Dict[str, Dict[str, Any]]:
    """Index a dataframe by one id column."""
    result: Dict[str, Dict[str, Any]] = {}
    if df.empty or key not in df.columns:
        return result
    for _, row in df.iterrows():
        item_id = _as_str(row.get(key))
        if item_id:
            result[item_id] = row.to_dict()
    return result
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_count_by_company-427"></a>

### _count_by_company

文件：`boardroom_sim/pitchbook.py:427-431`

**签名**

~~~~python
def _count_by_company(df: pd.DataFrame) -> Dict[str, int]:
~~~~

**功能**

源码文档说明：Count relation rows by company id.

**参数**

| 参数 | 含义 |
| --- | --- |
| `df` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |

**返回值**

返回 `Dict[str, int]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `df.groupby.size.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `df.groupby.size` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `df.groupby` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _count_by_company(df: pd.DataFrame) -> Dict[str, int]:
    """Count relation rows by company id."""
    if df.empty or "companyid" not in df.columns:
        return {}
    return {_as_str(company_id): int(count) for company_id, count in df.groupby("companyid").size().items()}
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_prior_deals-434"></a>

### _prior_deals

文件：`boardroom_sim/pitchbook.py:434-442`

**签名**

~~~~python
def _prior_deals(deal_df: pd.DataFrame, company_id: str, decision_date: Any) -> pd.DataFrame:
~~~~

**功能**

源码文档说明：Return same-company VC deals strictly before the decision date.

**参数**

| 参数 | 含义 |
| --- | --- |
| `deal_df` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |
| `company_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `decision_date` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `pd.DataFrame` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `deal_df.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `company_deals.sort_values` | 库函数或对象方法 | pandas DataFrame 排序，返回新的 DataFrame。 |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `deal_df.iloc.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `pd.to_datetime` | 库函数或对象方法 | 把日期文本/序列转换为 datetime，返回 Timestamp 或 Series。 |
| `deal_df.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _prior_deals(deal_df: pd.DataFrame, company_id: str, decision_date: Any) -> pd.DataFrame:
    """Return same-company VC deals strictly before the decision date."""
    if not company_id or pd.isna(decision_date):
        return deal_df.iloc[0:0].copy()
    company_deals = deal_df[deal_df["companyid"].astype(str) == company_id].copy()
    company_deals = company_deals[pd.to_datetime(company_deals["dealdate"], errors="coerce") < decision_date]
    if company_deals.empty:
        return company_deals
    return company_deals.sort_values(["dealdate", "dealid"])
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_investor_context-445"></a>

### _investor_context

文件：`boardroom_sim/pitchbook.py:445-498`

**签名**

~~~~python
def _investor_context(deal_id: Any, deal_investors: pd.DataFrame, investor_by_id: Dict[str, Dict[str, Any]]) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Summarize lead-investor participation for one already-known deal.

**参数**

| 参数 | 含义 |
| --- | --- |
| `deal_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `deal_investors` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `investor_by_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `deal_investors.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `pd.to_numeric.fillna` | 库函数或对象方法 | pandas 缺失值填充，返回 Series/DataFrame。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `sorted` | 库函数或对象方法 | 内置排序；返回新的 list。 |
| `amounts.sum` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `pd.to_numeric.fillna.sum` | 库函数或对象方法 | pandas 库函数；用于表格读取、转换、筛选或时间/数值处理，返回 pandas 对象或标量。 |
| `investor_by_id.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `_min_positive` | [boardroom_sim.pitchbook._min_positive](#fn-boardroom_sim-pitchbook-py-_min_positive-733) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_max_positive` | [boardroom_sim.pitchbook._max_positive](#fn-boardroom_sim-pitchbook-py-_max_positive-739) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `pd.to_numeric` | 库函数或对象方法 | 把 Series 或标量转换为数值，返回数值或 Series。 |
| `pd.to_numeric.fillna.astype` | 库函数或对象方法 | pandas 库函数；用于表格读取、转换、筛选或时间/数值处理，返回 pandas 对象或标量。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `profile.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `deal_investors.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _investor_context(deal_id: Any, deal_investors: pd.DataFrame, investor_by_id: Dict[str, Dict[str, Any]]) -> Dict[str, Any]:
    """Summarize lead-investor participation for one already-known deal."""
    empty = {
        "lead_investor_count": 0,
        "lead_investor_types": [],
        "lead_investor_amount_share": None,
        "lead_preferred_deal_size_min_usd_m": None,
        "lead_preferred_deal_size_max_usd_m": None,
        "lead_preferred_company_valuation_min_usd_m": None,
        "lead_preferred_company_valuation_max_usd_m": None,
    }
    deal_id_text = _as_str(deal_id)
    if not deal_id_text or deal_investors.empty:
        return empty

    relation = deal_investors[deal_investors["dealid"].astype(str) == deal_id_text].copy()
    if relation.empty:
        return empty

    amounts = pd.to_numeric(relation["investorinvestmentamount"], errors="coerce").fillna(0.0)
    total_amount = float(amounts.sum())
    lead_relation = relation[pd.to_numeric(relation["isleadinvestor"], errors="coerce").fillna(0).astype(int) == 1]
    lead_amount = float(pd.to_numeric(lead_relation["investorinvestmentamount"], errors="coerce").fillna(0.0).sum())
    lead_profiles = [
        investor_by_id.get(_as_str(investor_id), {})
        for investor_id in lead_relation["investorid"]
        if _as_str(investor_id)
    ]
    lead_types = sorted(
        {
            _as_str(profile.get("primaryinvestortype"))
            for profile in lead_profiles
            if _as_str(profile.get("primaryinvestortype"))
        }
    )
    return {
        "lead_investor_count": int(len(lead_relation)),
        "lead_investor_types": lead_types,
        "lead_investor_amount_share": lead_amount / total_amount if total_amount > 0 else None,
        "lead_preferred_deal_size_min_usd_m": _min_positive(
            profile.get("preferreddealsizemin") or profile.get("preferreddealsize") for profile in lead_profiles
        ),
        "lead_preferred_deal_size_max_usd_m": _max_positive(
            profile.get("preferreddealsizemax") or profile.get("preferreddealsize") for profile in lead_profiles
        ),
        "lead_preferred_company_valuation_min_usd_m": _min_positive(
            profile.get("preferredcompanyvaluationmin") or profile.get("preferredcompanyvaluation")
            for profile in lead_profiles
        ),
        "lead_preferred_company_valuation_max_usd_m": _max_positive(
            profile.get("preferredcompanyvaluationmax") or profile.get("preferredcompanyvaluation")
            for profile in lead_profiles
        ),
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_employee_context-501"></a>

### _employee_context

文件：`boardroom_sim/pitchbook.py:501-518`

**签名**

~~~~python
def _employee_context(company_id: str, decision_date: Any, employee_history: pd.DataFrame) -> Dict[str, Optional[float]]:
~~~~

**功能**

源码文档说明：Compute employee count and growth from records available before the decision date.

**参数**

| 参数 | 含义 |
| --- | --- |
| `company_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `decision_date` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `employee_history` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |

**返回值**

返回 `Dict[str, Optional[float]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `employee_history.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `history.sort_values` | 库函数或对象方法 | pandas DataFrame 排序，返回新的 DataFrame。 |
| `_optional_float` | [boardroom_sim.pitchbook._optional_float](#fn-boardroom_sim-pitchbook-py-_optional_float-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `history.iloc.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `employee_history.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _employee_context(company_id: str, decision_date: Any, employee_history: pd.DataFrame) -> Dict[str, Optional[float]]:
    """Compute employee count and growth from records available before the decision date."""
    history = employee_history[employee_history["companyid"].astype(str) == company_id].copy()
    if history.empty:
        return {"employee_count_at_decision": None, "previous_employee_count": None, "employee_growth_rate": None}
    history = history.sort_values("date")
    if not pd.isna(decision_date):
        history = history[history["date"] <= decision_date]
    if history.empty:
        return {"employee_count_at_decision": None, "previous_employee_count": None, "employee_growth_rate": None}
    current = _optional_float(history.iloc[-1].get("employeecount"))
    previous = _optional_float(history.iloc[-2].get("employeecount")) if len(history) > 1 else None
    growth = (current - previous) / previous if current is not None and previous is not None and previous > 0 else None
    return {
        "employee_count_at_decision": current,
        "previous_employee_count": previous,
        "employee_growth_rate": growth,
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_governance_context-521"></a>

### _governance_context

文件：`boardroom_sim/pitchbook.py:521-571`

**签名**

~~~~python
def _governance_context( *, company_id: str, decision_date: Any, person_roles: pd.DataFrame, person_positions: pd.DataFrame, board_team: pd.DataFrame, board_seats: pd.DataFrame, ) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Summarize founder, CEO, engineering, executive, and board composition at decision time.

**参数**

| 参数 | 含义 |
| --- | --- |
| `company_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `decision_date` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `person_roles` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `person_positions` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `board_team` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `board_seats` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `person_roles.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `_active_as_of` | [boardroom_sim.pitchbook._active_as_of](#fn-boardroom_sim-pitchbook-py-_active_as_of-632) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `person_positions.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `board_team.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `pd.to_numeric.fillna.astype` | 库函数或对象方法 | pandas 库函数；用于表格读取、转换、筛选或时间/数值处理，返回 pandas 对象或标量。 |
| `board_seats.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `bool` | 库函数或对象方法 | 内置类型转换；返回布尔值。 |
| `active_roles.get.fillna.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `roles.get.fillna.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `founders.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `_is_ceo_series` | [boardroom_sim.pitchbook._is_ceo_series](#fn-boardroom_sim-pitchbook-py-_is_ceo_series-645) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `ceo_rows.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `pd.to_numeric.fillna` | 库函数或对象方法 | pandas 缺失值填充，返回 Series/DataFrame。 |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |
| `_is_engineering_series.sum` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `_is_executive_series.sum` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `person_roles.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `active_roles.get.fillna` | 库函数或对象方法 | pandas 缺失值填充，返回 Series/DataFrame。 |
| `roles.get.fillna` | 库函数或对象方法 | pandas 缺失值填充，返回 Series/DataFrame。 |
| `board_rows.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `board_seat_rows.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `person_positions.get.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `board_team.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `pd.to_numeric` | 库函数或对象方法 | 把 Series 或标量转换为数值，返回数值或 Series。 |
| `board_seats.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `_is_engineering_series` | [boardroom_sim.pitchbook._is_engineering_series](#fn-boardroom_sim-pitchbook-py-_is_engineering_series-654) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_is_executive_series` | [boardroom_sim.pitchbook._is_executive_series](#fn-boardroom_sim-pitchbook-py-_is_executive_series-664) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `active_roles.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `roles.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `person_positions.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `board_rows.get.notna` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `board_seat_rows.get.notna` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _governance_context(
    *,
    company_id: str,
    decision_date: Any,
    person_roles: pd.DataFrame,
    person_positions: pd.DataFrame,
    board_team: pd.DataFrame,
    board_seats: pd.DataFrame,
) -> Dict[str, Any]:
    """Summarize founder, CEO, engineering, executive, and board composition at decision time."""
    roles = person_roles[person_roles["companyid"].astype(str) == company_id].copy()
    active_roles = _active_as_of(roles, decision_date)
    founders = active_roles[active_roles.get("is_founder", False).fillna(False).astype(bool)] if not active_roles.empty else active_roles
    if founders.empty and not roles.empty:
        founders = roles[roles.get("is_founder", False).fillna(False).astype(bool)]
    founder_ids = {_as_str(person_id) for person_id in founders.get("personid", []) if _as_str(person_id)}

    positions = person_positions[
        (person_positions.get("entitytype", "").astype(str) == "Company")
        & (person_positions.get("entityid", "").astype(str) == company_id)
    ].copy()
    active_positions = _active_as_of(positions, decision_date)
    ceo_rows = active_positions[_is_ceo_series(active_positions)] if not active_positions.empty else active_positions
    ceo_ids = {_as_str(person_id) for person_id in ceo_rows.get("personid", []) if _as_str(person_id)}

    board_rows = _active_as_of(board_team[board_team["companyid"].astype(str) == company_id].copy(), decision_date)
    board_rows = board_rows[pd.to_numeric(board_rows.get("isonboard"), errors="coerce").fillna(0).astype(bool)]
    board_seat_rows = _active_as_of(board_seats[board_seats["companyid"].astype(str) == company_id].copy(), decision_date)
    board_ids = {
        _as_str(person_id)
        for person_id in list(board_rows.get("personid", [])) + list(board_seat_rows.get("personid", []))
        if _as_str(person_id)
    }
    investor_board_count = len(
        {
            _as_str(person_id)
            for person_id in list(board_rows[board_rows.get("representingid").notna()].get("personid", []))
            + list(board_seat_rows[board_seat_rows.get("representingid").notna()].get("personid", []))
            if _as_str(person_id)
        }
    )

    return {
        "founder_count": len(founder_ids),
        "current_ceo_count": len(ceo_ids),
        "current_ceo_is_founder": bool(ceo_ids & founder_ids),
        "engineering_role_count": int(_is_engineering_series(active_positions).sum()) if not active_positions.empty else 0,
        "executive_role_count": int(_is_executive_series(active_positions).sum()) if not active_positions.empty else 0,
        "board_member_count": len(board_ids),
        "investor_board_member_count": investor_board_count,
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_ceo_replacement_label-574"></a>

### _ceo_replacement_label

文件：`boardroom_sim/pitchbook.py:574-629`

**签名**

~~~~python
def _ceo_replacement_label( company_id: str, decision_date: Any, person_roles: pd.DataFrame, person_positions: pd.DataFrame, window_months: int = 24, ) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Infer whether CEO changes within a fixed post-financing window.

**参数**

| 参数 | 含义 |
| --- | --- |
| `company_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `decision_date` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `person_roles` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `person_positions` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `window_months` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `pd.Timestamp` | 库函数或对象方法 | 构造 pandas 时间戳对象。 |
| `person_positions.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `_active_as_of` | [boardroom_sim.pitchbook._active_as_of](#fn-boardroom_sim-pitchbook-py-_active_as_of-632) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `person_roles.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `pd.DateOffset` | 库函数或对象方法 | pandas 库函数；用于表格读取、转换、筛选或时间/数值处理，返回 pandas 对象或标量。 |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `bool` | 库函数或对象方法 | 内置类型转换；返回布尔值。 |
| `sorted` | 库函数或对象方法 | 内置排序；返回新的 list。 |
| `_is_ceo_series` | [boardroom_sim.pitchbook._is_ceo_series](#fn-boardroom_sim-pitchbook-py-_is_ceo_series-645) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `initial_rows.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `future_rows.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `roles.get.fillna.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `founders.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `ceo_positions.notna` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `initial_rows.notna` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `roles.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `person_roles.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `roles.get.fillna` | 库函数或对象方法 | pandas 缺失值填充，返回 Series/DataFrame。 |
| `person_positions.get.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `person_positions.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `roles.isna` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _ceo_replacement_label(
    company_id: str,
    decision_date: Any,
    person_roles: pd.DataFrame,
    person_positions: pd.DataFrame,
    window_months: int = 24,
) -> Dict[str, Any]:
    """Infer whether CEO changes within a fixed post-financing window."""
    if not company_id or pd.isna(decision_date):
        return {"label": "unknown", "window_months": window_months, "reason": "missing company or decision date"}

    date = pd.Timestamp(decision_date)
    window_end = date + pd.DateOffset(months=window_months)
    positions = person_positions[
        (person_positions.get("entitytype", "").astype(str) == "Company")
        & (person_positions.get("entityid", "").astype(str) == company_id)
    ].copy()
    ceo_positions = positions[_is_ceo_series(positions)] if not positions.empty else positions
    if ceo_positions.empty:
        return {"label": "unknown", "window_months": window_months, "reason": "no CEO position rows"}

    initial_rows = _active_as_of(ceo_positions, date)
    initial_ceo_ids = {_as_str(person_id) for person_id in initial_rows.get("personid", []) if _as_str(person_id)}
    if not initial_ceo_ids:
        return {"label": "unknown", "window_months": window_months, "reason": "no CEO active at decision date"}

    future_rows = ceo_positions[
        (ceo_positions["startdate"].notna()) & (ceo_positions["startdate"] > date) & (ceo_positions["startdate"] <= window_end)
    ]
    future_ceo_ids = {_as_str(person_id) for person_id in future_rows.get("personid", []) if _as_str(person_id)}
    ended_initial = initial_rows[
        (initial_rows["enddate"].notna()) & (initial_rows["enddate"] > date) & (initial_rows["enddate"] <= window_end)
    ]
    replaced = bool(future_ceo_ids - initial_ceo_ids) or not ended_initial.empty

    roles = person_roles[person_roles["companyid"].astype(str) == company_id].copy()
    founders = roles[roles.get("is_founder", False).fillna(False).astype(bool)] if not roles.empty else roles
    founder_ids = {_as_str(person_id) for person_id in founders.get("personid", []) if _as_str(person_id)}
    successor_type = "unknown"
    if future_ceo_ids - initial_ceo_ids:
        successor_ids = future_ceo_ids - initial_ceo_ids
        prior_company_people = {
            _as_str(person_id)
            for person_id in roles[roles["startdate"].isna() | (roles["startdate"] <= date)].get("personid", [])
            if _as_str(person_id)
        }
        successor_type = "internal" if successor_ids & prior_company_people else "external"

    return {
        "label": "replace" if replaced else "keep",
        "window_months": window_months,
        "initial_ceo_ids": sorted(initial_ceo_ids),
        "future_ceo_ids": sorted(future_ceo_ids),
        "founder_ceo_at_decision": bool(initial_ceo_ids & founder_ids),
        "successor_type": successor_type,
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_active_as_of-632"></a>

### _active_as_of

文件：`boardroom_sim/pitchbook.py:632-642`

**签名**

~~~~python
def _active_as_of(df: pd.DataFrame, date: Any) -> pd.DataFrame:
~~~~

**功能**

源码文档说明：Return rows whose start/end dates imply activity at the given date.

**参数**

| 参数 | 含义 |
| --- | --- |
| `df` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |
| `date` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `pd.DataFrame` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `pd.Timestamp` | 库函数或对象方法 | 构造 pandas 时间戳对象。 |
| `df.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `df.iloc.copy` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `active.isna` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.pitchbook._governance_context](#fn-boardroom_sim-pitchbook-py-_governance_context-521)、[boardroom_sim.pitchbook._ceo_replacement_label](#fn-boardroom_sim-pitchbook-py-_ceo_replacement_label-574)

**完整源码**

~~~~python
def _active_as_of(df: pd.DataFrame, date: Any) -> pd.DataFrame:
    """Return rows whose start/end dates imply activity at the given date."""
    if df.empty or pd.isna(date):
        return df.iloc[0:0].copy()
    point = pd.Timestamp(date)
    active = df.copy()
    if "startdate" in active.columns:
        active = active[active["startdate"].isna() | (active["startdate"] <= point)]
    if "enddate" in active.columns:
        active = active[active["enddate"].isna() | (active["enddate"] >= point)]
    return active
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_is_ceo_series-645"></a>

### _is_ceo_series

文件：`boardroom_sim/pitchbook.py:645-651`

**签名**

~~~~python
def _is_ceo_series(df: pd.DataFrame) -> pd.Series:
~~~~

**功能**

源码文档说明：Return a boolean series identifying CEO position rows.

**参数**

| 参数 | 含义 |
| --- | --- |
| `df` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |

**返回值**

返回 `pd.Series` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `df.get.fillna.astype.str.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `pd.Series` | 库函数或对象方法 | pandas 库函数；用于表格读取、转换、筛选或时间/数值处理，返回 pandas 对象或标量。 |
| `title.str.contains` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `level.isin` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `df.get.fillna.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `df.get.fillna` | 库函数或对象方法 | pandas 缺失值填充，返回 Series/DataFrame。 |
| `df.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.pitchbook._governance_context](#fn-boardroom_sim-pitchbook-py-_governance_context-521)、[boardroom_sim.pitchbook._ceo_replacement_label](#fn-boardroom_sim-pitchbook-py-_ceo_replacement_label-574)

**完整源码**

~~~~python
def _is_ceo_series(df: pd.DataFrame) -> pd.Series:
    """Return a boolean series identifying CEO position rows."""
    if df.empty:
        return pd.Series(dtype=bool)
    title = df.get("fulltitle", pd.Series("", index=df.index)).fillna("").astype(str).str.lower()
    level = df.get("positionlevel", pd.Series("", index=df.index)).fillna("").astype(str).str.lower()
    return title.str.contains("chief executive officer|\\bceo\\b", regex=True) | level.isin(CEO_TITLES)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_is_engineering_series-654"></a>

### _is_engineering_series

文件：`boardroom_sim/pitchbook.py:654-661`

**签名**

~~~~python
def _is_engineering_series(df: pd.DataFrame) -> pd.Series:
~~~~

**功能**

源码文档说明：Return a boolean series identifying engineering or technical leadership rows.

**参数**

| 参数 | 含义 |
| --- | --- |
| `df` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |

**返回值**

返回 `pd.Series` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `df.get.fillna.astype.str.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `combined.apply` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `pd.Series` | 库函数或对象方法 | pandas 库函数；用于表格读取、转换、筛选或时间/数值处理，返回 pandas 对象或标量。 |
| `any` | 库函数或对象方法 | 内置逻辑聚合；任一元素为真时返回 True。 |
| `df.get.fillna.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `df.get.fillna` | 库函数或对象方法 | pandas 缺失值填充，返回 Series/DataFrame。 |
| `df.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.pitchbook._governance_context](#fn-boardroom_sim-pitchbook-py-_governance_context-521)

**完整源码**

~~~~python
def _is_engineering_series(df: pd.DataFrame) -> pd.Series:
    """Return a boolean series identifying engineering or technical leadership rows."""
    if df.empty:
        return pd.Series(dtype=bool)
    title = df.get("fulltitle", pd.Series("", index=df.index)).fillna("").astype(str).str.lower()
    level = df.get("positionlevel", pd.Series("", index=df.index)).fillna("").astype(str).str.lower()
    combined = title + " " + level
    return combined.apply(lambda text: any(token in text for token in ENGINEERING_TITLE_TOKENS))
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_is_executive_series-664"></a>

### _is_executive_series

文件：`boardroom_sim/pitchbook.py:664-670`

**签名**

~~~~python
def _is_executive_series(df: pd.DataFrame) -> pd.Series:
~~~~

**功能**

源码文档说明：Return a boolean series identifying executive rows.

**参数**

| 参数 | 含义 |
| --- | --- |
| `df` | pandas DataFrame，表示 Excel 中的一张表或筛选后的表。 |

**返回值**

返回 `pd.Series` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `df.get.fillna.astype.str.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `pd.Series` | 库函数或对象方法 | pandas 库函数；用于表格读取、转换、筛选或时间/数值处理，返回 pandas 对象或标量。 |
| `title.str.contains` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `level.str.contains` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `df.get.fillna.astype` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `df.get.fillna` | 库函数或对象方法 | pandas 缺失值填充，返回 Series/DataFrame。 |
| `df.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.pitchbook._governance_context](#fn-boardroom_sim-pitchbook-py-_governance_context-521)

**完整源码**

~~~~python
def _is_executive_series(df: pd.DataFrame) -> pd.Series:
    """Return a boolean series identifying executive rows."""
    if df.empty:
        return pd.Series(dtype=bool)
    title = df.get("fulltitle", pd.Series("", index=df.index)).fillna("").astype(str).str.lower()
    level = df.get("positionlevel", pd.Series("", index=df.index)).fillna("").astype(str).str.lower()
    return title.str.contains("chief|president|founder|executive", regex=True) | level.str.contains("chief|executive|founder", regex=True)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_company_age_years-673"></a>

### _company_age_years

文件：`boardroom_sim/pitchbook.py:673-678`

**签名**

~~~~python
def _company_age_years(year_founded: Any, decision_date: Any) -> Optional[float]:
~~~~

**功能**

源码文档说明：Compute company age in years at the decision date.

**参数**

| 参数 | 含义 |
| --- | --- |
| `year_founded` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `decision_date` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_optional_int` | [boardroom_sim.pitchbook._optional_int](#fn-boardroom_sim-pitchbook-py-_optional_int-792) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `pd.Timestamp` | 库函数或对象方法 | 构造 pandas 时间戳对象。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _company_age_years(year_founded: Any, decision_date: Any) -> Optional[float]:
    """Compute company age in years at the decision date."""
    founded = _optional_int(year_founded)
    if founded is None or founded <= 0 or pd.isna(decision_date):
        return None
    return max(0.0, float(pd.Timestamp(decision_date).year - founded))
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_months_between-681"></a>

### _months_between

文件：`boardroom_sim/pitchbook.py:681-686`

**签名**

~~~~python
def _months_between(start: Any, end: Any) -> Optional[float]:
~~~~

**功能**

源码文档说明：Approximate elapsed months between two dates.

**参数**

| 参数 | 含义 |
| --- | --- |
| `start` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `end` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `round` | 库函数或对象方法 | 内置四舍五入；返回数值。 |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `pd.Timestamp` | 库函数或对象方法 | 构造 pandas 时间戳对象。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _months_between(start: Any, end: Any) -> Optional[float]:
    """Approximate elapsed months between two dates."""
    if pd.isna(start) or pd.isna(end):
        return None
    delta_days = (pd.Timestamp(end) - pd.Timestamp(start)).days
    return round(max(0.0, delta_days / 30.4375), 2)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_valuation_markup-689"></a>

### _valuation_markup

文件：`boardroom_sim/pitchbook.py:689-691`

**签名**

~~~~python
def _valuation_markup(current: Optional[float], previous: Optional[float]) -> Optional[float]:
~~~~

**功能**

源码文档说明：Return valuation markup when prior post-money valuation is available.

**参数**

| 参数 | 含义 |
| --- | --- |
| `current` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `previous` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)、[boardroom_sim.pitchbook._deal_history_payload](#fn-boardroom_sim-pitchbook-py-_deal_history_payload-357)

**完整源码**

~~~~python
def _valuation_markup(current: Optional[float], previous: Optional[float]) -> Optional[float]:
    """Return valuation markup when prior post-money valuation is available."""
    return current / previous if current is not None and previous is not None and current > 0 and previous > 0 else None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_valuation_direction_from_label_or_markup-694"></a>

### _valuation_direction_from_label_or_markup

文件：`boardroom_sim/pitchbook.py:694-703`

**签名**

~~~~python
def _valuation_direction_from_label_or_markup(raw_label: Any, markup: Optional[float]) -> ValuationDirection:
~~~~

**功能**

源码文档说明：Normalize PitchBook direction label, falling back to valuation markup.

**参数**

| 参数 | 含义 |
| --- | --- |
| `raw_label` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `markup` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `ValuationDirection` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_as_str.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `_valuation_direction_from_markup` | [boardroom_sim.pitchbook._valuation_direction_from_markup](#fn-boardroom_sim-pitchbook-py-_valuation_direction_from_markup-706) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_as_str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `_as_str` | [boardroom_sim.pitchbook._as_str](#fn-boardroom_sim-pitchbook-py-_as_str-752) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)、[boardroom_sim.pitchbook._deal_history_payload](#fn-boardroom_sim-pitchbook-py-_deal_history_payload-357)

**完整源码**

~~~~python
def _valuation_direction_from_label_or_markup(raw_label: Any, markup: Optional[float]) -> ValuationDirection:
    """Normalize PitchBook direction label, falling back to valuation markup."""
    label = _as_str(raw_label).strip().lower()
    if "up" in label:
        return "up"
    if "down" in label:
        return "down"
    if "flat" in label:
        return "flat"
    return _valuation_direction_from_markup(markup)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_valuation_direction_from_markup-706"></a>

### _valuation_direction_from_markup

文件：`boardroom_sim/pitchbook.py:706-714`

**签名**

~~~~python
def _valuation_direction_from_markup(markup: Optional[float]) -> ValuationDirection:
~~~~

**功能**

源码文档说明：Infer valuation direction from an observed valuation markup.

**参数**

| 参数 | 含义 |
| --- | --- |
| `markup` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `ValuationDirection` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.pitchbook._valuation_direction_from_label_or_markup](#fn-boardroom_sim-pitchbook-py-_valuation_direction_from_label_or_markup-694)

**完整源码**

~~~~python
def _valuation_direction_from_markup(markup: Optional[float]) -> ValuationDirection:
    """Infer valuation direction from an observed valuation markup."""
    if markup is None or markup <= 0:
        return "unknown"
    if markup >= 1.15:
        return "up"
    if markup <= 0.85:
        return "down"
    return "flat"
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_field_sources-717"></a>

### _field_sources

文件：`boardroom_sim/pitchbook.py:717-730`

**签名**

~~~~python
def _field_sources() -> Dict[str, str]:
~~~~

**功能**

源码文档说明：Document where generated BoardCase fields and labels came from.

**参数**

无显式参数。

**返回值**

返回 `Dict[str, str]` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)

**完整源码**

~~~~python
def _field_sources() -> Dict[str, str]:
    """Document where generated BoardCase fields and labels came from."""
    return {
        "decision_date": "target deal.dealdate, used only as a decision timestamp",
        "prior_deal_size_usd_m": "previous same-company VC deal.dealsize",
        "prior_post_money_valuation_usd_m": "previous same-company VC deal.postvaluation",
        "prior_valuation_markup_multiple": "previous VC deal.postvaluation divided by the deal before it",
        "prior_company_deal_history": "same-company VC-like deals before decision_date, truncated by history_limit",
        "prior_lead_investor_deal_history": "historical VC-like investments before decision_date by lead investors from the target financing round",
        "prior_followon_investor_deal_history": "historical VC-like investments before decision_date by follow-on or non-lead investors from the target financing round",
        "employee_growth_rate": "companyemployeehistoryrelation.employeecount before decision_date",
        "governance_fields": "_sample_person_roles, personpositionrelation, companyboardteamrelation, personboardseatrelation before decision_date",
        "labels": "current target deal outcomes and derived 24-month CEO replacement label; not exposed to agents",
    }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_min_positive-733"></a>

### _min_positive

文件：`boardroom_sim/pitchbook.py:733-736`

**签名**

~~~~python
def _min_positive(values: Any) -> Optional[float]:
~~~~

**功能**

源码文档说明：Return the smallest positive numeric value from an iterable.

**参数**

| 参数 | 含义 |
| --- | --- |
| `values` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `min` | 库函数或对象方法 | 内置最小值函数；返回最小元素。 |
| `_optional_float` | [boardroom_sim.pitchbook._optional_float](#fn-boardroom_sim-pitchbook-py-_optional_float-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.pitchbook._investor_context](#fn-boardroom_sim-pitchbook-py-_investor_context-445)

**完整源码**

~~~~python
def _min_positive(values: Any) -> Optional[float]:
    """Return the smallest positive numeric value from an iterable."""
    positives = [number for number in (_optional_float(value) for value in values) if number is not None and number > 0]
    return min(positives) if positives else None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_max_positive-739"></a>

### _max_positive

文件：`boardroom_sim/pitchbook.py:739-742`

**签名**

~~~~python
def _max_positive(values: Any) -> Optional[float]:
~~~~

**功能**

源码文档说明：Return the largest positive numeric value from an iterable.

**参数**

| 参数 | 含义 |
| --- | --- |
| `values` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `_optional_float` | [boardroom_sim.pitchbook._optional_float](#fn-boardroom_sim-pitchbook-py-_optional_float-771) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.pitchbook._investor_context](#fn-boardroom_sim-pitchbook-py-_investor_context-445)

**完整源码**

~~~~python
def _max_positive(values: Any) -> Optional[float]:
    """Return the largest positive numeric value from an iterable."""
    positives = [number for number in (_optional_float(value) for value in values) if number is not None and number > 0]
    return max(positives) if positives else None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_date_str-745"></a>

### _date_str

文件：`boardroom_sim/pitchbook.py:745-749`

**签名**

~~~~python
def _date_str(value: Any) -> str:
~~~~

**功能**

源码文档说明：Serialize a timestamp-like value as YYYY-MM-DD.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `pd.Timestamp.date.isoformat` | 库函数或对象方法 | pandas 库函数；用于表格读取、转换、筛选或时间/数值处理，返回 pandas 对象或标量。 |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `pd.Timestamp.date` | 库函数或对象方法 | pandas 库函数；用于表格读取、转换、筛选或时间/数值处理，返回 pandas 对象或标量。 |
| `pd.Timestamp` | 库函数或对象方法 | 构造 pandas 时间戳对象。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)、[boardroom_sim.pitchbook._deal_history_payload](#fn-boardroom_sim-pitchbook-py-_deal_history_payload-357)

**完整源码**

~~~~python
def _date_str(value: Any) -> str:
    """Serialize a timestamp-like value as YYYY-MM-DD."""
    if value is None or pd.isna(value):
        return ""
    return pd.Timestamp(value).date().isoformat()
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_as_str-752"></a>

### _as_str

文件：`boardroom_sim/pitchbook.py:752-757`

**签名**

~~~~python
def _as_str(value: Any, default: str = "") -> str:
~~~~

**功能**

源码文档说明：Convert a dataframe cell to a clean string.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `math.isnan` | 库函数或对象方法 | 判断浮点 NaN，返回 bool。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `text.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)、[boardroom_sim.pitchbook._investor_ids_for_deal](#fn-boardroom_sim-pitchbook-py-_investor_ids_for_deal-275)、[boardroom_sim.pitchbook._investor_deal_history](#fn-boardroom_sim-pitchbook-py-_investor_deal_history-297)、[boardroom_sim.pitchbook._deal_history_payload](#fn-boardroom_sim-pitchbook-py-_deal_history_payload-357)、[boardroom_sim.pitchbook._primary_industry_by_company](#fn-boardroom_sim-pitchbook-py-_primary_industry_by_company-391)、[boardroom_sim.pitchbook._list_by_company](#fn-boardroom_sim-pitchbook-py-_list_by_company-404)、[boardroom_sim.pitchbook._index_by_id](#fn-boardroom_sim-pitchbook-py-_index_by_id-415)、[boardroom_sim.pitchbook._count_by_company](#fn-boardroom_sim-pitchbook-py-_count_by_company-427)、[boardroom_sim.pitchbook._investor_context](#fn-boardroom_sim-pitchbook-py-_investor_context-445)、[boardroom_sim.pitchbook._governance_context](#fn-boardroom_sim-pitchbook-py-_governance_context-521)、[boardroom_sim.pitchbook._ceo_replacement_label](#fn-boardroom_sim-pitchbook-py-_ceo_replacement_label-574)、[boardroom_sim.pitchbook._valuation_direction_from_label_or_markup](#fn-boardroom_sim-pitchbook-py-_valuation_direction_from_label_or_markup-694)

**完整源码**

~~~~python
def _as_str(value: Any, default: str = "") -> str:
    """Convert a dataframe cell to a clean string."""
    if value is None or (isinstance(value, float) and math.isnan(value)) or pd.isna(value):
        return default
    text = str(value).strip()
    return text if text and text.lower() != "nan" else default
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_as_float-760"></a>

### _as_float

文件：`boardroom_sim/pitchbook.py:760-768`

**签名**

~~~~python
def _as_float(value: Any, default: float = 0.0) -> float:
~~~~

**功能**

源码文档说明：Convert a dataframe cell to float.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `float` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `math.isfinite` | 库函数或对象方法 | math 数学函数；返回数值或布尔判断。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def _as_float(value: Any, default: float = 0.0) -> float:
    """Convert a dataframe cell to float."""
    if value is None or pd.isna(value):
        return default
    try:
        number = float(value)
    except (TypeError, ValueError):
        return default
    return number if math.isfinite(number) else default
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_optional_float-771"></a>

### _optional_float

文件：`boardroom_sim/pitchbook.py:771-779`

**签名**

~~~~python
def _optional_float(value: Any) -> Optional[float]:
~~~~

**功能**

源码文档说明：Convert a dataframe cell to float while preserving missing values.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `math.isfinite` | 库函数或对象方法 | math 数学函数；返回数值或布尔判断。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)、[boardroom_sim.pitchbook._company_deal_history](#fn-boardroom_sim-pitchbook-py-_company_deal_history-260)、[boardroom_sim.pitchbook._investor_deal_history](#fn-boardroom_sim-pitchbook-py-_investor_deal_history-297)、[boardroom_sim.pitchbook._deal_history_payload](#fn-boardroom_sim-pitchbook-py-_deal_history_payload-357)、[boardroom_sim.pitchbook._employee_context](#fn-boardroom_sim-pitchbook-py-_employee_context-501)、[boardroom_sim.pitchbook._min_positive](#fn-boardroom_sim-pitchbook-py-_min_positive-733)、[boardroom_sim.pitchbook._max_positive](#fn-boardroom_sim-pitchbook-py-_max_positive-739)

**完整源码**

~~~~python
def _optional_float(value: Any) -> Optional[float]:
    """Convert a dataframe cell to float while preserving missing values."""
    if value is None or pd.isna(value):
        return None
    try:
        number = float(value)
    except (TypeError, ValueError):
        return None
    return number if math.isfinite(number) else None
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_as_int-782"></a>

### _as_int

文件：`boardroom_sim/pitchbook.py:782-789`

**签名**

~~~~python
def _as_int(value: Any, default: int = 0) -> int:
~~~~

**功能**

源码文档说明：Convert a dataframe cell to int.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `int` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def _as_int(value: Any, default: int = 0) -> int:
    """Convert a dataframe cell to int."""
    if value is None or pd.isna(value):
        return default
    try:
        return int(float(value))
    except (TypeError, ValueError):
        return default
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-pitchbook-py-_optional_int-792"></a>

### _optional_int

文件：`boardroom_sim/pitchbook.py:792-799`

**签名**

~~~~python
def _optional_int(value: Any) -> Optional[int]:
~~~~

**功能**

源码文档说明：Convert a dataframe cell to int while preserving missing values.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[int]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `pd.isna` | 库函数或对象方法 | 判断 pandas 缺失值，返回 bool 或布尔 Series。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |

**被谁调用**

[boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31)、[boardroom_sim.pitchbook._investor_deal_history](#fn-boardroom_sim-pitchbook-py-_investor_deal_history-297)、[boardroom_sim.pitchbook._deal_history_payload](#fn-boardroom_sim-pitchbook-py-_deal_history_payload-357)、[boardroom_sim.pitchbook._company_age_years](#fn-boardroom_sim-pitchbook-py-_company_age_years-673)

**完整源码**

~~~~python
def _optional_int(value: Any) -> Optional[int]:
    """Convert a dataframe cell to int while preserving missing values."""
    if value is None or pd.isna(value):
        return None
    try:
        return int(float(value))
    except (TypeError, ValueError):
        return None
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-process-py"></a>

## boardroom_sim/process.py

董事会流程控制模块：控制发言顺序、可见历史、archetype、L5 文化和每轮 prompt 上下文。

**类**：[BoardProcessController](#class-boardroom_sim-process-py-boardprocesscontroller-99)

<a id="fn-boardroom_sim-process-py-boardprocesscontroller-__init__-102"></a>

### BoardProcessController.__init__

文件：`boardroom_sim/process.py:102-104`

**签名**

~~~~python
def __init__(self, config: BoardProcessConfig, role_order: Sequence[str]) -> None:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `config` | 配置对象，控制 LLM、角色、prompt、过程或输出。 |
| `role_order` | 角色发言或构造顺序。 |

**返回值**

初始化方法，通常返回 `None`；主要副作用是给 `self` 写入属性。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
    def __init__(self, config: BoardProcessConfig, role_order: Sequence[str]) -> None:
        self.config = config
        self.role_order = list(role_order)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-process-py-boardprocesscontroller-to_trace_payload-106"></a>

### BoardProcessController.to_trace_payload

文件：`boardroom_sim/process.py:106-108`

**签名**

~~~~python
def to_trace_payload(self) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Return a serializable process snapshot for traces and manifests.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `asdict` | 库函数或对象方法 | dataclasses 工具；把 dataclass 实例递归转换为 dict。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator._run_bargaining_round](#fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208)

**完整源码**

~~~~python
    def to_trace_payload(self) -> Dict[str, Any]:
        """Return a serializable process snapshot for traces and manifests."""
        return asdict(self.config)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-process-py-boardprocesscontroller-effective_role_weights-110"></a>

### BoardProcessController.effective_role_weights

文件：`boardroom_sim/process.py:110-121`

**签名**

~~~~python
def effective_role_weights(self, base_weights: Dict[str, float]) -> Dict[str, float]:
~~~~

**功能**

根据 archetype 调整角色投票/聚合权重，用于最终预测的加权汇总。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `base_weights` | 角色原始聚合权重。 |

**返回值**

返回 `Dict[str, float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `base_weights.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `configured_multipliers.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `round` | 库函数或对象方法 | 内置四舍五入；返回数值。 |
| `base_weights.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.__init__](#fn-boardroom_sim-simulator-py-boardroomsimulator-__init__-32)

**完整源码**

~~~~python
    def effective_role_weights(self, base_weights: Dict[str, float]) -> Dict[str, float]:
        """Return board-process-adjusted role weights."""
        configured_multipliers = self.config.role_weight_multipliers
        weights: Dict[str, float] = {}
        for role_name in self.role_order:
            base = float(base_weights.get(role_name, 1.0))
            multiplier = configured_multipliers.get(role_name, 1.0)
            weights[role_name] = round(base * multiplier, 6)
        for role_name, weight in base_weights.items():
            if role_name not in weights:
                weights[role_name] = float(weight)
        return weights
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-process-py-boardprocesscontroller-speaking_order_for_round-123"></a>

### BoardProcessController.speaking_order_for_round

文件：`boardroom_sim/process.py:123-142`

**签名**

~~~~python
def speaking_order_for_round( self, round_index: int, effective_weights: Dict[str, float], ) -> List[str]:
~~~~

**功能**

决定某一轮谁先发言、谁发言、谁不发言。这里体现 board archetype 对讨论流程的影响。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `round_index` | 从 0 开始的讨论轮次索引。 |
| `effective_weights` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `List[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |
| `sorted` | 库函数或对象方法 | 内置排序；返回新的 list。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `min` | 库函数或对象方法 | 内置最小值函数；返回最小元素。 |
| `effective_weights.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator._run_bargaining_round](#fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208)

**完整源码**

~~~~python
    def speaking_order_for_round(
        self,
        round_index: int,
        effective_weights: Dict[str, float],
    ) -> List[str]:
        """Return the roles that speak in one bargaining round."""
        protocol = self.config.protocol
        if protocol == "free_interjection" and self.role_order:
            shift = round_index % len(self.role_order)
            return self.role_order[shift:] + self.role_order[:shift]
        if protocol == "top3_dominance":
            ranked = sorted(self.role_order, key=lambda role: effective_weights.get(role, 1.0), reverse=True)
            return ranked[: min(3, len(ranked))]
        if protocol == "rubber_stamp":
            if round_index == 0:
                return [role for role in self.role_order if role in {"Founder_CEO", "Lead_VC_Director"}]
            if round_index == 1:
                return [role for role in self.role_order if role in {"Lead_VC_Director", "Followon_VC_Director"}]
            return [role for role in self.role_order if role in {"Founder_CEO", "Lead_VC_Director"}]
        return list(self.role_order)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-process-py-boardprocesscontroller-visible_history-144"></a>

### BoardProcessController.visible_history

文件：`boardroom_sim/process.py:144-154`

**签名**

~~~~python
def visible_history(self, history: List[Dict[str, Any]], round_number: int) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Filter bargaining history according to process memory scope.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `history` | 讨论历史事件列表，通常会被裁剪后放入 prompt。 |
| `round_number` | 从 1 开始展示给人看的讨论轮次。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `item.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator._run_bargaining_round](#fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208)

**完整源码**

~~~~python
    def visible_history(self, history: List[Dict[str, Any]], round_number: int) -> List[Dict[str, Any]]:
        """Filter bargaining history according to process memory scope."""
        scope = self.config.memory_scope
        if scope == "none":
            return []
        if scope == "round":
            return [item for item in history if int(item.get("round", -1)) == round_number]
        if scope == "limited":
            window = max(0, self.config.memory_window)
            return history[-window:] if window else []
        return list(history)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-process-py-boardprocesscontroller-prompt_context-156"></a>

### BoardProcessController.prompt_context

文件：`boardroom_sim/process.py:156-187`

**签名**

~~~~python
def prompt_context( self, *, role_name: str, round_index: int, speakers: Sequence[str], effective_weights: Dict[str, float], ) -> Dict[str, Any]:
~~~~

**功能**

生成每轮发给角色的过程上下文，包含讨论协议、当前会议阶段、可见历史、L5 文化解释和可操作行为要求。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `role_name` | 角色标识，例如 founder_ceo、lead_vc_director。 |
| `round_index` | 从 0 开始的讨论轮次索引。 |
| `speakers` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `effective_weights` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `interpret_l5_culture` | [boardroom_sim.culture.interpret_l5_culture](#fn-boardroom_sim-culture-py-interpret_l5_culture-77) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self.meeting_phase_for_round` | [boardroom_sim.process.BoardProcessController.meeting_phase_for_round](#fn-boardroom_sim-process-py-boardprocesscontroller-meeting_phase_for_round-189) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |
| `effective_weights.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `self._process_instruction` | [boardroom_sim.process.BoardProcessController._process_instruction](#fn-boardroom_sim-process-py-boardprocesscontroller-_process_instruction-196) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._challenge_instruction` | [boardroom_sim.process.BoardProcessController._challenge_instruction](#fn-boardroom_sim-process-py-boardprocesscontroller-_challenge_instruction-210) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator._run_bargaining_round](#fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208)

**完整源码**

~~~~python
    def prompt_context(
        self,
        *,
        role_name: str,
        round_index: int,
        speakers: Sequence[str],
        effective_weights: Dict[str, float],
    ) -> Dict[str, Any]:
        """Return process constraints to expose to an agent prompt."""
        protocol = self.config.protocol
        l5_policy = interpret_l5_culture(self.config.culture)
        meeting_phase = self.meeting_phase_for_round(round_index)
        return {
            "board_archetype": self.config.archetype,
            "board_archetype_label": self.config.archetype_label,
            "board_archetype_description": self.config.archetype_description,
            "discussion_protocol": protocol,
            "dominance_pattern": self.config.dominance_pattern,
            "meeting_phase": meeting_phase,
            "culture_layer": self.config.culture_layer,
            "l5_culture": l5_policy,
            "culture": dict(self.config.culture),
            "challenge_required": self.config.challenge_required,
            "memory_scope": self.config.memory_scope,
            "memory_window": self.config.memory_window,
            "round_number": round_index + 1,
            "speaking_order_this_round": list(speakers),
            "current_role": role_name,
            "current_role_effective_weight": effective_weights.get(role_name, 1.0),
            "process_instruction": self._process_instruction(protocol, meeting_phase, l5_policy),
            "challenge_instruction": self._challenge_instruction(l5_policy),
        }
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-process-py-boardprocesscontroller-meeting_phase_for_round-189"></a>

### BoardProcessController.meeting_phase_for_round

文件：`boardroom_sim/process.py:189-194`

**签名**

~~~~python
def meeting_phase_for_round(self, round_index: int) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Return the archetype-specific meeting phase for the current round.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `round_index` | 从 0 开始的讨论轮次索引。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `MEETING_PHASES.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `min` | 库函数或对象方法 | 内置最小值函数；返回最小元素。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |

**被谁调用**

[boardroom_sim.process.BoardProcessController.prompt_context](#fn-boardroom_sim-process-py-boardprocesscontroller-prompt_context-156)

**完整源码**

~~~~python
    def meeting_phase_for_round(self, round_index: int) -> Dict[str, Any]:
        """Return the archetype-specific meeting phase for the current round."""
        phases = MEETING_PHASES.get(self.config.archetype, MEETING_PHASES["value_creating"])
        phase = dict(phases[min(round_index, len(phases) - 1)])
        phase["round_number"] = round_index + 1
        return phase
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-process-py-boardprocesscontroller-_process_instruction-196"></a>

### BoardProcessController._process_instruction

文件：`boardroom_sim/process.py:196-208`

**签名**

~~~~python
def _process_instruction(self, protocol: str, meeting_phase: Dict[str, Any], l5_policy: Dict[str, Any]) -> str:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `protocol` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `meeting_phase` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `l5_policy` | L5 文化解释后的策略对象。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `PROCESS_INSTRUCTIONS.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `ARCHETYPE_INSTRUCTIONS.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `l5_policy.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |

**被谁调用**

[boardroom_sim.process.BoardProcessController.prompt_context](#fn-boardroom_sim-process-py-boardprocesscontroller-prompt_context-156)

**完整源码**

~~~~python
    def _process_instruction(self, protocol: str, meeting_phase: Dict[str, Any], l5_policy: Dict[str, Any]) -> str:
        protocol_instruction = PROCESS_INSTRUCTIONS.get(protocol, PROCESS_INSTRUCTIONS["chair_gated"])
        archetype_instruction = ARCHETYPE_INSTRUCTIONS.get(self.config.archetype, ARCHETYPE_INSTRUCTIONS["value_creating"])
        l5_levels = ", ".join(
            f"{name}={item['score']:.2f}/{item['level_label']}"
            for name, item in l5_policy["dimensions"].items()
        )
        l5_instruction = f"L5 强度解释：{l5_levels}。行为控制：{l5_policy['meeting_directive']}"
        phase_instruction = (
            f"当前会议阶段={meeting_phase['phase']}；目的：{meeting_phase['purpose']}；"
            f"本轮贡献要求：{meeting_phase['expected_contribution']}"
        )
        return f"{protocol_instruction} {archetype_instruction} {phase_instruction} {l5_instruction}"
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-process-py-boardprocesscontroller-_challenge_instruction-210"></a>

### BoardProcessController._challenge_instruction

文件：`boardroom_sim/process.py:210-217`

**签名**

~~~~python
def _challenge_instruction(self, l5_policy: Dict[str, Any]) -> str:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `l5_policy` | L5 文化解释后的策略对象。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.process.BoardProcessController.prompt_context](#fn-boardroom_sim-process-py-boardprocesscontroller-prompt_context-156)

**完整源码**

~~~~python
    def _challenge_instruction(self, l5_policy: Dict[str, Any]) -> str:
        controls = l5_policy["behavior_controls"]
        challenge_mode = controls["challenge_mode"]
        if self.config.challenge_required or challenge_mode in {"required_challenge", "hard_challenge"}:
            return "本轮要求角色主动检查当前提案或他人推理中的薄弱假设；只有在没有更强证据时才维持原预测。"
        if challenge_mode in {"confirm_unless_contradiction", "soft_question"}:
            return "本轮不强制提出反对意见；如提出异议，应以温和、证据化方式说明，而不是为了制造冲突。"
        return "本轮不强制提出反对意见；角色可以选择确认、补充或温和修正当前预测。"
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-process-py-changed_fields-220"></a>

### changed_fields

文件：`boardroom_sim/process.py:220-232`

**签名**

~~~~python
def changed_fields(before: RoleDecision, after: RoleDecision) -> List[str]:
~~~~

**功能**

源码文档说明：Return changed prediction fields between two role decisions.

**参数**

| 参数 | 含义 |
| --- | --- |
| `before` | 变化前的结构化判断或数值。 |
| `after` | 变化后的结构化判断或数值。 |

**返回值**

返回 `List[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `getattr` | 库函数或对象方法 | 内置属性读取；返回对象属性值或默认值。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator._run_bargaining_round](#fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208)

**完整源码**

~~~~python
def changed_fields(before: RoleDecision, after: RoleDecision) -> List[str]:
    """Return changed prediction fields between two role decisions."""
    fields = [
        "financing_intent",
        "completion_view",
        "predicted_deal_size_usd_m",
        "predicted_post_money_valuation_usd_m",
        "predicted_investor_ownership_pct",
        "predicted_deal_type",
        "valuation_direction",
        "satisfaction_score",
    ]
    return [field for field in fields if getattr(before, field) != getattr(after, field)]
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-prompts-py"></a>

## boardroom_sim/prompts.py

Prompt 模板模块：读取 Markdown prompt 并替换变量。

**类**：[PromptRenderer](#class-boardroom_sim-prompts-py-promptrenderer-15)

<a id="fn-boardroom_sim-prompts-py-promptrenderer-__init__-18"></a>

### PromptRenderer.__init__

文件：`boardroom_sim/prompts.py:18-22`

**签名**

~~~~python
def __init__(self, prompts_dir: Path | None = None, response_generator: str = "critical") -> None:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `prompts_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `response_generator` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

初始化方法，通常返回 `None`；主要副作用是给 `self` 写入属性。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
    def __init__(self, prompts_dir: Path | None = None, response_generator: str = "critical") -> None:
        self.prompts_dir = prompts_dir or DEFAULT_PROMPTS_DIR
        self.response_generator = response_generator
        self.round_tasks: list[str] = []
        self._cache: Dict[str, str] = {}
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-prompts-py-promptrenderer-render-24"></a>

### PromptRenderer.render

文件：`boardroom_sim/prompts.py:24-27`

**签名**

~~~~python
def render(self, template_name: str, **values: object) -> str:
~~~~

**功能**

源码文档说明：Render a top-level prompt template such as system_agent.md.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `template_name` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `**values` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self._read_template` | [boardroom_sim.prompts.PromptRenderer._read_template](#fn-boardroom_sim-prompts-py-promptrenderer-_read_template-36) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `Template.safe_substitute.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `Template.safe_substitute` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `Template` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `values.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent._system_prompt](#fn-boardroom_sim-agents-py-boardagent-_system_prompt-160)、[boardroom_sim.agents.BoardAgent._decision_prompt](#fn-boardroom_sim-agents-py-boardagent-_decision_prompt-172)、[boardroom_sim.agents.BoardAgent._bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197)、[boardroom_sim.baselines._baseline_messages](#fn-boardroom_sim-baselines-py-_baseline_messages-139)

**完整源码**

~~~~python
    def render(self, template_name: str, **values: object) -> str:
        """Render a top-level prompt template such as system_agent.md."""
        text = self._read_template(self.prompts_dir / f"{template_name}.md")
        return Template(text).safe_substitute({key: str(value) for key, value in values.items()}).strip()
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-prompts-py-promptrenderer-response_guidance-29"></a>

### PromptRenderer.response_guidance

文件：`boardroom_sim/prompts.py:29-34`

**签名**

~~~~python
def response_guidance(self) -> str:
~~~~

**功能**

源码文档说明：Return the configured response-generator guidance block.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self._read_template.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `path.exists` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `self._read_template` | [boardroom_sim.prompts.PromptRenderer._read_template](#fn-boardroom_sim-prompts-py-promptrenderer-_read_template-36) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.agents.BoardAgent._bargaining_prompt](#fn-boardroom_sim-agents-py-boardagent-_bargaining_prompt-197)

**完整源码**

~~~~python
    def response_guidance(self) -> str:
        """Return the configured response-generator guidance block."""
        path = self.prompts_dir / "response_generators" / f"{self.response_generator}.md"
        if not path.exists():
            path = self.prompts_dir / "response_generators" / "critical.md"
        return self._read_template(path).strip()
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-prompts-py-promptrenderer-_read_template-36"></a>

### PromptRenderer._read_template

文件：`boardroom_sim/prompts.py:36-42`

**签名**

~~~~python
def _read_template(self, path: Path) -> str:
~~~~

**功能**

读取函数。它从文件或结果对象中取出结构化数据，返回后续评估、渲染或模拟流程可直接使用的 Python 对象。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `path` | 文件路径对象。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `path.read_text` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `path.exists` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `FileNotFoundError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.prompts.PromptRenderer.render](#fn-boardroom_sim-prompts-py-promptrenderer-render-24)、[boardroom_sim.prompts.PromptRenderer.response_guidance](#fn-boardroom_sim-prompts-py-promptrenderer-response_guidance-29)

**完整源码**

~~~~python
    def _read_template(self, path: Path) -> str:
        key = str(path)
        if key not in self._cache:
            if not path.exists():
                raise FileNotFoundError(f"Prompt template not found: {path}")
            self._cache[key] = path.read_text(encoding="utf-8")
        return self._cache[key]
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-roles-py"></a>

## boardroom_sim/roles.py

角色策略模块：从 TOML 或内置默认值构建各董事会角色的 RolePolicy。

**类**：无类定义

<a id="fn-boardroom_sim-roles-py-build_role_policies-17"></a>

### build_role_policies

文件：`boardroom_sim/roles.py:17-27`

**签名**

~~~~python
def build_role_policies( policy_dir: Path | str | None = None, role_order: Sequence[str] | None = None, ) -> Dict[str, RolePolicy]:
~~~~

**功能**

源码文档说明：Create role policies from TOML files, with a built-in fallback.

**参数**

| 参数 | 含义 |
| --- | --- |
| `policy_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `role_order` | 角色发言或构造顺序。 |

**返回值**

返回 `Dict[str, RolePolicy]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `resolved_policy_dir.exists` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `_ordered_policy_dict` | [boardroom_sim.roles._ordered_policy_dict](#fn-boardroom_sim-roles-py-_ordered_policy_dict-60) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `Path` | 库函数或对象方法 | pathlib 路径构造器；返回 Path 对象。 |
| `_load_role_policies_from_dir` | [boardroom_sim.roles._load_role_policies_from_dir](#fn-boardroom_sim-roles-py-_load_role_policies_from_dir-30) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_build_builtin_role_policies` | [boardroom_sim.roles._build_builtin_role_policies](#fn-boardroom_sim-roles-py-_build_builtin_role_policies-72) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.baselines._baseline_messages](#fn-boardroom_sim-baselines-py-_baseline_messages-139)、[boardroom_sim.simulator.BoardroomSimulator.__init__](#fn-boardroom_sim-simulator-py-boardroomsimulator-__init__-32)

**完整源码**

~~~~python
def build_role_policies(
    policy_dir: Path | str | None = None,
    role_order: Sequence[str] | None = None,
) -> Dict[str, RolePolicy]:
    """Create role policies from TOML files, with a built-in fallback."""
    resolved_policy_dir = Path(policy_dir) if policy_dir is not None else DEFAULT_ROLE_POLICY_DIR
    if resolved_policy_dir.exists():
        loaded = _load_role_policies_from_dir(resolved_policy_dir)
        if loaded:
            return _ordered_policy_dict(loaded, role_order)
    return _ordered_policy_dict(_build_builtin_role_policies(), role_order)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-roles-py-_load_role_policies_from_dir-30"></a>

### _load_role_policies_from_dir

文件：`boardroom_sim/roles.py:30-37`

**签名**

~~~~python
def _load_role_policies_from_dir(policy_dir: Path) -> Dict[str, RolePolicy]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `policy_dir` | 目录路径参数，用于读取配置或写出结果。 |

**返回值**

返回 `Dict[str, RolePolicy]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `sorted` | 库函数或对象方法 | 内置排序；返回新的 list。 |
| `policy_dir.glob` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `_policy_from_toml` | [boardroom_sim.roles._policy_from_toml](#fn-boardroom_sim-roles-py-_policy_from_toml-40) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `tomllib.load` | 库函数或对象方法 | 读取 TOML 文件对象并解析，返回 dict。 |

**被谁调用**

[boardroom_sim.roles.build_role_policies](#fn-boardroom_sim-roles-py-build_role_policies-17)

**完整源码**

~~~~python
def _load_role_policies_from_dir(policy_dir: Path) -> Dict[str, RolePolicy]:
    policies: Dict[str, RolePolicy] = {}
    for path in sorted(policy_dir.glob("*.toml")):
        with path.open("rb") as handle:
            raw = tomllib.load(handle)
        policy = _policy_from_toml(raw, source_path=path)
        policies[policy.role_name] = policy
    return policies
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-roles-py-_policy_from_toml-40"></a>

### _policy_from_toml

文件：`boardroom_sim/roles.py:40-51`

**签名**

~~~~python
def _policy_from_toml(raw: Dict[str, Any], *, source_path: Path) -> RolePolicy:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `raw` | 从文件、TOML、JSON 或 LLM 读取的原始数据。 |
| `source_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |

**返回值**

返回 `RolePolicy` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `RolePolicy` | [boardroom_sim.models.RolePolicy](#class-boardroom_sim-models-py-rolepolicy-281) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `_string_list` | [boardroom_sim.roles._string_list](#fn-boardroom_sim-roles-py-_string_list-54) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `raw.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.roles._load_role_policies_from_dir](#fn-boardroom_sim-roles-py-_load_role_policies_from_dir-30)

**完整源码**

~~~~python
def _policy_from_toml(raw: Dict[str, Any], *, source_path: Path) -> RolePolicy:
    role_name = str(raw.get("role_name", "")).strip()
    if not role_name:
        raise ValueError(f"Missing role_name in {source_path}")
    return RolePolicy(
        role_name=role_name,
        layer_1_goals=_string_list(raw.get("layer_1_goals")),
        layer_2_attention_fields=_string_list(raw.get("layer_2_attention_fields")),
        layer_2_ignored_fields=_string_list(raw.get("layer_2_ignored_fields")),
        layer_3_heuristics=_string_list(raw.get("layer_3_heuristics")),
        layer_4_interaction_protocol=dict(raw.get("layer_4_interaction_protocol", {})),
    )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-roles-py-_string_list-54"></a>

### _string_list

文件：`boardroom_sim/roles.py:54-57`

**签名**

~~~~python
def _string_list(value: Any) -> List[str]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `List[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[boardroom_sim.roles._policy_from_toml](#fn-boardroom_sim-roles-py-_policy_from_toml-40)

**完整源码**

~~~~python
def _string_list(value: Any) -> List[str]:
    if not isinstance(value, list):
        return []
    return [str(item) for item in value if str(item)]
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-roles-py-_ordered_policy_dict-60"></a>

### _ordered_policy_dict

文件：`boardroom_sim/roles.py:60-69`

**签名**

~~~~python
def _ordered_policy_dict( policies: Dict[str, RolePolicy], role_order: Sequence[str] | None, ) -> Dict[str, RolePolicy]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `policies` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `role_order` | 角色发言或构造顺序。 |

**返回值**

返回 `Dict[str, RolePolicy]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |
| `policies.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |

**被谁调用**

[boardroom_sim.roles.build_role_policies](#fn-boardroom_sim-roles-py-build_role_policies-17)

**完整源码**

~~~~python
def _ordered_policy_dict(
    policies: Dict[str, RolePolicy],
    role_order: Sequence[str] | None,
) -> Dict[str, RolePolicy]:
    order = list(role_order or DEFAULT_ROLE_ORDER)
    ordered = {role_name: policies[role_name] for role_name in order if role_name in policies}
    for role_name, policy in policies.items():
        if role_name not in ordered:
            ordered[role_name] = policy
    return ordered
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-roles-py-_build_builtin_role_policies-72"></a>

### _build_builtin_role_policies

文件：`boardroom_sim/roles.py:72-258`

**签名**

~~~~python
def _build_builtin_role_policies() -> Dict[str, RolePolicy]:
~~~~

**功能**

源码文档说明：Create the four strict role policies with the required four layers.

**参数**

无显式参数。

**返回值**

返回 `Dict[str, RolePolicy]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `RolePolicy` | [boardroom_sim.models.RolePolicy](#class-boardroom_sim-models-py-rolepolicy-281) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.roles.build_role_policies](#fn-boardroom_sim-roles-py-build_role_policies-17)

**完整源码**

~~~~python
def _build_builtin_role_policies() -> Dict[str, RolePolicy]:
    """Create the four strict role policies with the required four layers."""
    policies = [
        RolePolicy(
            role_name="Founder_CEO",
            layer_1_goals=[
                "Rank 1: maximize long-term company value and exit value, not just the next markup.",
                "Rank 2: preserve founder control through voting influence and board majority where possible.",
                "Rank 3: preserve execution freedom and minimize veto triggers over major operating decisions.",
            ],
            layer_2_attention_fields=[
                "prior_deal_size_usd_m",
                "prior_company_deal_history",
                "prior_post_money_valuation_usd_m",
                "prior_valuation_markup_multiple",
                "prior_valuation_direction_label",
                "prior_deal_type",
                "prior_vc_round",
                "prior_raised_to_date_usd_m",
                "prior_investor_ownership_pct",
                "prior_lead_investor_count",
                "prior_lead_investor_types",
                "employee_count_at_decision",
                "employee_growth_rate",
                "founder_count",
                "current_ceo_is_founder",
                "board_member_count",
                "investor_board_member_count",
            ],
            layer_2_ignored_fields=[
                "current target deal size, type, round, valuation, and completion status",
                "investor internal AUM and total active portfolio metrics",
                "horizontal comp-set valuations not central to founder control and financing terms",
            ],
            layer_3_heuristics=[
                "Control red lines dominate valuation optimization when terms threaten voting influence, board majority, or major veto rights.",
                "Prior valuation markup is a status signal; weak or down historical markup makes future fundraising emotionally and politically costly.",
                "Dilution is acceptable only when the capital meaningfully increases growth resources, strategic value, or survival odds.",
                "Cash pressure allows flexible compromise, but survival concessions should still protect core founder control.",
                "Public commitments to employees, early investors, customers, product direction, and milestones are sticky and hard to reverse.",
            ],
            layer_4_interaction_protocol={
                "speaking_order": 1,
                "board_vote": "founder equity influence; early 30-50%, often falling to 10-25% after several rounds",
                "information_behavior": "introduces company needs and selectively discloses operating upside and internal strain",
                "stickiness": "high; once the founder states a position, later concessions should be explicit and costly",
            },
        ),
        RolePolicy(
            role_name="CTO",
            layer_1_goals=[
                "Rank 1: protect engineering team health, hiring budget, and technical debt budget.",
                "Rank 2: protect technical feasibility and product milestone deliverability.",
                "Rank 3: protect personal technical reputation and scope of technical authority.",
            ],
            layer_2_attention_fields=[
                "prior_deal_size_usd_m",
                "prior_company_deal_history",
                "prior_raised_to_date_usd_m",
                "employee_count_at_decision",
                "previous_employee_count",
                "employee_growth_rate",
                "engineering_role_count",
                "executive_role_count",
                "primary_industry",
                "industry_group",
                "industry_sector",
                "verticals",
                "keywords",
                "description",
                "company_age_at_decision_years",
            ],
            layer_2_ignored_fields=[
                "current target term-sheet size, valuation, liquidation preference, and anti-dilution terms",
                "investor portfolio performance",
                "company geography or city-level distribution unless it affects engineering hiring",
            ],
            layer_3_heuristics=[
                "Engineering resource share should not clearly fall after financing; shrinking technical capacity is a reason to oppose or resize financing.",
                "Technical roadmap autonomy should not be overridden by investors or non-technical directors without clear evidence.",
                "Hiring and technical debt budget should be protected before sales, marketing, or administrative expansion.",
                "Short-term financial targets should not force irrational architecture shortcuts.",
                "Unverified technical breakthroughs should be validated before using them to justify financing scale.",
            ],
            layer_4_interaction_protocol={
                "speaking_order": 2,
                "board_vote": "usually no direct board vote unless also a founder; participates through technical advice",
                "information_behavior": "must disclose technical debt, recruiting difficulty, and architecture risk under fiduciary constraints",
                "stickiness": "high for headcount growth, infrastructure spending, and technical commitments",
            },
        ),
        RolePolicy(
            role_name="Lead_VC_Director",
            layer_1_goals=[
                "Rank 1: maximize fund exit multiple, with lead-led pricing discipline.",
                "Rank 2: secure favorable round terms, including downside protection, anti-dilution, and board seats.",
                "Rank 3: protect portfolio markup signal value for future fundraising.",
            ],
            layer_2_attention_fields=[
                "prior_post_money_valuation_usd_m",
                "prior_lead_investor_deal_history",
                "prior_pre_money_valuation_usd_m",
                "prior_valuation_markup_multiple",
                "prior_valuation_direction_label",
                "prior_deal_size_usd_m",
                "prior_raised_to_date_usd_m",
                "prior_investor_ownership_pct",
                "prior_investor_count",
                "prior_new_investor_count",
                "prior_followon_investor_count",
                "prior_lead_investor_count",
                "prior_lead_investor_amount_share",
                "prior_lead_preferred_deal_size_min_usd_m",
                "prior_lead_preferred_deal_size_max_usd_m",
                "prior_lead_preferred_company_valuation_min_usd_m",
                "prior_lead_preferred_company_valuation_max_usd_m",
                "employee_growth_rate",
                "similar_company_count",
                "board_member_count",
                "investor_board_member_count",
            ],
            layer_2_ignored_fields=[
                "specific product architecture details that belong to the CTO",
                "employee workforce analytics below board-level signal quality",
                "current target transaction labels hidden for backtesting",
                "PitchBook snapshot company status fields that may leak post-decision outcomes",
            ],
            layer_3_heuristics=[
                "Downside protection should take priority over upside narratives when risk is high.",
                "Valuation reasonableness matters more than valuation number; compare prior markup, sector context, and operating signals.",
                "Fund concentration discipline cannot be broken for a single attractive opportunity.",
                "The next-round financing path must be clear; if the path is ambiguous, require stronger protections or lower valuation.",
                "Governance influence should be preserved through board seats, information rights, or protective provisions when financing risk rises.",
            ],
            layer_4_interaction_protocol={
                "speaking_order": 3,
                "board_vote": "usually 1-2 seats plus possible lead observer; equity often 10-25%",
                "information_behavior": "can cite company disclosure and external market intelligence",
                "stickiness": "medium to high due to LP commitments, sector focus, and stage mandate",
            },
        ),
        RolePolicy(
            role_name="Followon_VC_Director",
            layer_1_goals=[
                "Rank 1: protect pro-rata rights and avoid dilution by the lead.",
                "Rank 2: protect investment IRR with smaller and more diversified exposure than the lead.",
                "Rank 3: preserve portfolio diversification and co-investing signal value with a reputable lead.",
            ],
            layer_2_attention_fields=[
                "prior_deal_size_usd_m",
                "prior_followon_investor_deal_history",
                "prior_post_money_valuation_usd_m",
                "prior_valuation_markup_multiple",
                "prior_valuation_direction_label",
                "prior_investor_ownership_pct",
                "prior_investor_count",
                "prior_followon_investor_count",
                "prior_lead_investor_count",
                "prior_lead_investor_types",
                "prior_lead_investor_amount_share",
                "prior_lead_preferred_deal_size_min_usd_m",
                "prior_lead_preferred_deal_size_max_usd_m",
                "employee_growth_rate",
                "similar_company_count",
            ],
            layer_2_ignored_fields=[
                "product and architecture details",
                "full term-sheet drafting details that the lead should originate",
                "current target transaction labels hidden for backtesting",
                "PitchBook snapshot company status fields that may leak post-decision outcomes",
            ],
            layer_3_heuristics=[
                "Following is the default after a prior investment; not following requires explicit reasons such as valuation, deterioration, or fund timing.",
                "Pro-rata dilution protection comes before perfect terms when not following would visibly dilute ownership.",
                "The follow-on director can resist lead-driven terms that harm follow-on ownership or liquidation priority, but should not dominate pricing.",
                "Lead reputation is a key signal; weak lead history or weak lead commitment lowers willingness to follow.",
                "Ticket size and sector focus boundaries should not be broken for a single opportunity.",
            ],
            layer_4_interaction_protocol={
                "speaking_order": 4,
                "board_vote": "usually 0-1 seat; equity often 3-10%",
                "information_behavior": "can access company disclosure and lead-shared information, but has weaker independent market intelligence",
                "stickiness": "continuing to follow is the default after prior participation; refusing requires explicit rationale",
            },
        ),
    ]
    return {policy.role_name: policy for policy in policies}
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-roles-py-ordered_role_names-261"></a>

### ordered_role_names

文件：`boardroom_sim/roles.py:261-263`

**签名**

~~~~python
def ordered_role_names() -> List[str]:
~~~~

**功能**

源码文档说明：Return the fixed speaking order specified by the role-design document.

**参数**

无显式参数。

**返回值**

返回 `List[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def ordered_role_names() -> List[str]:
    """Return the fixed speaking order specified by the role-design document."""
    return list(DEFAULT_ROLE_ORDER)
~~~~

[回到顶部](#top)

<a id="module-boardroom_sim-simulator-py"></a>

## boardroom_sim/simulator.py

模拟器模块：组织角色初始预测、会议讨论、最终聚合和 trace 记录。

**类**：[BoardroomSimulator](#class-boardroom_sim-simulator-py-boardroomsimulator-29)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-__init__-32"></a>

### BoardroomSimulator.__init__

文件：`boardroom_sim/simulator.py:32-62`

**签名**

~~~~python
def __init__( self, llm_client: LLMClient, bargaining_rounds: int | None = None, config: ExperimentConfig | None = None, ) -> None:
~~~~

**功能**

源码文档说明：Create a simulator with fixed role policies, LLM agents, and a round count.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `llm_client` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `bargaining_rounds` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `config` | 配置对象，控制 LLM、角色、prompt、过程或输出。 |

**返回值**

初始化方法，通常返回 `None`；主要副作用是给 `self` 写入属性。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |
| `BoardProcessController` | [boardroom_sim.process.BoardProcessController](#class-boardroom_sim-process-py-boardprocesscontroller-99) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `self.process.effective_role_weights` | [boardroom_sim.process.BoardProcessController.effective_role_weights](#fn-boardroom_sim-process-py-boardprocesscontroller-effective_role_weights-110) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `build_role_policies` | [boardroom_sim.roles.build_role_policies](#fn-boardroom_sim-roles-py-build_role_policies-17) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `PromptRenderer` | [boardroom_sim.prompts.PromptRenderer](#class-boardroom_sim-prompts-py-promptrenderer-15) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `build_agents` | [boardroom_sim.agents.build_agents](#fn-boardroom_sim-agents-py-build_agents-323) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `load_experiment_config` | [boardroom_sim.config.load_experiment_config](#fn-boardroom_sim-config-py-load_experiment_config-185) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
    def __init__(
        self,
        llm_client: LLMClient,
        bargaining_rounds: int | None = None,
        config: ExperimentConfig | None = None,
    ) -> None:
        """Create a simulator with fixed role policies, LLM agents, and a round count."""
        self.config = config or load_experiment_config()
        if self.config.discussion_paradigm != "memory":
            raise ValueError(
                f"Unsupported discussion paradigm for this implementation: {self.config.discussion_paradigm}. "
                "Currently supported: memory."
            )
        if self.config.decision_protocol != "weighted_vote":
            raise ValueError(
                f"Unsupported decision protocol for this implementation: {self.config.decision_protocol}. "
                "Currently supported: weighted_vote."
        )
        self.bargaining_rounds = bargaining_rounds if bargaining_rounds is not None else self.config.bargaining_rounds
        self.role_order = list(self.config.role_order)
        self.process = BoardProcessController(self.config.board_process, self.role_order)
        self.base_role_weights = dict(self.config.role_weights)
        self.role_weights = self.process.effective_role_weights(self.base_role_weights)
        self.policies = build_role_policies(self.config.role_policy_dir, self.role_order)
        self.llm_client = llm_client
        prompt_renderer = PromptRenderer(
            prompts_dir=self.config.prompts_dir,
            response_generator=self.config.response_generator,
        )
        prompt_renderer.round_tasks = list(self.config.round_tasks)
        self.agents = build_agents(self.policies, llm_client, prompt_renderer=prompt_renderer)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64"></a>

### BoardroomSimulator.simulate

文件：`boardroom_sim/simulator.py:64-151`

**签名**

~~~~python
def simulate(self, case: BoardCase) -> SimulationResult:
~~~~

**功能**

单个案例的主流程。它按角色生成初始判断，记录开场观点，执行若干轮董事会讨论，聚合最终 term sheet，并输出 SimulationResult。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |

**返回值**

返回 `SimulationResult` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self._record` | [boardroom_sim.simulator.BoardroomSimulator._record](#fn-boardroom_sim-simulator-py-boardroomsimulator-_record-372) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._build_proposal` | [boardroom_sim.simulator.BoardroomSimulator._build_proposal](#fn-boardroom_sim-simulator-py-boardroomsimulator-_build_proposal-172) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `range` | 库函数或对象方法 | 内置整数序列；返回 range 对象。 |
| `self._aggregate_financing_intent` | [boardroom_sim.simulator.BoardroomSimulator._aggregate_financing_intent](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_financing_intent-297) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._aggregate_completion_view` | [boardroom_sim.simulator.BoardroomSimulator._aggregate_completion_view](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_completion_view-301) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._aggregate_deal_size` | [boardroom_sim.simulator.BoardroomSimulator._aggregate_deal_size](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_deal_size-305) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._aggregate_positive_numeric` | [boardroom_sim.simulator.BoardroomSimulator._aggregate_positive_numeric](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_positive_numeric-309) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._aggregate_text_choice` | [boardroom_sim.simulator.BoardroomSimulator._aggregate_text_choice](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_text_choice-324) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._aggregate_valuation` | [boardroom_sim.simulator.BoardroomSimulator._aggregate_valuation](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_valuation-328) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._consensus_score` | [boardroom_sim.simulator.BoardroomSimulator._consensus_score](#fn-boardroom_sim-simulator-py-boardroomsimulator-_consensus_score-344) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._deal_break_risk` | [boardroom_sim.simulator.BoardroomSimulator._deal_break_risk](#fn-boardroom_sim-simulator-py-boardroomsimulator-_deal_break_risk-359) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `SimulationResult` | [boardroom_sim.models.SimulationResult](#class-boardroom_sim-models-py-simulationresult-336) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._background_summary` | [boardroom_sim.simulator.BoardroomSimulator._background_summary](#fn-boardroom_sim-simulator-py-boardroomsimulator-_background_summary-153) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._policy_snapshot` | [boardroom_sim.simulator.BoardroomSimulator._policy_snapshot](#fn-boardroom_sim-simulator-py-boardroomsimulator-_policy_snapshot-168) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self.agents.evaluate` | [boardroom_sim.agents.BoardAgent.evaluate](#fn-boardroom_sim-agents-py-boardagent-evaluate-94) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self.agents.opening_statement` | [boardroom_sim.agents.BoardAgent.opening_statement](#fn-boardroom_sim-agents-py-boardagent-opening_statement-111) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `proposal.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `self._run_bargaining_round` | [boardroom_sim.simulator.BoardroomSimulator._run_bargaining_round](#fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self.process.to_trace_payload` | [boardroom_sim.process.BoardProcessController.to_trace_payload](#fn-boardroom_sim-process-py-boardprocesscontroller-to_trace_payload-106) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self.agents.rationale_line` | [boardroom_sim.agents.BoardAgent.rationale_line](#fn-boardroom_sim-agents-py-boardagent-rationale_line-154) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `decision.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `final_proposal.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `case.notes.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `context.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |

**被谁调用**

[run_batch_experiments._run_cases](#fn-run_batch_experiments-py-_run_cases-208)、[run_batch_experiments._run_cases_with_checkpoints](#fn-run_batch_experiments-py-_run_cases_with_checkpoints-227)、[run_experiment.run_experiment](#fn-run_experiment-py-run_experiment-60)

**完整源码**

~~~~python
    def simulate(self, case: BoardCase) -> SimulationResult:
        """Run one full point-in-time boardroom simulation case."""
        trace: List[Dict[str, Any]] = []
        context: Dict[str, RoleDecision] = {}
        bargaining_history: List[Dict[str, Any]] = []

        self._record(trace, "preliminary", "system", self._background_summary(case), {})
        self._record(trace, "role_policy", "system", "Loaded strict four-layer role policies.", self._policy_snapshot())
        self._record(
            trace,
            "board_process",
            "system",
            "Loaded configurable board process settings.",
            {
                "process": self.process.to_trace_payload(),
                "base_role_weights": self.base_role_weights,
                "effective_role_weights": self.role_weights,
            },
        )

        for role_name in self.role_order:
            decision = self.agents[role_name].evaluate(case, context)
            context[role_name] = decision
            self._record(
                trace,
                "private_assessment",
                role_name,
                self.agents[role_name].rationale_line(decision.rationale),
                decision.to_dict(),
            )

        for role_name in self.role_order:
            statement = self.agents[role_name].opening_statement(context[role_name])
            self._record(trace, "opening_statement", role_name, statement, {})

        proposal = self._build_proposal(case, context)
        self._record(trace, "initial_proposal", "system", "Initial aggregated financing proposal generated.", proposal.to_dict())

        for round_index in range(self.bargaining_rounds):
            proposal = self._run_bargaining_round(case, context, proposal, trace, bargaining_history, round_index)

        financing_intent = self._aggregate_financing_intent(context)
        completion_view = self._aggregate_completion_view(context)
        predicted_deal_size = self._aggregate_deal_size(context)
        predicted_post_money_valuation = self._aggregate_positive_numeric(
            context,
            "predicted_post_money_valuation_usd_m",
        )
        predicted_investor_ownership = self._aggregate_positive_numeric(
            context,
            "predicted_investor_ownership_pct",
        )
        predicted_deal_type = self._aggregate_text_choice(context, "predicted_deal_type", "unknown")
        valuation_direction = self._aggregate_valuation(context)
        consensus = self._consensus_score(context)
        deal_break_risk = self._deal_break_risk(financing_intent, completion_view, consensus)
        final_proposal = self._build_proposal(case, context)

        final_payload = {
            "financing_initiation_decision": financing_intent,
            "financing_completion_view": completion_view,
            "predicted_deal_size_usd_m": predicted_deal_size,
            "predicted_post_money_valuation_usd_m": predicted_post_money_valuation,
            "predicted_investor_ownership_pct": predicted_investor_ownership,
            "predicted_deal_type": predicted_deal_type,
            "valuation_direction": valuation_direction,
            "consensus_score": consensus,
            "deal_break_risk": deal_break_risk,
        }
        self._record(trace, "closure", "system", "Final boardroom predictions aggregated.", final_payload)

        return SimulationResult(
            case_id=case.case_id,
            company_name=case.company_label,
            financing_initiation_decision=financing_intent,
            financing_completion_view=completion_view,
            predicted_deal_size_usd_m=predicted_deal_size,
            predicted_post_money_valuation_usd_m=predicted_post_money_valuation,
            predicted_investor_ownership_pct=predicted_investor_ownership,
            predicted_deal_type=predicted_deal_type,
            valuation_direction=valuation_direction,
            role_decisions={role: decision.to_dict() for role, decision in context.items()},
            proposal=final_proposal.to_dict(),
            consensus_score=consensus,
            deal_break_risk=deal_break_risk,
            labels=case.notes.get("labels", {}) if isinstance(case.notes.get("labels", {}), dict) else {},
            trace=trace,
        )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_background_summary-153"></a>

### BoardroomSimulator._background_summary

文件：`boardroom_sim/simulator.py:153-166`

**签名**

~~~~python
def _background_summary(self, case: BoardCase) -> str:
~~~~

**功能**

源码文档说明：Create a short shared case background for the trace.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `case.prior_valuation_direction` | [boardroom_sim.models.BoardCase.prior_valuation_direction](#fn-boardroom_sim-models-py-boardcase-prior_valuation_direction-260) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._format_money` | [boardroom_sim.simulator.BoardroomSimulator._format_money](#fn-boardroom_sim-simulator-py-boardroomsimulator-_format_money-202) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)

**完整源码**

~~~~python
    def _background_summary(self, case: BoardCase) -> str:
        """Create a short shared case background for the trace."""
        prior = (
            f"prior VC round {case.prior_vc_round} on {case.prior_deal_date} "
            f"with size {self._format_money(case.prior_deal_size_usd_m)} and "
            f"post-money {self._format_money(case.prior_post_money_valuation_usd_m)}"
            if case.prior_vc_deal_count > 0
            else "no prior observable VC round"
        )
        return (
            f"{case.company_label} ({case.primary_industry}) is evaluated at decision date {case.decision_date}. "
            f"The company has {case.prior_vc_deal_count} prior VC rounds; {prior}. "
            f"Historical valuation direction is {case.prior_valuation_direction()}."
        )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_policy_snapshot-168"></a>

### BoardroomSimulator._policy_snapshot

文件：`boardroom_sim/simulator.py:168-170`

**签名**

~~~~python
def _policy_snapshot(self) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Serialize all role policies for auditability.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `policy.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `self.policies.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)

**完整源码**

~~~~python
    def _policy_snapshot(self) -> Dict[str, Any]:
        """Serialize all role policies for auditability."""
        return {name: policy.to_dict() for name, policy in self.policies.items()}
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_build_proposal-172"></a>

### BoardroomSimulator._build_proposal

文件：`boardroom_sim/simulator.py:172-200`

**签名**

~~~~python
def _build_proposal(self, case: BoardCase, context: Dict[str, RoleDecision]) -> TermSheetProposal:
~~~~

**功能**

把各角色的最终 RoleDecision 聚合成一个 TermSheetProposal，也就是实验最终预测的交易结果。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |

**返回值**

返回 `TermSheetProposal` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self._aggregate_deal_size` | [boardroom_sim.simulator.BoardroomSimulator._aggregate_deal_size](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_deal_size-305) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._aggregate_positive_numeric` | [boardroom_sim.simulator.BoardroomSimulator._aggregate_positive_numeric](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_positive_numeric-309) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._aggregate_text_choice` | [boardroom_sim.simulator.BoardroomSimulator._aggregate_text_choice](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_text_choice-324) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._aggregate_valuation` | [boardroom_sim.simulator.BoardroomSimulator._aggregate_valuation](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_valuation-328) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `context.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `bool` | 库函数或对象方法 | 内置类型转换；返回布尔值。 |
| `case.estimated_dilution_pct` | [boardroom_sim.models.BoardCase.estimated_dilution_pct](#fn-boardroom_sim-models-py-boardcase-estimated_dilution_pct-273) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `TermSheetProposal` | [boardroom_sim.models.TermSheetProposal](#class-boardroom_sim-models-py-termsheetproposal-318) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `round` | 库函数或对象方法 | 内置四舍五入；返回数值。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator._run_bargaining_round](#fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208)

**完整源码**

~~~~python
    def _build_proposal(self, case: BoardCase, context: Dict[str, RoleDecision]) -> TermSheetProposal:
        """Build a minimal proposal from current role predictions."""
        size = self._aggregate_deal_size(context)
        post_money_valuation = self._aggregate_positive_numeric(context, "predicted_post_money_valuation_usd_m")
        investor_ownership = self._aggregate_positive_numeric(context, "predicted_investor_ownership_pct")
        deal_type = self._aggregate_text_choice(context, "predicted_deal_type", "unknown")
        valuation_direction = self._aggregate_valuation(context)
        lead = context.get("Lead_VC_Director")

        protection_level = "standard"
        if lead and lead.valuation_direction in {"down", "unknown"}:
            protection_level = "strong"
        elif lead and lead.financing_intent == "raise_now" and lead.valuation_direction == "up":
            protection_level = "light"

        cto = context.get("CTO")
        tech_budget_protected = bool(cto and cto.financing_intent != "avoid" and cto.satisfaction_score >= 45.0)

        estimated_dilution = case.estimated_dilution_pct(size)
        return TermSheetProposal(
            recommended_deal_size_usd_m=size,
            recommended_post_money_valuation_usd_m=post_money_valuation,
            recommended_investor_ownership_pct=investor_ownership,
            recommended_deal_type=deal_type,
            valuation_direction=valuation_direction,
            estimated_dilution_pct=round(estimated_dilution, 3) if estimated_dilution is not None else None,
            investor_protection_level=protection_level,  # type: ignore[arg-type]
            tech_budget_protected=tech_budget_protected,
        )
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_format_money-202"></a>

### BoardroomSimulator._format_money

文件：`boardroom_sim/simulator.py:202-206`

**签名**

~~~~python
def _format_money(self, value: Any) -> str:
~~~~

**功能**

源码文档说明：Format a nullable million-USD value for trace text.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator._background_summary](#fn-boardroom_sim-simulator-py-boardroomsimulator-_background_summary-153)

**完整源码**

~~~~python
    def _format_money(self, value: Any) -> str:
        """Format a nullable million-USD value for trace text."""
        if value is None:
            return "unknown"
        return f"{value}M USD"
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208"></a>

### BoardroomSimulator._run_bargaining_round

文件：`boardroom_sim/simulator.py:208-295`

**签名**

~~~~python
def _run_bargaining_round( self, case: BoardCase, context: Dict[str, RoleDecision], proposal: TermSheetProposal, trace: List[Dict[str, Any]], bargaining_history: List[Dict[str, Any]], round_index: int, ) -> TermSheetProposal:
~~~~

**功能**

执行一轮讨论：按流程控制器给出的发言顺序调用角色 agent，记录发言、预测变化、会议阶段和 L5 文化约束。

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |
| `proposal` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `trace` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `bargaining_history` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `round_index` | 从 0 开始的讨论轮次索引。 |

**返回值**

返回 `TermSheetProposal` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self.process.speaking_order_for_round` | [boardroom_sim.process.BoardProcessController.speaking_order_for_round](#fn-boardroom_sim-process-py-boardprocesscontroller-speaking_order_for_round-123) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._record` | [boardroom_sim.simulator.BoardroomSimulator._record](#fn-boardroom_sim-simulator-py-boardroomsimulator-_record-372) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self._build_proposal` | [boardroom_sim.simulator.BoardroomSimulator._build_proposal](#fn-boardroom_sim-simulator-py-boardroomsimulator-_build_proposal-172) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self.process.to_trace_payload` | [boardroom_sim.process.BoardProcessController.to_trace_payload](#fn-boardroom_sim-process-py-boardprocesscontroller-to_trace_payload-106) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self.process.prompt_context` | [boardroom_sim.process.BoardProcessController.prompt_context](#fn-boardroom_sim-process-py-boardprocesscontroller-prompt_context-156) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self.process.visible_history` | [boardroom_sim.process.BoardProcessController.visible_history](#fn-boardroom_sim-process-py-boardprocesscontroller-visible_history-144) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `self.agents.bargaining_step` | [boardroom_sim.agents.BoardAgent.bargaining_step](#fn-boardroom_sim-agents-py-boardagent-bargaining_step-123) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `changed_fields` | [boardroom_sim.process.changed_fields](#fn-boardroom_sim-process-py-changed_fields-220) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `bargaining_history.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `proposal.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `updated_decision.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)

**完整源码**

~~~~python
    def _run_bargaining_round(
        self,
        case: BoardCase,
        context: Dict[str, RoleDecision],
        proposal: TermSheetProposal,
        trace: List[Dict[str, Any]],
        bargaining_history: List[Dict[str, Any]],
        round_index: int,
    ) -> TermSheetProposal:
        """Run one bargaining round and let roles update their predictions."""
        speakers = self.process.speaking_order_for_round(round_index, self.role_weights)
        round_payload = {
            "round": round_index + 1,
            "speaking_order": speakers,
            "base_role_weights": self.base_role_weights,
            "effective_role_weights": self.role_weights,
            "process": self.process.to_trace_payload(),
        }
        self._record(
            trace,
            f"process_round_{round_index + 1}",
            "system",
            "Board process controller selected the round speakers and process constraints.",
            round_payload,
        )
        if not speakers:
            self._record(
                trace,
                f"bargaining_round_{round_index + 1}",
                "system",
                "No role speaks in this round under the configured board process.",
                {"case_id": case.case_id, **round_payload},
            )

        for role_name in speakers:
            process_context = self.process.prompt_context(
                role_name=role_name,
                round_index=round_index,
                speakers=speakers,
                effective_weights=self.role_weights,
            )
            visible_history = self.process.visible_history(bargaining_history, round_index + 1)
            previous_decision = context[role_name]
            message, updated_decision, meeting_artifacts = self.agents[role_name].bargaining_step(
                case=case,
                decision=previous_decision,
                proposal=proposal,
                round_index=round_index,
                context=context,
                prior_messages=visible_history,
                process_context=process_context,
            )
            context[role_name] = updated_decision
            changed = changed_fields(previous_decision, updated_decision)
            event = {
                "round": round_index + 1,
                "role": role_name,
                "message": message,
                "changed_fields": changed,
                "process_context": process_context,
                "meeting_artifacts": meeting_artifacts,
                "updated_decision": updated_decision.to_dict(),
            }
            bargaining_history.append(event)
            self._record(
                trace,
                f"bargaining_round_{round_index + 1}",
                role_name,
                message,
                {
                    "case_id": case.case_id,
                    "proposal_before_message": proposal.to_dict(),
                    "process_context": process_context,
                    "meeting_artifacts": meeting_artifacts,
                    "visible_history_count": len(visible_history),
                    "changed_fields": changed,
                    "updated_decision": updated_decision.to_dict(),
                },
            )
        proposal = self._build_proposal(case, context)
        self._record(
            trace,
            f"proposal_after_round_{round_index + 1}",
            "system",
            "Aggregated proposal updated after bargaining round.",
            proposal.to_dict(),
        )
        return proposal
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_financing_intent-297"></a>

### BoardroomSimulator._aggregate_financing_intent

文件：`boardroom_sim/simulator.py:297-299`

**签名**

~~~~python
def _aggregate_financing_intent(self, context: Dict[str, RoleDecision]) -> FinancingIntent:
~~~~

**功能**

源码文档说明：Aggregate role predictions into the final financing initiation decision.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |

**返回值**

返回 `FinancingIntent` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self._weighted_choice` | [boardroom_sim.simulator.BoardroomSimulator._weighted_choice](#fn-boardroom_sim-simulator-py-boardroomsimulator-_weighted_choice-332) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)

**完整源码**

~~~~python
    def _aggregate_financing_intent(self, context: Dict[str, RoleDecision]) -> FinancingIntent:
        """Aggregate role predictions into the final financing initiation decision."""
        return self._weighted_choice(context, "financing_intent", "raise_now")  # type: ignore[return-value]
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_completion_view-301"></a>

### BoardroomSimulator._aggregate_completion_view

文件：`boardroom_sim/simulator.py:301-303`

**签名**

~~~~python
def _aggregate_completion_view(self, context: Dict[str, RoleDecision]) -> DealCompletionView:
~~~~

**功能**

源码文档说明：Aggregate role views on whether the proposed financing is likely to complete.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |

**返回值**

返回 `DealCompletionView` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self._weighted_choice` | [boardroom_sim.simulator.BoardroomSimulator._weighted_choice](#fn-boardroom_sim-simulator-py-boardroomsimulator-_weighted_choice-332) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)

**完整源码**

~~~~python
    def _aggregate_completion_view(self, context: Dict[str, RoleDecision]) -> DealCompletionView:
        """Aggregate role views on whether the proposed financing is likely to complete."""
        return self._weighted_choice(context, "completion_view", "likely_complete")  # type: ignore[return-value]
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_deal_size-305"></a>

### BoardroomSimulator._aggregate_deal_size

文件：`boardroom_sim/simulator.py:305-307`

**签名**

~~~~python
def _aggregate_deal_size(self, context: Dict[str, RoleDecision]) -> float:
~~~~

**功能**

源码文档说明：Aggregate predicted deal size using board influence weights.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |

**返回值**

返回 `float` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self._aggregate_positive_numeric` | [boardroom_sim.simulator.BoardroomSimulator._aggregate_positive_numeric](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_positive_numeric-309) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator._build_proposal](#fn-boardroom_sim-simulator-py-boardroomsimulator-_build_proposal-172)

**完整源码**

~~~~python
    def _aggregate_deal_size(self, context: Dict[str, RoleDecision]) -> float:
        """Aggregate predicted deal size using board influence weights."""
        return self._aggregate_positive_numeric(context, "predicted_deal_size_usd_m")
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_positive_numeric-309"></a>

### BoardroomSimulator._aggregate_positive_numeric

文件：`boardroom_sim/simulator.py:309-322`

**签名**

~~~~python
def _aggregate_positive_numeric(self, context: Dict[str, RoleDecision], attr: str) -> float:
~~~~

**功能**

源码文档说明：Aggregate a positive numeric prediction using board influence weights.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |
| `attr` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `float` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `context.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `round` | 库函数或对象方法 | 内置四舍五入；返回数值。 |
| `getattr` | 库函数或对象方法 | 内置属性读取；返回对象属性值或默认值。 |
| `self.role_weights.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator._build_proposal](#fn-boardroom_sim-simulator-py-boardroomsimulator-_build_proposal-172)、[boardroom_sim.simulator.BoardroomSimulator._aggregate_deal_size](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_deal_size-305)

**完整源码**

~~~~python
    def _aggregate_positive_numeric(self, context: Dict[str, RoleDecision], attr: str) -> float:
        """Aggregate a positive numeric prediction using board influence weights."""
        weighted_total = 0.0
        weight_total = 0.0
        for role, decision in context.items():
            value = getattr(decision, attr, 0.0)
            if value <= 0 or decision.financing_intent == "avoid":
                continue
            weight = self.role_weights.get(role, 1.0)
            weighted_total += value * weight
            weight_total += weight
        if weight_total <= 0:
            return 0.0
        return round(weighted_total / weight_total, 6)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_text_choice-324"></a>

### BoardroomSimulator._aggregate_text_choice

文件：`boardroom_sim/simulator.py:324-326`

**签名**

~~~~python
def _aggregate_text_choice(self, context: Dict[str, RoleDecision], attr: str, default: str) -> str:
~~~~

**功能**

源码文档说明：Aggregate free-text categorical choices using influence weights.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |
| `attr` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self._weighted_choice` | [boardroom_sim.simulator.BoardroomSimulator._weighted_choice](#fn-boardroom_sim-simulator-py-boardroomsimulator-_weighted_choice-332) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator._build_proposal](#fn-boardroom_sim-simulator-py-boardroomsimulator-_build_proposal-172)

**完整源码**

~~~~python
    def _aggregate_text_choice(self, context: Dict[str, RoleDecision], attr: str, default: str) -> str:
        """Aggregate free-text categorical choices using influence weights."""
        return self._weighted_choice(context, attr, default)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_valuation-328"></a>

### BoardroomSimulator._aggregate_valuation

文件：`boardroom_sim/simulator.py:328-330`

**签名**

~~~~python
def _aggregate_valuation(self, context: Dict[str, RoleDecision]) -> ValuationDirection:
~~~~

**功能**

源码文档说明：Aggregate valuation-direction views using board influence weights.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |

**返回值**

返回 `ValuationDirection` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `self._weighted_choice` | [boardroom_sim.simulator.BoardroomSimulator._weighted_choice](#fn-boardroom_sim-simulator-py-boardroomsimulator-_weighted_choice-332) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator._build_proposal](#fn-boardroom_sim-simulator-py-boardroomsimulator-_build_proposal-172)

**完整源码**

~~~~python
    def _aggregate_valuation(self, context: Dict[str, RoleDecision]) -> ValuationDirection:
        """Aggregate valuation-direction views using board influence weights."""
        return self._weighted_choice(context, "valuation_direction", "flat")  # type: ignore[return-value]
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_weighted_choice-332"></a>

### BoardroomSimulator._weighted_choice

文件：`boardroom_sim/simulator.py:332-342`

**签名**

~~~~python
def _weighted_choice(self, context: Dict[str, RoleDecision], attr: str, default: str) -> str:
~~~~

**功能**

源码文档说明：Return the weighted modal value for an attribute across role decisions.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |
| `attr` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `Counter` | 库函数或对象方法 | collections 计数器；返回可统计频次的字典子类。 |
| `context.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `getattr` | 库函数或对象方法 | 内置属性读取；返回对象属性值或默认值。 |
| `self.role_weights.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `score.most_common` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator._aggregate_financing_intent](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_financing_intent-297)、[boardroom_sim.simulator.BoardroomSimulator._aggregate_completion_view](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_completion_view-301)、[boardroom_sim.simulator.BoardroomSimulator._aggregate_text_choice](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_text_choice-324)、[boardroom_sim.simulator.BoardroomSimulator._aggregate_valuation](#fn-boardroom_sim-simulator-py-boardroomsimulator-_aggregate_valuation-328)

**完整源码**

~~~~python
    def _weighted_choice(self, context: Dict[str, RoleDecision], attr: str, default: str) -> str:
        """Return the weighted modal value for an attribute across role decisions."""
        score = Counter()
        for role, decision in context.items():
            value = getattr(decision, attr, default)
            if not value:
                continue
            score[str(value)] += self.role_weights.get(role, 1.0)
        if not score:
            return default
        return score.most_common(1)[0][0]
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_consensus_score-344"></a>

### BoardroomSimulator._consensus_score

文件：`boardroom_sim/simulator.py:344-357`

**签名**

~~~~python
def _consensus_score(self, context: Dict[str, RoleDecision]) -> float:
~~~~

**功能**

源码文档说明：Compute a simple consensus score from agreement on core predicted events.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `context` | 角色名到 RoleDecision 的映射，表示当前各角色的判断状态。 |

**返回值**

返回 `float` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `round` | 库函数或对象方法 | 内置四舍五入；返回数值。 |
| `Counter.most_common` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `Counter` | 库函数或对象方法 | collections 计数器；返回可统计频次的字典子类。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `getattr` | 库函数或对象方法 | 内置属性读取；返回对象属性值或默认值。 |
| `context.values` | 库函数或对象方法 | 字典值视图；返回所有 value。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)

**完整源码**

~~~~python
    def _consensus_score(self, context: Dict[str, RoleDecision]) -> float:
        """Compute a simple consensus score from agreement on core predicted events."""
        if not context:
            return 0.0
        attrs = [
            "financing_intent",
            "completion_view",
            "predicted_deal_type",
            "valuation_direction",
        ]
        agreement = 0
        for attr in attrs:
            agreement += Counter(str(getattr(decision, attr)) for decision in context.values()).most_common(1)[0][1]
        return round(agreement / (len(context) * len(attrs)), 3)
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_deal_break_risk-359"></a>

### BoardroomSimulator._deal_break_risk

文件：`boardroom_sim/simulator.py:359-370`

**签名**

~~~~python
def _deal_break_risk( self, financing_intent: FinancingIntent, completion_view: DealCompletionView, consensus_score: float, ) -> RiskLevel:
~~~~

**功能**

源码文档说明：Map financing intent, completion view, and consensus into deal-break risk.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `financing_intent` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `completion_view` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `consensus_score` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `RiskLevel` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)

**完整源码**

~~~~python
    def _deal_break_risk(
        self,
        financing_intent: FinancingIntent,
        completion_view: DealCompletionView,
        consensus_score: float,
    ) -> RiskLevel:
        """Map financing intent, completion view, and consensus into deal-break risk."""
        if financing_intent == "avoid" or completion_view == "unlikely_complete" or consensus_score < 0.45:
            return "high"
        if financing_intent == "wait" or consensus_score < 0.70:
            return "medium"
        return "low"
~~~~

[回到顶部](#top)

<a id="fn-boardroom_sim-simulator-py-boardroomsimulator-_record-372"></a>

### BoardroomSimulator._record

文件：`boardroom_sim/simulator.py:372-388`

**签名**

~~~~python
def _record( self, trace: List[Dict[str, Any]], stage: str, actor: str, message: str, payload: Dict[str, Any], ) -> None:
~~~~

**功能**

源码文档说明：Append one structured trace event to the simulation trace.

**参数**

| 参数 | 含义 |
| --- | --- |
| `self` | 当前对象实例，方法通过它读取配置、状态或其他方法。 |
| `trace` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `stage` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `actor` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `message` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `payload` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `trace.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |

**被谁调用**

[boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64)、[boardroom_sim.simulator.BoardroomSimulator._run_bargaining_round](#fn-boardroom_sim-simulator-py-boardroomsimulator-_run_bargaining_round-208)

**完整源码**

~~~~python
    def _record(
        self,
        trace: List[Dict[str, Any]],
        stage: str,
        actor: str,
        message: str,
        payload: Dict[str, Any],
    ) -> None:
        """Append one structured trace event to the simulation trace."""
        trace.append(
            {
                "stage": stage,
                "actor": actor,
                "message": message,
                "payload": payload,
            }
        )
~~~~

[回到顶部](#top)

<a id="module-evaluate_results-py"></a>

## evaluate_results.py

结果评估入口：从 results.jsonl 读取模拟输出并生成指标。

**类**：无类定义

<a id="fn-evaluate_results-py-parse_args-17"></a>

### parse_args

文件：`evaluate_results.py:17-23`

**签名**

~~~~python
def parse_args() -> argparse.Namespace:
~~~~

**功能**

源码文档说明：Parse command-line arguments for offline result evaluation.

**参数**

无显式参数。

**返回值**

返回 `argparse.Namespace` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `argparse.ArgumentParser` | 库函数或对象方法 | 构造命令行参数解析器，返回 ArgumentParser。 |
| `parser.add_argument` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `parser.parse_args` | [evaluate_results.parse_args](#fn-evaluate_results-py-parse_args-17) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[evaluate_results.parse_args](#fn-evaluate_results-py-parse_args-17)、[evaluate_results.main](#fn-evaluate_results-py-main-26)

**完整源码**

~~~~python
def parse_args() -> argparse.Namespace:
    """Parse command-line arguments for offline result evaluation."""
    parser = argparse.ArgumentParser(description="Evaluate boardroom simulation result JSONL files.")
    parser.add_argument("--input", type=Path, nargs="+", required=True, help="One or more compact results JSONL files.")
    parser.add_argument("--metrics-output", type=Path, default=None, help="Optional aggregate metrics JSON path.")
    parser.add_argument("--case-metrics-output", type=Path, default=None, help="Optional per-case metrics CSV path.")
    return parser.parse_args()
~~~~

[回到顶部](#top)

<a id="fn-evaluate_results-py-main-26"></a>

### main

文件：`evaluate_results.py:26-38`

**签名**

~~~~python
def main() -> None:
~~~~

**功能**

源码文档说明：Read result files, compute metrics, and optionally write reports.

**参数**

无显式参数。

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `parse_args` | [evaluate_results.parse_args](#fn-evaluate_results-py-parse_args-17) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `read_result_rows` | [boardroom_sim.evaluation.read_result_rows](#fn-boardroom_sim-evaluation-py-read_result_rows-103) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `evaluate_results` | [boardroom_sim.evaluation.evaluate_results](#fn-boardroom_sim-evaluation-py-evaluate_results-124) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |
| `metrics_summary` | [boardroom_sim.evaluation.metrics_summary](#fn-boardroom_sim-evaluation-py-metrics_summary-148) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `write_metrics_json` | [boardroom_sim.evaluation.write_metrics_json](#fn-boardroom_sim-evaluation-py-write_metrics_json-257) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `write_case_metrics_csv` | [boardroom_sim.evaluation.write_case_metrics_csv](#fn-boardroom_sim-evaluation-py-write_case_metrics_csv-266) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def main() -> None:
    """Read result files, compute metrics, and optionally write reports."""
    args = parse_args()
    rows = read_result_rows(args.input)
    metrics = evaluate_results(rows)
    print(metrics_summary(metrics))

    if args.metrics_output is not None:
        write_metrics_json(args.metrics_output, metrics)
        print(f"Wrote aggregate metrics to {args.metrics_output}.")
    if args.case_metrics_output is not None:
        write_case_metrics_csv(args.case_metrics_output, metrics["case_metrics"])
        print(f"Wrote per-case metrics to {args.case_metrics_output}.")
~~~~

[回到顶部](#top)

<a id="module-render_trace_report-py"></a>

## render_trace_report.py

trace 渲染脚本：把机器可读 traces.json 转为人类可读 Markdown 报告。

**类**：无类定义

<a id="fn-render_trace_report-py-parse_args-20"></a>

### parse_args

文件：`render_trace_report.py:20-27`

**签名**

~~~~python
def parse_args() -> argparse.Namespace:
~~~~

**功能**

源码文档说明：Parse command-line arguments.

**参数**

无显式参数。

**返回值**

返回 `argparse.Namespace` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `argparse.ArgumentParser` | 库函数或对象方法 | 构造命令行参数解析器，返回 ArgumentParser。 |
| `parser.add_argument` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `parser.parse_args` | [render_trace_report.parse_args](#fn-render_trace_report-py-parse_args-20) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[render_trace_report.parse_args](#fn-render_trace_report-py-parse_args-20)、[render_trace_report.main](#fn-render_trace_report-py-main-30)

**完整源码**

~~~~python
def parse_args() -> argparse.Namespace:
    """Parse command-line arguments."""
    parser = argparse.ArgumentParser(description="Render boardroom trace JSON as a readable text report.")
    parser.add_argument("--trace-json", type=Path, required=True, help="Path to trace JSON generated by run_experiment.py.")
    parser.add_argument("--output", type=Path, required=True, help="Path to the generated text report.")
    parser.add_argument("--case-index", type=int, default=None, help="Optional zero-based case index to render.")
    parser.add_argument("--case-id", default=None, help="Optional case_id to render.")
    return parser.parse_args()
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-main-30"></a>

### main

文件：`render_trace_report.py:30-38`

**签名**

~~~~python
def main() -> None:
~~~~

**功能**

源码文档说明：Load trace JSON and write the narrative report.

**参数**

无显式参数。

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `parse_args` | [render_trace_report.parse_args](#fn-render_trace_report-py-parse_args-20) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `load_trace_cases` | [render_trace_report.load_trace_cases](#fn-render_trace_report-py-load_trace_cases-41) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `select_cases` | [render_trace_report.select_cases](#fn-render_trace_report-py-select_cases-52) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `render_report` | [render_trace_report.render_report](#fn-render_trace_report-py-render_report-73) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `args.output.parent.mkdir` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `args.output.write_text` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def main() -> None:
    """Load trace JSON and write the narrative report."""
    args = parse_args()
    cases = load_trace_cases(args.trace_json)
    selected = select_cases(cases, case_index=args.case_index, case_id=args.case_id)
    report = render_report(selected)
    args.output.parent.mkdir(parents=True, exist_ok=True)
    args.output.write_text(report, encoding="utf-8")
    print(f"Wrote readable trace report to {args.output}.")
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-load_trace_cases-41"></a>

### load_trace_cases

文件：`render_trace_report.py:41-49`

**签名**

~~~~python
def load_trace_cases(path: Path) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Load a trace JSON file that contains either one case object or a case list.

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `json.load` | 库函数或对象方法 | json 标准库函数；用于 JSON 序列化或反序列化。 |

**被谁调用**

[render_trace_report.main](#fn-render_trace_report-py-main-30)

**完整源码**

~~~~python
def load_trace_cases(path: Path) -> List[Dict[str, Any]]:
    """Load a trace JSON file that contains either one case object or a case list."""
    with path.open("r", encoding="utf-8") as handle:
        payload = json.load(handle)
    if isinstance(payload, list):
        return [item for item in payload if isinstance(item, dict)]
    if isinstance(payload, dict):
        return [payload]
    raise ValueError(f"Expected a JSON object or list in {path}.")
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-select_cases-52"></a>

### select_cases

文件：`render_trace_report.py:52-70`

**签名**

~~~~python
def select_cases( cases: List[Dict[str, Any]], *, case_index: Optional[int], case_id: Optional[str], ) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Select cases by index or case_id, defaulting to all cases.

**参数**

| 参数 | 含义 |
| --- | --- |
| `cases` | 多个 BoardCase 或 trace case，用于批量运行或统计。 |
| `case_index` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `case_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `IndexError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[render_trace_report.main](#fn-render_trace_report-py-main-30)

**完整源码**

~~~~python
def select_cases(
    cases: List[Dict[str, Any]],
    *,
    case_index: Optional[int],
    case_id: Optional[str],
) -> List[Dict[str, Any]]:
    """Select cases by index or case_id, defaulting to all cases."""
    if case_index is not None and case_id is not None:
        raise ValueError("Use either --case-index or --case-id, not both.")
    if case_index is not None:
        if case_index < 0 or case_index >= len(cases):
            raise IndexError(f"--case-index {case_index} is out of range for {len(cases)} cases.")
        return [cases[case_index]]
    if case_id is not None:
        selected = [case for case in cases if str(case.get("case_id", "")) == case_id]
        if not selected:
            raise ValueError(f"No case with case_id={case_id} found.")
        return selected
    return cases
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-render_report-73"></a>

### render_report

文件：`render_trace_report.py:73-80`

**签名**

~~~~python
def render_report(cases: List[Dict[str, Any]]) -> str:
~~~~

**功能**

把 traces.json 中的模拟过程渲染成可读 Markdown 报告，便于检查每个案例的董事会发言与结果。

**参数**

| 参数 | 含义 |
| --- | --- |
| `cases` | 多个 BoardCase 或 trace case，用于批量运行或统计。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `enumerate` | 库函数或对象方法 | 内置枚举；返回 index 和元素组成的迭代器。 |
| `parts.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `join.rstrip` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `render_case` | [render_trace_report.render_case](#fn-render_trace_report-py-render_case-83) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[render_trace_report.main](#fn-render_trace_report-py-main-30)

**完整源码**

~~~~python
def render_report(cases: List[Dict[str, Any]]) -> str:
    """Render all selected cases as one text report."""
    parts = ["董事会智能体模拟过程记录", ""]
    for index, case in enumerate(cases, start=1):
        parts.append(render_case(case, index=index, total=len(cases)))
        if index < len(cases):
            parts.append("\n" + "=" * 72 + "\n")
    return "\n".join(parts).rstrip() + "\n"
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-render_case-83"></a>

### render_case

文件：`render_trace_report.py:83-136`

**签名**

~~~~python
def render_case(case: Dict[str, Any], *, index: int, total: int) -> str:
~~~~

**功能**

源码文档说明：Render one simulation case as readable prose.

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |
| `index` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `total` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `ensure_list` | [render_trace_report.ensure_list](#fn-render_trace_report-py-ensure_list-387) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `text` | [render_trace_report.text](#fn-render_trace_report-py-text-394) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `first_event` | [render_trace_report.first_event](#fn-render_trace_report-py-first_event-374) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `lines.extend` | 库函数或对象方法 | 列表扩展方法；把多个元素追加到列表末尾，返回 None。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `render_member_background` | [render_trace_report.render_member_background](#fn-render_trace_report-py-render_member_background-139) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `render_bargaining` | [render_trace_report.render_bargaining](#fn-render_trace_report-py-render_bargaining-157) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `render_final_result` | [render_trace_report.render_final_result](#fn-render_trace_report-py-render_final_result-191) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `render_similarity` | [render_trace_report.render_similarity](#fn-render_trace_report-py-render_similarity-215) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `ensure_dict` | [render_trace_report.ensure_dict](#fn-render_trace_report-py-ensure_dict-382) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `event.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `preliminary.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `format_stance` | [render_trace_report.format_stance](#fn-render_trace_report-py-format_stance-284) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[render_trace_report.render_report](#fn-render_trace_report-py-render_report-73)

**完整源码**

~~~~python
def render_case(case: Dict[str, Any], *, index: int, total: int) -> str:
    """Render one simulation case as readable prose."""
    trace = ensure_list(case.get("trace"))
    case_id = text(case.get("case_id"), "unknown_case")
    company = text(case.get("company_name"), "unknown_company")
    preliminary = first_event(trace, "preliminary")
    role_policy = first_event(trace, "role_policy")

    lines: List[str] = []
    title = f"案例 {index}/{total}：{company}（case_id: {case_id}）" if total > 1 else f"案例：{company}（case_id: {case_id}）"
    lines.extend([title, ""])

    lines.append("一、背景信息")
    if preliminary:
        lines.append(f"本次模拟的共同背景为：{text(preliminary.get('message'), '未记录背景摘要')}")
    else:
        lines.append("本次模拟没有在 trace 中记录单独的背景摘要。")
    lines.append(render_member_background(role_policy, case))
    lines.append("")

    lines.append("二、角色初始判断")
    private_events = [event for event in trace if event.get("stage") == "private_assessment"]
    if private_events:
        for event in private_events:
            actor = text(event.get("actor"), "unknown_actor")
            payload = ensure_dict(event.get("payload"))
            lines.append(f"{actor} 的初始立场是：{format_stance(payload)}")
            rationale = text(event.get("message"), "")
            if rationale:
                lines.append(f"该角色给出的主要理由是：{rationale}")
    else:
        lines.append("trace 中没有记录 private_assessment 阶段。")
    lines.append("")

    lines.append("三、开场陈述")
    opening_events = [event for event in trace if event.get("stage") == "opening_statement"]
    if opening_events:
        for event in opening_events:
            lines.append(f"{text(event.get('actor'), 'unknown_actor')}：{text(event.get('message'), '')}")
    else:
        lines.append("trace 中没有记录 opening_statement 阶段。")
    lines.append("")

    lines.append("四、辩论记录")
    lines.extend(render_bargaining(trace))
    lines.append("")

    lines.append("五、最终结果")
    lines.append(render_final_result(case))
    lines.append("")

    lines.append("六、模拟结果与真实结果相似度")
    lines.extend(render_similarity(case))
    return "\n".join(lines)
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-render_member_background-139"></a>

### render_member_background

文件：`render_trace_report.py:139-154`

**签名**

~~~~python
def render_member_background(role_policy_event: Optional[Dict[str, Any]], case: Dict[str, Any]) -> str:
~~~~

**功能**

源码文档说明：Render role identities from role policy payload or final role decisions.

**参数**

| 参数 | 含义 |
| --- | --- |
| `role_policy_event` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `ensure_dict` | [render_trace_report.ensure_dict](#fn-render_trace_report-py-ensure_dict-382) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `list` | 库函数或对象方法 | 内置容器构造；返回 list。 |
| `ROLE_DESCRIPTIONS.get.rstrip` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `descriptions.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `role_policy_event.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `ROLE_DESCRIPTIONS.keys` | 库函数或对象方法 | 字典键视图；返回所有 key。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `payload.keys` | 库函数或对象方法 | 字典键视图；返回所有 key。 |
| `ensure_dict.keys` | 库函数或对象方法 | 字典键视图；返回所有 key。 |
| `ROLE_DESCRIPTIONS.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[render_trace_report.render_case](#fn-render_trace_report-py-render_case-83)

**完整源码**

~~~~python
def render_member_background(role_policy_event: Optional[Dict[str, Any]], case: Dict[str, Any]) -> str:
    """Render role identities from role policy payload or final role decisions."""
    role_names: List[str] = []
    payload = ensure_dict(role_policy_event.get("payload")) if role_policy_event else {}
    if payload:
        role_names = [str(name) for name in payload.keys()]
    if not role_names:
        role_names = [str(name) for name in ensure_dict(case.get("role_decisions")).keys()]
    if not role_names:
        role_names = list(ROLE_DESCRIPTIONS.keys())

    descriptions = []
    for role in role_names:
        description = ROLE_DESCRIPTIONS.get(role, "董事会模拟成员。").rstrip("。")
        descriptions.append(f"{role}：{description}")
    return "参与本轮讨论的成员包括：" + "；".join(descriptions) + "。"
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-render_bargaining-157"></a>

### render_bargaining

文件：`render_trace_report.py:157-188`

**签名**

~~~~python
def render_bargaining(trace: List[Dict[str, Any]]) -> List[str]:
~~~~

**功能**

源码文档说明：Render bargaining events grouped by round.

**参数**

| 参数 | 含义 |
| --- | --- |
| `trace` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `List[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `sorted` | 库函数或对象方法 | 内置排序；返回新的 list。 |
| `text` | [render_trace_report.text](#fn-render_trace_report-py-text-394) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `stage.startswith` | 库函数或对象方法 | 字符串前缀判断，返回 bool。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `proposals.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `event.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `rounds.setdefault.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `stage.rsplit` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `ensure_dict` | [render_trace_report.ensure_dict](#fn-render_trace_report-py-ensure_dict-382) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `proposal_event.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `rounds.setdefault` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `stage.replace` | 库函数或对象方法 | 字符串替换，返回新字符串。 |
| `payload.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `format_proposal` | [render_trace_report.format_proposal](#fn-render_trace_report-py-format_proposal-305) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `format_stance` | [render_trace_report.format_stance](#fn-render_trace_report-py-format_stance-284) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[render_trace_report.render_case](#fn-render_trace_report-py-render_case-83)

**完整源码**

~~~~python
def render_bargaining(trace: List[Dict[str, Any]]) -> List[str]:
    """Render bargaining events grouped by round."""
    lines: List[str] = []
    rounds: Dict[str, List[Dict[str, Any]]] = {}
    proposals: Dict[str, Dict[str, Any]] = {}
    for event in trace:
        stage = text(event.get("stage"), "")
        if stage.startswith("bargaining_round_"):
            rounds.setdefault(stage, []).append(event)
        elif stage.startswith("proposal_after_round_"):
            proposals[stage.replace("proposal_after_", "bargaining_")] = event

    if not rounds:
        lines.append("本次模拟没有设置辩论轮，或 trace 中没有记录 bargaining_round 阶段。")
        return lines

    for stage in sorted(rounds, key=round_sort_key):
        round_no = stage.rsplit("_", 1)[-1]
        lines.append(f"第 {round_no} 轮辩论：")
        for event in rounds[stage]:
            actor = text(event.get("actor"), "unknown_actor")
            message = text(event.get("message"), "")
            payload = ensure_dict(event.get("payload"))
            updated = ensure_dict(payload.get("updated_decision") or payload.get("current_decision"))
            lines.append(f"{actor} 发言：{message}")
            if updated:
                lines.append(f"发言后，该角色的立场更新为：{format_stance(updated)}")
        proposal_event = proposals.get(stage)
        if proposal_event:
            proposal = ensure_dict(proposal_event.get("payload"))
            lines.append(f"本轮结束后的综合提案为：{format_proposal(proposal)}")
    return lines
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-render_final_result-191"></a>

### render_final_result

文件：`render_trace_report.py:191-212`

**签名**

~~~~python
def render_final_result(case: Dict[str, Any]) -> str:
~~~~

**功能**

源码文档说明：Render final prediction in prose.

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `text` | [render_trace_report.text](#fn-render_trace_report-py-text-394) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `number` | [render_trace_report.number](#fn-render_trace_report-py-number-402) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[render_trace_report.render_case](#fn-render_trace_report-py-render_case-83)

**完整源码**

~~~~python
def render_final_result(case: Dict[str, Any]) -> str:
    """Render final prediction in prose."""
    if "financing_initiation_decision" in case:
        return (
            f"最终聚合结果认为，公司融资意向为 {text(case.get('financing_initiation_decision'))}，"
            f"交易完成判断为 {text(case.get('financing_completion_view'))}。"
            f"模拟预测融资额为 {number(case.get('predicted_deal_size_usd_m'))} 百万美元，"
            f"预测投后估值为 {number(case.get('predicted_post_money_valuation_usd_m'))} 百万美元，"
            f"预测投资人持股为 {number(case.get('predicted_investor_ownership_pct'))}%，"
            f"交易类型为 {text(case.get('predicted_deal_type'))}，"
            f"估值方向判断为 {text(case.get('valuation_direction'))}，"
            f"共识分数为 {number(case.get('consensus_score'))}，"
            f"交易破裂风险为 {text(case.get('deal_break_risk'))}。"
        )
    return (
        f"最终聚合结果认为，融资决策为 {text(case.get('financing_decision'))}，"
        f"估值方向为 {text(case.get('valuation_direction'))}，"
        f"CEO 更换判断为 {text(case.get('ceo_replacement_decision'))}。"
        f"共识分数为 {number(case.get('consensus_score'))}，"
        f"交易破裂风险为 {text(case.get('deal_break_risk'))}，"
        f"治理冲突风险为 {text(case.get('governance_conflict_risk'))}。"
    )
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-render_similarity-215"></a>

### render_similarity

文件：`render_trace_report.py:215-281`

**签名**

~~~~python
def render_similarity(case: Dict[str, Any]) -> List[str]:
~~~~

**功能**

源码文档说明：Render similarity between simulated predictions and labels.

**参数**

| 参数 | 含义 |
| --- | --- |
| `case` | 单个 BoardCase，代表一家公司融资/董事会决策案例。 |

**返回值**

返回 `List[str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `ensure_dict` | [render_trace_report.ensure_dict](#fn-render_trace_report-py-ensure_dict-382) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `comparisons.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `case.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `compare_exact` | [render_trace_report.compare_exact](#fn-render_trace_report-py-compare_exact-320) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `compare_numeric` | [render_trace_report.compare_numeric](#fn-render_trace_report-py-compare_numeric-348) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `sum` | 库函数或对象方法 | 内置求和；返回可迭代数值总和。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `lines.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `text` | [render_trace_report.text](#fn-render_trace_report-py-text-394) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `map_completion_label` | [render_trace_report.map_completion_label](#fn-render_trace_report-py-map_completion_label-362) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `to_float` | [render_trace_report.to_float](#fn-render_trace_report-py-to_float-410) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `labels.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[render_trace_report.render_case](#fn-render_trace_report-py-render_case-83)

**完整源码**

~~~~python
def render_similarity(case: Dict[str, Any]) -> List[str]:
    """Render similarity between simulated predictions and labels."""
    labels = ensure_dict(case.get("labels"))
    if not labels:
        return ["该 trace 没有包含 labels 字段，因此无法计算模拟结果与真实结果的相似度。"]

    comparisons: List[Tuple[str, Optional[float], str]] = []
    comparisons.append(
        compare_exact(
            "融资意向",
            text(case.get("financing_initiation_decision"), ""),
            text(labels.get("true_financing_initiated_label"), ""),
        )
    )
    comparisons.append(
        compare_exact(
            "交易完成状态",
            text(case.get("financing_completion_view"), ""),
            map_completion_label(labels.get("real_deal_completed_label")),
        )
    )
    comparisons.append(
        compare_numeric(
            "融资额",
            to_float(case.get("predicted_deal_size_usd_m")),
            to_float(labels.get("real_deal_size_usd_m")),
            unit="百万美元",
        )
    )
    comparisons.append(compare_exact("交易类型", text(case.get("predicted_deal_type"), ""), text(labels.get("real_deal_type"), "")))
    comparisons.append(
        compare_numeric(
            "投后估值",
            to_float(case.get("predicted_post_money_valuation_usd_m")),
            to_float(labels.get("real_post_money_valuation_usd_m")),
            unit="百万美元",
        )
    )
    comparisons.append(
        compare_numeric(
            "投资人持股",
            to_float(case.get("predicted_investor_ownership_pct")),
            to_float(labels.get("real_investor_ownership_pct")),
            unit="%",
        )
    )
    comparisons.append(
        compare_exact(
            "估值方向",
            text(case.get("valuation_direction"), ""),
            text(labels.get("real_valuation_direction_label"), ""),
            skip_values={"", "unknown"},
        )
    )

    available = [(name, score, detail) for name, score, detail in comparisons if score is not None]
    if not available:
        return ["真实标签均缺失或不可比较，因此无法计算相似度。"]

    overall = sum(score for _, score, _ in available) / len(available)
    lines = [f"总体相似度为 {overall:.1%}，基于 {len(available)} 个可比较指标。"]
    for _, _, detail in available:
        lines.append(detail)
    skipped = [name for name, score, _ in comparisons if score is None]
    if skipped:
        lines.append("未纳入相似度计算的指标包括：" + "、".join(skipped) + "。")
    return lines
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-format_stance-284"></a>

### format_stance

文件：`render_trace_report.py:284-302`

**签名**

~~~~python
def format_stance(decision: Dict[str, Any]) -> str:
~~~~

**功能**

源码文档说明：Format a role decision payload into one sentence.

**参数**

| 参数 | 含义 |
| --- | --- |
| `decision` | 某个角色当前的 RoleDecision。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `text` | [render_trace_report.text](#fn-render_trace_report-py-text-394) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `number` | [render_trace_report.number](#fn-render_trace_report-py-number-402) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `decision.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[render_trace_report.render_case](#fn-render_trace_report-py-render_case-83)、[render_trace_report.render_bargaining](#fn-render_trace_report-py-render_bargaining-157)

**完整源码**

~~~~python
def format_stance(decision: Dict[str, Any]) -> str:
    """Format a role decision payload into one sentence."""
    if "financing_intent" in decision:
        return (
            f"融资意向 {text(decision.get('financing_intent'))}，"
            f"完成判断 {text(decision.get('completion_view'))}，"
            f"预计融资额 {number(decision.get('predicted_deal_size_usd_m'))} 百万美元，"
            f"预计投后估值 {number(decision.get('predicted_post_money_valuation_usd_m'))} 百万美元，"
            f"预计投资人持股 {number(decision.get('predicted_investor_ownership_pct'))}%，"
            f"交易类型 {text(decision.get('predicted_deal_type'))}，"
            f"估值方向 {text(decision.get('valuation_direction'))}，"
            f"满意度 {number(decision.get('satisfaction_score'))}。"
        )
    return (
        f"融资投票 {text(decision.get('financing_vote'))}，"
        f"估值方向 {text(decision.get('valuation_direction'))}，"
        f"CEO 更换观点 {text(decision.get('ceo_replacement_view'))}，"
        f"满意度 {number(decision.get('satisfaction_score'))}。"
    )
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-format_proposal-305"></a>

### format_proposal

文件：`render_trace_report.py:305-317`

**签名**

~~~~python
def format_proposal(proposal: Dict[str, Any]) -> str:
~~~~

**功能**

源码文档说明：Format an aggregated proposal payload.

**参数**

| 参数 | 含义 |
| --- | --- |
| `proposal` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `join` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `number` | [render_trace_report.number](#fn-render_trace_report-py-number-402) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `text` | [render_trace_report.text](#fn-render_trace_report-py-text-394) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `proposal.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `proposal.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[render_trace_report.render_bargaining](#fn-render_trace_report-py-render_bargaining-157)

**完整源码**

~~~~python
def format_proposal(proposal: Dict[str, Any]) -> str:
    """Format an aggregated proposal payload."""
    if not proposal:
        return "未记录具体提案。"
    if "recommended_deal_size_usd_m" in proposal:
        return (
            f"建议融资额 {number(proposal.get('recommended_deal_size_usd_m'))} 百万美元，"
            f"交易类型 {text(proposal.get('recommended_deal_type'))}，"
            f"估值方向 {text(proposal.get('valuation_direction'))}，"
            f"预计稀释 {number(proposal.get('estimated_dilution_pct'))}%，"
            f"投资人保护强度 {text(proposal.get('investor_protection_level'))}。"
        )
    return ", ".join(f"{key}={value}" for key, value in proposal.items())
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-compare_exact-320"></a>

### compare_exact

文件：`render_trace_report.py:320-332`

**签名**

~~~~python
def compare_exact( name: str, predicted: str, actual: str, *, skip_values: Optional[Iterable[str]] = None, ) -> Tuple[str, Optional[float], str]:
~~~~

**功能**

源码文档说明：Compare categorical predictions.

**参数**

| 参数 | 含义 |
| --- | --- |
| `name` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `predicted` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `actual` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `skip_values` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Tuple[str, Optional[float], str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `set` | 库函数或对象方法 | 内置容器构造；返回 set。 |

**被谁调用**

[render_trace_report.render_similarity](#fn-render_trace_report-py-render_similarity-215)

**完整源码**

~~~~python
def compare_exact(
    name: str,
    predicted: str,
    actual: str,
    *,
    skip_values: Optional[Iterable[str]] = None,
) -> Tuple[str, Optional[float], str]:
    """Compare categorical predictions."""
    skips = set(skip_values or {"", "unknown"})
    if predicted in skips or actual in skips:
        return name, None, f"{name}缺少可比较标签。"
    score = 1.0 if predicted == actual else 0.0
    return name, score, f"{name}：模拟为 {predicted}，真实为 {actual}，相似度 {score:.0%}。"
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-compare_ceo-335"></a>

### compare_ceo

文件：`render_trace_report.py:335-345`

**签名**

~~~~python
def compare_ceo(predicted: str, actual: str) -> Tuple[str, Optional[float], str]:
~~~~

**功能**

源码文档说明：Compare CEO replacement labels, treating monitor as partial pressure.

**参数**

| 参数 | 含义 |
| --- | --- |
| `predicted` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `actual` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Tuple[str, Optional[float], str]` 类型或兼容该注解的对象。

**调用的函数**

这个函数没有显式函数调用，主要是字段访问、表达式或常量返回。

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def compare_ceo(predicted: str, actual: str) -> Tuple[str, Optional[float], str]:
    """Compare CEO replacement labels, treating monitor as partial pressure."""
    if not predicted or not actual or actual == "unknown":
        return "CEO 更换", None, "CEO 更换缺少可比较标签。"
    if predicted == actual:
        score = 1.0
    elif predicted == "monitor" and actual in {"keep", "replace"}:
        score = 0.5
    else:
        score = 0.0
    return "CEO 更换", score, f"CEO 更换：模拟为 {predicted}，真实为 {actual}，相似度 {score:.0%}。"
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-compare_numeric-348"></a>

### compare_numeric

文件：`render_trace_report.py:348-359`

**签名**

~~~~python
def compare_numeric(name: str, predicted: Optional[float], actual: Optional[float], *, unit: str) -> Tuple[str, Optional[float], str]:
~~~~

**功能**

源码文档说明：Compare numeric prediction with relative-error similarity.

**参数**

| 参数 | 含义 |
| --- | --- |
| `name` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `predicted` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `actual` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `unit` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Tuple[str, Optional[float], str]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `abs` | 库函数或对象方法 | 内置绝对值；返回数值。 |

**被谁调用**

[render_trace_report.render_similarity](#fn-render_trace_report-py-render_similarity-215)

**完整源码**

~~~~python
def compare_numeric(name: str, predicted: Optional[float], actual: Optional[float], *, unit: str) -> Tuple[str, Optional[float], str]:
    """Compare numeric prediction with relative-error similarity."""
    if predicted is None or actual is None or actual <= 0:
        return name, None, f"{name}缺少可比较数值。"
    relative_error = abs(predicted - actual) / max(abs(actual), 1.0)
    score = max(0.0, 1.0 - relative_error)
    return (
        name,
        score,
        f"{name}：模拟为 {predicted:.3f} {unit}，真实为 {actual:.3f} {unit}，"
        f"相对误差 {relative_error:.1%}，相似度 {score:.1%}。",
    )
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-map_completion_label-362"></a>

### map_completion_label

文件：`render_trace_report.py:362-371`

**签名**

~~~~python
def map_completion_label(label: Any) -> str:
~~~~

**功能**

源码文档说明：Map raw deal status into the simulation completion-view label space.

**参数**

| 参数 | 含义 |
| --- | --- |
| `label` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `text.strip.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `text.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `text` | [render_trace_report.text](#fn-render_trace_report-py-text-394) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[render_trace_report.render_similarity](#fn-render_trace_report-py-render_similarity-215)

**完整源码**

~~~~python
def map_completion_label(label: Any) -> str:
    """Map raw deal status into the simulation completion-view label space."""
    raw = text(label, "").strip().lower()
    if raw in {"completed", "closed", "done"}:
        return "likely_complete"
    if raw in {"cancelled", "canceled", "failed", "terminated", "withdrawn"}:
        return "unlikely_complete"
    if raw:
        return "uncertain"
    return ""
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-first_event-374"></a>

### first_event

文件：`render_trace_report.py:374-379`

**签名**

~~~~python
def first_event(trace: List[Dict[str, Any]], stage: str) -> Optional[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Return the first trace event with a given stage.

**参数**

| 参数 | 含义 |
| --- | --- |
| `trace` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `stage` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Optional[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `event.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[render_trace_report.render_case](#fn-render_trace_report-py-render_case-83)

**完整源码**

~~~~python
def first_event(trace: List[Dict[str, Any]], stage: str) -> Optional[Dict[str, Any]]:
    """Return the first trace event with a given stage."""
    for event in trace:
        if event.get("stage") == stage:
            return event
    return None
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-ensure_dict-382"></a>

### ensure_dict

文件：`render_trace_report.py:382-384`

**签名**

~~~~python
def ensure_dict(value: Any) -> Dict[str, Any]:
~~~~

**功能**

源码文档说明：Return value if it is a dict, else an empty dict.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Dict[str, Any]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |

**被谁调用**

[render_trace_report.render_case](#fn-render_trace_report-py-render_case-83)、[render_trace_report.render_member_background](#fn-render_trace_report-py-render_member_background-139)、[render_trace_report.render_bargaining](#fn-render_trace_report-py-render_bargaining-157)、[render_trace_report.render_similarity](#fn-render_trace_report-py-render_similarity-215)

**完整源码**

~~~~python
def ensure_dict(value: Any) -> Dict[str, Any]:
    """Return value if it is a dict, else an empty dict."""
    return value if isinstance(value, dict) else {}
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-ensure_list-387"></a>

### ensure_list

文件：`render_trace_report.py:387-391`

**签名**

~~~~python
def ensure_list(value: Any) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Return value if it is a list of dict-like events, else an empty list.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |

**被谁调用**

[render_trace_report.render_case](#fn-render_trace_report-py-render_case-83)

**完整源码**

~~~~python
def ensure_list(value: Any) -> List[Dict[str, Any]]:
    """Return value if it is a list of dict-like events, else an empty list."""
    if not isinstance(value, list):
        return []
    return [item for item in value if isinstance(item, dict)]
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-text-394"></a>

### text

文件：`render_trace_report.py:394-399`

**签名**

~~~~python
def text(value: Any, default: str = "unknown") -> str:
~~~~

**功能**

源码文档说明：Convert a value into readable text.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |
| `default` | 值缺失或非法时使用的默认值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `str.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[render_trace_report.render_case](#fn-render_trace_report-py-render_case-83)、[render_trace_report.render_bargaining](#fn-render_trace_report-py-render_bargaining-157)、[render_trace_report.render_final_result](#fn-render_trace_report-py-render_final_result-191)、[render_trace_report.render_similarity](#fn-render_trace_report-py-render_similarity-215)、[render_trace_report.format_stance](#fn-render_trace_report-py-format_stance-284)、[render_trace_report.format_proposal](#fn-render_trace_report-py-format_proposal-305)、[render_trace_report.map_completion_label](#fn-render_trace_report-py-map_completion_label-362)

**完整源码**

~~~~python
def text(value: Any, default: str = "unknown") -> str:
    """Convert a value into readable text."""
    if value is None:
        return default
    value_text = str(value).strip()
    return value_text if value_text else default
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-number-402"></a>

### number

文件：`render_trace_report.py:402-407`

**签名**

~~~~python
def number(value: Any) -> str:
~~~~

**功能**

源码文档说明：Format numeric values without noisy trailing zeros.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `str` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `to_float` | [render_trace_report.to_float](#fn-render_trace_report-py-to_float-410) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[render_trace_report.render_final_result](#fn-render_trace_report-py-render_final_result-191)、[render_trace_report.format_stance](#fn-render_trace_report-py-format_stance-284)、[render_trace_report.format_proposal](#fn-render_trace_report-py-format_proposal-305)

**完整源码**

~~~~python
def number(value: Any) -> str:
    """Format numeric values without noisy trailing zeros."""
    numeric = to_float(value)
    if numeric is None:
        return "未知"
    return f"{numeric:.6g}"
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-to_float-410"></a>

### to_float

文件：`render_trace_report.py:410-418`

**签名**

~~~~python
def to_float(value: Any) -> Optional[float]:
~~~~

**功能**

源码文档说明：Convert value to float if possible.

**参数**

| 参数 | 含义 |
| --- | --- |
| `value` | 待清洗、转换或校验的输入值。 |

**返回值**

返回 `Optional[float]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `float` | 库函数或对象方法 | 内置类型转换；返回浮点数。 |
| `math.isfinite` | 库函数或对象方法 | math 数学函数；返回数值或布尔判断。 |

**被谁调用**

[render_trace_report.render_similarity](#fn-render_trace_report-py-render_similarity-215)、[render_trace_report.number](#fn-render_trace_report-py-number-402)

**完整源码**

~~~~python
def to_float(value: Any) -> Optional[float]:
    """Convert value to float if possible."""
    try:
        if value is None or value == "":
            return None
        number_value = float(value)
        return number_value if math.isfinite(number_value) else None
    except (TypeError, ValueError):
        return None
~~~~

[回到顶部](#top)

<a id="fn-render_trace_report-py-round_sort_key-421"></a>

### round_sort_key

文件：`render_trace_report.py:421-426`

**签名**

~~~~python
def round_sort_key(stage: str) -> int:
~~~~

**功能**

源码文档说明：Sort bargaining round stage names by trailing integer.

**参数**

| 参数 | 含义 |
| --- | --- |
| `stage` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `int` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `int` | 库函数或对象方法 | 内置类型转换；返回整数。 |
| `stage.rsplit` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def round_sort_key(stage: str) -> int:
    """Sort bargaining round stage names by trailing integer."""
    try:
        return int(stage.rsplit("_", 1)[-1])
    except ValueError:
        return 0
~~~~

[回到顶部](#top)

<a id="module-run_batch_experiments-py"></a>

## run_batch_experiments.py

批量实验主脚本：读取 Excel/JSONL 输入，运行多次模拟、断点续跑、基线预测、指标计算和 manifest 输出。

**类**：无类定义

<a id="fn-run_batch_experiments-py-parse_args-34"></a>

### parse_args

文件：`run_batch_experiments.py:34-76`

**签名**

~~~~python
def parse_args() -> argparse.Namespace:
~~~~

**功能**

源码文档说明：Parse command-line arguments for a batch experiment.

**参数**

无显式参数。

**返回值**

返回 `argparse.Namespace` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `argparse.ArgumentParser` | 库函数或对象方法 | 构造命令行参数解析器，返回 ArgumentParser。 |
| `parser.add_argument` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `parser.parse_args` | [run_batch_experiments.parse_args](#fn-run_batch_experiments-py-parse_args-34) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[run_batch_experiments.parse_args](#fn-run_batch_experiments-py-parse_args-34)、[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)

**完整源码**

~~~~python
def parse_args() -> argparse.Namespace:
    """Parse command-line arguments for a batch experiment."""
    parser = argparse.ArgumentParser(description="Run repeated boardroom simulation experiments.")
    parser.add_argument(
        "--input",
        default="input/260524_02_03_pitchbook_sample_100_shared.xlsx",
        type=Path,
        help="Path to input JSONL cases or PitchBook XLSX workbook.",
    )
    parser.add_argument("--output-dir", type=Path, required=True, help="Directory for batch outputs.")
    parser.add_argument("--repeats", type=int, default=1, help="Number of repeated full experiment runs.")
    parser.add_argument("--config", type=Path, default=None, help="Path to experiment TOML config.")
    parser.add_argument("--bargaining-rounds", type=int, default=None, help="Override bargaining rounds from config.")
    parser.add_argument("--case-limit", type=int, default=None, help="Optional maximum number of input cases to run.")
    parser.add_argument(
        "--history-limit",
        type=int,
        default=None,
        help="Override config history limit. Use -1 for all history.",
    )
    parser.add_argument("--resume", action="store_true", help="Skip completed run directories with full result files.")
    parser.add_argument("--skip-traces", action="store_true", help="Do not write detailed trace JSON files.")
    parser.add_argument("--skip-baselines", action="store_true", help="Do not run the two single-agent baselines.")
    parser.add_argument(
        "--skip-debate-accuracy-report",
        action="store_true",
        help="Do not write debate_accuracy_report.md and debate_accuracy_cases.csv for each run.",
    )
    parser.add_argument(
        "--deal-size-tolerance",
        type=float,
        default=0.50,
        help="Relative tolerance used by debate accuracy report numeric correctness. Default: 0.50.",
    )
    parser.add_argument("--api-key", default=None, help="API key. Defaults to BOARDROOM_LLM_API_KEY.")
    parser.add_argument("--api-key-env", default="BOARDROOM_LLM_API_KEY", help="Environment variable containing API key.")
    parser.add_argument("--model", default="GLM-4-Flash-250414", help="LLM model name. Defaults to GLM-4-Flash-250414.")
    parser.add_argument("--base-url", default="https://api.z.ai/api/paas/v4", help="OpenAI-compatible base URL. Defaults to zhipuai.")
    parser.add_argument("--temperature", type=float, default=None, help="LLM temperature. Defaults to BOARDROOM_LLM_TEMPERATURE or 0.2.")
    parser.add_argument("--timeout-seconds", type=int, default=None, help="HTTP timeout. Defaults to BOARDROOM_LLM_TIMEOUT_SECONDS or 120.")
    parser.add_argument("--http-max-retries", type=int, default=None, help="HTTP retry count for transient LLM errors. Defaults to BOARDROOM_LLM_HTTP_MAX_RETRIES or 6.")
    parser.add_argument("--retry-base-seconds", type=float, default=None, help="Base seconds for exponential retry backoff. Defaults to BOARDROOM_LLM_RETRY_BASE_SECONDS or 2.0.")
    return parser.parse_args()
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-main-79"></a>

### main

文件：`run_batch_experiments.py:79-205`

**签名**

~~~~python
def main() -> None:
~~~~

**功能**

批量实验入口。它读取配置和数据，运行多次重复实验、断点续跑、基线预测、指标计算和 manifest 写入。

**参数**

无显式参数。

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `parse_args` | [run_batch_experiments.parse_args](#fn-run_batch_experiments-py-parse_args-34) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `load_experiment_config` | [boardroom_sim.config.load_experiment_config](#fn-boardroom_sim-config-py-load_experiment_config-185) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `args.output_dir.mkdir` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `load_cases` | [run_experiment.load_cases](#fn-run_experiment-py-load_cases-48) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `LLMConfig.from_env` | [boardroom_sim.llm.LLMConfig.from_env](#fn-boardroom_sim-llm-py-llmconfig-from_env-44) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `LLMClient` | [boardroom_sim.llm.LLMClient](#class-boardroom_sim-llm-py-llmclient-95) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_write_manifest` | [run_batch_experiments._write_manifest](#fn-run_batch_experiments-py-_write_manifest-593) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `range` | 库函数或对象方法 | 内置整数序列；返回 range 对象。 |
| `evaluate_results` | [boardroom_sim.evaluation.evaluate_results](#fn-boardroom_sim-evaluation-py-evaluate_results-124) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `write_metrics_json` | [boardroom_sim.evaluation.write_metrics_json](#fn-boardroom_sim-evaluation-py-write_metrics_json-257) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `write_case_metrics_csv` | [boardroom_sim.evaluation.write_case_metrics_csv](#fn-boardroom_sim-evaluation-py-write_case_metrics_csv-266) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `run_dir.mkdir` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `_maybe_read_completed_run` | [run_batch_experiments._maybe_read_completed_run](#fn-run_batch_experiments-py-_maybe_read_completed_run-311) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `all_rows.extend` | 库函数或对象方法 | 列表扩展方法；把多个元素追加到列表末尾，返回 None。 |
| `metrics_summary` | [boardroom_sim.evaluation.metrics_summary](#fn-boardroom_sim-evaluation-py-metrics_summary-148) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `baseline_rows_by_name.items` | 库函数或对象方法 | 字典遍历方法；返回 key/value 对迭代视图。 |
| `_run_cases_with_checkpoints` | [run_batch_experiments._run_cases_with_checkpoints](#fn-run_batch_experiments-py-_run_cases_with_checkpoints-227) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `traces_path.exists` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `_read_trace_cases` | [run_batch_experiments._read_trace_cases](#fn-run_batch_experiments-py-_read_trace_cases-564) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_run_or_resume_baseline` | [run_batch_experiments._run_or_resume_baseline](#fn-run_batch_experiments-py-_run_or_resume_baseline-331) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `baseline_rows_by_name.extend` | 库函数或对象方法 | 列表扩展方法；把多个元素追加到列表末尾，返回 None。 |
| `_write_debate_accuracy_report` | [run_batch_experiments._write_debate_accuracy_report](#fn-run_batch_experiments-py-_write_debate_accuracy_report-569) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def main() -> None:
    """Run batch experiments and write per-run plus aggregate metrics."""
    args = parse_args()
    if args.repeats < 1:
        raise ValueError("--repeats must be at least 1.")

    experiment_config = load_experiment_config(args.config)
    debate_accuracy_module.ROLE_WEIGHTS = dict(experiment_config.role_weights)
    bargaining_rounds = (
        args.bargaining_rounds if args.bargaining_rounds is not None else experiment_config.bargaining_rounds
    )
    history_limit = args.history_limit if args.history_limit is not None else experiment_config.history_limit
    args.bargaining_rounds = bargaining_rounds
    args.history_limit = history_limit

    args.output_dir.mkdir(parents=True, exist_ok=True)
    cases = load_cases(args.input, case_limit=args.case_limit, history_limit=history_limit)
    if not cases:
        raise ValueError(f"No cases loaded from {args.input}.")

    llm_config = LLMConfig.from_env(
        api_key=args.api_key,
        api_key_env=args.api_key_env,
        model=args.model,
        base_url=args.base_url,
        temperature=args.temperature,
        timeout_seconds=args.timeout_seconds,
        http_max_retries=args.http_max_retries,
        retry_base_seconds=args.retry_base_seconds,
    )
    llm_client = LLMClient(llm_config)

    _write_manifest(args.output_dir / "manifest.json", args, cases, llm_config, experiment_config)

    all_rows: List[Dict[str, Any]] = []
    baseline_rows_by_name: Dict[str, List[Dict[str, Any]]] = {
        "baseline_history_only": [],
        "baseline_history_with_roles": [],
    }
    for repeat_index in range(1, args.repeats + 1):
        run_id = f"run_{repeat_index:03d}"
        run_dir = args.output_dir / run_id
        run_dir.mkdir(parents=True, exist_ok=True)
        results_path = run_dir / "results.jsonl"
        traces_path = run_dir / "traces.json"
        trace_cases: List[Dict[str, Any]] | None = None

        rows = _maybe_read_completed_run(results_path, expected_count=len(cases), resume=args.resume)
        if rows is None:
            print(f"Starting {run_id}: {len(cases)} cases.")
            tokens_before = llm_client.total_tokens
            rows, trace_cases = _run_cases_with_checkpoints(
                cases=cases,
                llm_client=llm_client,
                bargaining_rounds=bargaining_rounds,
                experiment_config=experiment_config,
                run_dir=run_dir,
                run_id=run_id,
                resume=args.resume,
                skip_traces=args.skip_traces,
            )
            token_delta = llm_client.total_tokens - tokens_before
            print(f"Finished {run_id}; provider-reported tokens this run: {token_delta}.")
        else:
            print(f"Skipping {run_id}; found completed results at {results_path}.")
            if traces_path.exists():
                trace_cases = _read_trace_cases(traces_path)

        run_metrics = evaluate_results(rows)
        write_metrics_json(run_dir / "metrics.json", run_metrics)
        write_case_metrics_csv(run_dir / "case_metrics.csv", run_metrics["case_metrics"])
        print(metrics_summary(run_metrics))
        print("")
        all_rows.extend(rows)

        run_baseline_rows_by_name: Dict[str, List[Dict[str, Any]]] = {}
        if not args.skip_baselines:
            for baseline_name, include_role_rules in [
                ("baseline_history_only", False),
                ("baseline_history_with_roles", True),
            ]:
                baseline_rows = _run_or_resume_baseline(
                    cases=cases,
                    llm_client=llm_client,
                    run_dir=run_dir,
                    run_id=run_id,
                    baseline_name=baseline_name,
                    include_role_rules=include_role_rules,
                    role_policy_dir=experiment_config.role_policy_dir,
                    prompts_dir=experiment_config.prompts_dir,
                    resume=args.resume,
                )
                baseline_metrics = evaluate_results(baseline_rows)
                write_metrics_json(run_dir / f"{baseline_name}_metrics.json", baseline_metrics)
                write_case_metrics_csv(run_dir / f"{baseline_name}_case_metrics.csv", baseline_metrics["case_metrics"])
                baseline_rows_by_name[baseline_name].extend(baseline_rows)
                run_baseline_rows_by_name[baseline_name] = baseline_rows
                print(f"{baseline_name} metrics:")
                print(metrics_summary(baseline_metrics))
                print("")

        if not args.skip_debate_accuracy_report:
            if trace_cases is not None:
                _write_debate_accuracy_report(
                    run_dir,
                    trace_cases,
                    args.deal_size_tolerance,
                    baseline_rows_by_name=run_baseline_rows_by_name,
                )
            else:
                print(f"Cannot write debate accuracy report for {run_id}; missing {traces_path}.")

    aggregate_metrics = evaluate_results(all_rows)
    write_metrics_json(args.output_dir / "aggregate_metrics.json", aggregate_metrics)
    write_case_metrics_csv(args.output_dir / "case_metrics.csv", aggregate_metrics["case_metrics"])
    print("Aggregate metrics across all runs:")
    print(metrics_summary(aggregate_metrics))
    if not args.skip_baselines:
        for baseline_name, baseline_rows in baseline_rows_by_name.items():
            baseline_metrics = evaluate_results(baseline_rows)
            write_metrics_json(args.output_dir / f"{baseline_name}_aggregate_metrics.json", baseline_metrics)
            write_case_metrics_csv(args.output_dir / f"{baseline_name}_case_metrics.csv", baseline_metrics["case_metrics"])
            print(f"Aggregate {baseline_name} metrics:")
            print(metrics_summary(baseline_metrics))
    print(f"Wrote aggregate metrics to {args.output_dir / 'aggregate_metrics.json'}.")
    print(f"Wrote aggregate per-case metrics to {args.output_dir / 'case_metrics.csv'}.")
    print(f"LLM total tokens reported by provider: {llm_client.total_tokens}.")
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_run_cases-208"></a>

### _run_cases

文件：`run_batch_experiments.py:208-224`

**签名**

~~~~python
def _run_cases( cases: List[BoardCase], llm_client: LLMClient, bargaining_rounds: int, experiment_config: ExperimentConfig, run_id: str, ) -> List[SimulationResult]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `cases` | 多个 BoardCase 或 trace case，用于批量运行或统计。 |
| `llm_client` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `bargaining_rounds` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `experiment_config` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `run_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |

**返回值**

返回 `List[SimulationResult]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `BoardroomSimulator` | [boardroom_sim.simulator.BoardroomSimulator](#class-boardroom_sim-simulator-py-boardroomsimulator-29) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `enumerate` | 库函数或对象方法 | 内置枚举；返回 index 和元素组成的迭代器。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |
| `results.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `simulator.simulate` | [boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def _run_cases(
    cases: List[BoardCase],
    llm_client: LLMClient,
    bargaining_rounds: int,
    experiment_config: ExperimentConfig,
    run_id: str,
) -> List[SimulationResult]:
    simulator = BoardroomSimulator(
        llm_client=llm_client,
        bargaining_rounds=bargaining_rounds,
        config=experiment_config,
    )
    results: List[SimulationResult] = []
    for index, case in enumerate(cases, start=1):
        print(f"{run_id}: case {index}/{len(cases)} {case.case_id}", flush=True)
        results.append(simulator.simulate(case))
    return results
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_run_cases_with_checkpoints-227"></a>

### _run_cases_with_checkpoints

文件：`run_batch_experiments.py:227-308`

**签名**

~~~~python
def _run_cases_with_checkpoints( *, cases: List[BoardCase], llm_client: LLMClient, bargaining_rounds: int, experiment_config: ExperimentConfig, run_dir: Path, run_id: str, resume: bool, skip_traces: bool, ) -> Tuple[List[Dict[str, Any]], List[Dict[str, Any]] | None]:
~~~~

**功能**

带断点文件的批量模拟循环。每完成一个 case 就落盘结果、trace 和 checkpoint，方便 Ctrl+C 后继续。

**参数**

| 参数 | 含义 |
| --- | --- |
| `cases` | 多个 BoardCase 或 trace case，用于批量运行或统计。 |
| `llm_client` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `bargaining_rounds` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `experiment_config` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `run_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `run_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `resume` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `skip_traces` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `Tuple[List[Dict[str, Any]], List[Dict[str, Any]] | None]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `BoardroomSimulator` | [boardroom_sim.simulator.BoardroomSimulator](#class-boardroom_sim-simulator-py-boardroomsimulator-29) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `enumerate` | 库函数或对象方法 | 内置枚举；返回 index 和元素组成的迭代器。 |
| `_write_checkpoint_status` | [run_batch_experiments._write_checkpoint_status](#fn-run_batch_experiments-py-_write_checkpoint_status-512) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_read_existing_rows` | [run_batch_experiments._read_existing_rows](#fn-run_batch_experiments-py-_read_existing_rows-419) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_truncate_file` | [run_batch_experiments._truncate_file](#fn-run_batch_experiments-py-_truncate_file-475) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |
| `simulator.simulate` | [boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `result.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `_append_jsonl_row` | [run_batch_experiments._append_jsonl_row](#fn-run_batch_experiments-py-_append_jsonl_row-467) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `rows.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `_write_partial_run_outputs` | [run_batch_experiments._write_partial_run_outputs](#fn-run_batch_experiments-py-_write_partial_run_outputs-486) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_write_trace_cases_json` | [run_batch_experiments._write_trace_cases_json](#fn-run_batch_experiments-py-_write_trace_cases_json-480) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_read_existing_trace_cases` | [run_batch_experiments._read_existing_trace_cases](#fn-run_batch_experiments-py-_read_existing_trace_cases-442) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `trace_cases.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `rows.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)

**完整源码**

~~~~python
def _run_cases_with_checkpoints(
    *,
    cases: List[BoardCase],
    llm_client: LLMClient,
    bargaining_rounds: int,
    experiment_config: ExperimentConfig,
    run_dir: Path,
    run_id: str,
    resume: bool,
    skip_traces: bool,
) -> Tuple[List[Dict[str, Any]], List[Dict[str, Any]] | None]:
    """Run main simulation and persist results after every completed case."""
    simulator = BoardroomSimulator(
        llm_client=llm_client,
        bargaining_rounds=bargaining_rounds,
        config=experiment_config,
    )
    results_path = run_dir / "results.jsonl"
    traces_jsonl_path = run_dir / "traces.jsonl"
    traces_path = run_dir / "traces.json"
    metrics_path = run_dir / "metrics.json"
    case_metrics_path = run_dir / "case_metrics.csv"
    status_path = run_dir / "checkpoint_status.json"

    if resume:
        rows = _read_existing_rows(results_path, run_id=run_id)
        trace_cases = _read_existing_trace_cases(traces_jsonl_path, traces_path) if not skip_traces else None
    else:
        _truncate_file(results_path)
        if not skip_traces:
            _truncate_file(traces_jsonl_path)
        rows = []
        trace_cases = [] if not skip_traces else None

    completed_case_ids = {str(row.get("case_id", "")) for row in rows if row.get("case_id")}
    if completed_case_ids:
        print(f"{run_id}: resuming with {len(completed_case_ids)}/{len(cases)} completed cases.", flush=True)

    for index, case in enumerate(cases, start=1):
        if case.case_id in completed_case_ids:
            print(f"{run_id}: skip completed case {index}/{len(cases)} {case.case_id}", flush=True)
            continue

        print(f"{run_id}: case {index}/{len(cases)} {case.case_id}", flush=True)
        result = simulator.simulate(case)
        row_payload = result.to_dict(include_trace=False)
        _append_jsonl_row(results_path, row_payload)
        row = dict(row_payload)
        row["_source_file"] = str(results_path)
        row["_run_id"] = run_id
        rows.append(row)

        if not skip_traces:
            trace_payload = result.to_dict(include_trace=True)
            _append_jsonl_row(traces_jsonl_path, trace_payload)
            if trace_cases is not None:
                trace_cases.append(trace_payload)

        _write_partial_run_outputs(
            rows=rows,
            metrics_path=metrics_path,
            case_metrics_path=case_metrics_path,
            status_path=status_path,
            run_id=run_id,
            expected_count=len(cases),
            completed_count=len(rows),
            last_case_id=case.case_id,
            llm_client=llm_client,
        )

    if not skip_traces and trace_cases is not None:
        _write_trace_cases_json(traces_path, trace_cases)
    _write_checkpoint_status(
        status_path,
        run_id=run_id,
        expected_count=len(cases),
        completed_count=len(rows),
        last_case_id=rows[-1].get("case_id", "") if rows else "",
        llm_client=llm_client,
        status="completed" if len(rows) == len(cases) else "partial",
    )
    return rows, trace_cases
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_maybe_read_completed_run-311"></a>

### _maybe_read_completed_run

文件：`run_batch_experiments.py:311-318`

**签名**

~~~~python
def _maybe_read_completed_run(results_path: Path, *, expected_count: int, resume: bool) -> List[Dict[str, Any]] | None:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `results_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |
| `expected_count` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `resume` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `List[Dict[str, Any]] | None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `read_result_rows` | [boardroom_sim.evaluation.read_result_rows](#fn-boardroom_sim-evaluation-py-read_result_rows-103) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `results_path.exists` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)、[run_batch_experiments._run_or_resume_baseline](#fn-run_batch_experiments-py-_run_or_resume_baseline-331)

**完整源码**

~~~~python
def _maybe_read_completed_run(results_path: Path, *, expected_count: int, resume: bool) -> List[Dict[str, Any]] | None:
    if not resume or not results_path.exists():
        return None
    rows = read_result_rows([results_path])
    if len(rows) == expected_count:
        return rows
    print(f"Found {results_path} with {len(rows)} rows; expected {expected_count}. Resuming remaining cases.")
    return None
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_result_rows-321"></a>

### _result_rows

文件：`run_batch_experiments.py:321-328`

**签名**

~~~~python
def _result_rows(results: List[SimulationResult], results_path: Path, run_id: str) -> List[Dict[str, Any]]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `results` | SimulationResult 或基线预测结果集合。 |
| `results_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |
| `run_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `result.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `rows.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def _result_rows(results: List[SimulationResult], results_path: Path, run_id: str) -> List[Dict[str, Any]]:
    rows = []
    for result in results:
        row = result.to_dict(include_trace=False)
        row["_source_file"] = str(results_path)
        row["_run_id"] = run_id
        rows.append(row)
    return rows
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_run_or_resume_baseline-331"></a>

### _run_or_resume_baseline

文件：`run_batch_experiments.py:331-409`

**签名**

~~~~python
def _run_or_resume_baseline( *, cases: List[BoardCase], llm_client: LLMClient, run_dir: Path, run_id: str, baseline_name: str, include_role_rules: bool, role_policy_dir: Path | None, prompts_dir: Path | None, resume: bool, ) -> List[Dict[str, Any]]:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `cases` | 多个 BoardCase 或 trace case，用于批量运行或统计。 |
| `llm_client` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `run_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `run_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `baseline_name` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `include_role_rules` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `role_policy_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `prompts_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `resume` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_maybe_read_completed_run` | [run_batch_experiments._maybe_read_completed_run](#fn-run_batch_experiments-py-_maybe_read_completed_run-311) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |
| `enumerate` | 库函数或对象方法 | 内置枚举；返回 index 和元素组成的迭代器。 |
| `_write_checkpoint_status` | [run_batch_experiments._write_checkpoint_status](#fn-run_batch_experiments-py-_write_checkpoint_status-512) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_read_existing_rows` | [run_batch_experiments._read_existing_rows](#fn-run_batch_experiments-py-_read_existing_rows-419) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_truncate_file` | [run_batch_experiments._truncate_file](#fn-run_batch_experiments-py-_truncate_file-475) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `predict_single_case` | [boardroom_sim.baselines.predict_single_case](#fn-boardroom_sim-baselines-py-predict_single_case-102) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_append_jsonl_row` | [run_batch_experiments._append_jsonl_row](#fn-run_batch_experiments-py-_append_jsonl_row-467) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `dict` | 库函数或对象方法 | 内置容器构造；返回 dict。 |
| `rows.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `_write_partial_run_outputs` | [run_batch_experiments._write_partial_run_outputs](#fn-run_batch_experiments-py-_write_partial_run_outputs-486) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |
| `rows.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)

**完整源码**

~~~~python
def _run_or_resume_baseline(
    *,
    cases: List[BoardCase],
    llm_client: LLMClient,
    run_dir: Path,
    run_id: str,
    baseline_name: str,
    include_role_rules: bool,
    role_policy_dir: Path | None,
    prompts_dir: Path | None,
    resume: bool,
) -> List[Dict[str, Any]]:
    results_path = run_dir / f"{baseline_name}_results.jsonl"
    existing_rows = _maybe_read_completed_run(results_path, expected_count=len(cases), resume=resume)
    if existing_rows is not None:
        for row in existing_rows:
            row["_run_id"] = f"{run_id}/{baseline_name}"
        print(f"Skipping {run_id} {baseline_name}; found completed results at {results_path}.")
        return existing_rows

    print(f"Starting {run_id} {baseline_name}: {len(cases)} cases.")
    metrics_path = run_dir / f"{baseline_name}_metrics.json"
    case_metrics_path = run_dir / f"{baseline_name}_case_metrics.csv"
    status_path = run_dir / f"{baseline_name}_checkpoint_status.json"
    baseline_run_id = f"{run_id}/{baseline_name}"
    if resume:
        rows = _read_existing_rows(results_path, run_id=baseline_run_id)
    else:
        _truncate_file(results_path)
        rows = []

    completed_case_ids = {str(row.get("case_id", "")) for row in rows if row.get("case_id")}
    if completed_case_ids:
        print(
            f"{run_id} {baseline_name}: resuming with {len(completed_case_ids)}/{len(cases)} completed cases.",
            flush=True,
        )

    for index, case in enumerate(cases, start=1):
        if case.case_id in completed_case_ids:
            print(f"{run_id} {baseline_name}: skip completed case {index}/{len(cases)} {case.case_id}", flush=True)
            continue
        print(f"{run_id} {baseline_name}: case {index}/{len(cases)} {case.case_id}", flush=True)
        row_payload = predict_single_case(
            case,
            llm_client,
            include_role_rules=include_role_rules,
            baseline_name=baseline_name,
            role_policy_dir=role_policy_dir,
            prompts_dir=prompts_dir,
        )
        _append_jsonl_row(results_path, row_payload)
        row = dict(row_payload)
        row["_source_file"] = str(results_path)
        row["_run_id"] = baseline_run_id
        rows.append(row)
        _write_partial_run_outputs(
            rows=rows,
            metrics_path=metrics_path,
            case_metrics_path=case_metrics_path,
            status_path=status_path,
            run_id=baseline_run_id,
            expected_count=len(cases),
            completed_count=len(rows),
            last_case_id=case.case_id,
            llm_client=llm_client,
        )

    _write_checkpoint_status(
        status_path,
        run_id=baseline_run_id,
        expected_count=len(cases),
        completed_count=len(rows),
        last_case_id=rows[-1].get("case_id", "") if rows else "",
        llm_client=llm_client,
        status="completed" if len(rows) == len(cases) else "partial",
    )
    print(f"Finished {run_id} {baseline_name}.")
    return rows
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_write_rows_jsonl-412"></a>

### _write_rows_jsonl

文件：`run_batch_experiments.py:412-416`

**签名**

~~~~python
def _write_rows_jsonl(path: Path, rows: List[Dict[str, Any]]) -> None:
~~~~

**功能**

写入函数。它把结果、指标、trace 或中间 checkpoint 落盘，通常返回 None。

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `rows` | 结果行或 DataFrame 行集合。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `handle.write` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `json.dumps` | 库函数或对象方法 | 把 Python 对象序列化为 JSON 字符串，返回 str。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def _write_rows_jsonl(path: Path, rows: List[Dict[str, Any]]) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    with path.open("w", encoding="utf-8") as handle:
        for row in rows:
            handle.write(json.dumps(row, ensure_ascii=False, allow_nan=False) + "\n")
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_read_existing_rows-419"></a>

### _read_existing_rows

文件：`run_batch_experiments.py:419-426`

**签名**

~~~~python
def _read_existing_rows(results_path: Path, *, run_id: str) -> List[Dict[str, Any]]:
~~~~

**功能**

读取函数。它从文件或结果对象中取出结构化数据，返回后续评估、渲染或模拟流程可直接使用的 Python 对象。

**参数**

| 参数 | 含义 |
| --- | --- |
| `results_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |
| `run_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `read_result_rows` | [boardroom_sim.evaluation.read_result_rows](#fn-boardroom_sim-evaluation-py-read_result_rows-103) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_dedupe_rows_by_case` | [run_batch_experiments._dedupe_rows_by_case](#fn-run_batch_experiments-py-_dedupe_rows_by_case-429) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `results_path.exists` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |

**被谁调用**

[run_batch_experiments._run_cases_with_checkpoints](#fn-run_batch_experiments-py-_run_cases_with_checkpoints-227)、[run_batch_experiments._run_or_resume_baseline](#fn-run_batch_experiments-py-_run_or_resume_baseline-331)

**完整源码**

~~~~python
def _read_existing_rows(results_path: Path, *, run_id: str) -> List[Dict[str, Any]]:
    if not results_path.exists():
        return []
    rows = read_result_rows([results_path])
    for row in rows:
        row["_source_file"] = str(results_path)
        row["_run_id"] = run_id
    return _dedupe_rows_by_case(rows)
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_dedupe_rows_by_case-429"></a>

### _dedupe_rows_by_case

文件：`run_batch_experiments.py:429-439`

**签名**

~~~~python
def _dedupe_rows_by_case(rows: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
~~~~

**功能**

源码文档说明：Keep the first completed row for each case id, preserving file order.

**参数**

| 参数 | 含义 |
| --- | --- |
| `rows` | 结果行或 DataFrame 行集合。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `set` | 库函数或对象方法 | 内置容器构造；返回 set。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `seen.add` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `deduped.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `row.get` | 库函数或对象方法 | 字典或对象映射读取；按键取值，缺失时可返回默认值。 |

**被谁调用**

[run_batch_experiments._read_existing_rows](#fn-run_batch_experiments-py-_read_existing_rows-419)、[run_batch_experiments._read_jsonl_objects](#fn-run_batch_experiments-py-_read_jsonl_objects-450)

**完整源码**

~~~~python
def _dedupe_rows_by_case(rows: List[Dict[str, Any]]) -> List[Dict[str, Any]]:
    """Keep the first completed row for each case id, preserving file order."""
    seen = set()
    deduped = []
    for row in rows:
        case_id = str(row.get("case_id", ""))
        if not case_id or case_id in seen:
            continue
        seen.add(case_id)
        deduped.append(row)
    return deduped
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_read_existing_trace_cases-442"></a>

### _read_existing_trace_cases

文件：`run_batch_experiments.py:442-447`

**签名**

~~~~python
def _read_existing_trace_cases(traces_jsonl_path: Path, traces_path: Path) -> List[Dict[str, Any]]:
~~~~

**功能**

读取函数。它从文件或结果对象中取出结构化数据，返回后续评估、渲染或模拟流程可直接使用的 Python 对象。

**参数**

| 参数 | 含义 |
| --- | --- |
| `traces_jsonl_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |
| `traces_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `traces_jsonl_path.exists` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `traces_path.exists` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `_read_jsonl_objects` | [run_batch_experiments._read_jsonl_objects](#fn-run_batch_experiments-py-_read_jsonl_objects-450) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_read_trace_cases` | [run_batch_experiments._read_trace_cases](#fn-run_batch_experiments-py-_read_trace_cases-564) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[run_batch_experiments._run_cases_with_checkpoints](#fn-run_batch_experiments-py-_run_cases_with_checkpoints-227)

**完整源码**

~~~~python
def _read_existing_trace_cases(traces_jsonl_path: Path, traces_path: Path) -> List[Dict[str, Any]]:
    if traces_jsonl_path.exists():
        return _read_jsonl_objects(traces_jsonl_path)
    if traces_path.exists():
        return _read_trace_cases(traces_path)
    return []
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_read_jsonl_objects-450"></a>

### _read_jsonl_objects

文件：`run_batch_experiments.py:450-464`

**签名**

~~~~python
def _read_jsonl_objects(path: Path) -> List[Dict[str, Any]]:
~~~~

**功能**

读取函数。它从文件或结果对象中取出结构化数据，返回后续评估、渲染或模拟流程可直接使用的 Python 对象。

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_dedupe_rows_by_case` | [run_batch_experiments._dedupe_rows_by_case](#fn-run_batch_experiments-py-_dedupe_rows_by_case-429) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `enumerate` | 库函数或对象方法 | 内置枚举；返回 index 和元素组成的迭代器。 |
| `line.strip` | 库函数或对象方法 | 字符串去除首尾空白，返回新字符串。 |
| `rows.append` | 库函数或对象方法 | 列表追加方法；把元素加入列表末尾，返回 None。 |
| `json.loads` | 库函数或对象方法 | 把 JSON 字符串解析为 Python 对象，通常返回 dict 或 list。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |
| `ValueError` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[run_batch_experiments._read_existing_trace_cases](#fn-run_batch_experiments-py-_read_existing_trace_cases-442)

**完整源码**

~~~~python
def _read_jsonl_objects(path: Path) -> List[Dict[str, Any]]:
    rows: List[Dict[str, Any]] = []
    with path.open("r", encoding="utf-8") as handle:
        for line_number, line in enumerate(handle, start=1):
            stripped = line.strip()
            if not stripped:
                continue
            try:
                item = json.loads(stripped)
            except json.JSONDecodeError as exc:
                raise ValueError(f"Invalid JSON on line {line_number} of {path}") from exc
            if not isinstance(item, dict):
                raise ValueError(f"Expected object on line {line_number} of {path}")
            rows.append(item)
    return _dedupe_rows_by_case(rows)
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_append_jsonl_row-467"></a>

### _append_jsonl_row

文件：`run_batch_experiments.py:467-472`

**签名**

~~~~python
def _append_jsonl_row(path: Path, row: Dict[str, Any]) -> None:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `row` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `handle.write` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `handle.flush` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `os.fsync` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `handle.fileno` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `json.dumps` | 库函数或对象方法 | 把 Python 对象序列化为 JSON 字符串，返回 str。 |

**被谁调用**

[run_batch_experiments._run_cases_with_checkpoints](#fn-run_batch_experiments-py-_run_cases_with_checkpoints-227)、[run_batch_experiments._run_or_resume_baseline](#fn-run_batch_experiments-py-_run_or_resume_baseline-331)

**完整源码**

~~~~python
def _append_jsonl_row(path: Path, row: Dict[str, Any]) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    with path.open("a", encoding="utf-8") as handle:
        handle.write(json.dumps(row, ensure_ascii=False, allow_nan=False) + "\n")
        handle.flush()
        os.fsync(handle.fileno())
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_truncate_file-475"></a>

### _truncate_file

文件：`run_batch_experiments.py:475-477`

**签名**

~~~~python
def _truncate_file(path: Path) -> None:
~~~~

**功能**

项目辅助函数。它的具体行为可从参数、返回值和下方源码确认；调用关系区域列出了它依赖的内部函数和外部库函数。

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `path.write_text` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |

**被谁调用**

[run_batch_experiments._run_cases_with_checkpoints](#fn-run_batch_experiments-py-_run_cases_with_checkpoints-227)、[run_batch_experiments._run_or_resume_baseline](#fn-run_batch_experiments-py-_run_or_resume_baseline-331)

**完整源码**

~~~~python
def _truncate_file(path: Path) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text("", encoding="utf-8")
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_write_trace_cases_json-480"></a>

### _write_trace_cases_json

文件：`run_batch_experiments.py:480-483`

**签名**

~~~~python
def _write_trace_cases_json(path: Path, trace_cases: List[Dict[str, Any]]) -> None:
~~~~

**功能**

写入函数。它把结果、指标、trace 或中间 checkpoint 落盘，通常返回 None。

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `trace_cases` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `json.dump` | 库函数或对象方法 | 把 Python 对象写入文件对象为 JSON，返回 None。 |

**被谁调用**

[run_batch_experiments._run_cases_with_checkpoints](#fn-run_batch_experiments-py-_run_cases_with_checkpoints-227)

**完整源码**

~~~~python
def _write_trace_cases_json(path: Path, trace_cases: List[Dict[str, Any]]) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    with path.open("w", encoding="utf-8") as handle:
        json.dump(trace_cases, handle, ensure_ascii=False, indent=2, allow_nan=False)
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_write_partial_run_outputs-486"></a>

### _write_partial_run_outputs

文件：`run_batch_experiments.py:486-509`

**签名**

~~~~python
def _write_partial_run_outputs( *, rows: List[Dict[str, Any]], metrics_path: Path, case_metrics_path: Path, status_path: Path, run_id: str, expected_count: int, completed_count: int, last_case_id: str, llm_client: LLMClient, ) -> None:
~~~~

**功能**

写入函数。它把结果、指标、trace 或中间 checkpoint 落盘，通常返回 None。

**参数**

| 参数 | 含义 |
| --- | --- |
| `rows` | 结果行或 DataFrame 行集合。 |
| `metrics_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |
| `case_metrics_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |
| `status_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |
| `run_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `expected_count` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `completed_count` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `last_case_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `llm_client` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `evaluate_results` | [boardroom_sim.evaluation.evaluate_results](#fn-boardroom_sim-evaluation-py-evaluate_results-124) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `write_metrics_json` | [boardroom_sim.evaluation.write_metrics_json](#fn-boardroom_sim-evaluation-py-write_metrics_json-257) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `write_case_metrics_csv` | [boardroom_sim.evaluation.write_case_metrics_csv](#fn-boardroom_sim-evaluation-py-write_case_metrics_csv-266) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_write_checkpoint_status` | [run_batch_experiments._write_checkpoint_status](#fn-run_batch_experiments-py-_write_checkpoint_status-512) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[run_batch_experiments._run_cases_with_checkpoints](#fn-run_batch_experiments-py-_run_cases_with_checkpoints-227)、[run_batch_experiments._run_or_resume_baseline](#fn-run_batch_experiments-py-_run_or_resume_baseline-331)

**完整源码**

~~~~python
def _write_partial_run_outputs(
    *,
    rows: List[Dict[str, Any]],
    metrics_path: Path,
    case_metrics_path: Path,
    status_path: Path,
    run_id: str,
    expected_count: int,
    completed_count: int,
    last_case_id: str,
    llm_client: LLMClient,
) -> None:
    metrics = evaluate_results(rows)
    write_metrics_json(metrics_path, metrics)
    write_case_metrics_csv(case_metrics_path, metrics["case_metrics"])
    _write_checkpoint_status(
        status_path,
        run_id=run_id,
        expected_count=expected_count,
        completed_count=completed_count,
        last_case_id=last_case_id,
        llm_client=llm_client,
        status="running",
    )
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_write_checkpoint_status-512"></a>

### _write_checkpoint_status

文件：`run_batch_experiments.py:512-537`

**签名**

~~~~python
def _write_checkpoint_status( path: Path, *, run_id: str, expected_count: int, completed_count: int, last_case_id: str, llm_client: LLMClient, status: str, ) -> None:
~~~~

**功能**

写入函数。它把结果、指标、trace 或中间 checkpoint 落盘，通常返回 None。

**参数**

| 参数 | 含义 |
| --- | --- |
| `path` | 文件路径对象。 |
| `run_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `expected_count` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `completed_count` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `last_case_id` | 业务对象标识符，用于关联案例、公司、交易或投资人。 |
| `llm_client` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `status` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `path.parent.mkdir` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `datetime.now.isoformat` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `max` | 库函数或对象方法 | 内置最大值函数；返回最大元素。 |
| `path.open` | 库函数或对象方法 | pathlib 路径方法；用于路径拼接、检测、读写或遍历。 |
| `json.dump` | 库函数或对象方法 | 把 Python 对象写入文件对象为 JSON，返回 None。 |
| `handle.write` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `datetime.now` | 库函数或对象方法 | 返回当前本地时间 datetime。 |

**被谁调用**

[run_batch_experiments._run_cases_with_checkpoints](#fn-run_batch_experiments-py-_run_cases_with_checkpoints-227)、[run_batch_experiments._run_or_resume_baseline](#fn-run_batch_experiments-py-_run_or_resume_baseline-331)、[run_batch_experiments._write_partial_run_outputs](#fn-run_batch_experiments-py-_write_partial_run_outputs-486)

**完整源码**

~~~~python
def _write_checkpoint_status(
    path: Path,
    *,
    run_id: str,
    expected_count: int,
    completed_count: int,
    last_case_id: str,
    llm_client: LLMClient,
    status: str,
) -> None:
    payload = {
        "updated_at": datetime.now().isoformat(timespec="seconds"),
        "run_id": run_id,
        "status": status,
        "expected_count": expected_count,
        "completed_count": completed_count,
        "remaining_count": max(0, expected_count - completed_count),
        "last_case_id": last_case_id,
        "total_tokens": llm_client.total_tokens,
        "total_prompt_tokens": llm_client.total_prompt_tokens,
        "total_completion_tokens": llm_client.total_completion_tokens,
    }
    path.parent.mkdir(parents=True, exist_ok=True)
    with path.open("w", encoding="utf-8") as handle:
        json.dump(payload, handle, ensure_ascii=False, indent=2, allow_nan=False)
        handle.write("\n")
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_write_debate_accuracy_report_from_results-540"></a>

### _write_debate_accuracy_report_from_results

文件：`run_batch_experiments.py:540-547`

**签名**

~~~~python
def _write_debate_accuracy_report_from_results( run_dir: Path, results: List[SimulationResult], deal_size_tolerance: float, baseline_rows_by_name: Dict[str, List[Dict[str, Any]]] | None = None, ) -> None:
~~~~

**功能**

写入函数。它把结果、指标、trace 或中间 checkpoint 落盘，通常返回 None。

**参数**

| 参数 | 含义 |
| --- | --- |
| `run_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `results` | SimulationResult 或基线预测结果集合。 |
| `deal_size_tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |
| `baseline_rows_by_name` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_write_debate_accuracy_report` | [run_batch_experiments._write_debate_accuracy_report](#fn-run_batch_experiments-py-_write_debate_accuracy_report-569) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `result.to_dict` | [boardroom_sim.models.BoardCase.to_dict](#fn-boardroom_sim-models-py-boardcase-to_dict-250) (函数)<br>[boardroom_sim.models.RolePolicy.to_dict](#fn-boardroom_sim-models-py-rolepolicy-to_dict-291) (函数)<br>[boardroom_sim.models.RoleDecision.to_dict](#fn-boardroom_sim-models-py-roledecision-to_dict-312) (函数)<br>[boardroom_sim.models.TermSheetProposal.to_dict](#fn-boardroom_sim-models-py-termsheetproposal-to_dict-330) (函数)<br>[boardroom_sim.models.SimulationResult.to_dict](#fn-boardroom_sim-models-py-simulationresult-to_dict-355) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def _write_debate_accuracy_report_from_results(
    run_dir: Path,
    results: List[SimulationResult],
    deal_size_tolerance: float,
    baseline_rows_by_name: Dict[str, List[Dict[str, Any]]] | None = None,
) -> None:
    cases = [result.to_dict(include_trace=True) for result in results]
    _write_debate_accuracy_report(run_dir, cases, deal_size_tolerance, baseline_rows_by_name=baseline_rows_by_name)
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_write_debate_accuracy_report_from_trace_file-550"></a>

### _write_debate_accuracy_report_from_trace_file

文件：`run_batch_experiments.py:550-561`

**签名**

~~~~python
def _write_debate_accuracy_report_from_trace_file( run_dir: Path, traces_path: Path, deal_size_tolerance: float, baseline_rows_by_name: Dict[str, List[Dict[str, Any]]] | None = None, ) -> None:
~~~~

**功能**

写入函数。它把结果、指标、trace 或中间 checkpoint 落盘，通常返回 None。

**参数**

| 参数 | 含义 |
| --- | --- |
| `run_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `traces_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |
| `deal_size_tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |
| `baseline_rows_by_name` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `_write_debate_accuracy_report` | [run_batch_experiments._write_debate_accuracy_report](#fn-run_batch_experiments-py-_write_debate_accuracy_report-569) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `_read_trace_cases` | [run_batch_experiments._read_trace_cases](#fn-run_batch_experiments-py-_read_trace_cases-564) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def _write_debate_accuracy_report_from_trace_file(
    run_dir: Path,
    traces_path: Path,
    deal_size_tolerance: float,
    baseline_rows_by_name: Dict[str, List[Dict[str, Any]]] | None = None,
) -> None:
    _write_debate_accuracy_report(
        run_dir,
        _read_trace_cases(traces_path),
        deal_size_tolerance,
        baseline_rows_by_name=baseline_rows_by_name,
    )
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_read_trace_cases-564"></a>

### _read_trace_cases

文件：`run_batch_experiments.py:564-566`

**签名**

~~~~python
def _read_trace_cases(traces_path: Path) -> List[Dict[str, Any]]:
~~~~

**功能**

读取函数。它从文件或结果对象中取出结构化数据，返回后续评估、渲染或模拟流程可直接使用的 Python 对象。

**参数**

| 参数 | 含义 |
| --- | --- |
| `traces_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |

**返回值**

返回 `List[Dict[str, Any]]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `json.loads` | 库函数或对象方法 | 把 JSON 字符串解析为 Python 对象，通常返回 dict 或 list。 |
| `traces_path.read_text` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `isinstance` | 库函数或对象方法 | 内置类型检查；返回 bool。 |

**被谁调用**

[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)、[run_batch_experiments._read_existing_trace_cases](#fn-run_batch_experiments-py-_read_existing_trace_cases-442)、[run_batch_experiments._write_debate_accuracy_report_from_trace_file](#fn-run_batch_experiments-py-_write_debate_accuracy_report_from_trace_file-550)

**完整源码**

~~~~python
def _read_trace_cases(traces_path: Path) -> List[Dict[str, Any]]:
    data = json.loads(traces_path.read_text(encoding="utf-8"))
    return data if isinstance(data, list) else [data]
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_write_debate_accuracy_report-569"></a>

### _write_debate_accuracy_report

文件：`run_batch_experiments.py:569-590`

**签名**

~~~~python
def _write_debate_accuracy_report( run_dir: Path, cases: List[Dict[str, Any]], deal_size_tolerance: float, *, baseline_rows_by_name: Dict[str, List[Dict[str, Any]]] | None = None, ) -> None:
~~~~

**功能**

写入函数。它把结果、指标、trace 或中间 checkpoint 落盘，通常返回 None。

**参数**

| 参数 | 含义 |
| --- | --- |
| `run_dir` | 目录路径参数，用于读取配置或写出结果。 |
| `cases` | 多个 BoardCase 或 trace case，用于批量运行或统计。 |
| `deal_size_tolerance` | 误差容忍阈值，用于判断数值变化或预测是否可接受。 |
| `baseline_rows_by_name` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `run_dir.mkdir` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `analyze_cases` | [analyze_debate_accuracy.analyze_cases](#fn-analyze_debate_accuracy-py-analyze_cases-163) (函数)<br>[analyze_debate_changes.analyze_cases](#fn-analyze_debate_changes-py-analyze_cases-161) (函数) | 项目内同名候选；实际调用由对象类型或导入绑定决定，表中列出可跳转的候选说明。 |
| `render_debate_accuracy_report` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `report_path.write_text` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `write_case_stage_csv` | [analyze_debate_accuracy.write_case_stage_csv](#fn-analyze_debate_accuracy-py-write_case_stage_csv-568) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |
| `analyze_baseline_rows` | [analyze_debate_accuracy.analyze_baseline_rows](#fn-analyze_debate_accuracy-py-analyze_baseline_rows-180) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `items` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |

**被谁调用**

[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)、[run_batch_experiments._write_debate_accuracy_report_from_results](#fn-run_batch_experiments-py-_write_debate_accuracy_report_from_results-540)、[run_batch_experiments._write_debate_accuracy_report_from_trace_file](#fn-run_batch_experiments-py-_write_debate_accuracy_report_from_trace_file-550)

**完整源码**

~~~~python
def _write_debate_accuracy_report(
    run_dir: Path,
    cases: List[Dict[str, Any]],
    deal_size_tolerance: float,
    *,
    baseline_rows_by_name: Dict[str, List[Dict[str, Any]]] | None = None,
) -> None:
    run_dir.mkdir(parents=True, exist_ok=True)
    analysis = analyze_cases(cases, deal_size_tolerance=deal_size_tolerance)
    baseline_metrics = {
        name: analyze_baseline_rows(rows, baseline_name=name, deal_size_tolerance=deal_size_tolerance)
        for name, rows in (baseline_rows_by_name or {}).items()
    }
    report = render_debate_accuracy_report(
        analysis,
        deal_size_tolerance=deal_size_tolerance,
        baseline_metrics=baseline_metrics,
    )
    report_path = run_dir / "debate_accuracy_report.md"
    report_path.write_text(report + "\n", encoding="utf-8")
    write_case_stage_csv(run_dir / "debate_accuracy_cases.csv", analysis["case_stage_rows"])
    print(f"Wrote debate accuracy report to {report_path}.")
~~~~

[回到顶部](#top)

<a id="fn-run_batch_experiments-py-_write_manifest-593"></a>

### _write_manifest

文件：`run_batch_experiments.py:593-629`

**签名**

~~~~python
def _write_manifest( output_path: Path, args: argparse.Namespace, cases: List[BoardCase], llm_config: LLMConfig, experiment_config: ExperimentConfig, ) -> None:
~~~~

**功能**

写入函数。它把结果、指标、trace 或中间 checkpoint 落盘，通常返回 None。

**参数**

| 参数 | 含义 |
| --- | --- |
| `output_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |
| `args` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `cases` | 多个 BoardCase 或 trace case，用于批量运行或统计。 |
| `llm_config` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `experiment_config` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `output_path.parent.mkdir` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `datetime.now.isoformat` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `str` | 库函数或对象方法 | 内置类型转换；返回字符串表示。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |
| `experiment_config.to_manifest_dict` | [boardroom_sim.config.ExperimentConfig.to_manifest_dict](#fn-boardroom_sim-config-py-experimentconfig-to_manifest_dict-175) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `output_path.open` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `json.dump` | 库函数或对象方法 | 把 Python 对象写入文件对象为 JSON，返回 None。 |
| `bool` | 库函数或对象方法 | 内置类型转换；返回布尔值。 |
| `datetime.now` | 库函数或对象方法 | 返回当前本地时间 datetime。 |

**被谁调用**

[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)

**完整源码**

~~~~python
def _write_manifest(
    output_path: Path,
    args: argparse.Namespace,
    cases: List[BoardCase],
    llm_config: LLMConfig,
    experiment_config: ExperimentConfig,
) -> None:
    payload = {
        "created_at": datetime.now().isoformat(timespec="seconds"),
        "input": str(args.input),
        "output_dir": str(args.output_dir),
        "case_count": len(cases),
        "case_limit": args.case_limit,
        "history_limit": args.history_limit,
        "repeats": args.repeats,
        "bargaining_rounds": args.bargaining_rounds,
        "experiment_config": experiment_config.to_manifest_dict(),
        "resume": args.resume,
        "skip_traces": args.skip_traces,
        "skip_baselines": args.skip_baselines,
        "skip_debate_accuracy_report": args.skip_debate_accuracy_report,
        "deal_size_tolerance": args.deal_size_tolerance,
        "llm": {
            "api_key_env": args.api_key_env,
            "api_key_passed": bool(args.api_key),
            "model": llm_config.model,
            "base_url": llm_config.base_url,
            "temperature": llm_config.temperature,
            "timeout_seconds": llm_config.timeout_seconds,
            "http_max_retries": llm_config.http_max_retries,
            "retry_base_seconds": llm_config.retry_base_seconds,
        },
        "case_ids": [case.case_id for case in cases],
    }
    output_path.parent.mkdir(parents=True, exist_ok=True)
    with output_path.open("w", encoding="utf-8") as handle:
        json.dump(payload, handle, ensure_ascii=False, indent=2, allow_nan=False)
~~~~

[回到顶部](#top)

<a id="module-run_experiment-py"></a>

## run_experiment.py

单次实验脚本：适合小规模调试，从输入案例构造模拟器并写出 results/traces。

**类**：无类定义

<a id="fn-run_experiment-py-parse_args-17"></a>

### parse_args

文件：`run_experiment.py:17-45`

**签名**

~~~~python
def parse_args() -> argparse.Namespace:
~~~~

**功能**

源码文档说明：Parse Linux-friendly command-line arguments for one experiment run.

**参数**

无显式参数。

**返回值**

返回 `argparse.Namespace` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `argparse.ArgumentParser` | 库函数或对象方法 | 构造命令行参数解析器，返回 ArgumentParser。 |
| `parser.add_argument` | 库函数或对象方法 | 外部库函数或对象方法；当前函数调用它取得中间结果、执行 IO 或完成对象方法调用。 |
| `parser.parse_args` | [run_experiment.parse_args](#fn-run_experiment-py-parse_args-17) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[run_experiment.parse_args](#fn-run_experiment-py-parse_args-17)、[run_experiment.main](#fn-run_experiment-py-main-78)

**完整源码**

~~~~python
def parse_args() -> argparse.Namespace:
    """Parse Linux-friendly command-line arguments for one experiment run."""
    parser = argparse.ArgumentParser(description="Run the boardroom simulation MVP.")
    parser.add_argument(
        "--input",
        default="input/260524_02_03_pitchbook_sample_100_shared.xlsx",
        type=Path,
        help="Path to input JSONL cases or PitchBook XLSX workbook.",
    )
    parser.add_argument("--output", type=Path, required=True, help="Path to compact output JSONL results.")
    parser.add_argument("--trace-output", type=Path, required=True, help="Path to detailed trace JSON output.")
    parser.add_argument("--config", type=Path, default=None, help="Path to experiment TOML config.")
    parser.add_argument("--bargaining-rounds", type=int, default=None, help="Override bargaining rounds from config.")
    parser.add_argument("--case-limit", type=int, default=None, help="Optional maximum number of input cases to run.")
    parser.add_argument(
        "--history-limit",
        type=int,
        default=None,
        help="Override config history limit. Use -1 for all history.",
    )
    parser.add_argument("--api-key", default=None, help="API key. Defaults to BOARDROOM_LLM_API_KEY.")
    parser.add_argument("--api-key-env", default="BOARDROOM_LLM_API_KEY", help="Environment variable containing API key.")
    parser.add_argument("--model", default="GLM-4-Flash-250414", help="LLM model name. Defaults to GLM-4-Flash-250414.")
    parser.add_argument("--base-url", default="https://api.z.ai/api/paas/v4", help="OpenAI-compatible base URL. Defaults to zhipuai.")
    parser.add_argument("--temperature", type=float, default=None, help="LLM temperature. Defaults to BOARDROOM_LLM_TEMPERATURE or 0.2.")
    parser.add_argument("--timeout-seconds", type=int, default=None, help="HTTP timeout. Defaults to BOARDROOM_LLM_TIMEOUT_SECONDS or 120.")
    parser.add_argument("--http-max-retries", type=int, default=None, help="HTTP retry count for transient LLM errors. Defaults to BOARDROOM_LLM_HTTP_MAX_RETRIES or 6.")
    parser.add_argument("--retry-base-seconds", type=float, default=None, help="Base seconds for exponential retry backoff. Defaults to BOARDROOM_LLM_RETRY_BASE_SECONDS or 2.0.")
    return parser.parse_args()
~~~~

[回到顶部](#top)

<a id="fn-run_experiment-py-load_cases-48"></a>

### load_cases

文件：`run_experiment.py:48-57`

**签名**

~~~~python
def load_cases( input_path: Path, case_limit: int | None = None, history_limit: int | None = 10, ) -> List[BoardCase]:
~~~~

**功能**

源码文档说明：Load cases from either normalized JSONL or the PitchBook workbook.

**参数**

| 参数 | 含义 |
| --- | --- |
| `input_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |
| `case_limit` | 数量上限，用于截断历史记录、案例数量或输出条目。 |
| `history_limit` | 数量上限，用于截断历史记录、案例数量或输出条目。 |

**返回值**

返回 `List[BoardCase]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `read_cases_jsonl` | [boardroom_sim.io.read_cases_jsonl](#fn-boardroom_sim-io-py-read_cases_jsonl-12) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `input_path.suffix.lower` | 库函数或对象方法 | 字符串转小写，返回新字符串。 |
| `build_cases_from_pitchbook` | [boardroom_sim.pitchbook.build_cases_from_pitchbook](#fn-boardroom_sim-pitchbook-py-build_cases_from_pitchbook-31) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[run_batch_experiments.main](#fn-run_batch_experiments-py-main-79)、[run_experiment.run_experiment](#fn-run_experiment-py-run_experiment-60)

**完整源码**

~~~~python
def load_cases(
    input_path: Path,
    case_limit: int | None = None,
    history_limit: int | None = 10,
) -> List[BoardCase]:
    """Load cases from either normalized JSONL or the PitchBook workbook."""
    if input_path.suffix.lower() in {".xlsx", ".xlsm"}:
        return build_cases_from_pitchbook(input_path, limit=case_limit, history_limit=history_limit)
    cases = read_cases_jsonl(input_path)
    return cases[:case_limit] if case_limit is not None else cases
~~~~

[回到顶部](#top)

<a id="fn-run_experiment-py-run_experiment-60"></a>

### run_experiment

文件：`run_experiment.py:60-75`

**签名**

~~~~python
def run_experiment( input_path: Path, bargaining_rounds: int, llm_client: LLMClient, case_limit: int | None = None, history_limit: int | None = 10, experiment_config: ExperimentConfig | None = None, ) -> List[SimulationResult]:
~~~~

**功能**

源码文档说明：Load cases and run the boardroom simulator for each case.

**参数**

| 参数 | 含义 |
| --- | --- |
| `input_path` | 文件路径参数，通常是 pathlib.Path 或可转换为路径的值。 |
| `bargaining_rounds` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `llm_client` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |
| `case_limit` | 数量上限，用于截断历史记录、案例数量或输出条目。 |
| `history_limit` | 数量上限，用于截断历史记录、案例数量或输出条目。 |
| `experiment_config` | 函数局部输入参数；结合签名和源码可看到它如何参与计算。 |

**返回值**

返回 `List[SimulationResult]` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `load_cases` | [run_experiment.load_cases](#fn-run_experiment-py-load_cases-48) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `BoardroomSimulator` | [boardroom_sim.simulator.BoardroomSimulator](#class-boardroom_sim-simulator-py-boardroomsimulator-29) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `simulator.simulate` | [boardroom_sim.simulator.BoardroomSimulator.simulate](#fn-boardroom_sim-simulator-py-boardroomsimulator-simulate-64) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |

**被谁调用**

[run_experiment.main](#fn-run_experiment-py-main-78)

**完整源码**

~~~~python
def run_experiment(
    input_path: Path,
    bargaining_rounds: int,
    llm_client: LLMClient,
    case_limit: int | None = None,
    history_limit: int | None = 10,
    experiment_config: ExperimentConfig | None = None,
) -> List[SimulationResult]:
    """Load cases and run the boardroom simulator for each case."""
    cases = load_cases(input_path, case_limit=case_limit, history_limit=history_limit)
    simulator = BoardroomSimulator(
        llm_client=llm_client,
        bargaining_rounds=bargaining_rounds,
        config=experiment_config,
    )
    return [simulator.simulate(case) for case in cases]
~~~~

[回到顶部](#top)

<a id="fn-run_experiment-py-main-78"></a>

### main

文件：`run_experiment.py:78-110`

**签名**

~~~~python
def main() -> None:
~~~~

**功能**

源码文档说明：Run the command-line experiment and write both compact and trace outputs.

**参数**

无显式参数。

**返回值**

返回 `None` 类型或兼容该注解的对象。

**调用的函数**

| 调用名 | 目标 | 作用与返回 |
| --- | --- | --- |
| `parse_args` | [run_experiment.parse_args](#fn-run_experiment-py-parse_args-17) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `load_experiment_config` | [boardroom_sim.config.load_experiment_config](#fn-boardroom_sim-config-py-load_experiment_config-185) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `LLMConfig.from_env` | [boardroom_sim.llm.LLMConfig.from_env](#fn-boardroom_sim-llm-py-llmconfig-from_env-44) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `LLMClient` | [boardroom_sim.llm.LLMClient](#class-boardroom_sim-llm-py-llmclient-95) (类) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `run_experiment` | [run_experiment.run_experiment](#fn-run_experiment-py-run_experiment-60) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `write_results_jsonl` | [boardroom_sim.io.write_results_jsonl](#fn-boardroom_sim-io-py-write_results_jsonl-36) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `write_traces_json` | [boardroom_sim.io.write_traces_json](#fn-boardroom_sim-io-py-write_traces_json-44) (函数) | 项目内调用；点击链接可跳到对应函数或类说明。 |
| `print` | 库函数或对象方法 | 内置输出函数；把文本写到控制台，返回 None。 |
| `len` | 库函数或对象方法 | 内置长度函数；返回序列、字典或集合的元素数量。 |

**被谁调用**

未发现项目内其他函数直接调用；可能由命令行入口、类实例化或外部调用触发。

**完整源码**

~~~~python
def main() -> None:
    """Run the command-line experiment and write both compact and trace outputs."""
    args = parse_args()
    experiment_config = load_experiment_config(args.config)
    bargaining_rounds = (
        args.bargaining_rounds if args.bargaining_rounds is not None else experiment_config.bargaining_rounds
    )
    history_limit = args.history_limit if args.history_limit is not None else experiment_config.history_limit
    llm_config = LLMConfig.from_env(
        api_key=args.api_key,
        api_key_env=args.api_key_env,
        model=args.model,
        base_url=args.base_url,
        temperature=args.temperature,
        timeout_seconds=args.timeout_seconds,
        http_max_retries=args.http_max_retries,
        retry_base_seconds=args.retry_base_seconds,
    )
    llm_client = LLMClient(llm_config)
    results = run_experiment(
        args.input,
        bargaining_rounds,
        llm_client,
        case_limit=args.case_limit,
        history_limit=history_limit,
        experiment_config=experiment_config,
    )
    write_results_jsonl(args.output, results)
    write_traces_json(args.trace_output, results)
    print(f"Processed {len(results)} cases.")
    print(f"Wrote compact results to {args.output}.")
    print(f"Wrote detailed traces to {args.trace_output}.")
    print(f"LLM total tokens reported by provider: {llm_client.total_tokens}.")
~~~~

[回到顶部](#top)

<a id="text-files"></a>

## 配置与 Prompt 文件

这些文件不是 Python 函数，但它们决定实验变量、角色策略和 LLM 行为。教程保留全文，方便直接修改。

<a id="file-configs-boardroom_default-toml"></a>

## configs/boardroom_default.toml

默认实验配置。这里是后续最常修改的位置：输入文件、输出目录、LLM、角色顺序、角色权重、讨论轮数、archetype、L5 文化层和可见历史字段都在这里控制。

~~~~toml
[experiment]
name = "boardroom_default"
bargaining_rounds = 3
history_limit = 10

[paths]
role_policy_dir = "policies/roles"
prompts_dir = "prompts"

[agents]
role_order = [
  "Founder_CEO",
  "CTO",
  "Lead_VC_Director",
  "Followon_VC_Director",
]

[board]
# 可选值：aunt, barbarian, clan, value_creating。
# archetype 会默认带出 L5 文化层、讨论流程、主导模式、挑战要求、记忆范围和角色权重乘数。
archetype = "value_creating"

# 如需覆盖 archetype 的默认 L5 文化层，可取消注释并只改需要的维度。
# [board.culture]
# criticality = 0.75
# creativity = 0.70
# cohesiveness = 0.70
# openness_generosity = 0.80
# preparation_involvement = 0.85
#
# 如需覆盖 archetype 的默认角色影响力乘数，可取消注释。
# [board.role_weight_multipliers]
# Founder_CEO = 1.0
# CTO = 1.0
# Lead_VC_Director = 1.0
# Followon_VC_Director = 1.0

[discussion]
paradigm = "memory"
response_generator = "critical"
# protocol、dominance_pattern、challenge_required、memory_scope、memory_window
# 默认由 board.archetype 决定；只在做消融实验时显式覆盖。
round_tasks = [
  "第 1 轮：按当前会议阶段暴露你的角色立场、核心证据锚点和对当前融资路径的第一反应。",
  "第 2 轮：回应此前至少一条具体观点，提出质询、替代融资情境或条款修正，并说明它对角色立场的影响。",
  "第 3 轮：整合争议，形成该角色愿意支持、反对、保留或要求修改的最终会议立场，并同步内部预测。",
]

[decision]
protocol = "weighted_vote"

[decision.weights]
Founder_CEO = 2.0
CTO = 0.5
Lead_VC_Director = 3.0
Followon_VC_Director = 1.5
~~~~

[回到顶部](#top)

<a id="file-one_case-sh"></a>

## one_case.sh

单案例运行脚本示例，便于快速 smoke test。

~~~~bash
python3 - <<'PY'
import json
import pandas as pd
from pathlib import Path
from boardroom_sim.pitchbook import build_cases_from_pitchbook

xlsx = Path("input/260524_02_03_pitchbook_sample_100_shared.xlsx")
case_id = "11830986_789374083T"

cases = build_cases_from_pitchbook(xlsx)
case = next(c for c in cases if c.case_id == case_id)

company_id = case.company_id
target_deal_id = case.target_deal_id
prior_deal_id = case.notes.get("prior_dealid", "")

tables = pd.read_excel(xlsx, sheet_name=None)

deal = tables["deal"].copy()
company = tables["company"].copy()

target_deal_row = deal[deal["dealid"].astype(str) == target_deal_id]
prior_deal_row = deal[deal["dealid"].astype(str) == prior_deal_id] if prior_deal_id else pd.DataFrame()
company_row = company[company["companyid"].astype(str) == company_id]

output = {
    "case_id": case_id,
    "boardcase_generated_fields": case.to_dict(),
    "raw_target_deal_row": target_deal_row.to_dict(orient="records"),
    "raw_prior_deal_row": prior_deal_row.to_dict(orient="records"),
    "raw_company_row": company_row.to_dict(orient="records"),
}

Path("outputs/one_case_raw_fields.json").write_text(
    json.dumps(output, ensure_ascii=False, indent=2, default=str),
    encoding="utf-8",
)

print("已导出到 outputs/one_case_raw_fields.json")
print("companyid =", company_id)
print("target_dealid =", target_deal_id)
print("prior_dealid =", prior_deal_id)
PY
~~~~

[回到顶部](#top)

<a id="file-policies-roles-cto-toml"></a>

## policies/roles/cto.toml

角色策略配置。每个 TOML 文件定义一个董事会角色的目标、可见字段、风险偏好、谈判杠杆和行为风格。

~~~~toml
role_name = "CTO"

layer_1_goals = [
  "优先级 1：保护工程团队健康、招聘预算和技术债预算。",
  "优先级 2：保护技术可行性和产品里程碑交付能力。",
  "优先级 3：保护个人技术声誉和技术决策权范围。",
]

layer_2_attention_fields = [
  "prior_deal_size_usd_m",
  "prior_company_deal_history",
  "prior_raised_to_date_usd_m",
  "employee_count_at_decision",
  "previous_employee_count",
  "employee_growth_rate",
  "engineering_role_count",
  "executive_role_count",
  "primary_industry",
  "industry_group",
  "industry_sector",
  "verticals",
  "keywords",
  "description",
  "company_age_at_decision_years",
]

layer_2_ignored_fields = [
  "当前目标 term sheet 的真实金额、估值、清算优先权和反稀释条款。",
  "投资人组合表现。",
  "除非直接影响工程招聘，否则忽略公司地理位置或城市分布。",
]

layer_3_heuristics = [
  "融资后工程资源占比不应明显下降；技术能力收缩是反对或缩小融资规模的理由。",
  "没有清晰证据时，技术路线自主权不应被投资人或非技术董事覆盖。",
  "招聘和技术债预算应优先于销售、市场或行政扩张得到保护。",
  "短期财务目标不应迫使团队做不合理的架构捷径。",
  "未经验证的技术突破不应被直接用于证明大规模融资合理。",
]

[layer_4_interaction_protocol]
speaking_order = 2
board_vote = "通常没有直接董事会投票权，除非同时也是创始人；主要通过技术建议影响决策。"
information_behavior = "在受托责任范围内披露技术债、招聘困难和架构风险。"
stickiness = "对 headcount 增长、基础设施投入和技术承诺的立场粘性高。"
~~~~

[回到顶部](#top)

<a id="file-policies-roles-followon_vc_director-toml"></a>

## policies/roles/followon_vc_director.toml

角色策略配置。每个 TOML 文件定义一个董事会角色的目标、可见字段、风险偏好、谈判杠杆和行为风格。

~~~~toml
role_name = "Followon_VC_Director"

layer_1_goals = [
  "优先级 1：保护 pro-rata 权利，避免被领投方稀释。",
  "优先级 2：在小于领投方、更加分散的风险暴露下保护投资 IRR。",
  "优先级 3：维护组合分散性，以及与优质领投方共同投资的信号价值。",
]

layer_2_attention_fields = [
  "prior_deal_size_usd_m",
  "prior_followon_investor_deal_history",
  "prior_post_money_valuation_usd_m",
  "prior_valuation_markup_multiple",
  "prior_valuation_direction_label",
  "prior_investor_ownership_pct",
  "prior_investor_count",
  "prior_followon_investor_count",
  "prior_lead_investor_count",
  "prior_lead_investor_types",
  "prior_lead_investor_amount_share",
  "prior_lead_preferred_deal_size_min_usd_m",
  "prior_lead_preferred_deal_size_max_usd_m",
  "employee_growth_rate",
  "similar_company_count",
]

layer_2_ignored_fields = [
  "产品和架构细节。",
  "应由领投方起草的完整 term sheet 条款细节。",
  "当前目标交易的隐藏真实标签。",
  "可能泄露未来状态的 PitchBook 快照公司状态字段。",
]

layer_3_heuristics = [
  "已有投资后，默认倾向跟投；不跟投需要明确理由，例如估值恶化、公司信号变差或基金时点不匹配。",
  "当不跟投会明显稀释所有权时，pro-rata 稀释保护优先于完美条款。",
  "跟投董事可以抵制损害跟投方所有权或清算优先级的领投条款，但不应主导定价。",
  "领投方声誉是关键信号；领投历史弱或本轮投入弱，会降低跟投意愿。",
  "单笔 ticket size 和行业集中度边界不能因为单个机会轻易突破。",
]

[layer_4_interaction_protocol]
speaking_order = 4
board_vote = "通常 0-1 个董事席位；股权常见 3-10%。"
information_behavior = "可以访问公司披露和领投方共享信息，但独立市场情报更弱。"
stickiness = "已有投资后继续跟投是默认选择；拒绝跟投必须给出明确理由。"
~~~~

[回到顶部](#top)

<a id="file-policies-roles-founder_ceo-toml"></a>

## policies/roles/founder_ceo.toml

角色策略配置。每个 TOML 文件定义一个董事会角色的目标、可见字段、风险偏好、谈判杠杆和行为风格。

~~~~toml
role_name = "Founder_CEO"

layer_1_goals = [
  "优先级 1：最大化公司长期价值和退出价值，而不是只追求下一轮估值标记。",
  "优先级 2：尽可能通过投票影响力和董事会席位保持创始人控制权。",
  "优先级 3：保持经营执行自由，减少重大经营事项上的 veto 触发。",
]

layer_2_attention_fields = [
  "prior_deal_size_usd_m",
  "prior_company_deal_history",
  "prior_post_money_valuation_usd_m",
  "prior_valuation_markup_multiple",
  "prior_valuation_direction_label",
  "prior_deal_type",
  "prior_vc_round",
  "prior_raised_to_date_usd_m",
  "prior_investor_ownership_pct",
  "prior_lead_investor_count",
  "prior_lead_investor_types",
  "employee_count_at_decision",
  "employee_growth_rate",
  "founder_count",
  "current_ceo_is_founder",
  "board_member_count",
  "investor_board_member_count",
]

layer_2_ignored_fields = [
  "当前目标交易的真实金额、类型、轮次、估值和完成状态。",
  "投资人内部 AUM 和完整基金组合指标。",
  "与创始人控制权和融资条款关系不强的横向可比公司估值细节。",
]

layer_3_heuristics = [
  "当条款威胁投票权、董事会多数或重大 veto 权时，控制权底线优先于估值优化。",
  "历史估值 markup 是重要状态信号；弱 markup 或 down/flat round 会提高下一轮融资的政治成本。",
  "只有当资本能明显提高增长资源、战略价值或生存概率时，稀释才值得接受。",
  "现金压力允许更灵活妥协，但生存型让步仍应保护核心创始人控制权。",
  "对员工、早期投资人、客户、产品方向和里程碑的公开承诺具有粘性，不能轻易反转。",
]

[layer_4_interaction_protocol]
speaking_order = 1
board_vote = "创始人股权影响力；早期可能 30-50%，多轮融资后常降至 10-25%。"
information_behavior = "介绍公司需求，并选择性披露经营上行空间和内部压力。"
stickiness = "高；创始人一旦表态，后续让步应明确且成本高。"
~~~~

[回到顶部](#top)

<a id="file-policies-roles-lead_vc_director-toml"></a>

## policies/roles/lead_vc_director.toml

角色策略配置。每个 TOML 文件定义一个董事会角色的目标、可见字段、风险偏好、谈判杠杆和行为风格。

~~~~toml
role_name = "Lead_VC_Director"

layer_1_goals = [
  "优先级 1：最大化基金退出倍数，并维持领投方定价纪律。",
  "优先级 2：争取有利融资条款，包括下行保护、反稀释和董事席位。",
  "优先级 3：保护组合公司的 markup 信号价值，以利于未来募资。",
]

layer_2_attention_fields = [
  "prior_post_money_valuation_usd_m",
  "prior_lead_investor_deal_history",
  "prior_pre_money_valuation_usd_m",
  "prior_valuation_markup_multiple",
  "prior_valuation_direction_label",
  "prior_deal_size_usd_m",
  "prior_raised_to_date_usd_m",
  "prior_investor_ownership_pct",
  "prior_investor_count",
  "prior_new_investor_count",
  "prior_followon_investor_count",
  "prior_lead_investor_count",
  "prior_lead_investor_amount_share",
  "prior_lead_preferred_deal_size_min_usd_m",
  "prior_lead_preferred_deal_size_max_usd_m",
  "prior_lead_preferred_company_valuation_min_usd_m",
  "prior_lead_preferred_company_valuation_max_usd_m",
  "employee_growth_rate",
  "similar_company_count",
  "board_member_count",
  "investor_board_member_count",
]

layer_2_ignored_fields = [
  "属于 CTO 范围的具体产品架构细节。",
  "低于董事会信号质量的员工细节分析。",
  "当前目标交易的隐藏真实标签。",
  "可能泄露未来状态的 PitchBook 快照公司状态字段。",
]

layer_3_heuristics = [
  "风险高时，下行保护应优先于上行叙事。",
  "估值合理性比估值数字本身更重要；应结合历史 markup、行业环境和经营信号判断。",
  "基金集中度纪律不能因为单个吸引人的机会轻易突破。",
  "下一轮融资路径必须清晰；路径不清晰时应要求更强保护或更低估值。",
  "融资风险上升时，应通过董事席位、信息权或保护性条款维持治理影响力。",
]

[layer_4_interaction_protocol]
speaking_order = 3
board_vote = "通常 1-2 个董事席位，可能另有 observer；股权常见 10-25%。"
information_behavior = "可以引用公司披露和外部市场情报。"
stickiness = "中高；受 LP 承诺、行业聚焦和阶段授权约束。"
~~~~

[回到顶部](#top)

<a id="file-prompts-bargaining_step-md"></a>

## prompts/bargaining_step.md

LLM prompt 模板。代码会用 `.format(...)` 注入案例、角色、历史、流程和 L5 文化变量。

~~~~markdown
你现在扮演的角色是：$role_name。

严格的四层角色规则：
$role_policy_json

仅该角色可见的决策前字段：
$observed_fields_json

当前聚合提案：
$proposal_json

你当前的预测：
$current_decision_json

董事会上下文：
$context_payload_json

此前的辩论发言：
$prior_messages_json

当前董事会过程约束：
$process_context_json

其中，l5_culture / culture 是 L5 董事会整体文化层，不是事实证据，也不是单个角色偏好。它约束你在本轮如何提出异议、吸收他人证据、维护凝聚或坚持批判；board_archetype_description 说明当前董事会类型的整体互动风格。

数据处理规则：

- JSON null 表示该值缺失或未观测到，不要把 null 理解为 0。
- 除非 PitchBook 快照状态字段出现在你的可见字段中，否则应视为不可用。
- 当前目标交易的真实结果被隐藏，不能假设、推断或泄露隐藏标签。

证据使用纪律：

- 当前聚合提案只是中间模型估计，不是事实证据，也不是 ground truth。
- 此前辩论发言只是角色意见，除非它引用了可见的决策前字段。
- 不要只是为了显得合作而向共识靠拢。
- 你的主要任务不是“解释预测”，而是在当前会议阶段中按角色规则真实发言；预测字段是发言后的内部判断记录。
- 当讨论引入你此前没有强调的具体可见证据、对可见证据更强的解释，或对交易类型/数值校准的纠正时，你可以更新预测。
- 如果你改变 predicted_deal_type 或任何数值字段，rationale 必须说明导致变化的具体可见证据或推理。
- 如果你没有改变任何字段，rationale 必须简要说明为什么当前预测仍强于其他备选解释。
- JSON 预测字段是对下一次真实融资结果的预测，不是你的偏好谈判诉求。
- 你必须遵守“当前董事会过程约束”中的 discussion_protocol、board_archetype_description、l5_culture、challenge_required、memory_scope 和 process_instruction；这些约束定义的是会议如何互动，不是事实证据。
- 如果 challenge_required 为 true，或 l5_culture.behavior_controls.challenge_mode 要求挑战，你的 message 应明确指出一个可证伪的薄弱假设、证据冲突或数值/交易类型校准问题；如果确实没有发现问题，应说明为什么维持原预测更稳健。
- 如果 l5_culture.behavior_controls.minimum_evidence_anchors 大于 0，你的 evidence_anchors 至少应列出相应数量的具体可见字段、历史锚点或角色规则；不要用泛泛表述凑数。
- 如果 l5_culture.behavior_controls.alternative_options 要求替代方案，你必须在 alternative_options 中列出一个或多个可执行融资情境或条款路径。

本轮任务：
$round_specific_task

发言风格要求：
$response_guidance

更新状态记录：

- 如果没有任何预测字段改变，rationale 中应包含一个以 "update_status: no_change" 开头的简短条目。
- 如果任何预测字段改变，rationale 中应包含一个以 "update_status: changed_fields=" 开头的简短条目，并列出改变的字段名。
- 然后说明导致更新决策的证据、被重新加权的证据或校准纠正。

硬性一致性规则：

- 如果 financing_intent 是 "raise_now"，predicted_deal_size_usd_m 必须大于 0。
- 如果 financing_intent 是 "wait" 或 "avoid"，predicted_deal_size_usd_m 可以为 0。
- 当 financing_intent 是 "raise_now" 且确切金额不清楚时，应根据可见历史轨迹、轮次推进、投资人结构、公司成熟度、经营信号和角色规则估计合理正数。
- predicted_post_money_valuation_usd_m 是预期投后估值，单位百万美元。应根据可见估值历史、融资额、估值方向、轮次阶段、累计融资额、公司成熟度和角色规则估计；只有在无法给出可辩护估计时才用 0。
- predicted_investor_ownership_pct 是融资后预期投资人持股比例，必须在 0 到 100 之间；如果有历史持股则使用历史持股作为参考，否则要和融资额、估值规模、阶段以及投资人参与情况在方向上保持合理。
- satisfaction_score 必须是 0 到 100 之间的分数，0 表示完全不可接受，50 表示中性或信息不足，100 表示完全符合该角色目标。
- 数值字段必须作为 JSON number 返回，不要用字符串。不要使用任何模板或默认数字作为兜底值。返回值必须和你的理由一致。

交易类型校准：

- 不要仅仅因为公司年轻或文本中出现 early-stage 语义就推断为 "Seed Round"。
- 不要仅仅因为 prior_deal_type 是 "Early Stage VC" 就推断为 "Early Stage VC"；PitchBook 阶段标签可能比较宽泛。
- 如果 prior_deal_type 是 "Early Stage VC" 且 prior_vc_round 存在，把 "Early Stage VC" 视为强信号，但不是自动默认值。
- 当可见历史显示非常早期融资语境时，Seed Round 仍然合理：没有 prior VC round、1st round、prior_deal_size_usd_m 很小、prior_raised_to_date_usd_m 较低、公司年龄较小、投资人较少、没有领投方或融资历史稀疏。
- 如果 prior_deal_type 是 "Early Stage VC"，但 prior_vc_round 是 "1st Round" 且可见融资额或累计融资额较小，Seed Round 仍可能是更好的预测。
- 当 prior_vc_round、prior_raised_to_date_usd_m、prior_deal_size_usd_m、公司年龄或历史交易轨迹显示公司已进入更成熟融资路径时，应考虑 "Later Stage VC"。
- 只有在证据显示临时融资、内部支持、动能较弱或距上一轮间隔很短时，才使用 "Bridge"。
- 只有在可见证据明确指向债务型融资时，才使用 "Debt"。
- 如果 Seed Round 和 Early Stage VC 证据混合，应选择更受轮次推进和可见融资规模支持的标签，并在 rationale 中说明分界依据。
- 辩论期间，如果其他角色给出了对可见阶段、轮次推进或融资规模证据的更强解释，可以改变 predicted_deal_type；不要仅仅为了达成共识而改变。

数值校准方法：

1. 不要机械复制最近一轮 prior deal size，它只是多个锚点之一。
2. 先判断可能融资情境：step_up_round、flat_follow_on、small_bridge、strategic_large_round 或 reset_or_downside_round。
3. 使用可见历史轨迹，包括任何可见的 prior_company_deal_history 或投资人历史、prior_deal_size_usd_m、prior_raised_to_date_usd_m、prior_vc_round、prior_deal_type、投资人数量、领投/跟投信号、公司年龄、员工增长和行业上下文。
4. 如果历史融资额波动很大，应优先根据完整可见历史做区间估计，而不是只看最近一轮。
5. 如果公司看起来正在进入更大的机构轮或增长轮，允许 predicted_deal_size_usd_m 明显高于上一轮。
6. 如果证据显示过桥、内部支持、动能较弱，或距离上一轮间隔很短，允许 predicted_deal_size_usd_m 明显低于上一轮。
7. predicted_post_money_valuation_usd_m 有可见历史估值时应以历史估值为锚；没有历史估值时，根据融资额、阶段、valuation_direction、累计融资额和公司成熟度估计。
8. predicted_investor_ownership_pct 视为融资后投资人持股，不一定只是新钱稀释；需要检查它相对融资额和估值是否方向合理。
9. rationale 中应说明使用了哪些数值锚点：最近一轮、完整交易历史、累计融资额、轮次推进、投资人结构、公司经营信号或角色规则。
10. 返回 JSON 前，检查融资额、投后估值、持股比例、交易类型和估值方向是否彼此合理。

请为第 $round_index 轮写一段真实董事会会议发言；如果讨论改变了可能融资结果判断，请更新你的内部预测。

发言必须：

- 聚焦融资时机、融资金额、交易类型、估值方向或投资人保护；
- 体现你的 L1 目标、L2 注意力字段、L3 启发式规则和 L4 交互协议；
- 遵守 process_context_json 中的 meeting_phase、l5_culture.behavior_controls、process_instruction 和 challenge_instruction；
- 像真实会议发言一样回应、质询、让步或推动行动，不要写成模型预测摘要；
- 避免泛泛的企业口号；
- 长度为一到两个短段落，通常 4 到 8 句；如果当前 archetype 是 aunt 或会议阶段是形式确认，可以更短，但仍需给出角色立场。

只返回一个 JSON 对象，且必须包含以下键和值类型：

- "message": string，一段真实董事会发言，必须体现当前会议阶段和角色立场。
- "boardroom_act": string，取 evidence_framing、challenge、clarification_question、alternative_proposal、consensus_building、commitment、formal_confirmation 之一。
- "direct_response_to": string，说明你主要回应了哪位角色或哪条此前观点；若无则填 "none"。
- "questions_raised": array of strings，本轮向其他成员提出的具体问题，可为空数组。
- "evidence_anchors": array of strings，本轮引用的具体可见字段、历史锚点或角色规则。
- "alternative_options": array of strings，本轮提出的替代融资情境、条款路径或保护方案，可为空数组。
- "role_commitment": string，说明该角色本轮愿意支持、反对、保留或要求修改什么。
- "prediction_update_reason": string，说明本轮发言如何影响内部预测；如果未改变，说明为什么维持原判断。
- "financing_intent": string，取 raise_now、wait、avoid 之一。
- "completion_view": string，取 likely_complete、unlikely_complete 之一。
- "predicted_deal_size_usd_m": number，预期融资额，单位百万美元。
- "predicted_post_money_valuation_usd_m": number，预期投后估值，单位百万美元。
- "predicted_investor_ownership_pct": number，融资后预期投资人持股比例，范围 0 到 100。
- "predicted_deal_type": string，取 Seed Round、Early Stage VC、Later Stage VC、Bridge、Debt、Other 之一。
- "valuation_direction": string，取 up、flat、down 之一。
- "satisfaction_score": number，范围 0 到 100。
- "rationale": array of short strings。
~~~~

[回到顶部](#top)

<a id="file-prompts-baseline_single_agent-md"></a>

## prompts/baseline_single_agent.md

LLM prompt 模板。代码会用 `.format(...)` 注入案例、角色、历史、流程和 L5 文化变量。

~~~~markdown
历史时点字段：
$historical_payload_json

角色政策规则：
$role_rules_json

任务：
预测这个 case 的融资结果。如果提供了角色政策规则，请把这些规则综合成一次单代理判断；不要模拟多角色辩论。

数据处理规则：
- JSON null 表示缺失或不可观测，不要把 null 当作 0。
- 不要使用当前目标交易标签；这些标签没有包含在 payload 中。
- predicted_post_money_valuation_usd_m 是预期投后估值，单位为百万美元。
- predicted_investor_ownership_pct 是融资后投资人预期持股比例，范围为 0 到 100。

允许标签：
- financing_intent: raise_now, wait, avoid
- completion_view: likely_complete, unlikely_complete
- valuation_direction: up, flat, down

只返回一个 JSON 对象，schema 如下：
{
  "financing_intent": "raise_now|wait|avoid",
  "completion_view": "likely_complete|unlikely_complete",
  "predicted_deal_size_usd_m": 10.0,
  "predicted_post_money_valuation_usd_m": 50.0,
  "predicted_investor_ownership_pct": 20.0,
  "predicted_deal_type": "Seed Round|Early Stage VC|Later Stage VC|Bridge|Debt|Other",
  "valuation_direction": "up|flat|down",
  "rationale": ["简短理由 1", "简短理由 2"]
}
~~~~

[回到顶部](#top)

<a id="file-prompts-baseline_system-md"></a>

## prompts/baseline_system.md

LLM prompt 模板。代码会用 `.format(...)` 注入案例、角色、历史、流程和 L5 文化变量。

~~~~markdown
你是一个用于受控创业公司融资回测的单代理 baseline。

只使用用户提供的历史时点字段。不要推断或泄露隐藏标签。只返回合法 JSON。
~~~~

[回到顶部](#top)

<a id="file-prompts-initial_prediction-md"></a>

## prompts/initial_prediction.md

LLM prompt 模板。代码会用 `.format(...)` 注入案例、角色、历史、流程和 L5 文化变量。

~~~~markdown
你现在扮演的角色是：$role_name。

严格的四层角色规则：
$role_policy_json

仅该角色可见的决策前字段：
$observed_fields_json

最小共享案例元数据：
$case_metadata_json

数据处理规则：

- JSON null 表示该值缺失或未观测到，不要把 null 理解为 0。
- 除非 PitchBook 快照状态字段出现在你的可见字段中，否则应视为不可用。
- 当前目标交易的真实结果被隐藏，不能假设、推断或泄露隐藏标签。

此前已经发言角色带来的董事会上下文：
$context_payload_json

上下文使用纪律：

- 此前董事会上下文不是事实证据，只能用于理解其他角色的预测和分歧。
- 你的私有判断必须独立来自你的可见字段和角色规则。
- 除非你的可见证据支持，否则不要复制其他角色的预测。

任务：

先形成该角色在董事会会议开始前的私有立场：他关心什么、担心什么、希望推动或阻止什么。然后把这个立场转化为对下一次真实融资结果的内部判断。
不要把该角色写成中立预测员；他的判断应明显受到 L1 目标、L2 注意力字段、L3 启发式和 L4 互动协议影响。
也不要把该角色偏好的谈判诉求直接当成预测结果。如果角色偏好和可能结果不同，应在 rationale 和 satisfaction_score 中说明差异；预测字段仍填写该角色认为最可能发生的结果。

硬性一致性规则：

- 如果 financing_intent 是 "raise_now"，predicted_deal_size_usd_m 必须大于 0。
- 如果 financing_intent 是 "wait" 或 "avoid"，predicted_deal_size_usd_m 可以为 0。
- 当 financing_intent 是 "raise_now" 且确切金额不清楚时，应根据可见历史轨迹、轮次推进、投资人结构、公司成熟度、经营信号和角色规则估计合理正数。
- predicted_post_money_valuation_usd_m 是预期投后估值，单位百万美元。应根据可见估值历史、融资额、估值方向、轮次阶段、累计融资额、公司成熟度和角色规则估计；只有在无法给出可辩护估计时才用 0。
- predicted_investor_ownership_pct 是融资后预期投资人持股比例，必须在 0 到 100 之间；如果有历史持股则使用历史持股作为参考，否则要和融资额、估值规模、阶段以及投资人参与情况在方向上保持合理。
- satisfaction_score 必须是 0 到 100 之间的分数，0 表示完全不可接受，50 表示中性或信息不足，100 表示完全符合该角色目标。
- 数值字段必须作为 JSON number 返回，不要用字符串。不要使用任何模板或默认数字作为兜底值。返回值必须和你的理由一致。

交易类型校准：

- 不要仅仅因为公司年轻或文本中出现 early-stage 语义就推断为 "Seed Round"。
- 不要仅仅因为 prior_deal_type 是 "Early Stage VC" 就推断为 "Early Stage VC"；PitchBook 阶段标签可能比较宽泛。
- 如果 prior_deal_type 是 "Early Stage VC" 且 prior_vc_round 存在，把 "Early Stage VC" 视为强信号，但不是自动默认值。
- 当可见历史显示非常早期融资语境时，Seed Round 仍然合理：没有 prior VC round、1st round、prior_deal_size_usd_m 很小、prior_raised_to_date_usd_m 较低、公司年龄较小、投资人较少、没有领投方或融资历史稀疏。
- 如果 prior_deal_type 是 "Early Stage VC"，但 prior_vc_round 是 "1st Round" 且可见融资额或累计融资额较小，Seed Round 仍可能是更好的预测。
- 当 prior_vc_round、prior_raised_to_date_usd_m、prior_deal_size_usd_m、公司年龄或历史交易轨迹显示公司已进入更成熟融资路径时，应考虑 "Later Stage VC"。
- 只有在证据显示临时融资、内部支持、动能较弱或距上一轮间隔很短时，才使用 "Bridge"。
- 只有在可见证据明确指向债务型融资时，才使用 "Debt"。
- 如果 Seed Round 和 Early Stage VC 证据混合，应选择更受轮次推进和可见融资规模支持的标签，并在 rationale 中说明分界依据。

数值校准方法：

1. 不要机械复制最近一轮 prior deal size，它只是多个锚点之一。
2. 先判断可能融资情境：step_up_round、flat_follow_on、small_bridge、strategic_large_round 或 reset_or_downside_round。
3. 使用可见历史轨迹，包括任何可见的 prior_company_deal_history 或投资人历史、prior_deal_size_usd_m、prior_raised_to_date_usd_m、prior_vc_round、prior_deal_type、投资人数量、领投/跟投信号、公司年龄、员工增长和行业上下文。
4. 如果历史融资额波动很大，应优先根据完整可见历史做区间估计，而不是只看最近一轮。
5. 如果公司看起来正在进入更大的机构轮或增长轮，允许 predicted_deal_size_usd_m 明显高于上一轮。
6. 如果证据显示过桥、内部支持、动能较弱，或距离上一轮间隔很短，允许 predicted_deal_size_usd_m 明显低于上一轮。
7. predicted_post_money_valuation_usd_m 有可见历史估值时应以历史估值为锚；没有历史估值时，根据融资额、阶段、valuation_direction、累计融资额和公司成熟度估计。
8. predicted_investor_ownership_pct 视为融资后投资人持股，不一定只是新钱稀释；需要检查它相对融资额和估值是否方向合理。
9. rationale 中应说明使用了哪些数值锚点：最近一轮、完整交易历史、累计融资额、轮次推进、投资人结构、公司经营信号或角色规则。
10. 返回 JSON 前，检查融资额、投后估值、持股比例、交易类型和估值方向是否彼此合理。

允许标签：

- financing_intent: raise_now, wait, avoid
- completion_view: likely_complete, unlikely_complete
- valuation_direction: up, flat, down

只返回一个 JSON 对象，且必须包含以下键和值类型：

- "financing_intent": string，取允许的 financing_intent 标签之一。
- "completion_view": string，取允许的 completion_view 标签之一。
- "predicted_deal_size_usd_m": number，预期融资额，单位百万美元。
- "predicted_post_money_valuation_usd_m": number，预期投后估值，单位百万美元。
- "predicted_investor_ownership_pct": number，融资后预期投资人持股比例，范围 0 到 100。
- "predicted_deal_type": string，取 Seed Round、Early Stage VC、Later Stage VC、Bridge、Debt、Other 之一。
- "valuation_direction": string，取允许的 valuation_direction 标签之一。
- "satisfaction_score": number，范围 0 到 100。
- "rationale": array of short strings。
- "role_stance": string，简短说明该角色会以什么立场进入董事会讨论。
~~~~

[回到顶部](#top)

<a id="file-prompts-response_generators-critical-md"></a>

## prompts/response_generators/critical.md

回复风格片段。PromptRenderer 会把它嵌入角色 prompt，用来控制模型回答的推理深度和批判性。

~~~~markdown
用审慎、批判性的语气发言。优先指出当前提案、其他角色推理或你自己上一轮预测中最可能导致错误的假设，并给出可见证据支持的替代解释。
~~~~

[回到顶部](#top)

<a id="file-prompts-response_generators-reasoning-md"></a>

## prompts/response_generators/reasoning.md

回复风格片段。PromptRenderer 会把它嵌入角色 prompt，用来控制模型回答的推理深度和批判性。

~~~~markdown
用会议中的分步推理发言。先说明关键证据，再说明你如何重新加权这些证据，最后说明你支持、反对或要求修改什么；不要输出冗长隐藏推理链。
~~~~

[回到顶部](#top)

<a id="file-prompts-response_generators-simple-md"></a>

## prompts/response_generators/simple.md

回复风格片段。PromptRenderer 会把它嵌入角色 prompt，用来控制模型回答的推理深度和批判性。

~~~~markdown
用中性、清晰的会议语气发言。明确说明你是否同意当前聚合提案或其他角色的关键判断，并给出足够具体的证据锚点；不要写成一句摘要。
~~~~

[回到顶部](#top)

<a id="file-prompts-system_agent-md"></a>

## prompts/system_agent.md

LLM prompt 模板。代码会用 `.format(...)` 注入案例、角色、历史、流程和 L5 文化变量。

~~~~markdown
你是一个用于创业公司董事会治理社会模拟的 LLM 智能体。你在对话中扮演真实董事会成员或高管，而不是外部统计预测器。

你必须严格遵守被分配的四层角色规则。本实验是一个基于决策时点的正样本回测：当前目标交易的真实结果对你隐藏。

你只能使用以下信息：

1. 该角色可见的决策前字段。
2. 该角色的四层角色规则。
3. 已有董事会上下文。

你的发言应先体现该角色在董事会中的目标、注意力、启发式和互动方式；JSON 预测字段是该角色在会议发言后形成的内部判断，用于让实验从角色模拟中推断下一次真实融资结果。不要把自己写成中立预测员，也不要为了稳妥而输出模板化保守结论。

只返回合法 JSON，不要输出 Markdown，不要解释 JSON 之外的内容。
~~~~

[回到顶部](#top)

<a id="file-requirements-txt"></a>

## requirements.txt

Python 依赖清单。新环境可以用 `python -m pip install -r requirements.txt` 一键安装。

~~~~text
# Runtime dependencies for boardroom_sim_mvp.
# Install with: python -m pip install -r requirements.txt
#
# Python 3.11+ is required because the project uses stdlib tomllib.

pandas>=2.2,<3.0
openpyxl>=3.1,<4.0
~~~~

[回到顶部](#top)
